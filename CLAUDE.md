# Claude Code向けガイド

このプロジェクトの共通の開発方針は、同じディレクトリの `AGENTS.md` に定義している。作業前に必ず読み、適用する。

- 設計書、作業範囲、技術方針、テスト・レビュー方針は `AGENTS.md` に従う。
- 共通ルールを変更する場合は `AGENTS.md` を更新し、このファイルに重複して定義しない。
- Claude Code固有の補足が必要になった場合だけ、このファイルへ追記する。

## レビュー

3 ファイル以上に触る変更、新機能、外部仕様が変わる変更は、実装とテストが通った後に
**reviewer** サブエージェントを起動する。1〜2 ファイルの小修正・typo は不要。

- reviewer には **diff だけ**を渡す。計画や「なぜこう実装したか」は渡さない
- レビューに `/subtask`（fork）を使わない。会話履歴ごと渡ると独立レビューの意味が消える
- 再現コードのない Critical では止まらない。Warning として扱い、人間が判断する
- reviewer が Opus 4.8 に降格された通知が出た場合は、`model: opus` で再実行する

## 記憶

- `.claude/agent-memory/` は git 管理下に置く。gitignore しない
- reviewer が書いた記憶は PR の diff に現れる。おかしな学習は人間がレビューで潰す


