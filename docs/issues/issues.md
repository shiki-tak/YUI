# 課題一覧

フェーズ1の完了時点で残っている課題。フェーズ1の完了条件は満たしているが、
運用や後のフェーズで効いてくるものを記録する。

最終更新：2026-09-06

## 一覧

| ID | 状態 | 重要度 | 種別 | 概要 | 着手の目安 |
| --- | --- | --- | --- | --- | --- |
| [ISSUE-001](#issue-001) | 未着手 | 低 | 不具合 | 空白だけの本文が空の候補として保存される | すぐ |
| [ISSUE-002](#issue-002) | 未着手 | 中 | 品質 | 明示的な約束を振り返りが取りこぼす | 評価用会話の整備と同時 |
| [ISSUE-003](#issue-003) | 未着手 | 中 | 未実装 | 会話履歴を画面から読み直せない | フェーズ2の前 |
| [ISSUE-004](#issue-004) | 未着手 | 中 | 未実装 | 記憶検索の「時刻」が減衰のみで occurred_at を埋めていない | フェーズ5 |
| [ISSUE-005](#issue-005) | 未着手 | 中 | 未実装 | 理想の返答を見返す手段と書き出しが無い | フェーズ7の前 |
| [ISSUE-006](#issue-006) | 未着手 | 中 | 未整備 | 評価用会話が未整備 | すぐ（設計書はフェーズ1から開始） |
| [ISSUE-007](#issue-007) | 未着手 | 低 | 設計 | 順序制御が単一プロセス前提 | 複数プロセス化する段階 |
| [ISSUE-008](#issue-008) | 未着手 | 低 | 設計 | 記憶検索が全件をPythonで採点する | 記憶が増えた段階 |
| [ISSUE-009](#issue-009) | 未着手 | 低 | 設計 | 候補の本文が500文字を超えると振り返り全体が失敗する | フェーズ4 |
| [ISSUE-010](#issue-010) | 未着手 | 低 | 設計 | 相手を特定できない記憶は参照範囲が限定されない | フェーズ4 |

---

## ISSUE-001

**空白だけの本文が、空の候補として保存される**

- 状態：未着手
- 重要度：低
- 種別：不具合
- 出所：Codex 5回目のレビュー
- 場所：[backend/app/agent/reflection.py](../../backend/app/agent/reflection.py)

### 内容

候補の本文は `min_length=1` で検証しているが、検証は空白を除去する前に行い、保存時に `strip()` している。そのため空白・改行・タブだけの本文は検証を通り、本文が空の候補として保存される。

### 再現

LLMが `[{"kind":"promise","content":"   \n\t", ...}]` を返すと、**200・`content=""` の候補1件・会話終了・再試行409** になる。利用できる本文が無いのに抽出成功として扱われる。再現済み。

### 修正方針

本文を正規化してから最小長を検証する。空白だけなら既存の `ReflectionParseError` へ流し、正常な候補と混在しても全体を失敗にする。空白・改行・タブのみの入力を再発検知テストへ追加する。

---

## ISSUE-002

**明示的な約束を、振り返りが取りこぼすことがある**

- 状態：未着手
- 重要度：中
- 種別：品質
- 出所：実装中の観測、Codex 5回目のレビューでも継続評価が必要と指摘

### 内容

抽出の指示文には「次に話すと決めたことは必ず promise として残す」とあるが、実モデルが `[]` を返して候補が1件も出ないことがある。

### 再現

「最近はランニングを始めたよ。朝5キロ走ってる。次はシューズの話をしよう」という発言で、qwen3.5:9b が3回とも `[]` を返した。会話を長くしても変わらなかった。

一方、同じ構造でも「実は最近ギターを始めたんだ。指が痛くてなかなか進まないけど楽しいよ／好きなバンドのバラードかな。次はコードの練習の話をしよう」では候補が出る。入力の内容によってモデルの判断が変わる。

解析処理の変更に起因するものではないことは確認済み（同じ会話文を同条件で流すと候補が出る）。

### 影響

記憶を作る経路はこの振り返り一本しかない。取りこぼすと、その会話の経験は残らず、失われたことに気づく手段もない。フェーズ1の中心機能の品質に関わる。

### 修正方針

まず [ISSUE-006](#issue-006) の評価用会話を整え、期待する約束・経験が候補に含まれるかを測る。そのうえで抽出の指示文を調整する。形式検証が通ることと、残すべき内容を実際に抽出できることは別の評価である。

---

## ISSUE-003

**会話履歴を画面から読み直せない**

- 状態：未着手
- 重要度：中
- 種別：未実装

### 内容

`GET /api/conversations` と `/api/conversations/{id}` はあるが、フロントエンドから呼んでいない。過去の会話を開く導線が無く、「終了して振り返る」を押すと画面上の会話は消える。DBには残るので失われてはいない。

設計書フェーズ1の実装内容4「SQLiteへ会話履歴を保存する」は満たしているが、保存したものを確認できない。

### 修正方針

会話一覧と、選んだ会話の詳細を表示する。終了済みの会話は読み取り専用として開く。

---

## ISSUE-004

**記憶検索の「時刻」が減衰のみで、occurred_at を埋めていない**

- 状態：未着手
- 重要度：中
- 種別：未実装

### 内容

設計書フェーズ1の実装内容6は「相手・時刻・話題のキーワードで関連する記憶を取り出し」だが、実装している時刻の扱いは「新しい記憶をわずかに優先する」減衰だけである。

`memories.occurred_at`（いつの出来事か）の列はあるが、振り返りの抽出では埋めていない。手動のAPIからしか入らない。「昨日話したこと」のような時間を指す問いかけも解釈しない。

### 修正方針

振り返りで出来事の日時を推定して `occurred_at` に入れる。検索側では、問いかけに含まれる時間表現を範囲条件として扱う。フェーズ5で「経験が次の話題に反映される」段階に入ると重要度が上がる。

---

## ISSUE-005

**理想の返答を見返す手段と、書き出しが無い**

- 状態：未着手
- 重要度：中
- 種別：未実装

### 内容

理想の返答は記録できるが、フロントエンドに一覧が無く、保存後に見返せない（`GET /api/conversations/messages/{id}/ideal` は存在する）。フェーズ7の教師データとして書き出す経路も無い。

設計書 §10 では「理想の返答の収集」をフェーズ1から進める作業としている。集めても使えない状態が続くと、フェーズ7の開始条件（良い応答・修正した応答の蓄積）を満たしているか判断できない。

### 修正方針

画面に一覧と検索を付ける。JSONL への書き出しはフェーズ7で実装する。

---

## ISSUE-006

**評価用会話が未整備**

- 状態：未着手
- 重要度：中
- 種別：未整備

### 内容

設計書 §10 は「評価用会話の整備」をフェーズ1から開始する作業とし、「記憶、人格、根拠、繰り返しなどの観点で用意」としている。

現在あるのは pytest 41件だが、これは実装の振る舞いを固定するもので、人格や記憶利用の質を測る会話集ではない。

### 影響

モデルやプロンプトを変えたときに、良くなったか悪くなったかを判断できない。[ISSUE-002](#issue-002) の取りこぼしも、これが無いと頻度を測れない。

### 修正方針

観点ごとに会話と期待する振る舞いを用意する。少なくとも次を含める。

- 記憶：保存した経験・約束を、別の会話で引けるか
- 根拠：返答が渡した記憶の範囲に収まっているか
- 人格：口調と「知らないことは正直に言う」が保たれているか
- 振り返り：残すべき経験・約束が候補に含まれるか
- 繰り返し：同じ話を繰り返していないか

---

## ISSUE-007

**順序制御が単一プロセス前提**

- 状態：未着手
- 重要度：低
- 種別：設計

### 内容

会話ごとの順序制御（[backend/app/agent/turn_lock.py](../../backend/app/agent/turn_lock.py)）はプロセス内の asyncio ロックで実装している。設計書が「MVPは単一プロセス」としているため現時点では足りるが、複数プロセスに分けると排他が効かなくなる。

振り返りの開始権にも同じ前提がある。プロセスが落ちて開始だけが残った場合は、`reflection_stale_seconds`（既定180秒）を過ぎたら回収する仕組みで補っている。

### 修正方針

複数プロセスに分ける段階で、DB上の排他か外部のロックへ置き換える。フェーズ4で発話キューを実装する際に、同じ仕組みへ統合する。

---

## ISSUE-008

**記憶検索が全件をPythonで採点する**

- 状態：未着手
- 重要度：低
- 種別：設計

### 内容

`search_memories()` はSQLで「有効な記憶」「参照範囲」「相手」の3条件で絞り込んだあと、残り全件をPython側で採点している。記憶が増えると、この採点が最初に効かなくなる。

### 修正方針

件数が問題になった段階で、SQLite の全文検索（FTS5）か、設計書が言う意味検索を導入する。導入するまでは、一致の理由を返せる現在の方式を保つ。

---

## ISSUE-009

**候補の本文が500文字を超えると、振り返り全体が失敗する**

- 状態：未着手
- 重要度：低
- 種別：設計

### 内容

候補の本文は500文字を上限として検証し、1件でも超えると振り返り全体を失敗として扱う。現在の開発者との会話では最大34文字で余裕があるが、フェーズ4で視聴者コメントを含む長時間の配信ログを渡すと、上限に触れる可能性がある。

同じ入力で再試行しても同じ結果になるため、その会話の振り返りが通らなくなる。

### 修正方針

フェーズ4で配信ログを扱う段階で、上限に触れる頻度を測る。頻発するなら、上限を上げるか、長い本文を分割するよう指示文を調整する。本文を黙って切り詰める対応は取らない（内容が変わったことに気づけなくなるため）。

---

## ISSUE-010

**相手を特定できない記憶は、参照範囲が限定されない**

- 状態：未着手
- 重要度：低
- 種別：設計

### 内容

`visible_to_speaker_id` が NULL の記憶は、ローカル会話ではすべての相手に渡る。次の場合に NULL になる。

- 手動で追加した記憶（由来する会話が無い）
- 移行時に、由来した会話に利用者の発言が無かった記憶

フェーズ1は開発者との会話だけなので実害は無いが、フェーズ4で複数の相手が入ると、意図せず共有される記憶が生まれうる。

### 修正方針

手動追加の画面で参照範囲を選べるようにする。フェーズ4に入る前に、NULL のまま残っている記憶を洗い出して振り分ける。
