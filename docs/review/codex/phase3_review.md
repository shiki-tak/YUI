# フェーズ3：Pre review

レビュー日：2026-09-07

対象：固定人格の版管理（ISSUE-014、設計書フェーズ3の3A）の未コミット実装。フェーズ3全体の完了レビューではありません。本書は直前に報告したレビュー結果の保存であり、保存時の再レビューは行っていません。行番号はレビュー時点のものです。

**結論：ISSUE-014の実装を現時点で完了とするのは妥当ではありません。人格読込失敗時の後処理と、ディレクトリ外読込の防止に不備があります。**

## 対象

指定された未コミットの実装、基準版TOML、マイグレーション、関連テスト、指定の設計・課題記録を確認しました。設計資料やコメント変更自体はレビュー対象にしていません。

- `backend/personas/2026-09-06.2.toml`
- `backend/app/persona.py`、`config.py`、`models.py`、`schemas.py`、`main.py`
- `backend/alembic/versions/152507a25337_人格の版を実行記録に残す.py`
- `backend/app/agent/conversation.py`、`agent/__init__.py`
- `backend/app/api/chat.py`、`api/conversations.py`
- `backend/tests/test_persona_versions.py`、`tests/conftest.py`
- `frontend/src/types.ts`、`components/ChatPanel.tsx`、`App.test.tsx`
- `backend/.env.example`

実行した主なコマンド：

- バックエンド：一時パスの `YUI_DATABASE_URL` と `PYTHONDONTWRITEBYTECODE=1` を指定し、`.venv/bin/python -m pytest -p no:cacheprovider` — **99件成功**
- `.venv/bin/ruff check app tests --no-cache` — **成功**
- フロントエンド：`npx tsc --noEmit`、`npm test -- --no-cache` — **型チェック成功、16件成功**
- `git diff`、`git status --short`、関連ファイルの読み取り

レビューではソースコード・設定・ドキュメント・通常利用のDBを変更していません。その後のユーザー指示により、このレビュー文書のみを新規作成しました。

確認できた正常範囲：

- 基準版の文面は、旧コードの定義と照合し、完全一致テストも成功しました。
- 通常の生成では、同じローカル変数 `persona` からプロンプトと `persona_version` を作成しています。要求ごとに共有エージェントの人格を書き換える構造ではありません。
- 未知の版はAPIで404となり、空文字・パストラバーサル形式はローダーの既存テストで拒否されます。
- 会話途中の版変更は、要求単位の比較を許す意図的な仕様です。それ自体を不具合とは扱いません。
- 既存行の `persona_version` をNULLにする判断は妥当です。根拠なく現在の版へ帰属させず、既存の `system_prompt` を保持しています。

## 指摘事項

### 1. [中] 人格の読込に失敗すると、振り返り開始済みの状態だけがDBに残ります

- 場所：`backend/app/api/conversations.py:151`、`:165`、`:174`、`:176`／`backend/app/main.py:24`／`backend/app/persona.py:25`
- 確認区分：コード確認のみ
- 発生条件：未終了の会話が存在し、既定人格がまだキャッシュされていないプロセスで、人格ファイルの欠落・TOML破損・定義不正がある状態から会話終了APIを呼ぶ。
- 影響：`reflection_started_at` をコミットした後に `load_persona()` が失敗します。`PersonaError` は例外処理の対象外なので、開始権を解除する `_release_reflection()` が実行されません。HTTP 500となり、人格ファイルを直しても、期限切れになるまでは再試行が409になります。既定値では180秒です。不変条件5の、振り返り失敗後に再試行できる既存動作を損ないます。また、人格の起動時検証は登録されていません。`PersonaError` の「起動時に落とす」という説明とは異なり、リクエスト処理まで障害を検出しません。
- 修正方針：振り返りの開始権を確定する前に必要な人格を解決する。読込失敗をAPIで適切なエラーへ変換し、開始後に失敗し得る場合は必ず開始権を解除する。既定人格を起動時に検証する方針なら、実際の起動処理へ組み込む。

### 2. [中] personas内のシンボリックリンクからディレクトリ外のファイルを読めます

- 場所：`backend/app/persona.py:90`、`:91`、`:97`、`:124`
- 確認区分：コード確認のみ
- 発生条件：`personas/test-b.toml` がディレクトリ外のファイルへのシンボリックリンクになっており、`test-b` を指定する。
- 影響：版名の文字列検証は通り、`available_versions()` にも列挙されます。`is_file()` と `read_text()` はリンク先を参照するため、外部ファイルが読み込まれます。内容が有効な人格TOMLなら人格として採用されます。不変条件4の「personasディレクトリの外を読めない」を満たしません。APIからリンクを作成できる経路は確認していません。これはローカルにリンクが存在する条件での不備であり、遠隔から任意ファイルを読めると断定するものではありません。
- 修正方針：人格ディレクトリと候補パスを解決し、解決後の候補がディレクトリ内にあることを検証する。一覧取得と読込で同じ制約を適用するか、人格ファイルのシンボリックリンクを禁止する。

## 対象外・未確認

- **キャッシュ：コード確認のみ。** キーに設定の版・ディレクトリが含まれず、`load_persona()`、`load_persona(None)`、明示的な版指定は別のキャッシュになり得ます。設定キャッシュだけを消しても人格・エージェントのキャッシュは残ります。ただし、稼働中の設定再読込を保証する仕様や本番経路は確認できないため、再起動まで反映されないこと自体は独立した不具合に数えていません。ファイル編集後の比較では再起動が必要です。
- **共有リスト：コード確認のみ。** `frozen=True` でも `traits` 等のリスト内容は変更可能です。ただし、今回読んだ呼出元にリストを変更する処理はなく、それによる文面の汚染は確認していません。
- **壊れた定義：コード確認のみ。** TOML破損、name欠落・空白、配列内の非文字列は拒否します。versionの省略はファイル名から補い、各配列の省略は空配列になります。追加の障害試験は未実施です。
- **テストの隔離：** 対象テストは `finally` で人格キャッシュを消し、プロパティ差し替えはmonkeypatchで復元します。依存差し替えもfixture終了時に解除します。今回の通常順序の実行では漏れを確認していません。任意順序・並列実行は未確認です。
- **移行：** 既存の移行テストはheadまで成功しました。新列の追加・削除はコード上対称ですが、既存run_recordsを含むupgrade／downgrade往復のデータ保持は未実行です。downgradeでは追加した版情報が失われます。
- **並行処理：** 人格を要求ごとの変数として保持し、既存の会話ロック内で生成します。異なる版を使う同時要求・複数プロセスでの追加検証は未実施です。
- 実Ollama・VOICEVOX、実ブラウザ、人格の生成品質、3A全体の会話例・評価基盤は確認していません。

---

## Pre review 再評価：対応後の確認とフェーズ3の開始判断

レビュー日：2026-09-07

**結論：ISSUE-014の前回指摘2件は解消を確認しました。固定人格の版管理の対応を完了とし、フェーズ3の開発を開始してよいと判断します。** 今回確認した範囲で、新たな着手阻害事項は見つかりませんでした。これはフェーズ3全体の完了判定ではありません。

### 対象

[Claudeの対応記録](../claude/phase3_review_response.md)、現在の未コミットの `persona.py`、`main.py`、`api/conversations.py`、関連テストを再確認しました。HEADは `07c77d9` で、対応は未コミットの作業ツリーにあります。直前の口頭評価と同じ対応内容を再確認し、本節へ記録しています。

### 指摘への対応評価

| 項目 | 確認区分 | 評価・確認結果 |
| --- | --- | --- |
| 人格読込失敗で振り返り開始状態が残る | 実行して確認 | 一時DBと模擬LLMを使う回帰テストが成功。人格を読めない状態で終了APIが503となり、設定を復旧した直後に200で再試行できる。コードでも、人格の解決が開始権を更新・コミットする前に移動していることを確認 |
| シンボリックリンクによるディレクトリ外読込 | 実行して確認 | 一時ディレクトリに外部ファイルへのリンクを作る回帰テストが成功。外部リンクは一覧から除外され、直接読み込むとPersonaErrorになる。内部の実ファイルは正常に読める |
| 起動時の既定人格検証 | コード確認のみ | FastAPIにlifespanが登録され、yield前にload_personaを実行する。今回のpytestのASGITransport経路はlifespanの実行確認ではないため、起動実行の検証済みとは扱わない |
| PersonaErrorのAPI応答 | 実行して確認 | 例外ハンドラが登録され、終了APIの読込失敗が503と原因を含む本文になることを回帰テストで確認 |

対応記録の主要な主張は、コードと今回のテスト結果に対応しています。修正前へ戻した場合のテスト失敗と、lifespanの単独実行についてはClaude側の報告であり、今回は独立に再実行していません。

### 新たな指摘事項

指摘なし。

キャッシュを維持し、人格定義を編集した場合は再起動する運用は、現在の仕様として受け入れ可能です。ただし対応記録の「どちらも同じ定義を返すため、動作の差はありません」は、稼働中に定義や設定を変更しない前提の説明です。別キャッシュが存在する点まで解消したわけではありません。比較時は別の版を追加し、既存版を編集した場合は再起動する運用を守る必要があります。

### 実施した検証

- `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-persona-recheck-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider`：**101件成功**。通常利用のDBを使わず、各テストの一時DBで実行。
- `.venv/bin/ruff check app tests --no-cache`：成功。
- フロントエンドの `npx tsc --noEmit`：成功。
- `npm test -- --no-cache`：**16件成功**。
- 基準版の文面、指定版の記録・プロンプト、未知の版の拒否、既存の記憶・音声・再生状態についても、関連する既存テストが引き続き成功。

### フェーズ3の進め方と未確認事項

版を選んで生成し、実行記録と実際のプロンプトを追える土台は整っています。設計書第12節に沿って、まずISSUE-006の評価用会話と基準挙動の記録を整備し、3Aの人格比較・調整へ進めます。ISSUE-015等の可変状態や継続性に関する課題は、フェーズ3で実装・評価する対象として残ります。

実Ollama・VOICEVOX・実ブラウザ、人格の生成品質、起動時検証の独立実行、既存run_recordsを含むupgrade／downgrade往復、任意順序・並列・複数プロセスの追加試験は未確認です。これらを確認済みとする判断ではありません。

今回変更したのは、このレビュー文書への追記だけです。ソースコード・設定・通常利用のDB・既存の未コミット変更は変更していません。

---

フェーズ3を完了とは判断できません。複数話者の帰属と、訂正・公開範囲の可変状態への継承に未解消の問題があります。

## 対象

2026-09-07、フェーズ3全体レビュー。`07c77d9..3277611`（HEAD、13コミット、71ファイル、+7018/-474行）を確認しました。AGENTS.md、README.md、指定の設計書、実装記録、ISSUE-001〜021、前回レビューを読み、人格、評価器と30シナリオ、会話・振り返り、記憶・状態、API・画面、5版のマイグレーション、関連テストを確認しました。

実行結果：

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase3-review-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider`：164件成功。テスト内の一時DBを使用。
- backend：`.venv/bin/ruff check app tests --no-cache`：成功。
- frontend：`npx tsc --noEmit`、`npm test -- --no-cache`：成功、テスト16件成功。
- `mktemp -d /private/tmp/yui-phase3-migration.XXXXXX` で作った一時ディレクトリのDBを `YUI_DATABASE_URL` に指定し、`.venv/bin/alembic upgrade head` → `check` → `downgrade d24b3a0aa761` → `upgrade head` → `current`：すべて成功。モデルとの差分なし、headは `3187f6b349d3`。
- `git log`、`git diff`、ファイル読み取りで対象を確認。開始時の作業ツリーはクリーン。

変更は本レビューへの追記だけです。ソースコード、設定、通常利用の `backend/data/yui.db` と会話・記憶は変更していません。以下の不具合はコード確認による指摘であり、追加の失敗再現実験や実モデル評価は行っていません。

## 完了条件の評価

| 条件 | 評価 | 根拠 |
| --- | --- | --- |
| 1. 複数セッション・再起動をまたぐ約束・共有経験 | 確認できない | DB保存と別会話での参照の既存テストは成功。ただし評価器は記憶を事前投入して1会話を実行するため、抽出・採用・プロセス再起動までつないだ保証ではなく、日時の問題も残る。 |
| 2. 訂正後に古い好み・予定を現在の事実にしない | 満たしていない | 記憶そのものの訂正テストは成功するが、振り返りで作った状態に訂正が波及しない。古い内容が状態経由で次のプロンプトに残る。 |
| 3. 記憶がないとき共有経験を創作しない | 確認できない | 人格・プロンプトに抑制指示がある。ただし実モデルを再評価しておらず、機械判定の3/3だけでは意味的な非創作を保証しない。 |
| 4. 相手に合わせながら自分の好み・価値観を保持 | 確認できない | 固定人格と可変状態の分離・参照は実装されている。人手判定だけのシナリオも自動では通過扱いとなるため、提示された成功率だけでは保持を判断できない。 |
| 5. 参照した記憶・人格版・可変状態の追跡 | 満たしている | `conversation.py:151` で人格版、記憶・状態ID、実際のsystem_promptを同じ実行記録に保存する。関連テストも成功。ただし状態の根拠への訂正波及は条件2の別問題。 |
| 6. 別人・非公開の情報を不適切に参照しない | 満たしていない | 振り返りが全利用者の発言を最後の話者に帰属させる。手動作成の状態には根拠記憶の参照範囲を引き継ぐ検証もない。 |
| 7. 文字入力と読み上げの既存会話 | 確認できない | モックによるバックエンド回帰・フロント再生テストは成功。実Ollama・VOICEVOX・ブラウザの結合動作は未確認で、別会話の振り返り中のDB競合も残る。 |

## 指摘事項

### 1. [高] 複数話者の振り返りで、全員の発言と抽出結果が最後の話者に帰属する

- 場所：`backend/app/agent/reflection.py:290`、`:383`、`:385`、`backend/app/api/conversations.py:179`、`:188`、`backend/app/agent/state_reflection.py:162`。
- 確認区分：コード確認のみ。
- 発生条件：同じ会話でA、Bが発言し、Bを最後に会話を終了する。Aの好みなどが本人情報として抽出される。
- 影響：`format_transcript` は全利用者の発言に同じ `partner_name` を付ける。さらに記憶の対象・参照範囲、関係性の対象を最後のBのIDで保存する。根拠の発言IDがAを指していても修正されないため、採用するとAの情報をBの情報として利用する。条件6と3Cに反する。通常応答の履歴に名前を付けたISSUE-020の対応は、この振り返り経路には届いていない。
- 修正方針：各発言に実際の話者ID・表示名を付け、抽出対象者と発言者を区別する。保存時にも根拠発言と対象・参照範囲を照合し、特定不能なものを最後の話者へ寄せない。

### 2. [高] 振り返りから作った状態に記憶の訂正・削除が波及せず、復元時にも再評価されない

- 場所：`backend/app/agent/state_reflection.py:156`、`backend/app/agent/character_state.py:147`、`backend/app/api/memories.py:209`。
- 確認区分：コード確認のみ。
- 発生条件：同じ発言から記憶と関心・関係性候補を作り、両方を採用した後に記憶を訂正・削除する。また、明示的な記憶ID付き状態でも、訂正に合わせて再評価した後、記憶を以前の内容へ復元する。
- 影響：自動抽出状態は `source_conversation_id` のみを持ち、`basis_memory_ids` は空。`mark_for_review` は記憶IDを持つ状態だけを対象にするため、古い状態が有効なまま次の応答へ渡る。復元APIもこの関数を呼ばない。条件2に反し、実装記録20・59行の訂正波及の説明は、通常の自動抽出経路まで成立していない。
- 修正方針：候補・根拠発言・採用記憶と状態を結び付ける。粒度が会話単位しかない間は、同じ根拠会話の状態を保守的に再評価するなど、訂正を取りこぼさない仕組みにする。復元も根拠変更として扱う。

### 3. [高] 状態の作成APIが根拠記憶の公開範囲を継承せず、省略時に別人へ渡せる

- 場所：`backend/app/schemas.py:314`、`backend/app/api/states.py:53`、`backend/app/agent/character_state.py:63`、`:127`。
- 確認区分：コード確認のみ。
- 発生条件：A限定の非公開記憶を `basis_memory_ids` に指定して関心を追加し、参照範囲を省略して採用する。または、その根拠のまま状態に `public` を指定する。
- 影響：入力の既定は `private`・`visible_to_speaker_id=None` で、根拠の制約を検証しない。ローカル参照ではこの状態がBにも渡り、`public` とした場合は既存のSTREAM分岐でも渡る。記憶側で範囲を制限しても派生状態経由で条件6を破る。これは管理APIの通常操作で起きる範囲継承の欠落であり、配信機能の新規実装を求める指摘ではない。
- 修正方針：状態でも参照範囲を明示させ、根拠の存在と範囲を検証する。複数根拠からの派生状態は許可範囲の共通部分を引き継ぎ、無指定で全利用者向けにしない。根拠を持たないYUI自身の一般的関心とは区別する。

### 4. [中] 3回目のモデル呼び出しが失敗すると、失敗した振り返りの記憶候補だけが確定保存される

- 場所：`backend/app/agent/reflection.py:394`、`backend/app/api/conversations.py:194`、`:210`、`:55`。
- 確認区分：コード確認のみ。
- 発生条件：2段階の記憶抽出で1件以上をflushした後、状態抽出がLLMErrorまたはStateReflectionErrorになる。
- 影響：例外処理はrollbackせず、`_release_reflection` が同じセッションをcommitする。503を返した試行の記憶候補が残り、再試行すると候補をもう一度抽出・保存する。依存関数の後続rollbackでは確定済み候補を戻せない。開始権は解放されるが、再試行の整合性は保てない。既存の状態抽出失敗テストは記憶候補が空で、この経路を測っていない。
- 修正方針：失敗試行の書き込みをrollbackしてから、別トランザクションで開始権を解放する。抽出結果は成功時にまとめて確定し、再試行で候補を重複させない。

### 5. [中] 記憶候補のflush後に状態を推論するため、別会話のDB書き込みもモデル待ちになる

- 場所：`backend/app/agent/reflection.py:396`、`backend/app/api/conversations.py:194`、`:222`、`backend/app/agent/state_reflection.py:137`。
- 確認区分：コード確認のみ。
- 発生条件：記憶候補が1件以上あり、3回目の推論中に別会話の送信や記憶訂正などがSQLiteへ書き込む。
- 影響：候補INSERTのトランザクションを開いたままLLMを待つため、SQLiteの書き込みロックを保持する。別会話の書き込みが待たされ、待機許容時間を超えればDBロックエラーになる。同一会話のロックでは別会話を保護できない。開始時のcommitで書き込みロックを手放す方針が、追加の推論前には守られていない。
- 修正方針：モデル呼び出し中は保存用トランザクションを保持しない。抽出結果をメモリ上のデータとして受け取り、全段階成功後の短いトランザクションで保存する。途中commitだけで回避すると指摘4の部分保存を固定化するため、両方を解決する。

### 6. [中] 人手判定が未入力でも通過数に含まれ、振り返りの人手確認項目は出力されない

- 場所：`backend/app/evaluation/runner.py:48`、`:88`、`:313`、`backend/app/evaluation/report.py:83`、`:100`、`backend/app/evaluation/scenario.py:131`、`docs/result/phase3.md:19`。
- 確認区分：コード確認のみ。
- 発生条件：`keeps-own-preference-against-two-people` など機械チェックがなく `human_check` だけの会話、または振り返りに `human_check` を設定したシナリオを評価する。
- 影響：空のチェック配列は `all([])` により成功になる。返答が条件を満たさなくてもモデル呼び出しが成功すれば通過数が増える。振り返りの `human_check` は結果へ引き継がれず、候補も種別と本文だけになるため、provenance・対象者・根拠IDをレポートで判定できない。レポート自体は自動判定と説明しているが、その成功率を意味的な完了条件の根拠にすることはできない。実装記録52行にも、架空体験を語った基準版が自動3/3だったと記載されている。
- 修正方針：自動チェック成功と人手未判定・合格・不合格を分離して集計する。振り返りの確認項目と必要な候補属性を保存・表示し、完了判定には実際の人手結果を対応付ける。82/90という集計値自体の誤算を指摘しているのではなく、数値が保証する範囲の問題である。

### 7. [中] 相対日付を発言日ではなく会話終了日から解釈させている

- 場所：`backend/app/agent/reflection.py:290`、`:324`、`:346`。
- 確認区分：コード確認のみ。
- 発生条件：9月1日に「昨日登った」と話し、会話を終了するのが9月3日になるなど、日をまたいで振り返る。
- 影響：モデルに渡る履歴には発言日時がなく、「今日」は振り返り実行日になる。本来8月31日を指す発言に9月3日基準を与え、誤った出来事日時を保存する経路になる。実モデルが実際に返す日付は未確認だが、正しい基準日が入力に存在しないことはコードから確定できる。条件1の共有経験の時間的整合を損なう。
- 修正方針：履歴へ各発言の日時を渡し、相対日付は根拠発言の日付から解釈する。不確かな日付は確定値にせず、日またぎ・複数日にわたる会話をテストする。

## 評価用会話で測っていない観点

以下は評価器・シナリオのコード確認に基づきます。機械チェックと人手確認の併用自体は妥当ですが、確認済みの範囲を分ける必要があります。

| 観点 | 現在の不足と追加すべきシナリオ |
| --- | --- |
| 条件1：記憶が作られるところから再起動後まで | `runner.py:211` は試行ごとに一時DBを作り、記憶を事前投入する。`promise-across-sessions` は実際の終了・採用・再起動を通さない。空DBで約束・経験を話す→3段階抽出→採用→プロセス/接続を作り直す→別セッションで参照、を測る。既存pytestの「再起動に相当」も実プロセス再起動そのものではない。 |
| 条件2：訂正・削除・復元の伝播 | `correction-is-reflected` は訂正内容を含む記憶を事前投入しており、訂正APIを実行しない。実際に抽出した記憶と状態の両方を採用し、訂正・削除・復元後の次のプロンプトと返答を確認する。訂正前の発言が会話履歴に残る場合も含める。 |
| 条件4：経験による状態変化 | `StateSpec` の状態はactiveとして直接投入される。評価器の振り返りは `extract_candidates` だけで、`extract_state_candidates` を呼ばない。関心のない状態から複数の経験を与え、候補・採用前後・反例後を比較し、固定人格と本人の好みを混同しないか測る。 |
| 条件6・3C：同一会話の複数話者から保存まで | 相反する好みの参照だけでなく、A/Bが発言→終了→候補の対象者・発言者・範囲を確認→採用→それぞれ別会話で参照、まで通す。発言順の逆転、同じ表示名で異なるID、話者不明、AがBについて話す伝聞も追加する。 |
| 条件6：可変状態による非公開情報の持ち出し | `StateSpec` には公開範囲の指定がなく、評価はローカル会話。A限定の記憶から作った状態をB・話者不明・STREAMの参照処理へ渡さないことを測る。既存の参照処理の検証であり、配信システムの実装は不要。 |
| 条件3・4と3Aの意味的合格条件 | 自動チェックだけでなく、人手の判定結果と根拠となる返答を残す。作成時に使わなかった問い、言い換え、続けて同調を求める問いでも比較する。`expect_not_repeating` は完全一致を調べるため、言い換えた同じ質問・相づちの反復は別に判定する。 |
| 2段階抽出の過剰抽出と伝聞 | 種別や単語の有無に加え、provenance、対象者、根拠ID、誤った候補を人が識別できるかを確認する。「却下できる」は意図的な採用方針として理解できるが、状態候補は同じ生の会話から独立に作られる。記憶候補を却下するだけでは対応する状態候補まで却下されないため、両方を確認する運用と評価が必要。自動採用されるとは指摘していない。 |
| 振り返りの障害・同時実行 | 各呼び出しの接続失敗・不正JSON・必須項目欠落・不正型、特に「記憶候補あり→状態抽出失敗」で、残存候補ゼロと再試行の一意性を確認する。別会話の書き込み、同じ終了要求の競合も測る。モデル品質の30シナリオとは別の制御テストとして追加する。 |
| 時間と検索・類似判定 | `expect_occurred_at` は日付の存在であって正しい日付の照合ではない。発言日・終了日が違う例を足す。tokenizeの変更と類似閾値0.6については、同じ出来事の言い換え、同じ話題の別イベント、訂正・否定、短い語、記憶件数増加時の検索順位を比較する。今回、閾値による具体的な検索退行は再現していないため、不具合とは断定しない。 |

## 対象外・未確認

- 実Ollamaによる `python -m app.evaluation.run` は実行していません。82/90、個別3/3、抽出率、待ち時間の数値は独立に再測定していません。164件のpytest成功は今回確認できた値です。
- 実VOICEVOX、実音声出力、実ブラウザ、採用人格 `2026-09-07.1` の生成品質・読み上げ品質は未確認。モックの回帰成功と区別しています。
- 5版の空DBでのupgrade・downgrade往復とモデル整合は確認済み。旧データを移行する既存テストも成功していますが、フェーズ3の全テーブルへ実データを入れた往復の保存内容、稼働プロセスの再起動、並列・複数プロセスでの追加実験は未確認です。
- 人格の版管理について、新しい具体的な不具合は見つかりませんでした。基準版の同一性、指定版と記録、未知の版の拒否などの既存テストは成功しています。
- 記録済みの過剰抽出・伝聞抽出率の低さは、既知の品質課題として扱いました。意図的な2段階化そのものを不具合とはしていません。
- フェーズ4以降の目標、自発行動、検索、配信システム、音声入力の未実装は対象外です。

---

フェーズ3完了はまだapproveできません。前回の部分保存・書き込みロック等は修正されていますが、完了条件2に直接反する訂正波及の欠落が残っています。

## 対象（2026-09-08 再レビュー）

HEAD `3277611` 上の未コミット修正を対象に、前回7件への対応、`docs/review/claude/phase3_review_response.md`、関連テストと呼び出し元を再確認しました。AGENTS.md、README.md、フェーズ3の設計・完了条件、実装記録・ISSUE-016も照合しました。開始時からソース・テスト・両レビュー文書に未コミット変更があり、新規 `backend/tests/test_review_phase3.py` も存在していました。それらは保持し、本節だけを追記しています。

実行した確認：

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase3-rereview-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider`：**170件成功**。テストの一時DBを使用。
- backend：`.venv/bin/ruff check app tests --no-cache`：成功。
- frontend：`npx tsc --noEmit`：成功。`npm test -- --no-cache`：**16件成功**。
- `git status --short`、`git log`、`git diff` とファイル読み取りで修正範囲を確認。

ソース・設定・通常利用のDBには変更を加えていません。追加の不具合再現コードは実行していません。テスト成功と、以下のコード確認による指摘は区別します。

### 前回7件の対応評価

| 前回 | 評価 | 根拠・限界 |
| --- | --- | --- |
| 1. 最後の話者への一括帰属 | 対象経路は修正済み | 話者を読み込み、根拠発言から候補の対象・範囲を決める。複数話者の名前とAの根拠発言への帰属を確認する追加テストが成功。複数話者では状態抽出を見送る方針は、誤帰属防止の意図的な制限として扱う。 |
| 2. 訂正波及・復元 | 一部のみ修正 | 復元から `mark_for_review` を呼ぶ修正はコードで確認。自動抽出状態への波及は未修正で、対応記録もその旨を明記している。 |
| 3. 状態作成時の範囲 | 対象経路は修正済み | 明示指定を必須化し、根拠の不存在・範囲拡大・非公開から公開への変更を拒否する。範囲省略と範囲拡大を拒否する追加テストが成功。 |
| 4. 状態抽出失敗時の部分保存 | 修正済み | 抽出結果をセッションに追加せず、全段階成功後に保存する。失敗時rollbackも追加。候補ありの失敗で保存ゼロ、再試行成功を確認する追加テストが成功。 |
| 5. 3回目の推論中の書き込みロック | コード上は修正済み | 書き込みを全推論の後へ移動。ただし追加テストのgateは最初のモデル呼び出しで停止するため、以前問題だった3回目の待機を直接再現するテストにはなっていない。 |
| 6. 人手未判定の通過扱い | 一部修正、集計に新たな欠落 | 人手だけの成功シナリオを総数から外し、振り返りの確認項目・属性も表示する。一方、モデル失敗を人手専用と誤分類する経路と、詳細欄の「通過」表示が残る（指摘3）。 |
| 7. 日付の基準 | 方針は修正、UTC補完が不足 | 発言ごとの日付を付ける修正と指示変更を確認。ただしSQLiteから読んだnaive日時の変換に問題がある（指摘2）。 |

対応記録冒頭の「7件とも修正しました」は、同じ文書の指摘2の「直していません」と矛盾します。`docs/issues/issues.md` のISSUE-016も現在は「完了」、`docs/result/phase3.md:20` も訂正条件が○のままです。残課題として記録する判断は説明できますが、設計3Bの実装内容5と完了条件2から除外する合意はなく、完了の根拠にはできません。根拠を画面で選べるようにするまで待つことだけが解決策ではなく、採用時の関連付けや一時的な再評価・参照保留でも不変条件を守れます。

## 完了条件の評価

| 条件 | 評価 | 根拠 |
| --- | --- | --- |
| 1. 複数セッション・再起動後の約束・共有経験 | 確認できない | 既存の保存・参照テストは成功。実際の抽出・採用・再起動を通す評価は追加されていない。 |
| 2. 訂正後に古い好み・予定を現在の事実にしない | 満たしていない | 自動抽出状態への訂正波及が未実装。復元の修正だけでは満たさない。 |
| 3. 架空の共有経験を作らない | 確認できない | 今回は実モデル未実行。人手評価の根拠を含む完了確認も追加されていない。 |
| 4. 自分の好み・価値観を保持 | 確認できない | 分離実装の回帰は成功するが、状態抽出から継続会話までの品質検証は未追加。 |
| 5. 記憶・人格版・可変状態を追跡 | 満たしている | 記録機構の変更はなく、関連回帰テストも成功。 |
| 6. 別人・非公開情報の不適切な参照を防止 | 確認できない | 前回の具体的な帰属・範囲拡大の経路は改善。複数話者から抽出・採用し、各人の次の会話まで通す確認は未追加。 |
| 7. 文字会話・読み上げの継続 | 確認できない | バックエンド・フロントのモック回帰は成功。実Ollama・VOICEVOX・ブラウザの通し確認は未実施。 |

## 指摘事項

### 1. [高] 自動抽出した状態に記憶の訂正・削除が依然として届かない

- 場所：`backend/app/api/conversations.py:228`、`:239`、`backend/app/agent/character_state.py:155`。
- 確認区分：コード確認のみ。
- 発生条件：会話から記憶候補と状態候補を採用し、その後に記憶を訂正・削除する。
- 影響：状態は会話IDだけを持ち、再評価が検索する `basis_memory_ids` を持たない。古い内容の状態がactiveのまま次のプロンプトに渡る。前回指摘2の主要部分が残り、完了条件2に反する。
- 修正方針：採用時に根拠を関連付けるか、会話単位で再評価するなど、参照中の状態に訂正が必ず届く仕組みにする。未対応の間は対象状態の利用を保留するなど、古い情報を渡さない措置が必要。画面改善への先送りだけでは解消しない。

### 2. [中] 発言日時のUTC補完がなく、日本時間の発言日を前日として渡す

- 場所：`backend/app/agent/reflection.py:310`、`backend/app/schemas.py:14`。
- 確認区分：コード確認のみ。
- 発生条件：OSのタイムゾーンがAsia/Tokyoで、SQLiteにUTC保存された発言日時を再読込する。例えば9月8日00:30 JSTの発言は、DBから9月7日15:30のnaive日時として返る。
- 影響：naive日時に直接 `astimezone(LOCAL_TZ)` を呼ぶため、UTCではなくOSローカル時刻として解釈する。例では発言日を9月7日と渡し、「昨日」などの基準日が1日ずれる。API出力用のUtcDatetime補完は、このORM値を使う処理には適用されない。実モデルが返す日付は未確認だが、入力日付の変換ロジックに欠落がある。
- 修正方針：naive日時には保存形式のUTCを補ってからLOCAL_TZへ変換する。UTC日付と日本日付が異なる発言をDBへ保存・再読込し、OSタイムゾーンに依存せず正しい発言日が出ることを確認する。

### 3. [中] モデル失敗でチェック未実行のシナリオが人手専用に分類され、集計から消える

- 場所：`backend/app/evaluation/runner.py:108`、`:264`、`backend/app/evaluation/report.py:55`、`:94`。
- 確認区分：コード確認のみ。
- 発生条件：機械チェックを持つシナリオの全試行が、チェックを作る前の最初のLLM呼び出しで失敗する。
- 影響：失敗したTurnResultにはchecksがなく、`human_only` がtrueになる。レポートはそのシナリオを「人手（未判定）」として自動判定の分母から除外し、接続障害等による失敗を成績から落とす。また、本当に人手専用の成功試行は総括が未判定でも詳細見出しが引き続き「通過」になる。
- 修正方針：機械判定の有無は、実行できたチェック数ではなくシナリオの宣言から決める。実行失敗を未判定と区別して集計し、人手専用の詳細見出しも未判定へ統一する。機械チェックあり・全試行LLMError、人手専用・応答成功の両方をテストする。

## 評価用会話で測っていない観点

前回挙げた記憶生成から再起動後まで、実際の訂正APIから状態・次の回答まで、状態抽出の品質、複数話者から保存・参照までの評価シナリオは未追加です。対応記録も追加していないと明記しています。まず条件1・2を通す評価を追加するという優先順位には同意します。

追加されたテストの限界も区別します。`test_other_conversations_can_write_while_reflecting` のgateはFakeLLMの各呼び出しの先頭で待機し、最初の「拾う」段階で別の書き込みを行います。したがって、この成功だけを前回の「候補flush後、3回目でロック保持」の再現・解消の証拠にはできません。3回目だけを停止させ、その間の別書き込みが完了することを確認するのが必要です。コード自体は書き込みを後ろへ移動しており、今回その修正を否定しているわけではありません。

日時テストも当日の日付文字列と区切りの有無を確認しており、日またぎ・UTCとJSTの日付差を検証していません。評価器修正については、機械判定の全失敗と人手未判定の分類を追加で固定する必要があります。

## 対象外・未確認

- 実Ollama・VOICEVOX・実ブラウザは使用していません。対応記録の20/22、伝聞2/2、修正を戻した場合のテスト失敗は、今回は独立検証していません。
- マイグレーションは今回変更されていないため、新しい往復実験は行っていません。既存の移行テストは今回も成功。前回の一時空DBでの往復確認と、実データ入り往復の未確認を引き継ぎます。
- 複数プロセス・実プロセス再起動・追加の障害注入は未実施。既存テストが成功した範囲を超えて確認済みとはしていません。
- フェーズ4以降の未実装機能は判定対象に含めていません。

---

フェーズ3完了はまだapproveできません。日時・評価集計の前回指摘は修正されていますが、訂正波及が抜ける採用順序と状態の再有効化を、一時DBで再現しました。

## 対象（2026-09-08、全体レビュー後の再々レビュー）

HEAD `3277611` 上の未コミット修正と、Claudeの「フェーズ3 再レビューへの対応」を確認しました。AGENTS.md、README.md、設計書の3B・3Cと完了条件、根拠変更の設計、実装記録、ISSUE-016、前回レビューを照合しています。開始時から存在する未コミット変更は保持し、リポジトリ内で今回変更したのは本節の追記だけです。

実行結果：

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase3-third-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider`：**175件成功**。
- backend：`.venv/bin/ruff check app tests --no-cache`：成功。
- frontend：`npx tsc --noEmit`、`npm test -- --no-cache`：成功、**16件成功**。
- 追加の一時テスト：`/private/tmp/yui_phase3_order_review_test.py` を作り、既存の `tests.conftest` の一時DB・モックLLM・APIクライアントを使用。backendから `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase3-order-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/yui_phase3_order_review_test.py -q` を実行。下記2件とも、期待する `needs_review=True` に対してFalseとなり失敗した。
- 一時テスト初回は削除APIの成功コードを204と誤記して停止したため、実際の仕様の200へ修正して再実行した。以下の再現結果は修正後の実行に基づく。

ソース・設定・通常利用のDB・会話・記憶は変更していません。実モデルは使っていません。

### 前回3件への対応評価

| 前回指摘 | 評価 | 確認できたこと |
| --- | --- | --- |
| 自動抽出状態への訂正波及 | 改善したが未完了 | 記憶1件を先に採用する場合と、記憶ゼロの時点で状態を採用する場合の追加テストは成功。複数候補の一部だけを採用した場合は指摘1のとおり抜ける。 |
| UTC補完 | 修正済み | `to_local` がnaive日時にUTCを補い、振り返り・記憶表示・評価の候補日付表示で使用される。UTCとJSTの日付が異なる値の追加テストが成功。 |
| モデル失敗を人手専用として除外 | 対象経路は修正済み | 判定種別をシナリオ宣言から決定し、失敗試行数も表示する。全試行LLMErrorの追加テストが成功。人手専用の詳細見出しも未判定に変更されている。 |

対応記録の訂正と、実装記録の完了条件2を△へ変更した点は妥当です。ただし「3件とも修正」は、訂正波及の全経路が解消したという意味ではまだ成立しません。会話由来の根拠を自動で列挙したことと、根拠が完全に特定できたことを区別する必要があります。

## 完了条件の評価

| 条件 | 評価 | 根拠 |
| --- | --- | --- |
| 1. 複数セッション・再起動後の約束・共有経験 | 確認できない | 永続化と別会話参照の回帰は成功。実際の抽出・採用・プロセス再起動まで通す評価は未追加。 |
| 2. 訂正後に古い好み・予定を現在の事実にしない | 満たしていない | 今回、根拠削除後も状態がactive・needs_review=Falseになる2経路を再現した。 |
| 3. 架空の共有経験を作らない | 確認できない | 人格・プロンプトの抑制は維持されるが、今回実モデルの意味的評価は未実施。 |
| 4. 自分の好み・価値観を保持 | 確認できない | 状態と固定人格の分離は維持。実際の状態生成から継続会話までの品質評価は未追加。 |
| 5. 記憶・人格版・可変状態を追跡 | 満たしている | 記録機構と関連回帰は引き続き成功。根拠の網羅性は条件2の別問題。 |
| 6. 別人・非公開情報を不適切に参照しない | 確認できない | 前回修正した具体的な帰属・作成時範囲制限は維持。複数話者の採用後まで通す検証は未追加。 |
| 7. 文字会話・読み上げの継続 | 確認できない | モックの回帰は成功。実Ollama・VOICEVOX・ブラウザの通し確認は未実施。 |

## 指摘事項

### 1. [高] 記憶候補を一部だけ採用した時点で状態を採用すると、後から採用した記憶への訂正が届かない

- 場所：`backend/app/api/states.py:132`、`backend/app/agent/character_state.py:170`。
- 確認区分：実行して再現。
- 発生条件：同じ会話に記憶候補M1・M2と状態Sがあり、M1→S→M2の順に採用してからM2を訂正・削除する。
- 再現手順：一時DBで会話を作り、モック抽出に「写真が好き」「紅茶が好き」の2記憶候補と「紅茶が好きな相手と紅茶の話を楽しむ」という関係性候補を返す。写真の記憶を採用→状態を採用→紅茶の記憶を採用→紅茶の記憶を削除→active状態を取得する。
- 観測結果：状態採用時の `basis_memory_ids` は先に採用された写真の記憶だけ。その後に紅茶の記憶を削除しても、状態はactiveのままで `needs_review=False`。追加テスト `test_partial_adoption` のTrue期待が失敗した。
- 影響：状態の内容を支える情報を削除しても、古い状態が次のプロンプトの参照対象になる。条件2を満たさない。今回追加された既存テストは候補1件であり、「全件採用済み」と「1件も未採用」の間にある順序を検出しない。
- 修正方針：会話単位で自動付与した記憶IDを、完全に特定した根拠とみなさない。会話由来の保守的な再評価を維持するか、根拠発言・候補を追跡し、後から記憶が採用されても関連を補完する。明示的に選んだ根拠と自動で暫定列挙した根拠を区別する。

### 2. [中] 撤回中に根拠が削除された状態を、再評価なしで有効に戻せる

- 場所：`backend/app/agent/character_state.py:165`、`backend/app/api/states.py:159`。
- 確認区分：実行して再現。
- 発生条件：根拠記憶を持つ状態をwithdrawnにする→根拠を訂正・削除する→状態をactiveへ戻す。
- 再現手順：一時DBのAPIで「紅茶が好き」という記憶と、それを根拠にした関心を作成・採用。状態を `PATCH {status:"withdrawn"}` で撤回し、記憶を削除。その後 `PATCH {status:"active"}` だけで再有効化する。
- 観測結果：各APIは成功し、再有効化した状態は `needs_review=False` のまま。`reviewed:true` も内容修正も指定していない。追加テスト `test_withdrawn_state_reactivation` のTrue期待が失敗した。
- 影響：再評価処理はactive・pendingしか検索せず、再有効化も根拠変更を検証しない。削除済み根拠の古い内容が再びプロンプトへ渡る。条件2の迂回経路になる。
- 修正方針：撤回中にも根拠変更の印を保持するか、再有効化時に根拠の有効性・変更版を検証し、変更済みなら再評価状態にする。単なるstatus変更を、根拠と内容の確認完了とは扱わない。

## 評価用会話で測っていない観点

前回までの空DBからの記憶生成・採用・再起動、実訂正APIから次の返答まで、状態抽出の品質、複数話者の保存・採用・参照をつないだ確認は引き続き必要です。今回の2件は、モデル品質の評価以前に制御テストで確認できる経路です。

採用順序のテストは、記憶1件を前後させるだけでなく、複数記憶の一部採用、状態の撤回・再有効化を含めてください。印を確認するだけでなく、次の会話の実行記録・system_promptに対象状態が入らないことまで確認すると、完了条件2との対応が明確になります。

3回目の推論だけを停止させる同時書き込みテストは未追加です。今回の既存175件成功を、その未検証経路までの実行証拠とは扱っていません。

## 対象外・未確認

- 今回の再現で実際に確認したのはAPI操作後の状態と再評価印です。実モデルが古い内容を発言するところは実行していません。active・needs_review=Falseの状態を参照するコードと区別して評価しています。
- 実Ollama・VOICEVOX・実ブラウザ、実モデル評価値20/22等、実プロセス再起動、複数プロセスは未確認です。
- マイグレーションに今回の変更はなく、往復を再実行していません。既存の移行テストは成功しています。
- 一時再現テストは `/private/tmp/yui_phase3_order_review_test.py` に置き、プロジェクトのテスト・ソースは編集していません。
- フェーズ4以降の未実装機能は対象外です。

---

フェーズ3完了はまだapproveできません。前回再現した2件は解消しましたが、既存状態の移行と不正な抽出出力の処理に残件があり、完了条件1・2の通し評価も未追加です。

## 対象（2026-09-08、全体レビューから第5回）

HEAD `3277611` 上の未コミット修正を再レビューしました。AGENTS.md、README.md、フェーズ3の完了条件・根拠変更の設計、Claudeの「再々レビューへの対応」、実装記録・課題記録を確認。今回特に `basis_is_provisional` の追加、状態再有効化、マイグレーション `71e361defdda`、抽出パーサーの追加変更と関連テストを確認しました。

開始時の未コミット変更は保持しています。リポジトリ内の変更は本レビューへの追記だけです。ソース・設定・通常利用のDB・会話・記憶は変更していません。

実行結果：

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase3-fourth-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider`：**179件成功**。既存の一時DB・モックを使用。
- backend：`.venv/bin/ruff check app tests --no-cache`：成功。
- frontend：`npx tsc --noEmit`、`npm test -- --no-cache`：成功、**16件成功**。
- 前回の `/private/tmp/yui_phase3_order_review_test.py` を同じ一時DB設定で再実行：**2件成功**。一部採用順序と撤回後の再有効化で、どちらも `needs_review=True` になった。
- `mktemp -d /private/tmp/yui-phase3-fourth-migration.XXXXXX` 内のDBを `YUI_DATABASE_URL` に指定し、`.venv/bin/alembic upgrade head` → `check` → `downgrade 3187f6b349d3` → `upgrade head`：成功。head `71e361defdda`、モデルとの差分なし。空DBの構造確認であり、既存状態の意味的な移行を保証する試験ではない。
- 新しい一時テスト `/private/tmp/yui_phase3_pickup_review_test.py` を、backendから `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/yui_phase3_pickup_review_test.py -q` で実行：1件失敗。指摘2の不正部分の黙示的な欠落を再現した。このテストはパーサーだけを呼び、DBには接続しない。

### 前回への対応評価

| 項目 | 評価 | 根拠 |
| --- | --- | --- |
| 一部採用後の訂正波及 | 新規に採用する状態では解消 | 自動列挙の根拠に暫定フラグを付け、会話単位でも再評価する。前回の独立再現テストと追加回帰が成功。既存行への適用には指摘1が残る。 |
| 撤回中の根拠削除と再有効化 | 対象経路は解消 | withdrawnも再評価対象に含め、再有効化時に無効な根拠を確認。前回の独立再現テストが成功。 |
| 3回目のモデル待機中の書き込み | 実行確認済み | 追加された `test_third_model_call_does_not_hold_the_write_lock` が成功。指示文の並びを確認して状態抽出だけを停止し、その間に別の記憶を保存している。 |
| UTC変換・評価集計 | 前回の修正を維持 | 関連回帰が引き続き成功。 |

前回の具体的な2件が修正されたという対応記録は、新しく作成・採用する状態について、今回の独立した再実行でも裏付けられました。記録どおり、実モデル・音声・ブラウザの通し確認と、完了条件1・2の評価シナリオはまだ追加されていません。

## 完了条件の評価

| 条件 | 評価 | 根拠 |
| --- | --- | --- |
| 1. 複数セッション・再起動後の約束・共有経験 | 確認できない | 保存と別会話参照の回帰は成功。抽出から採用・実プロセス再起動までの評価は未追加。 |
| 2. 訂正後に古い好み・予定を現在の事実にしない | 満たしていない | 新規状態で前回の2経路は解消したが、旧版が自動付与した根拠を持つ既存状態には指摘1が残る。通常利用DBに該当行があるかは未確認。 |
| 3. 架空の共有経験を作らない | 確認できない | 今回実モデルの意味的評価は未実施。 |
| 4. 自分の好み・価値観を保持 | 確認できない | 分離実装の回帰は成功。状態抽出から継続会話までの品質評価は未追加。 |
| 5. 記憶・人格版・可変状態を追跡 | 満たしている | 既存の実行記録に加え、暫定フラグも状態・変更履歴に残る。 |
| 6. 別人・非公開情報を不適切に参照しない | 確認できない | 以前の具体的な帰属・範囲制限の回帰は成功。複数話者の抽出・採用後の継続会話まで通す評価は未追加。 |
| 7. 文字会話・読み上げの継続 | 確認できない | モック回帰は成功。実Ollama・VOICEVOX・ブラウザ結合は未実施。 |

## 指摘事項

### 1. [中] 既存の自動付与された根拠を確定扱いで移行し、前回の訂正波及漏れを残す

- 場所：`backend/alembic/versions/71e361defdda_状態の根拠が暫定かどうかを持つ.py:29`、`backend/app/agent/character_state.py:184`。
- 確認区分：コード確認のみ。
- 発生条件：前回版でM1採用→状態採用→M2採用を行ったDBを、今回のマイグレーションで更新し、その後M2を訂正・削除する。
- 影響：新列は既存行すべてFalseで追加される。旧版が自動列挙した `[M1]` も確定根拠扱いになり、会話単位の再評価から除外される。新しい状態には効く修正が、すでに採用した状態には届かず、条件2の漏れが残る。通常利用DBにこの行が実在するとは断定していない。
- 修正方針：会話由来で自動付与の可能性がある既存行を保守的に暫定扱い、または要再評価とする。作成・採用履歴で区別できる場合は利用する。区別できないことを理由に確定扱いへ寄せない。旧版の自動根拠付き行を含む移行テストで、その後の訂正が届くことを確認する。

### 2. [中] 壊れた抽出出力の一部を黙って捨て、残りだけを成功として返す

- 場所：`backend/app/agent/reflection.py:193`、`:198`、`:201`。
- 確認区分：実行して再現。
- 発生条件：拾い出し出力の全体が不正JSONで、読み取れる内側配列と読み取れない内側配列が混在する。
- 再現手順：`_parse_pickups('[["写真が好き", "#1"], ["紅茶が好き", broken]]')` を呼び、`ReflectionParseError` を期待する一時pytestを実行。
- 観測結果：例外は発生せず、`[('写真が好き', 1)]` だけを返した。紅茶の項目は欠落し、例外期待のテストが失敗した。
- 影響：フォールバックの `except JSONDecodeError: continue` が不正部分を捨てる。次段階へ不完全な拾い出し結果を成功として渡し、後段も成功すれば振り返りが完了するため、形式エラーに起因する欠落を再試行できない。実際の終了APIまでの実験は行っておらず、そこは呼び出しコードからの判断。
- 修正方針：複数配列形式を許容する場合も全入力の読み取りを確認し、一部が解析不能なら失敗または明示的な再試行とする。「読める別形式の許容」と「壊れた部分の破棄」を分ける。追加の寛容化自体を否定する指摘ではない。

## 評価用会話で測っていない観点

3回目の推論中の書き込み検証は今回埋まりました。一方、実装記録・対応記録が認めるとおり、条件1・2の通し評価は未追加です。完了承認には、空DBから約束・経験を抽出して採用し、再起動後の別会話で参照する確認と、実際の訂正・削除後に記憶・状態・次のプロンプト／返答が更新される確認が必要です。

今回追加で必要なのは、旧版の自動根拠付き状態を移行した後の訂正と、拾い出しの一部だけが壊れた出力です。既存179件の成功を、これら未収録の経路まで成功した証拠とは扱えません。

## 対象外・未確認

- 実Ollama・VOICEVOX・ブラウザは未使用。過去の実モデル成功率・人手評価は独立再測定していません。
- 通常利用DBの状態行は調査・変更していません。移行の指摘はコード確認であり、既存データ入りの移行再現は未実施です。
- 複数プロセス・実プロセス再起動は未確認。マイグレーションの空DB往復と前回不具合の一時DB再試験は、上記の範囲で実行確認済みです。
- 一時テストは `/private/tmp` に置き、プロジェクトのテスト・ソースは変更していません。フェーズ4以降の未実装機能は対象外です。
