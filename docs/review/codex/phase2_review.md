# フェーズ2 コードレビュー

**結論：基本的な接続は成立していますが、現時点でフェーズ2を完了と判断することは勧めません。** 会話切り替え後の応答混入、アンマウント後の再生開始、再生状態の競合・終了通知漏れを確認しました。通常の停止と音声取得の追い越し対策は機能していますが、ライフサイクル全体では完了条件を満たし切れていません。

レビュー日：2026-09-07

対象：`a4895d4..1bee0cf` の8コミット、および現在の関連実装・テスト。AGENTS.md、README.md、設計書2点、実装記録、既知の課題を確認しました。ソースコードと通常利用のDB・会話・記憶は変更していません。

## 指摘事項

### 1. [高] 会話を切り替えた後、以前の会話の遅延応答が現在の画面へ混入します

- 場所：[frontend/src/components/ChatPanel.tsx](../../../frontend/src/components/ChatPanel.tsx) 86〜92行目、[frontend/src/App.tsx](../../../frontend/src/App.tsx) 73〜85・134〜139行目、[frontend/src/components/HistoryPanel.tsx](../../../frontend/src/components/HistoryPanel.tsx) 69〜70行目。
- 確認区分：**実コードを模擬フロントエンド環境で実行して再現**。実ブラウザでの操作・発音は未確認です。
- 発生条件：会話Aで送信し、LLM応答を待つ間に履歴から会話Bを開き、その後Aの応答が返る。送信中でも履歴の切り替えは可能です。
- 再現手順：`App` と `ChatPanel` をTypeScriptから変換して実行し、Reactのフック・APIを模擬化。Aの `api.chat()` を保留し、Bの履歴を読み込んでからAの応答を解放しました。
- 観測結果：会話IDはAへ戻る一方、表示メッセージは **Bの履歴＋Aの質問＋Aの遅延返答** となりました。またAの返答への音声取得が要求されました。実際に音が鳴るかは、その後の会話ID変更による停止と取得完了の順序にも依存します。
- 具体的な影響：表示している履歴と、次の送信先となる会話IDが食い違います。Bを続けるつもりの発言をAへ送るおそれがあります。既存のDB行が直接別会話へ移される問題ではありません。
- 関連するコード上の経路：履歴取得にも最新要求の識別がないため、複数の履歴を続けて開くと遅い応答が最後の選択を上書きできます。会話終了の応答も切り替え後の画面を無条件で終了扱いにします。この2経路はコード確認のみです。
- 修正方針：表示対象の世代を管理し、送信・履歴取得・終了の結果を、開始時の会話と世代が現在も一致するときだけ反映する。自動再生にも同じ判定を適用する。新規会話への初回ID付与と、ユーザーによる切り替えは区別してください。

### 2. [中] 音声取得中にアンマウントすると、後から音声の再生が始まります

- 場所：[frontend/src/useSpeechPlayer.ts](../../../frontend/src/useSpeechPlayer.ts) 78〜100・174〜187・241行目。
- 確認区分：**実コードを模擬環境で実行して再現**。Audioはモックであり、実音は出していません。
- 発生条件：音声の取得が完了する前に、このフックを所有するコンポーネントがアンマウントされる。ページ全体の破棄ではなく、同じドキュメント内でのアンマウントが対象です。
- 再現手順：`play()` の音声取得を保留し、登録されたeffectのcleanupを実行。その後、音声取得を成功させました。
- 観測結果：cleanup後に `Audio.play()` が1回呼ばれ、そのAudioには `pause()` が呼ばれませんでした。対照として、同じ取得中に `stop()` を呼んだ場合は再生回数が0でした。
- 原因と影響：cleanupは `teardown()` のみで、`stop()` が行う要求番号の更新をしません。取得後の古さ判定を通過し、所有元が消えた後に新しいAudioとURLが作られます。その後のcleanupもないため、操作できない再生が残り得ます。通常の会話切り替えではフック自体はアンマウントされないため、この指摘と前項は別の経路です。
- 修正方針：アンマウント時にも保留中の要求を無効化し、非同期処理の再開後に破棄済みでないことを確認する。`AudioContext.resume()` の後にも同じ確認を行い、破棄後に解析ノードやアニメーションフレームを登録しないようにする。

### 3. [中] 同時に届いた再生通知で、完了済みの状態が再生中へ戻ります

- 場所：[backend/app/api/conversations.py](../../../backend/app/api/conversations.py) 330〜342行目、[backend/app/agent/delivery.py](../../../backend/app/agent/delivery.py) 20〜36行目。通知元：[frontend/src/useSpeechPlayer.ts](../../../frontend/src/useSpeechPlayer.ts) 68〜75行目。
- 確認区分：**一時DBと実APIで再現**。競合順序を固定するため、メッセージ読込直後に待機する処理をテスト用にメモリ上で差し込みました。
- 再現手順：生成済みの返答について、`playing` 通知のDB読込直後で待機させる。別リクエストで `completed` を通知してコミットした後、先の `playing` を再開する。
- 観測結果：両方ともHTTP 200。最終的なDBの状態は **`playing` かつ `delivery_finished_at` が設定済み** でした。
- 原因と影響：終端状態の判定は各セッションが読み込んだオブジェクト上で行われ、更新時のDB状態を条件にしていません。直列での重複・逆順通知は防げても、古い状態を読み込んだ並行リクエストは終端状態を上書きできます。単一プロセスでも発生し、既知の複数プロセス制約とは別です。フロントは通知を待たずに送るため、短い再生や直後の停止では通知が並行し得ます。
- 修正方針：許可された現在状態をWHERE条件に含める原子的な更新などで、DB上の遷移を保証する。更新後は確定した行を返す。フロント側も遅れて返った通知レスポンスで新しい表示状態を上書きしないようにする。後者はコード確認のみです。

### 4. [中] 再生開始後のエラーで、記録が再生中のまま残ります

- 場所：[frontend/src/useSpeechPlayer.ts](../../../frontend/src/useSpeechPlayer.ts) 197〜204・233・241行目。
- 確認区分：**実コードを模擬環境で実行して再現**。実ブラウザのデコード障害は発生させていません。
- 発生条件：`audio.play()` が成功して `playing` を通知した後、Audioにエラーイベントが発生する。
- 再現手順：音声取得と `play()` を成功させ、開始通知を確認してから `audio.onerror` を呼び出しました。
- 観測結果：再生器はAudioを片付けて表示状態を解除しましたが、通知は **`playing` の1件だけ** で、`aborted` は送られませんでした。
- 具体的な影響：音声は止まっていても、保存された状態は再生中、終了時刻は未設定のままになります。再生中のアンマウントも `teardown()` のみなので、同様に終了通知がありません。このアンマウント時の記録漏れはコード確認です。
- 修正方針：開始済みの再生が異常終了・アンマウントで終わった場合も、中断通知を送る共通処理へまとめる。開始前の失敗では開始時刻を捏造しないこと。また現状は通知の通信失敗も黙って捨てるため、記録の保証範囲を明示し、必要なら未送信通知の再試行・再同期を設ける。再試行は前項の原子的な遷移と組み合わせてください。

## 完了条件と実装記録の評価

| 観点 | 評価 |
| --- | --- |
| 文字入力→返答生成→音声 | 実Ollama・VOICEVOXと一時DBで、API経由の返答・WAV取得まで成功。Reactの接続先と呼び出し経路も確認。実ブラウザの発音は今回未確認 |
| フェーズ1の人格・記憶 | 既存経路を継続利用し、関連回帰テストは成功。会話画面の切り替えには指摘1が残る |
| 重複再生しない・停止できる | 模擬環境では停止中の取得結果破棄、後続取得による先行取得の無効化は成功。アンマウントと会話切り替えは不十分 |
| 字幕と読み上げの一致 | 通常経路では同じメッセージ本文を使用。別の文章へ要約する処理はない。実際の発音・字幕の同期は未確認で、指摘1の会話混入も先に解消すべき |
| 音声エンジン障害 | HTTPエラー・接続失敗・タイムアウト等をSpeechErrorへ変換し、音声APIを503にする経路を既存テストで確認。全体healthは音声障害で失敗扱いにしない。実エンジンを途中停止させる試験は未実施 |
| Web Audio | Audio要素は再生ごとに新規作成し、その要素へのsource作成は1回。contextがrunningになる前は接続せず、destinationへの接続を解析より先に行う意図は妥当。ただし実ブラウザのsuspended復帰・自動再生制限・接続例外は未確認。非同期resumeと破棄の競合にも注意が必要 |
| DB・移行 | 旧形式からの移行を含む既存テスト成功。一時DBにheadを適用した後のalembic checkも差分なし。再生状態の並行更新には指摘3が残る |
| UtcDatetime | 出力スキーマでnaive日時にUTCを補う変更。入力のMemoryCreate/Updateは従来のdatetimeのままで、DB内の比較処理もこのvalidatorを通らない。今回の変更による入力・比較処理の新たな不具合は確認していない |

`docs/result/phase2.md` の構成説明、人格・記憶経路の継続利用、合成計測の保存は実装と対応しています。文字入力の継続、OnAirの方式だけを参考にした自前実装は、現在の設計・実装記録に明示された選択であり、不具合と扱いません。

一方、「同時に鳴らすのは1つ」を「記録の側からテストで固定」しても、フロントの非同期再生制御までは保証できません。「すでに話し終えた発言への通知は記録を動かさない」も、現在は並行更新に対して成立しません。実装記録の完了判定には、今回の再現結果を反映する必要があります。

ISSUE-011（生成・中断した返答の履歴／振り返り上の扱い）、ISSUE-012（待ち時間）、フロントのテスト基盤未整備、既知の素材差分は再指摘していません。未実装の音声入力・検索・配信・バックエンド発話キューをフェーズ2の欠陥として数えていません。

## 実施した検証と限界

- **pytest：82件すべて成功。** 一時DBを利用する既存テストを実行し、環境変数のDB接続先も一時パスに設定。pytestキャッシュとPython bytecodeの生成を抑止しました。
- **ruff、TypeScript型チェック、Viteビルド：成功。** ビルド先は一時ディレクトリに変更。通常の `npm run build` 全体ではなく、型チェックとViteを個別に実行しました。
- **alembic check：差分なし。** headへ移行した一時DBで確認しました。
- **追加再現：** リポジトリ外の一時スクリプトで、フロントの実コードを変換して模擬フック・Audio・APIとともに実行。DBの競合は一時DBとASGI経由の実APIで実行しました。恒久テストは追加していません。
- **実サービス：** Ollama `qwen3.5:9b`、VOICEVOX `0.25.2`・話者59でhealth成功。一時DBで「こんにちは。一文で挨拶してください。」を送信し、返答200・初期状態generated、音声200・170,540バイト・WAV約3,552msを確認。speech_runsは1件で、query 10ms・synthesis 725ms等が保存されました。この一例は総合的な生成品質・待ち時間の評価ではありません。
- **未確認：** 実ブラウザ上の連続操作・音の重なり・実スピーカー出力、口パクとまばたきの見た目、字幕の実時間同期、各ブラウザのWeb Audio挙動、実エンジンを停止／再起動する障害試験。実装記録にあるブラウザ検証や過去の時間計測を今回独立に追認したわけではありません。

まず指摘1〜4を修正し、対応する競合・ライフサイクルの検証と実ブラウザでの停止／切り替え確認を行ったうえで、完了を再判定するのが妥当です。

---

## 2回目のレビュー：指摘への対応の再評価

**結論：修正方針と回帰テストの追加は妥当ですが、フェーズ2の完了判定は引き続き保留を勧めます。** 前回の主要な再現経路は解消されています。一方、履歴の読み込み中に送信する経路で会話が混ざることと、通知レスポンスの逆転で画面上の再生状態が戻ることを追加検証で確認しました。

レビュー日：2026-09-07

対象：コミット `49e3520`、[Claudeの対応記録](../claude/phase2_review_response.md)、現在の関連実装・テスト。以下の行番号はこのコミット時点です。

### 前回の指摘への評価

| 前回の指摘 | 評価 |
| --- | --- |
| 1：会話切り替え後の遅延応答混入 | 送信後に別の履歴へ切り替える従来の経路は、世代判定と追加テストで解消を確認。ただし、履歴取得中の送信は同じ世代になるため、後述の別経路が残る |
| 2：アンマウント後の再生開始 | 解消を確認。要求番号更新・破棄判定により、取得中のアンマウント後は再生されない。`play()` 解決後の個別破棄と `resume()` 後の判定もコード上妥当 |
| 3：DBの終端状態が並行通知で戻る | DB側は解消を確認。現在状態を条件に含むUPDATEと、古いオブジェクトを保持した別セッションによるテストは適切。ただし、レスポンスの到着順に対する画面側の対策は残る |
| 4：異常終了・アンマウント時の中断通知漏れ | 解消を確認。`finishPlayback()` にまとめられ、開始後のエラーとアンマウントで中断通知が出ることをテストで確認。通信失敗による通知欠落は別途ISSUE-013として明記されている |

### 残る指摘1：[高] 履歴の読み込み中に送信すると、別会話への送信と履歴混入が発生します

- 場所：[frontend/src/App.tsx](../../../frontend/src/App.tsx) 85〜99・163〜166行目、[frontend/src/components/ChatPanel.tsx](../../../frontend/src/components/ChatPanel.tsx) 86〜100・211〜228行目。
- 確認区分：**一時コピーの実コンポーネントをVitest・happy-domで実行して再現**。APIはモック。実ブラウザ・実DBへの誤送信は行っていません。
- 発生条件：履歴Bを開いて詳細取得を待っている間に、入力して送信する。読み込み中でも入力欄と送信ボタンが有効で、`send()` 内にも `loading` の判定がありません。
- 再現手順：Bの詳細取得を保留し、「Bへの続き」を送信。その後Bの詳細を返し、最後にチャットの返答を返しました。
- 観測結果：API呼び出しは **`api.chat("Bへの続き", null)`** でした。Bへの追記ではなく、新規会話の作成要求です。返答後、画面にはBの履歴と新規会話の返答が共存し、ヘッダーの会話IDは新規会話へ変わりました。この不正な状態を期待する再現テストが成功しています。
- 原因と影響：`openConversation()` は会話IDを一旦nullにしますが、送信を止めません。履歴取得と送信は同じ世代なので、追加された世代判定はどちらも通過します。ユーザーが選んだ会話と実際の保存先が異なり、その後の送信先も変わります。
- 修正方針：詳細取得が完了して送信先・会話状態が確定するまで、送信ボタン、キーボード送信、`send()` 本体のすべてで送信を禁止する。履歴取得中に送信しようとするケースを回帰テストに追加する。世代判定はそのまま必要です。

### 残る指摘2：[中] 通知レスポンスの到着順によって、画面上の完了状態が再生中へ戻ります

- 場所：[frontend/src/useSpeechPlayer.ts](../../../frontend/src/useSpeechPlayer.ts) 71〜75行目、[frontend/src/App.tsx](../../../frontend/src/App.tsx) 55〜57行目。
- 確認区分：**一時コピーの実フックをVitest・happy-domで実行して再現**。Audioと通知APIを模擬化し、レスポンスの到着順を制御しました。実ネットワークでの遅延は発生させていません。
- 再現手順：再生開始時の通知レスポンスを保留。再生終了イベントを発生させ、`completed` のレスポンスを先に返す。その後、先の `playing` レスポンスを返しました。
- 観測結果：画面へ反映するコールバックは **`completed` → `playing`** の順に呼ばれました。Appは同じメッセージIDならそのまま置換するため、表示が逆戻りします。
- 原因と影響：サーバーが返すのは応答作成時点の確定状態です。開始通知が先にDBへ反映され、そのレスポンスだけ遅れた場合、後で完了したDBが正しくても古いレスポンスは `playing` のまま届きます。今回直ったDBの競合とは別で、音声そのものが再開する問題でもありません。通知欠落を扱うISSUE-013にも該当しません。
- 修正方針：画面へ反映する際も終端状態から戻さない。必要に応じてメッセージ単位の状態バージョンや通知順序を扱い、遅い応答が新しい状態を上書きしないようにする。単に「DBの確定状態を返す」だけでは不十分です。

### 対応記録と残課題の評価

フロントエンドの非同期制御を実際のReactフック・コンポーネントで検証できるようにした点は適切です。既存12件のテストは今回こちらでも成功しました。ただし、会話のテストは「送信→切り替え」の順序を対象にし、「切り替え開始→読込中の送信」は対象外です。また再生テストの通知APIは即時応答するため、レスポンスの逆転を検出しません。

ISSUE-013として、通知が届かない場合の記録欠落を明記し、現段階の制約として残す判断は、ローカル利用の範囲では受け入れ可能です。これ自体を新たな完了阻害事項には数えません。ただし、将来サーバーにキューを移すだけで、ブラウザが実際に再生し終えた事実まで保証できるとは限りません。再生主体をどこに置くかに応じ、実際の出力完了とキュー処理完了を区別して設計する必要があります。

「4件とも修正」という記録は、前回の中心的な再現ケースについては裏付けがありますが、会話切り替えと画面上の状態整合性まで対応完了とするには、上記2件の対応が必要です。

### 検証結果と完了判断の条件

- バックエンド：**85件成功**。一時DBを使う既存テストを実行し、接続先環境変数も一時パスへ設定。旧形式DBの移行テストを含みます。
- フロントエンド：**既存12件成功**。リポジトリ外の一時コピーへ追加した再現2件も成功し、不正な結果を確認しました。追加テストはプロジェクトへ取り込んでいません。
- ruff、TypeScript型チェック、Viteビルド：成功。ビルド出力は一時ディレクトリへ保存しました。
- 実ブラウザの発音、停止、聞き直し、口パク・字幕の同期、実エンジンの障害試験は今回未実施。前回の実Ollama・VOICEVOXによるWAV取得確認と、今回の模擬環境による回帰検証は区別しています。
- ソースコード・通常利用のDBは変更していません。リポジトリ内の変更は、このレビューの追記のみです。

残る2件を解消し、修正後の実ブラウザで「初回自動再生」「再生中／取得中の停止」「履歴切り替え」「聞き直し」「字幕と音声の対応」を確認できれば、既知のISSUE-011〜013を残課題として管理しつつ、フェーズ2を完了して次へ進む判断でよいと考えます。
