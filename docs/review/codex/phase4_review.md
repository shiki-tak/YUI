# フェーズ4 レビュー

## PR1：目標の器 — 第1回レビュー（2026-09-08）

### 結論

PR1の承認前に修正したい不具合を1件確認した。注入した現在時刻のタイムゾーンによって、目標の実行可否が変わる。それ以外には、今回の範囲で追加の不具合は見つからなかった。フェーズ4全体の完了を評価するレビューではない。

### 対象

- `phase4-goal-store` の未コミットのPR1実装：`backend/app/agent/goal.py`、`backend/app/models.py` の目標関連定義、マイグレーション `c667f41b8e3c`、`backend/tests/test_goal.py`、`backend/tests/test_migration.py` の追加部分。
- `AGENTS.md`、`README.md`、`docs/plan/phase4.md` の決定事項とPR1の範囲、設計書の目標・フェーズ4の記述を確認した。訂正波及は `character_state.mark_for_review` と比較した。
- API・画面、訂正経路からの呼び出し配線、振り返りからの抽出、行動選択、再生状態との連動、完了・繰り返し防止・期限切れの遷移、実行記録への目標参照の追加は後続PRの範囲として扱った。

### 指摘事項

#### 1. [中] `active_goals` が現在時刻をUTCへ正規化せず、実行日時を誤判定する

- **場所：** `backend/app/agent/goal.py:173`、`backend/app/agent/goal.py:198`（レビュー時点の行番号）
- **確認区分：** 実行して再現
- **発生条件：** `now` にUTC以外のタイムゾーン付き日時を渡す。
- **原因：** `due_at` は保存時にUTCへ正規化されるが、`now` はそのままSQL比較へ渡される。SQLiteへのバインド時にタイムゾーン情報が落ち、異なる基準の時刻を比較する。
- **再現手順：** 一時DBに `due_at=2026-09-13 15:30 UTC` の有効な目標を保存し、同じ瞬間を異なるタイムゾーンで表した `now` で `active_goals` を呼び出した。既存の `session_factory` fixtureを利用する再現テストをリポジトリ外の `/private/tmp/yui_goal_pr1_review_test.py` に作成して実行した。
- **観測結果：** 次の2組で、同じ瞬間にもかかわらず選択結果が異なった。

  | 渡した現在時刻 | 期待 | 実際 |
  |---|---|---|
  | 9月13日15:00 UTC | 対象外 | 対象外 |
  | 同じ瞬間の9月14日00:00 JST | 対象外 | 選択される |
  | 9月13日16:00 UTC | 選択される | 選択される |
  | 同じ瞬間の9月13日09:00 UTC−7 | 選択される | 対象外になる |

- **影響：** 実行可能になる前の目標を選択したり、実行可能な目標を落としたりする。既定の `utcnow()` を使う経路では再現しないが、現在すでに存在する `now` 引数で発生する。
- **修正方針：** SQL比較前に `now` もUTCへ正規化する。naive日時の解釈も明示し、同一瞬間のUTC・正負のオフセットで結果が一致するテストと、`now == due_at` の境界テストを追加する。

### その他の重点項目の評価

以下は追加の不具合指摘ではなく、確認した判断と検証の限界である。

- **訂正波及（コード確認）：** 明示的な記憶ID、会話単位の根拠、暫定根拠、撤回中の目標を拾う規則は `character_state.mark_for_review` と一致している。終了状態の `done`・`rejected`・`cancelled`・`expired` は実行対象にならないため、現在の実装で対象外にする判断は妥当。
- **配信と相手の絞り込み（コード確認）：** 配信では公開目標だけが対象。さらに、相手が不明なら相手指定の目標を除外し、相手Aなら相手なし・Aの目標だけを選ぶ。コード上、別人を対象にした目標を選ぶ経路は見つからなかった。
- **作成時の検査（コード確認）：** `create_goal` は条件と日時の組み合わせを検査する。ORM直接操作では迂回できるが、今回の正式な作成経路の不具合とは判定していない。PR4の更新処理にも同じ検査を適用する必要がある。
- **snapshot（コード確認）：** 現在変更する項目について、履歴から欠落する問題は見つからなかった。`source_conversation_id` はsnapshotに含まれないが、今回の処理では変更せず、目標本体に保持している。
- **テストの観点：** 不正な条件の拒否や対象外の除外も確認しており、成功経路だけのテストではない。ただし、相手の指定と公開範囲を独立に組み合わせたケース、配信で相手を指定するケースは補強できる。
- **schema一致テストの限界（コード確認）：** `backend/tests/test_migration.py:101` の比較対象は列名・型・NULL可否と索引名。外部キー、既定値、索引の構成列などまで一致を保証するテストではない。今回、実際の定義不一致は見つからなかった。
- **マイグレーション：** 既存テーブルを書き換えず、目標とその履歴のテーブルを追加する。downgradeは履歴側から削除する順序になっている。一時DBでupgrade・downgrade・再upgradeが成功した。`ix_goals_status_trigger` について、今回の確認で不具合と判断する根拠は得られなかった。

### 実行した確認

バックエンドディレクトリで実行した。一時DBを使い、通常利用のDBは変更していない。

```sh
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-phase4-pr1-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider
.venv/bin/ruff check app tests --no-cache
```

- 既存テスト：**206件成功**。
- ruff：成功。

追加の日時再現テスト：

```sh
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-goal-pr1-repro-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/yui_goal_pr1_review_test.py -q
```

- 同一瞬間で選択結果が一致するという検査が、UTC+9とUTC−7の**2ケースで失敗**。上記指摘の再現結果である。

別途、一時ディレクトリ内のDBを `YUI_DATABASE_URL` に指定して以下を実行した。

```sh
.venv/bin/alembic upgrade head
.venv/bin/alembic check
.venv/bin/alembic downgrade f76fb885504f
.venv/bin/alembic upgrade head
```

- すべて成功。`alembic check` は `No new upgrade operations detected.`。
- 既存テストには、旧形式のデータを投入してheadへ移行する確認も含まれる。

### 対象外・未確認

- 実モデル（Ollama）での評価は行っていない。PR1は会話経路に接続されていない。
- データを投入した状態でのPR1 downgrade後の保持確認は未実施。
- 大量データでの索引性能は未計測。
- 後続PRで実装するAPI・画面・会話経路との統合動作は対象外。
- レビュー時にソースコード、設定、ドキュメント、通常利用のDBは変更していない。その後のユーザーの保存依頼により、本レビュー資料のみを新規作成した。

## PR1：目標の器 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**PR1「目標の器」は承認してよく、次のPR2へ進める。** 前回の日時誤判定は解消しており、今回の再レビューで進行を妨げる新たな不具合は見つからなかった。この承認はPR1の範囲であり、フェーズ4全体の完了承認ではない。

### 対象

- [Claudeの対応記録](../claude/phase4_review_response.md)と、修正後の `backend/app/agent/goal.py`、`backend/tests/test_goal.py` を照合した。
- 前回の指摘の修正、再発防止テスト、配信時の相手指定に関する追加テストを確認した。

### 前回の指摘への評価

#### 指摘1：現在時刻のタイムゾーンによる実行日時の誤判定 — 解消

- **確認区分：** 実行して再現確認（修正後は不一致が解消）
- **コード確認：** `as_utc()` を保存側の `normalize_due_at` と比較側の `active_goals` の両方で使用している。タイムゾーン付き日時はUTCへ変換し、naive日時はUTCとして解釈する。
- **独立した再確認：** 前回こちらで作成した `/private/tmp/yui_goal_pr1_review_test.py` を修正後のコードに対して再実行した。UTC+9で基準日時より前の目標を選んでしまうケースと、UTC−7で基準日時を過ぎた目標を落とすケースの両方が成功し、同じ瞬間で選択結果が一致した。
- **再発防止テスト：** 追加された `test_the_same_moment_is_judged_the_same_in_any_timezone` はUTC・UTC+9・UTC−7で基準日時の前後を検査し、`now == due_at` の境界とnaive日時も検査している。前回指摘への対応として適切。

### 追加テストと対応記録の評価

- `test_a_public_goal_about_someone_stays_with_that_person` は、公開目標でも相手不明・別人の場合には選択せず、対象本人の場合だけ選択することを確認している。配信時の公開範囲と相手指定を区別する検証になっている。
- 今回再実行した既存テストの件数とruffの結果は、対応記録の「208件成功・ruff成功」と一致した。
- 対応記録にある「正規化を戻すと追加テストが失敗する」という作業自体は再実施していない。代わりに、前回失敗した独立の再現テストが修正後に成功することを確認した。

### 実行した確認

バックエンドディレクトリから、一時DBを使って実行した。

```sh
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr1-rereview-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider
.venv/bin/ruff check app tests --no-cache
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr1-rereview-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/yui_goal_pr1_review_test.py -q
```

- バックエンド既存テスト：**208件成功**（目標関連11件を含む）。
- ruff：成功。
- 前回の独立した日時再現テスト：**2件成功**。

### 次のタスク・未確認事項

- PR2では、`mark_for_review` を記憶の訂正・削除経路へ接続する作業へ進める。
- 実モデル評価は未実施。PR1は会話経路に未接続であり、今回の承認を妨げる未確認事項とは判断しない。
- フェーズ4全体の統合動作、後続PRの機能は今回の承認対象外。
- ソースコード、設定、通常利用のDBは変更していない。ユーザーの保存依頼により、本資料に再レビュー結果のみを追記した。

## PR2：記憶の訂正・削除が目標へ波及する — 第1回レビュー（2026-09-08）

### 結論

**PR2は承認してよく、次のタスクへ進める。** 現在の記憶更新経路に目標への波及漏れは見つからず、今回の範囲で修正を必要とする不具合・仕様漏れは確認されなかった。フェーズ4全体の完了承認ではない。

### 対象

- PR1コミット `00d34ab` 後の未コミット変更：`backend/app/agent/derived.py`、`backend/app/api/memories.py`、`backend/app/evaluation/steps.py`、`backend/tests/test_goal.py`。
- `AGENTS.md`、README、設計書の記憶訂正・フェーズ4完了条件、計画書のPR2、ISSUE-016、前回レビューと対応記録を参照した。
- `backend/app` を検索し、記憶の生成、属性代入、`setattr`、SQL更新・削除、候補採用、評価での初期データ作成、派生物への呼び出しを確認した。トランザクションについて `app/db.py` と評価runnerも確認した。

### 指摘事項

指摘なし。

### 経路の確認

以下の呼び出しはコードで確認した。実行欄は、今回実行した既存テストが目標への波及を検査している範囲を示す。

| 経路 | コード確認 | 実行した検査 |
|---|---|---|
| API訂正 `api/memories.py:178` | 記憶の更新・履歴作成後、共通関数へ渡す。内容以外の参照範囲等の更新も同じ位置を通る | 内容訂正後、状態と目標の両方に印が付く。目標が実行候補から外れる |
| API削除 `api/memories.py:206` | 論理削除と履歴作成後、共通関数へ渡す | 目標に削除理由の印が付く |
| API復元 `api/memories.py:251` | 履歴から復元後、共通関数へ渡す | 印を一度外した目標にも、復元によって再度印が付く |
| 評価訂正 `evaluation/steps.py:196` | 共通関数を呼び終えた後にcommitする | 記憶ID由来と会話単位由来の目標に印が付く |
| 評価削除 `evaluation/steps.py:215` | 共通関数を呼び終えた後にcommitする | 会話単位由来の目標に削除理由の印が付く |

- **候補の採用・振り返りの採用（コード確認のみ）：** `api/conversations.py` の `decide_candidate` と `evaluation/steps.py` の `run_reflection` は `create_memory` で新しい記憶を作る。候補の状態と採用先IDを変更するが、既存の記憶の内容・状態・参照範囲を書き換えない。今回の訂正波及を呼ばないことを漏れとは判定しない。
- **手動追加・評価の初期投入（コード確認のみ）：** 同じく新規記憶の作成であり、既存記憶の変更経路ではない。
- **直接ORM操作等（コード確認のみ）：** 現在の `backend/app` で、上記5経路以外に既存記憶を書き換える処理は見つからなかった。復元時の `setattr` も共通関数の呼び出し前に行われる。
- **根拠の種類：** PR1からのテストで、記憶ID、会話単位、暫定根拠、撤回中の目標という判定規則を検査している。PR2の追加テストは、その規則を各更新経路から呼び出す部分を検査する構成になっている。

### 集約・復元・評価との一致

- **集約は妥当（コード確認のみ）：** `derived.py` は派生先の列挙だけを担当し、根拠をどう照合するかは各モジュールに残している。現在の2種類に対して過剰な仕組みではなく、各更新経路で目標への呼び出しだけを忘れる可能性を減らしている。
- **例外と保存範囲（コード確認のみ）：** 状態、目標の順に同じセッションで処理し、共通関数内ではcommitしない。目標側で例外が発生しても、APIは `get_session` でrollbackする。評価側も波及成功後にcommitし、当該呼び出しの例外を握りつぶしてcommitする経路は見つからなかった。状態側のflushはcommitではなく、片方だけ永続化する構造ではない。障害を注入した実行検証は未実施。
- **戻り値（コード確認のみ）：** `DerivedMarks` を呼び出し側が参照しなくても、印と変更履歴はセッションへ登録される。戻り値を使わないことで波及が失われることはない。
- **復元（コード確認と既存テスト実行）：** 復元も根拠の変更なので、再評価の対象に含める判断は妥当。目標が訂正後の内容に合わせて確認・変更されている可能性があるため、記憶の復元だけで印を自動解除しない扱いも適切。
- **APIと評価（コード確認）：** 訂正・削除それぞれの `review_reason` は同じ文面であり、どちらも対象記憶の `source_conversation_id` を渡す。記憶そのものの変更履歴の理由は操作元に応じて異なるが、派生先の判定条件には影響しない。

### テストで保証できる範囲と補強の判断

- 追加3テストは5つの既存更新経路を実際に通る。PR1の判定規則テストと組み合わせ、単なる共通関数の単体呼び出しだけにはなっていない。
- 一方で、将来追加される更新経路が共通関数を呼ぶことまで、自動的に保証するものではない。個別の `mark_for_review` の直接呼び出しを禁じる静的検査も、波及を何も呼ばない経路は検知できない。
- 静的検査は集約方針を維持する補助としては有用だが、今回の承認条件にはしない。更新経路を追加するときに、その入口から記憶を変更し、派生物の印と実行対象からの除外を確認するテストを追加する方が、今回懸念されている漏れを直接検証できる。
- 参照範囲だけの訂正、各API経路での会話単位・暫定根拠、波及途中の例外による全体rollbackは、今回追加された3テストで個別には検査していない。コード確認では問題は見つからず、補強候補として扱う。
- ユーザーが実施した「目標側への呼び出しを外すと3件失敗する」という変更を伴う検証は、今回再実施していない。

### 実行した確認

バックエンドディレクトリで実行した。

```sh
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr2-review-unused.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider
.venv/bin/ruff check app tests --no-cache
```

- 既存テスト：**211件成功**（目標関連14件を含む）。
- ruff：成功。
- DBはテストfixtureの一時DBを使用。通常利用のDBは変更していない。

### 対象外・未確認

- 実モデル評価は未実施。今回検証したのはAPI・評価操作から保存された派生物への波及であり、実モデルが予定取消を認識したり、目標に基づいて発言したりする動作ではない。
- フロントエンドは変更対象外のため、tsc・フロントテストは今回再実行していない。
- 障害注入・並行リクエストの負荷試験は未実施。例外時のトランザクション評価はコード確認に基づく。
- 目標のAPI・画面、抽出、行動選択、完了・取消の遷移は後続PRの範囲。
- ソースコード・設定・通常利用のDBは変更せず、ユーザーの依頼により本レビュー資料のみ追記した。

## PR3：評価の器と proactive.toml — 第1回レビュー（2026-09-08）

### 結論

**PR3は修正後に再確認したい。現時点では承認を保留する。** 測定対象が未実装であることではなく、判定を実行せず合格する入力、訂正シナリオの根拠未接続、評価時計の不一致を確認した。基準測定の失敗原因の説明にも訂正が必要。

### 対象・検証

- `677bc56..13170c7` の評価器、シナリオ、会話・振り返りへの時刻注入、モデル・マイグレーション、関連テストを確認した。AGENTS、計画書PR3・2-5、ISSUE-006・021・022、前回レビュー資料を参照した。
- バックエンドで `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr3-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider` を実行：**218件成功**。
- `.venv/bin/ruff check app tests --no-cache`：成功。
- リポジトリ外の `/private/tmp/test_yui_pr3_review.py` に独立した観測テストを作成し、同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr3_review.py -q` を実行：**3件成功**。これは下記の問題が存在することを検査したテストであり、修正成功を意味しない。
- 通常利用のDB、ソースコード、設定は変更していない。本資料のみ追記した。

### 指摘事項

#### 1. [高] 手順に適用されない期待条件が受理され、自動合格に数えられる

- **場所：** `backend/app/evaluation/scenario.py:275`（手順検証）、同337行（機械判定の有無）、`backend/app/evaluation/runner.py:160`（ターン判定）。
- **確認区分：** 実行して再現。
- **発生条件：** `say` に `expect_goal_any` を書くなど、期待条件を別の種類の手順に指定する。
- **再現手順：** `steps=[{kind: "say", text: "こんにちは", expect_goal_any: ["絶対に作られない目標"]}]` のproactiveシナリオを `Scenario.model_validate` で読み、FakeLLMで `run_scenarios` を実行した。
- **観測結果：** 入力が受理され、`human_only=False`、`attempt.ok=True`、ターンの `checks=[]` となった。目標候補を一度も抽出・検査していない。
- **影響：** 期待条件を書いたのに評価せず、機械判定の成功数に入る。未実装機能の問題ではなく、現在の評価器で起こる誤合格。
- **修正方針：** 手順の種類ごとに使用可能な期待条件を検証し、適用できない条件は読み込み時に拒否する。機械判定ありと分類した条件が、実行時に判定結果または明示的な未実装失敗へ対応することも検査する。

#### 2. [中] 訂正波及シナリオの目標が、訂正する記憶を根拠に持っていない

- **場所：** `backend/evals/scenarios/proactive.toml:177`、`backend/app/evaluation/scenario.py:101`（GoalSpec）、`backend/app/evaluation/runner.py:317`（目標投入）。
- **確認区分：** 実行して再現。
- **発生条件：** 同梱の `corrected-basis-marks-the-goal` を実行する。
- **再現手順：** 実際のシナリオを読み込み、一時DBへ `_seed` で投入した後、評価の `correct_memory` で映画の予定を美術館へ訂正し、目標を取得した。
- **観測結果：** 目標の `basis_memory_ids=None`、`source_conversation_id=None`、`needs_review=False`。既存の訂正波及は正常に呼ばれるが、その目標は対象にならない。
- **影響：** 「根拠の訂正で目標に印が付く」という測定の前提が成立していない。現在は `start_conversation` の未実装失敗に隠れている。目標を一律に渡さない実装も否定条件だけなら満たせるため、目標の不参照だけでは波及成功を示せない。
- **修正方針：** GoalSpecに根拠の記憶キーを指定できる形を設け、投入時に実IDへ解決する。このシナリオで実際に結び付け、訂正前の実行可能性と訂正後の `needs_review`・不参照を検査する。

#### 3. [中] 評価時間を進めた後の発言が実時計の日付で保存され、振り返りへ矛盾した日付を渡す

- **場所：** `backend/app/agent/conversation.py:86`（reference_time）、同87行付近（Message生成）、`backend/app/agent/reflection.py:354`（発言日付）、同402行（振り返り日）。
- **確認区分：** 実行して再現。
- **発生条件：** `advance_time` → `say` → `reflect` の順に実行する。
- **再現手順：** 30日進めて「昨日、映画を見た」と発言し、その会話をFakeLLMで振り返った。実際にLLMへ渡したプロンプトを確認した。
- **観測結果：** 発言行の日付は実行当日。会話へ渡す現在時刻と振り返り日は進んでいるが、Messageの `created_at` はモデルの実時計の既定値のままである。
- **影響：** 振り返りは発言日を基準に「昨日」を解釈する設計なので、進めた日付から30日ずれた出来事として抽出する入力になる。生成結果の日時誤りそのものは実モデルで未検証だが、矛盾した日付を渡すことは再現した。記憶の `created_at` も実時計で、進めた時点で新規採用した記憶の減衰計算には同種の不一致が残る（後者はコード確認のみ）。
- **修正方針：** 評価上の出来事・発言・記憶作成の日時にも論理時計を一貫して使う。実際の処理時間や監査用の記録日時とは区別する。時間を進める「前」に発言したケースだけでなく、「後」に発言・採用してから振り返り・検索するケースを検査する。

#### 4. [低] 基準測定の訂正シナリオの失敗原因が、ログと異なる説明になっている

- **場所：** `docs/plan/phase4.md:491` 付近の基準値表、`logs/evals/20260908T063456-2026-09-07.1/report.md:182`。
- **確認区分：** コード確認のみ（保存済みレポートの確認。実モデル再実行なし）。
- **発生条件：** 基準値表を使って `correction-changes-the-next-answer` の不合格原因を判断する。
- **観測内容：** 表では「記憶は渡っているのに覚えていない」とするが、該当返答は好きな飲み物を「红茶」として覚えていると答えている。機械判定は期待語「紅茶」が含まれないため失敗している。他の情報を覚えていないという後半の文と、訂正対象の好みの認識は区別が必要。
- **影響：** 不合格6件をすべてISSUE-021・022の現れとして説明するのは正確でない。少なくとも1件は表記揺れによる不一致として切り分ける必要がある。これは返答全体の品質を良好と判定するものではない。
- **修正方針：** 内訳を、約束を覚えていない2件、訂正後の飲み物の表記不一致1件、過剰抽出3件へ訂正する。表記の適切さを測るのか、意味上の訂正反映を測るのかも明示する。

### その他の判断と評価用会話の限界

- **事前投入：** 参照・待機・繰り返し等を独立に検査するための採用済み目標投入は妥当。ただし `adopted-goal-is-asked-later` も事前作成した候補の採用であり、「会話 → 抽出 → 採用 → 期限後の参照」を一続きには測っていない。抽出単独の1本と採用単独の1本が成功しても、両者の接続成功は証明できない。通し測定には抽出した目標へキーを付けて後続の期待条件から参照するシナリオが必要。
- **否定条件の空振り：** `expect_not_goals` 単独では、渡す機能が一律に無効でも成功する。必ず事前に一度渡す必要はないが、対象の存在、採用状態、根拠、日時等の前提を検査し、抑制理由も別に観測すべき。指摘2はこの前提が実際に欠けた例。
- **start_conversation：** 未実装を明示的失敗にする判断は妥当で、未実装自体は指摘しない。現時点ではその手順の `expect_goals` 等は未評価であり、「条件を評価して失敗した」と区別して読む必要がある。PR7では発話・待機の結果に対する期待条件の検査も含めて接続する。
- **待機・質問数・調査の保留：** 渡した目標IDは、実際に選んだ行動や質問数を証明しない。`does-not-pile-up-questions` は3件を渡せば機械条件を満たし、全部質問しても人手確認にしか現れない。`expect_none` も言い換えを検知せず、否定の文脈でも一致する。現在の人手欄は妥当だが、機械通過を完了条件2達成と扱わないこと。選択行動の記録と人手判定を併用する必要がある。
- **完了条件3・6：** 再生未実行シナリオには実行・完了時刻の直接検査がなく、目標参照のみを見る。人格保持シナリオも、同じ人格で状態や目標を変えた前後比較はなく、口調は人手判定。これらの機械通過だけでは該当完了条件全体を証明しない。
- **列の先行追加：** NULL許容の2列を評価用に先行追加する判断に問題は見つからなかった。マイグレーションは両列を追加し、downgradeで両列を削除する。selected_action未記録と待機を同一視しないことが後続実装の前提。
- **基準値：** 保存レポートの78/84、機械判定28本、人手専用5本、実行失敗0は確認した。33本×3回の99試行すべてを自動採点した数字ではない。失敗原因の修正は指摘4のとおり。「回帰ではない」との因果判断は、今回のログだけでは断定できない。通常呼び出しはnow未指定で実時計を使い、検索とプロンプトへ同じ時刻を渡す変更であり、今回コード上で通常検索の回帰は見つからなかった。

### 対象外・未確認

- 実モデル評価は再実行していない。基準測定は指定された保存レポートを確認したもので、前後の実モデル比較を独立に再測定したものではない。
- 目標抽出・行動選択・再生との連動・完了遷移の未実装は不具合に数えていない。
- 既存218テストは成功したが、同梱proactive10本の全実行を独立に再実行する検証は行っていない。独立検証は上記3ケースに絞った。
- マイグレーションのschema比較を含む既存テストは実行した。今回の版のデータ入りdowngrade・再upgradeは独立には実行していない。

## PR4：目標のAPIと画面 — 第1回レビュー（2026-09-08）

### 結論

**PR4は承認保留。採用・再有効化の検査と管理画面の操作範囲を修正してから再確認したい。** 現在のAPIだけで、無効な根拠や訂正前の目標を要確認にせず実行候補へ渡せる。後続の自動判定の未実装を指摘しているのではない。

### 対象・確認結果

- 未コミットの `backend/app/api/goals.py`、関連schemas・ルータ・テスト、GoalPanel・フロントAPI・型・Appの接続を確認した。写し元のstates APIとStatePanel、AGENTS、計画書PR4・状態遷移を参照した。
- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr4-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider` → **227件成功**。`ruff check app tests --no-cache` → 成功。
- frontend：`npx tsc --noEmit` → 成功、`npm test -- --run` → **23件成功**。
- 独立したAPI観測テストをリポジトリ外の `/private/tmp/test_yui_pr4_review.py` に置き、同じ一時DB用環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr4_review.py -q` → **3件成功**。下記の問題が起きることを検査した結果である。
- 目標関連の既存テストは実出力では **19件**。依頼文の24件とは異なるが、バックエンド総数227件は一致する。

### 指摘事項

#### 1. [中] 削除済みの記憶を根拠に新規作成した目標を、要確認にせず採用できる

- **場所：** `backend/app/api/goals.py:148`、同176〜195行（採用）、同80行（採用から呼ばれない `_dead_basis`）。
- **確認区分：** 実行して再現。
- **発生条件・再現手順：** 記憶をAPIで作成して削除する。その記憶IDを `basis_memory_ids` に指定して目標をPOSTし、decideでacceptする。
- **観測結果：** POSTは201、採用は200。`needs_review=False` のactiveとなり、`active_goals` がその目標を返した。
- **影響：** 削除時点では目標が存在しないため、PR2の波及では拾えない。存在確認と参照範囲の検査だけでは、無効化した予定を根拠に質問できる目標が生まれる。PATCHでactiveへ戻す場合の検査とも不一致。
- **修正方針：** 採用時にも根拠の有効性を検査し、無効なら拒否または要確認にする。開発者による例外的な確認を許す場合も、通常の採用と区別して記録する。作成前に根拠が削除されたケースを追加する。

#### 2. [中] 終了中の根拠訂正を検知せず、古い目標を完了日時付きで再有効化できる

- **場所：** `backend/app/api/goals.py:225`、同228〜237行、`backend/app/agent/goal.py` の `mark_for_review`（終了状態を対象外にする規則）。
- **確認区分：** 実行して再現。
- **発生条件・再現手順：** 映画の予定を根拠に目標を作成・採用してdoneにする。記憶を「映画は中止、美術館へ行く」へPATCHする。目標を `status=active` だけでPATCHする。
- **観測結果：** `needs_review=False`、`completed_at` は以前の達成日時のままで、`active_goals` が古い「映画の感想を聞く」を返した。
- **影響：** PR1で終了状態を波及対象外にした前提は「もう実行されない」だった。今回再有効化を許したことでその前提が崩れる。内容訂正は記憶のstatusをactiveのままにするため、`_dead_basis` だけでは検出できない。再度doneにしても以前の完了日時が残る。
- **修正方針：** 終了状態からactiveへの遷移を拒否するか、明示的な再開として根拠の再確認を必須にする。再開を許すなら完了日時の扱いも定義し、過去の達成は履歴へ残して現在の状態と整合させる。withdrawnと終了状態は区別する。

#### 3. [中] PATCHが候補の採用処理を迂回し、採用の根拠と履歴が経路によって変わる

- **場所：** `backend/app/api/goals.py:205`（update_goal）、同225行、同181〜197行（decide側の根拠補完・accepted記録）。
- **確認区分：** 実行して再現。会話単位の根拠補完が飛ぶ点はコード確認のみ。
- **発生条件・再現手順：** POSTしたpending目標を `PATCH {"status":"active"}` で更新し、revisionsを取得する。
- **観測結果：** 200でactiveとなる。履歴は `corrected`、`created` のみで、`accepted` が存在しない。
- **影響：** decideの採用にある根拠の補完と暫定印の設定を通らない。開発者の手動操作でも「採用」という同じ操作が異なる記録・根拠を持つ。なお、会話単位で拾うPR2の規則は残るため、この迂回だけで訂正波及が必ず失われるとは判断しない。
- **修正方針：** pendingからactiveへのPATCHを拒否してdecideへ誘導するか、両経路から共通の採用処理を呼び、同じ検査・根拠補完・accepted履歴を保証する。

#### 4. [中] 管理画面から撤回・再有効化・期限切れの手動操作を行えない

- **場所：** `frontend/src/components/GoalPanel.tsx:173`（採用済みの操作）、同232行（終了一覧）、`docs/plan/phase4.md:530` 付近（画面から全部の遷移を起こす要件）。
- **確認区分：** コード確認のみ。
- **発生条件：** 採用済み目標をwithdrawnへ撤回したい、撤回済み目標を確認して戻したい、手動でexpiredにしたい場合。
- **観測内容：** 採用済みの操作はdoneとcancelledのみ。withdrawn・expiredは表示ラベルだけあり、終了一覧には操作ボタンがない。
- **影響：** APIが受け付ける管理操作の一部を画面から実行できず、PR4の「画面から全部の遷移」という範囲を満たしていない。自動期限切れ判定がPR9なのとは別の不足。
- **修正方針：** 許可する遷移をAPIで確定した上で、それに対応する画面操作を追加する。終了状態からの再開は指摘2を先に解決する。操作ボタンが適切なpayloadを送るテストも追加する。

### その他の重点項目

- **実行条件：** `_resolve_schedule` は指定項目を `model_fields_set` で見ている。after_dateでdue_atを明示的にnullにすると拒否され、next_conversationへの変更時に日時を省略した場合は消去される。日時も同時指定して矛盾する場合は拒否される。コード確認で組み合わせ検査の穴は見つからない。「次の会話」を選ぶと日時が無効になる仕様として、自動消去は妥当。
- **doneの重複：** すでにdoneのものをdoneにするだけならcompleted_atを上書きしない。問題はactiveへ戻す場合で、指摘2に含めた。last_executed_atを手動の完了操作で更新しない扱いは妥当。
- **相手と公開範囲：** 作成時の根拠に対する範囲検査はstatesと同じ。subject=A、visible_to=Bの矛盾した組み合わせは作れるが、ローカルの `active_goals` は両条件をANDで絞るため、BへAの目標を渡す根拠にはならない。公開目標の配信参照は別途visibilityとsubjectで絞る。設定不能・検索不能の区別は必要だが、今回この組み合わせを情報漏洩とは指摘しない。
- **保留：** 候補をpendingのまま保持することで、採用判断の保留は表現できる。専用の保留状態を増やすこと自体は必須ではない。
- **新規作成フォーム：** 指定された実装内容4は候補の管理であり、手動作成フォームがないこと単独は不具合に数えない。現段階で画面を試す候補はAPI経由で作成できる。
- **フロントテスト：** 現在の5件は主に表示検査で、ボタン操作によるAPI payload、失敗時表示、再読み込み後の分類を検査していない。とくに「指定日以降」のテストはラベルの存在だけで、日付の値自体を検査していない。表示テスト成功を実APIとの接続確認とは扱わない。

### 未確認・変更範囲

- 実ブラウザ・実サーバーを組み合わせた操作と、Ollama・VOICEVOXを使った実動作は未確認。
- APIの独立再現はASGIテストクライアントと一時DBによる。通常利用のDBは変更していない。
- ソースコード・設定は変更せず、依頼された本資料のみ追記した。

## PR4：目標のAPIと画面 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**前回の指摘1〜3は解消。指摘4の画面操作に不足が残るため、PR4全体の承認は保留する。**

### 対応の確認

[対応記録](../claude/phase4_review_response.md)と、修正後のgoals API、GoalPanel、関連テストを照合した。

- **指摘1：解消。** 作成時に無効な根拠を拒否する。採用時にも根拠の有効性を検査し、無効なものがあれば再評価の印を付ける。該当するAPIテストも成功した。
- **指摘2：解消。** done・rejected・cancelled・expiredからの状態変更を拒否する。撤回中だけ再有効化を許すため、終了状態を訂正波及の対象外にする前提と整合する。
- **指摘3：解消。** pendingからのPATCHによる状態変更を拒否し、採用・却下をdecideへ集約した。内容と実行条件は候補のまま修正できる。
- **指摘4：一部解消。** 採用済みの撤回・期限切れ操作、撤回中の独立した一覧と再有効化操作は追加された。ただし、以下の操作が残っている。

### 残る指摘事項

#### 1. [中] 一部の状態では、APIが許す管理操作を画面から実行できない

- **場所：** `frontend/src/components/GoalPanel.tsx:105`（要確認の操作）、同258行（撤回中の一覧）。API側の許可は `backend/app/api/goals.py:56`〜67行。
- **確認区分：** コード確認のみ。
- **発生条件：** 撤回中の目標を期限切れにする場合、または要確認の目標を撤回する場合。
- **確認内容：** 撤回中には「戻す」「取り消す」しかない。要確認には「このままでよい」「取り消す」しかない。一方、APIはwithdrawnからexpired、activeからwithdrawnへの遷移を許可している。
- **影響：** 撤回中の目標を期限切れにするには一度activeへ戻す必要がある。要確認の目標を撤回するには、不要な確認済み操作を先に行う必要がある。対応記録の「APIが許す遷移と画面をそろえた」という対応は完了していない。
- **修正方針：** 撤回中に期限切れ操作を追加し、要確認の目標も印を解除せず撤回できるようにする。各状態でボタンが適切なpayloadを送る操作テストも追加する。

### 実行した確認

バックエンドディレクトリで実行した。

```sh
YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr4-followup.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider
.venv/bin/ruff check app tests --no-cache
```

- バックエンド：**231件成功**（目標関連23件を含む）。ruff成功。

フロントエンドディレクトリで実行した。

```sh
npx tsc --noEmit
npm test -- --run
```

- tsc成功、**28件成功**（GoalPanel 10件を含む）。対応記録の件数と一致する。

### 対象外・未確認

- 実ブラウザ・実サーバーを組み合わせた接続確認は未実施。フロントのテストはモックAPIに対する検証である。
- 対応記録にあるstates側の同種の問題は、別課題として追跡する扱いで妥当。今回のPR4で修正を追加要求するものではない。
- 再レビュー時はソースコード・資料・通常利用のDBを変更していない。その後の保存依頼により、本資料へ結果のみ追記した。

## PR5：振り返りを会話後のジョブへ分離 — 第1回レビュー（2026-09-08）

### 結論

**PR5は承認保留。保存段階の失敗・中断処理と、画面の監視処理を修正してから再確認したい。** 単一プロセスのジョブという方式自体は今回の設計に合っている。

### 指摘事項

#### 1. [高] 保存段階の例外・キャンセルで、終了したジョブの開始権と処理中表示が残る

- **場所：** `backend/app/agent/reflection_job.py:184`（savingへの進行更新）、同186行（保存）、同171行（その前のtryだけに適用されるCancelledError処理）。
- **確認区分：** 実行して再現（障害・中断の注入）。
- **発生条件：** savingへの進行更新で例外が起きる、または `_save` のawait中にshutdownでタスクがキャンセルされる。
- **再現手順：** 一時DBで会話を作り、FakeLLMが正常な空候補を返す状態でendする。ケースAでは `_set_step` のsaving時だけRuntimeErrorを発生させる。ケースBでは `_save` を待機させ、到達を確認して `shutdown()` を呼ぶ。
- **観測結果：** Aはジョブ終了後に `is_running=False` だがGET進行は `running`、errorはnull。Bもshutdown完了後に `running`、stepは `saving` のまま残った。
- **影響：** 保存開始時の進行更新はtry外で、保存側の `except Exception` はCancelledErrorを捕まえない。実際には走っていないのに画面がポーリングし続け、再試行は開始権の回収期限まで409になる。通常再起動時の回収はあるが、「停止時に開始権を解放する」「想定外の例外も失敗として残す」という保証は満たさない。
- **修正方針：** 進行更新・抽出・保存を含むジョブ全体を例外・キャンセル処理の対象にする。保存commitと中断の境界も考慮し、すでに完了した結果を失敗へ上書きしない。上記2ケースと抽出待機中のshutdownを独立にテストする。

#### 2. [中] 会話切り替え・アンマウント後も旧会話のポーリングとローカル状態更新が続く

- **場所：** `frontend/src/components/ChatPanel.tsx:144`〜177行（endConversation）、同100行付近（view変更時の後始末）。
- **確認区分：** コード確認のみ。
- **発生条件：** 振り返り待機中に履歴から別の会話を開く、またはコンポーネントをアンマウントする。
- **確認内容・影響：** whileループに中断条件がなく、古いIDへ500msごとの取得が続く。`setReflection`・`setError`・`setBusy` はviewを検査せず実行されるので、別会話に旧会話の進行や失敗を表示できる。Appの `onEnded` はviewを検査するが、ChatPanel内の更新には効かない。view変更時にreflectionやbusyもリセットしていない。
- **修正方針：** 会話ID・viewに紐付いたuseEffectなどで監視し、cleanupでタイマー・通信を止める。既に返りかけた結果も世代を確認して捨てる。切替・アンマウント・旧要求の遅延完了をテストする。

#### 3. [中] 振り返り中の会話を開き直しても監視が始まらず、完了・失敗を画面へ反映できない

- **場所：** `frontend/src/components/ChatPanel.tsx:88`（readOnly）、同144行（監視の唯一の起点）、`frontend/src/App.tsx` の履歴読み込みとonEnded。
- **確認区分：** コード確認のみ。
- **発生条件：** 別画面・別タブでendした会話、または処理開始後に画面を再読み込みした会話を、履歴から `reflecting` 状態で開く。
- **影響：** 監視は終了ボタンのハンドラ内にしかなく、reflectingの会話を開いた際にはGET進行を呼ばない。終了ボタンもreadOnlyで使えないため、ジョブが終わってもその表示のままで、失敗後にその場で再試行する操作へ移れない。DBに進行を保存していても、開き直した画面が利用できていない。
- **修正方針：** reflecting状態で会話を開いた場合にも監視を開始し、completed/failedを親の会話状態へ反映する。失敗時は終了済みにせず再試行可能にする。指摘2と同じ監視のライフサイクルで処理できる。

### その他の確認と判断

- **開始権・二重実行（コード確認）：** 同一プロセスでは会話ロック内でDBの開始権を確定し、稼働中タスクも検査する。ロックを抜けてscheduleするまでに通常のawaitはなく、直後にタスクを辞書へ登録する。`_running.pop` もジョブの完了処理後に同期で実行される。この範囲で二重登録の具体的な経路は見つからなかった。
- **commit後のプロセス停止：** schedule前にプロセスが落ちた場合は開始だけがDBに残り、起動時のrecover_orphanedで回収する設計。単一プロセス前提に合っている。複数プロセスの制御はISSUE-025の対象として今回は要求しない。
- **キャンセル中のawait：** 最初のCancelledErrorを捕まえた後にawaitすること自体が常に失敗するわけではない。shutdownはタスクをcancelした後、その終了をawaitしている。今回の具体的な問題は保存段階がcatchの範囲外であること。DB自体が書けない場合や繰り返しキャンセルの保証までは検証していない。
- **セッション：** リクエストのセッションをジョブへ渡さずfactoryから作る形は妥当。extract_candidatesの候補は新規オブジェクトとして組み立てられ、抽出セッションへaddされていないため、別セッションでadd_allできる。書き込みは段階更新・最終保存の短いトランザクションに分かれている。
- **成功と失敗：** 抽出段階の既知・想定外の例外はreleaseし、ended_atを入れない。候補0件でも正常終了ならcompletedになるため、区別は存在する。保存段階の漏れは指摘1のとおり。
- **テスト書き換え：** end_and_waitは202後に実際のジョブを待つもので、サーバー処理を同期へ戻す変更ではない。LLMをゲートで止めても202が返る専用テストもある。失敗確認はHTTP503からDBのfailedへ変更され、候補の部分保存防止・再試行・並行会話の検査も残る。
- **テストの限界：** end_and_candidatesはPOSTの成功や最終completedをassertせず候補一覧を読むため、空候補を期待するテスト単独では失敗と区別できない。補助関数で202とcompletedを確認すると検証が強くなる。今回の具体的な保存段階の漏れは、追加6件が保存時の中断・失敗を測っていないため検知されなかった。
- **評価：** 評価は抽出・状態候補生成を直接呼び、APIのジョブ管理を測らない。今回の抽出ロジックは共有され、進行通知は任意callbackなので、内容の評価をそのまま維持する判断は妥当。ただし評価成功は開始権・停止・進行表示の成功を保証せず、APIテストで別に検査する必要がある。
- **画面の初回GET：** 最初の進行取得値はsetReflectionされない。POSTがrunning、直後のGETがcompleted/failedの場合、running表示だけ残る経路もある。各取得結果を一貫して適用する監視処理へ整理する際に併せて修正すること。

### 実行した確認

- backend：一時DB用に `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr5-review.db` と `PYTHONDONTWRITEBYTECODE=1` を指定し、`.venv/bin/python -m pytest -p no:cacheprovider` → **239件成功**。`.venv/bin/ruff check app tests --no-cache` → 成功。
- 独立観測：リポジトリ外の `/private/tmp/test_yui_pr5_review.py` に障害・キャンセルを注入するテストを作り、同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr5_review.py -q` → **2件成功**。問題が存在することを検査した結果。初回はモック出力が不正で抽出段階で失敗したため、正常な空候補出力へ修正して再実行した。
- frontend：`npx tsc --noEmit` → 成功、`npm test -- --run` → **33件成功**。

### 対象外・未確認

- 実モデル・実ブラウザ・実サーバーの通し確認は未実施。フロント指摘はコード確認に基づく。
- プロセスの強制終了、イベントループ強制終了、DB障害からの回復、複数プロセス制御の実験は未実施。
- マイグレーションの2列追加・削除をコード確認し、schema比較を含む既存テストは実行した。データ入りDBでのdowngradeは独立には未実施。
- ソースコード・設定・通常利用のDBは変更せず、本レビュー資料のみ追記した。

## PR5 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**PR5は引き続き承認保留。保存段階の問題は解消したが、画面の失敗復帰と終了リクエストの遅延応答に対応漏れが残る。**

### 対応の評価

- 前回指摘1：解消。抽出・進行更新・保存が `_run` にまとまり、全体を例外・キャンセル処理が包む。`_release` は完了済みを対象外にし、shutdown後の回収も追加された。保存直前の例外、保存中の停止、commit後の停止を扱う追加テストを含め、バックエンドテストが成功した。
- 前回指摘2：監視ループ自体のcleanupと世代検査は追加された。ただし、監視を開始するPOSTの応答には同じ保護がない（下記2）。
- 前回指摘3：reflectingで開いた会話の監視開始と完了通知は解消。失敗した場合に読み取り専用を解除する経路が残っている（下記1）。
- `end_and_candidates` に202とcompletedの検査が追加され、失敗を空候補の成功と誤認するテストの弱さは改善された。

### 残る指摘事項

#### 1. [中] reflectingで開いた会話の振り返りが失敗しても、その場で再試行できない

- **場所：** `frontend/src/components/ChatPanel.tsx:96` 付近（readOnly）、同137行付近（failed処理）、同202行付近（endConversation）、`frontend/src/App.tsx` の会話状態更新。
- **確認区分：** コード確認のみ。
- **発生条件：** 履歴から振り返り中の会話を開き、監視でfailedを受け取る。
- **影響：** failed処理はエラー表示と `watching=false` だけで、親の `state="reflecting"` を変更しない。readOnlyがtrueのままで、終了ボタンとendConversationのガードにより再試行できない。完了にはonEndedがあるが、失敗を親へ通知する経路がない。
- **修正方針：** view付きの失敗通知、または会話状態の再取得を通して親を再試行可能な状態へ戻す。reflectingで開く→failed→同じ画面で再試行できることをテストする。HTTP取得失敗とジョブのfailedは区別する。

#### 2. [中] 終了POSTの遅延応答が、切り替え先の会話で監視を開始する

- **場所：** `frontend/src/components/ChatPanel.tsx` の `endConversation`（約202〜220行）、`setReflection(await api.endConversation(conversationId))` と直後の `setWatching(true)`。
- **確認区分：** コード確認のみ。
- **発生条件：** 会話Aの終了POSTが返る前に履歴から会話Bへ切り替え、その後Aの応答が届く。
- **影響：** POST側はview・アンマウントを検査しないため、Aの進行をBの表示へ設定し、watchingをtrueにする。useEffectは現在のconversationIdで起動するためBを監視する。Bが完了済みならBに対するonEndedを余分に呼ぶこともある。GET監視側の世代検査だけでは防げない。
- **修正方針：** POST開始時のID・viewを保存し、応答・例外・finallyの状態更新前に有効な操作か確認する。会話切替時とアンマウント時に失効させる。POSTを保留→別会話へ切替→旧POSTを解決するテストを追加する。

### 実行した確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr5-followup.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider` → **242件成功**。`.venv/bin/ruff check app tests --no-cache` → 成功。
- frontend：`npx tsc --noEmit` → 成功。`npm test -- --run` → **35件成功**。
- 新しい監視テストは開き直し後の成功とGET監視中の切替を扱うが、開き直し後のfailed、POST待機中の切替は扱っていない。
- 実ブラウザ・実モデルの確認は未実施。今回の残存指摘はコード確認に基づく。
- ソースコード・設定・通常利用のDBは変更せず、本資料のみ追記した。

## PR5 — 第3回レビュー・対応確認（2026-09-08）

### 結論

**PR5は引き続き承認保留。失敗通知と旧POST応答の破棄は実装されたが、旧POSTの後始末を止めたことで、切り替え先の会話が操作不能になる問題が残る。**

### 対応の評価

- 第2回指摘1：`onReflectionFailed(view)` が追加され、Appが対象viewの会話をopenへ戻す。失敗を終了扱いせず再試行可能にする接続を確認した。
- 第2回指摘2：終了POSTの応答・例外にview検査が追加され、別会話の監視を始める経路は塞がれた。ただしfinallyも抑止する一方、切替時にbusyを解除していないため、下記の問題が生じる。

### 指摘事項

#### 1. [中] 終了POST待機中に会話を切り替えると、busyが解除されず操作不能になる

- **場所：** `frontend/src/components/ChatPanel.tsx:105`付近（view変更時のリセット）、同210行付近（endConversation）、同232行付近（finallyのview検査）。
- **確認区分：** コード確認のみ。
- **発生条件：** 会話Aで終了を押してPOSTを待機中に、履歴から会話Bへ切り替える。その後AのPOSTが成功または失敗する。
- **影響：** Aで設定した `busy=true` は同じChatPanelに保持される。切替時のeffectはreflectionとwatchingだけをリセットし、旧POSTのfinallyもviewが異なるため `setBusy(false)` を呼ばない。Bの送信・終了・新しい会話等がdisabledのままになる。旧応答を捨てる今回追加のテストもこの経路を通るが、操作可能性をassertしていない。
- **修正方針：** view変更時に終了要求が所有するbusyを解放するか、要求の世代に紐付けた状態として管理する。単に旧finallyを無条件に戻すと、新しい要求のbusyを消すため避ける。POSTを保留→切替→旧POSTを成功／失敗させた後、Bで送信・終了できることをテストする。

### 確認結果・未確認

- 修正コードと `phase4_review_response.md` の第2回対応記録、追加された2テストを確認した。
- frontendで `npx tsc --noEmit` 成功、`npm test -- --run` は **37件成功**。
- 追加テストは旧監視の未開始と失敗callbackを確認している。busyの解除や、親を含めた再試行操作の完遂までは確認していない。
- バックエンドは今回再実行していない。前回の検証結果は242件成功・ruff成功。
- 実ブラウザ・実モデル確認は未実施。今回の指摘はコード確認に基づく。
- ソースコード・通常利用のDBは変更せず、依頼されたレビュー資料のみ追記した。

## PR5 — 第4回レビュー・対応確認（2026-09-08）

### 結論

**PR5は承認保留。終了POSTに関する前回のbusy固定は解消したが、共有している送信処理の後始末との競合が残る。**

### 対応の評価

対応記録とChatPanel・関連テストを確認した。view変更時にbusyを解除することで、終了POSTが遅れて成功・失敗しても切替先が操作不能になる問題は解消した。追加2テストもそのケースを検査している。ただしbusyは終了処理専用ではなく、sendとも共有される。

### 指摘事項

#### 1. [中] 会話切替でbusyを解除する一方、旧送信のfinallyが新しい要求のbusyを解除できる

- **場所：** `frontend/src/components/ChatPanel.tsx:117`（view変更時のbusy解除）、同185〜213行（send、特にfinally）。
- **確認区分：** コード確認のみ。
- **発生条件：** 会話Aの通常送信を待機中に会話Bへ切り替え、Bで送信または終了POSTを開始する。その要求を待つ間にAの送信が完了する。
- **影響：** 今回のリセットでBの要求を開始できるが、Aのsendはfinallyで無条件に `setBusy(false)` を実行する。Bの要求が未完了でもボタンが有効になり、送信や終了POSTを重ねて実行できる。旧送信の成功時には世代判定より前の `setText("")`、失敗時には無条件の `setError` も現在の画面へ作用する。`onEntry` の世代検査は、これらのローカル状態更新を保護しない。
- **修正方針：** 終了POSTだけでなく、同じbusyを使うsendにも世代・要求の所有者の検査を適用する。履歴へ返答を記録する親への通知は維持しつつ、旧要求が現在の入力・エラー・busyを変更しないようにする。Aの送信待機→Bへ切替→Bの要求開始→Aの成功／失敗→Bが完了するまでbusyが維持されることをテストする。

### 確認結果・未確認

- frontend：`npx tsc --noEmit` 成功、`npm test -- --run` は **39件成功**（振り返り関連9件）。
- 今回の2テストは旧要求が終了POSTである場合を検査しており、旧要求が通常送信である組み合わせは検査していない。
- バックエンドは今回再実行していない。前回の検証結果は242件成功・ruff成功。
- 実ブラウザ・実モデル確認は未実施。残る指摘はコード確認に基づく。
- ソースコード・通常利用のDBは変更せず、本資料のみ追記した。

## PR5 — 第5回レビュー・対応確認（2026-09-08）

### 結論

**PR5は承認してよく、次のPR6へ進める。** 前回残った通常送信と終了POSTのbusyの競合は解消した。今回確認した修正範囲で、新たな承認を妨げる不具合は見つからなかった。これはPR5の承認であり、フェーズ4全体の完了承認ではない。

### 対応の評価

- 対応記録の第4回レビューへの対応と、`frontend/src/components/ChatPanel.tsx`、関連テストを確認した。
- 通常送信にも世代検査が追加され、旧送信のfinallyが切替先の要求のbusyを解除しなくなった。旧送信の失敗も、切替先のエラー・入力へ反映しない。
- 入力は送信時に空にし、失敗時は同じ世代の場合だけ書き戻す。遅延した成功応答が切替先の下書きを消す経路も解消している。
- 親へのonEntry通知を維持し、別会話の返答が履歴に残った旨を通知する扱いは意図的な既存動作として妥当。現在の要求のbusyや入力を変更することとは区別できている。
- 追加テストは、Aの通常送信を保留したままBへ移って終了POSTを開始し、Aの成功後もBの要求中は操作が無効のままであることを検査している。旧送信失敗時のエラー・入力の混入防止も検査している。

### 確認結果

- frontend：`npx tsc --noEmit` 成功、`npm test -- --run` は **41件成功**（振り返り関連11件）。対応記録の件数と一致した。
- バックエンドは今回再実行していない。PR5の前回検証結果は242件成功・ruff成功。
- 修正を戻してテストが失敗するという対応記録の検証は、今回独立には再実施していない。

### 未確認・変更範囲

- 実モデル・実ブラウザ・実サーバーを組み合わせた進行表示の確認は未実施。今回の実行確認はモックを使ったフロントテストである。
- 複数プロセス制御はISSUE-025の範囲。目標抽出・行動選択は後続PRで確認する。
- ソースコード・設定・通常利用のDBは変更せず、依頼された本資料のみ追記した。

## PR6 — 初回レビュー（2026-09-08）

### 結論

**PR6の承認は保留する。** 4回目の呼び出し・pending保存・本番ジョブの失敗処理は接続されているが、評価器の例外処理、相対日付の基準、無効な日付の扱いに修正が必要。進行表示にも追加漏れがある。PR7〜9の未実装を理由に保留するものではない。

### 対象・確認方法

- `1331f06` を基準とする未コミットのPR6変更、新規 `goal_reflection.py` とそのテスト、呼び出し元のジョブ・評価器、関連する目標選択・画面表示を確認した。AGENTS、README、設計書の関連節、計画PR6・2-6、ISSUE-017・021・023、前回レビューと対応記録も参照した。
- backendで `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr6-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q`：**250件成功**。
- backendで `.venv/bin/ruff check --no-cache app tests`：成功。
- `/private/tmp/test_yui_pr6_review.py` に再現専用テストを置き、同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr6_review.py -q`：**1件成功**。これは評価全体へ例外が漏れることを期待する再現テストで、修正済みという意味ではない。評価器は自身の一時DBを使用する。
- ソースコード・設定・通常利用のDBは変更していない。リポジトリへの変更は本レビューの追記のみ。

### 指摘事項

#### 1. [中] 目標抽出の不正出力で、評価が試行単位の失敗を記録せず全体を終了する

- **場所：** `backend/app/evaluation/runner.py:506`、`backend/app/evaluation/steps.py:129`、`backend/app/agent/goal_reflection.py:117`。
- **確認区分：** 実行して再現。
- **発生条件：** reflectの4回目がJSON配列でない返答、必須項目欠落などを返す。
- **再現手順：** FakeLLMに通常返答、記憶抽出の `[]`、目標抽出の `invalid output` を設定し、say→reflectのシナリオを `run_scenarios` で実行した。
- **観測結果：** `GoalReflectionError` が `run_scenarios` の外へ伝播した。失敗した試行を含む結果一覧は返らない。
- **影響：** 残りの試行・シナリオが走らず、評価の集計を取得できない。本番ジョブは同例外を捕まえるが、評価器の既知例外一覧には追加されていない。今回の正常なレポートでは見えない失敗経路である。
- **修正方針：** 評価器でもGoalReflectionErrorを既存の振り返り失敗と同様に記録し、後続試行へ進める。4回目だけ不正出力になる試行と、その後の正常試行を組み合わせて検査する。

#### 2. [中] 予定の相対日付を、発言日ではなく振り返り日から数えるよう指示している

- **場所：** `backend/app/agent/goal_reflection.py:180`、`backend/app/agent/reflection_job.py:231`、`backend/app/evaluation/steps.py:140`。
- **確認区分：** コード確認のみ。実モデルが誤った日付を返す再現は未実施。
- **発生条件：** 「明日」「今週の土曜」などを話した会話を翌日以降に振り返る。評価でもsay→advance_time→reflectで成立する。
- **影響：** ログには発言日があるのに、追加指示は明示的に振り返り日を基準にする。例えば9月8日の「明日」の予定を9月10日に振り返ると、指示どおりなら予定日を9月11日と解釈し、翌日という実行条件もずれる。既存の記憶抽出は `reflection.py:436` で発言日を基準にしており、同じ予定の記憶と目標で基準が一致しない。
- **修正方針：** 相対日付は根拠の発言日から数え、振り返り日は現在との前後判定だけに使う。同じログを異なる日に振り返っても予定の絶対日付が変わらない検査を追加する。

#### 3. [中] 不正な実行基準日が、日付制限のない「次の会話」に変換される

- **場所：** `backend/app/agent/goal_reflection.py:84`、同105〜111行。選択側は `backend/app/agent/goal.py:213`。
- **確認区分：** 実行して再現（既存の不正日付テストを含む250件の実行）。採用後の選択への影響はコード確認。
- **発生条件：** モデルが空欄ではなく `来週くらい` や `2026-13-45` のような読めないafter_dateを返す。
- **観測結果：** 既存テストが確認する通り、エラーではなく `next_conversation, None` に変換される。
- **影響：** 初期状態はpendingなので即時に質問するわけではない。しかし採用時点では「指定日の読み取りに失敗した」という情報がなく、日時を修正せず採用すると、選択側は日付を条件にしない。occurred_atの欠落と違い、実行条件の欠落は制限を緩める。意図的なフォールバックだが、予定後に聞くという意味を保存できない。
- **修正方針：** 空欄と不正な非空文字列を区別する。不正値は抽出エラーにするか、日付の修正を必要とする状態として保持し、黙ってnext_conversationへ変換しない。過去の有効な日付を許す判断は、この問題とは別で妥当。

#### 4. [低] 4回目の処理中に、画面の進行説明が空になる

- **場所：** `backend/app/models.py:172`、`frontend/src/types.ts:202`、`frontend/src/components/ChatPanel.tsx:54`・292行。
- **確認区分：** コード確認のみ。
- **発生条件：** ポーリングが新しい `step: "goals"` を受け取る。
- **影響：** フロントの型とラベル表にgoalsがないため、表示は「振り返っています：」だけになる。null用の既定値では未知の非null値を補えない。ポーリング停止や保存失敗ではなく、追加された待ち時間の進行説明が欠落する問題。
- **修正方針：** 型とラベルにgoalsを追加し、この段階を挟んで完了する画面テストを追加する。

### 評価の測り方と実モデル記録の評価

- `20260908T112434-2026-09-07.1/report.md` の **30/33**、人格変更要求だけ0/3という内訳は申告と一致する。ただし「既存抽出に回帰がない」は、この11シナリオの機械判定で観測されなかった範囲の結論であり、関心や目標の品質全般を保証しない。
- `20260908T114100-2026-09-07.1/report.md` は **3/33、実行失敗0**。抽出・採用操作は実行されているが、その時点では「映画」という語で判定している。通しシナリオ3回目は「見る映画のタイトルと、なぜ前から見たかったのか」を採用しているため、「予定後の感想を聞く目標を正しく抽出・採用できた」とまでは言えない。現在の「感想」判定へ変更する前の結果として扱う必要がある。
- 変更前 `20260908T114552` と変更後 `20260908T114810` の各レポートは、ともに **2/6**。完了した予定を示す文面の1/6→4/6という読み取りも一致する。ただし合計点は未実装のstart_conversationも含む。通しシナリオ側の目標候補チェックは1/3→3/3へ変化しており、「自動判定が変わらない」は総合点についてのみ正しい。
- 書式指定を残すこと自体には反対しない。6試行では生成の揺れと効果を分離できず、「改善を確立した」とは扱わない。同じシナリオ・時刻条件で再測定し、総合点ではなく目標抽出の判定を分けて比較する。
- PR6で先に検査すべきなのは**本文の語だけでなくtriggerとdue_at**。日時を固定した「土曜に映画」から日曜のJST午前0時を得ること、振り返りを遅らせても変わらないこと、期限前は選択されず期限後は選択されることは、PR7の発話実装を待たずに測れる。現状のレポートは目標のtrigger・本文は出すがdue_atを出さず、日付の正しさを読み手も確認できない。
- 余分な目標は、否定語一覧だけで防ごうとせず、単一目的の固定シナリオで候補数・実行条件を検査し、各候補の目的と根拠を個別に評価する。自由会話全般に件数制限を押し付ける必要はない。表現の自然さ・余計な質問かどうかは人手評価として分離する。

### 問題としなかった点・未確認

- 4回すべて成功してから一つのトランザクションで保存する構造、GoalReflectionErrorを本番ジョブで失敗として記録すること、相手を一人に決められない場合に目標を作らないことは確認できた。今回の250テストも成功している。
- pending/activeだけを重複回避用に渡すことはPR9での繰り返し防止と合わせて扱う。doneを渡すだけでは完了後の新しい別イベントまで抑える可能性があり、今回の承認条件へ追加しない。
- 実モデルの評価は新規に実行していない。上記は既存レポートを読んだ照合であり、独立した再測定ではない。実ブラウザ、実サーバー、VOICEVOXの接続確認も未実施。
- フロントのテスト・型検査は今回未実行。PR7〜9の行動選択、delivery連動、完了・重複防止は対象外。

## PR6 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**PR6は承認してよく、PR7へ進める。** 前回の4件は解消した。今回確認した修正範囲で、新たな承認を妨げるコード不具合は見つからなかった。承認対象は、目標候補を別の呼び出しで抽出し、手動採用前のpendingとして保存するPR6であり、抽出品質の確立やフェーズ4全体の完了承認ではない。

### 対応の評価

| 前回の指摘 | 評価・確認区分 |
| --- | --- |
| 1：不正出力で評価全体が終了する | 解消。`backend/app/evaluation/runner.py:508` の例外一覧にGoalReflectionErrorが追加された。既存テストに加え、一時テストで「4回目が不正出力→次の試行は正常」の2試行を実行し、最初だけが失敗として残り、次が成功することを確認した。**実行して確認。** |
| 2：相対日付の基準が振り返り日になる | 解消。`backend/app/agent/goal_reflection.py` のシステム指示とユーザー側の補足が、どちらも発言日基準になった。振り返り日は予定が過ぎたかの判断に限定している。追加テストも指示内容を検査している。**コード確認およびモックテスト。実モデルの日跨ぎ精度は未確認。** |
| 3：不正な日付がnext_conversationになる | 解消。非空の不正日付は読み取り段階でGoalReflectionErrorになり、scheduleにも防御がある。空欄・nullは従来どおり次の会話となる。不正日付3種類と空欄・nullのテストが成功した。**実行して確認。** |
| 4：goalsの進行説明が空になる | 解消。フロントの型・ラベルにgoalsが追加され、進行テストがその表示を経由して完了まで検査する。**モックを使った画面テストで確認。** |

日付の検証は保存より前に行われ、本番・評価とも4回の抽出が成功した後に候補を保存する構造を維持している。レポートへの基準日時追加もコードで確認し、一時テストでUTCの日付が地域時刻の `2026-09-13 00:00` として表示されることを検査した。

### 指摘事項

新たな承認を妨げる指摘なし。

### 測定記録と残る品質確認

- 対応記録と `logs/evals/20260908T120745-2026-09-07.1/report.md` を照合した。**3/6、実行失敗0、目標候補7件のうち「見た」という完了後の文面2件**は記録と一致する。書式指定の効果を確立したとは扱わない、という評価は妥当。
- 同レポートの通しシナリオ1回目には、タイトルを聞くnext_conversationの候補が混ざり、感想の候補の基準日も **9月14日** になっている。他の回は9月13日であり、9月12日土曜の翌日という指示に対して1日ずれた出力である。基準日時の表示により、この差を確認できるようになった。これは実モデルを今回動かして再現したものではなく、既存ログでの観測である。
- したがって、予定日と本文の意味が安定して正しいことは未確認。現在の語の判定は、上記の余分な候補や日付のずれを落とさない。対応記録が明記する、候補数・trigger・due_atの検査、say→advance_time→reflectによる日跨ぎ評価はPR10で残す必要がある。
- この未確認をPR6の保留理由にはしない。今回直したのは誤った日付基準の指示と不正値の処理であり、有効な形式でもモデルが誤った内容を出す品質課題とは区別する。候補はpendingで保存され、採用は開発者が判断するという現在のPR範囲に照らして承認する。自動採用やフェーズ全体の品質合格を意味しない。

### 実行した確認・変更範囲

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr6-rereview.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **253件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- frontend：`npx tsc --noEmit` — 成功。`npm test -- --run` — **41件成功**。
- 一時テスト：`/private/tmp/test_yui_pr6_followup.py` を同じ一時DB用環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr6_followup.py -q` — **1件成功**。評価器自身の一時DBで、失敗後の試行継続と基準日時の表示を確認した。
- 実モデルの再測定、実ブラウザ・実サーバー・音声接続確認は未実施。ラベルを削除するとテストが落ちるという対応記録の操作は再実施していない。
- ソースコード・設定・通常利用のDBは変更していない。リポジトリ内の編集は本レビューの追記のみ。

## PR7 — 初回レビュー（2026-09-08）

### 結論

**PR7の承認は保留する。** 行動選択と自発発話の経路は接続されたが、DBの書き込みロック、会話終了との競合、選択結果の引き継ぎ、記録と評価に不具合・仕様漏れがある。PR8・PR9や登録済みのモデル品質課題の未対応を保留理由にしているわけではない。

### 対象・実行確認

- `6334a94` からの未コミットPR7変更、新規action_selectorと9テスト、通常会話・終了・DB依存・評価の関連経路を確認した。AGENTS、設計書§6、計画PR7、ISSUE-024・027・029、前回までのレビューと対応を参照した。
- backendで `YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr7-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q`：**262件成功**。`.venv/bin/ruff check --no-cache app tests`：成功。
- `/private/tmp/test_yui_pr7_review.py` に再現専用テストを置き、同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr7_review.py -q`：**4件成功**。これは不具合を観測するテストが成功したという意味である。既存fixtureと評価器の一時DBを使用した。

### 指摘事項

#### 1. [高] 行動選択のLLM待機中、通常会話がSQLiteの書き込みロックを保持する

- **場所：** `backend/app/agent/conversation.py:119`（発言のflush）、147行（行動選択）、171行（commit）。
- **確認区分：** 実行して再現。
- **発生条件：** 実行可能な目標のある相手から通常発言を受け、行動選択が応答待ちになる。
- **再現手順・観測結果：** FakeLLMを行動選択の指示で停止させ、別セッションのbusy_timeoutを50msにして会話を挿入したところ、`OperationalError: database is locked` を観測した。門を開くと元の要求は完了した。50msは再現を短くするための一時接続設定で、本番設定を変更していない。
- **影響：** モデル待ちが他の会話・記憶訂正などの書き込みを妨げ、待ち時間がSQLiteのタイムアウトを超えると失敗する。また、この段階のLLM失敗は発言のcommitより前なので、既存の「生成失敗でも入力を履歴に残す」動作も維持できない（こちらはコード確認）。
- **修正方針：** 最初のモデル呼び出しである行動選択より前に、入力の保存と書き込みトランザクションを確定する。既存の応答生成だけでなく行動選択を止めて別接続から書けること、選択失敗後も入力が残ることを検査する。

#### 2. [高] openが振り返り中の会話へ発言を追加でき、ロック取得後の終了状態も確認しない

- **場所：** `backend/app/api/conversations.py:266`〜279行。
- **確認区分：** 実行して再現。ロック待ち後の終了との競合はコード確認。
- **発生条件：** reflection_started_atが設定され、まだ完了していない会話へopenを送る。またはopenがロックを待っている間にendが開始権を確定する。
- **再現手順・観測結果：** 一時DBの会話にreflection_started_atを設定し、askを返すモックでopenした。409ではなく **200で発言を返し、追加した**。
- **影響：** 振り返り対象を固定した後に発言が入り、抽出結果と終了した会話ログが食い違う。通常chatが持つ振り返り中・ended_atの検査と、ロック取得後のrefreshがopenにない。ロック自体は逐次実行にするだけで、状態の再検査にはならない。
- **修正方針：** chat同様、ロック取得後に会話を再読込して終了・振り返り中を拒否し、保存のcommitまでロック内で完了させる。会話開始の要求を一度だけ扱うかも明確にし、同じopenの重複要求を直列に二回生成することと、後続PRでの目標の実行済み判定を分けて検査する。

#### 3. [中] 通常応答では、選んだ目標を捨てて全目標から生成側に選び直させる

- **場所：** `backend/app/agent/conversation.py:155`〜164行、`backend/app/agent/prompt.py:115`付近のask指示。
- **確認区分：** コード確認のみ。
- **発生条件：** 実行可能な目標が複数あり、行動選択が特定の番号をaskとして選ぶ。
- **影響：** choice.goal_idsは生成へ渡されず、全goalsと「この中から1つだけ」という指示が渡る。選択段階で相手の様子に合わせて目標Bを選んでも、生成側にはAとBを区別する決定が伝わらない。実モデルが必ず別の目標を聞くという断定ではなく、決定した対象が引き継がれない実装上の欠落である。
- **修正方針：** openと同様に選択対象を明示する。未選択の目標も文脈として渡すなら、今回触れる目標と持ち出さない目標を区別し、二つの目標から2番を選んだときの生成入力をテストする。

#### 4. [中] actionの型が不正だと、待機へのフォールバックより先に例外が出る

- **場所：** `backend/app/agent/action_selector.py:125`付近（`action not in allowed`）、136行。
- **確認区分：** 実行して再現。
- **発生条件：** JSONとしては読めるがactionが配列・オブジェクトである出力。
- **再現手順・観測結果：** `_parse('{"action": [], "goal": 1}', ...)` を実行し、**TypeError（unhashable type）**を観測した。
- **影響：** 「読み取れなければ待機して会話を続ける」経路を外れ、通常会話・自発発話・評価を例外終了させる。またgoalにtrueを指定するとPythonではintとして1番に解釈されるため、番号の型検査も厳密でない（コード確認）。
- **修正方針：** 集合との比較前にactionが文字列であることを検査し、goalはboolを除く整数として検証する。不正な型・範囲外・欠落を待機へ倒すテストを追加する。

#### 5. [中] 待機の判断と、自発発話に使った状態の参照記録が残らない

- **場所：** `backend/app/agent/conversation.py:262`付近の早期return、287行、325行。通常応答のRunRecord生成は198行以降。
- **確認区分：** コード確認のみ。
- **発生条件：** openがwait/researchを選ぶ、または可変状態がある相手へ自発発話する。
- **影響：** 無発言なら永続的な行動記録が一切作られない。発言しても実際に渡した可変状態のIDを常にNoneとして記録する。選択理由と選択対象IDもRunRecordへ保存しておらず、通常応答の全候補IDだけでは決定の根拠を再確認できない。完了条件5の追跡対象から、待機や一部の根拠が欠落する。
- **修正方針：** 発言を必須としない行動判断の記録を用意する（既存run_recordsを拡張する場合は既存参照先・一覧・移行も整合させる）。会話・相手、候補と選択対象、行動、理由、時刻、モデル情報を保存し、生成時は実際に取得したstatesのIDを記録する。架空の空発言を作って辻褄を合わせない。

#### 6. [中] 自発発話の評価が、実際に渡した記憶・状態を空として判定する

- **場所：** `backend/app/evaluation/runner.py:535`付近（`_check_turn(step, reply, [], [], referenced_goals, replies)`）。
- **確認区分：** 実行して再現。
- **発生条件：** start_conversationで記憶を検索・利用し、expect_memoriesまたはexpect_not_memoriesを指定する。状態の判定も同じ空配列を使う。
- **再現手順・観測結果：** 「映画の感想は最高だった」という記憶と採用済み目標を投入し、start_conversationに `expect_not_memories = ["movie"]` を設定した。生成用system_promptにその記憶が入っていることを確認したが、**試行は合格した**。
- **影響：** 不適切な参照の見逃しと、正しい参照の誤不合格が起きる。openに追加した記憶検索が評価の入力へ反映されていない。
- **修正方針：** opened.runの参照IDを通常sayと同じキーへ変換し、記憶・状態の判定とレポートへ渡す。渡っている記憶へのexpect_not_memoriesが失敗し、expect_memoriesが成功する対のテストを追加する。

#### 7. [中] openのモデル接続失敗が、通常会話と同じ503応答にならない

- **場所：** `backend/app/api/conversations.py:279`。比較対象は `backend/app/api/chat.py` のLLMError処理。
- **確認区分：** コード確認のみ。
- **発生条件：** openの行動選択または発話生成で、タイムアウト・接続失敗などのLLMErrorが起きる。
- **影響：** ハンドラにもアプリ共通処理にもLLMErrorの処理がなく、理由付き503の代わりに未処理例外による500になる。モデル失敗と正常な待機を区別して呼び出し側へ伝える既存APIの契約と揃わない。
- **修正方針：** openでもLLMErrorを503へ変換し、選択・生成の各段階で失敗応答と部分保存がないことを検査する。型不正による指摘4とは別の、正常に分類された接続エラーの処理である。

### 評価記録と判断に対する意見

- `20260908T124448-2026-09-07.1/report.md` の **28/36、実行失敗0** と各シナリオの内訳は申告と一致した。ただし修正前の測定であり、現在コードの再測定結果ではない。指定された1236台のreport.mdは今回のファイル一覧では見つからず、別途3/3という記録は独立に確認できなかった。
- expect_not_goalsが無発言時に通ること自体は「目標を生成プロンプトに渡さなかった」の判定として矛盾しない。しかしそれでは正当なwait、読み取り失敗によるwait、research、無関係な発言を区別できない。期待するactionと発言の有無を別の判定項目にし、正常な待機とフォールバックを分けて集計すべきである。正例のexpect_goals・expect_anyは無発言なら落ちるので、全シナリオが空振りで通るわけではない。
- 新しい会話でのopenは現在のconversation_idの履歴だけを読む。評価のstart_conversationも新規会話を作るため、行動選択に渡る「前回までのやりとり」は空になる。計画の「通常は前の会話がある」という説明から、その履歴を判断に使えているとは結論できない。前セッションの疲労・拒否を踏まえた待機を測るなら、公開範囲を守った文脈取得と、それが選択へ渡る評価が必要。
- 行動選択の追加遅延は測るべきだが、直ちに生成と統合すべきという根拠はない。現在のretrieval_msは選択前に終わり、latency_msは返答生成の値なので、この追加待ち時間を既存記録から分離できない。選択単独と要求全体の時間を測って判断する。実モデルによる増分は今回未計測。
- last_executed_atと完了・繰り返し防止をPR8・9へ送る判断、画面をISSUE-027と合わせて実装する判断はPR分割として妥当。画面がないこと単独を保留理由にはしない。
- 通常のselect_action実装ではask/suggestに有効なgoal_idsが必須なので、openの `or goals[:1]` は到達しない防御に見える。今回の実害の指摘には含めない。既知のISSUE-028・029も再指摘しない。

### 未確認・変更範囲

- 実モデルの新規評価、実ブラウザ・音声接続確認は実施していない。フロントは変更対象外で、今回テストを再実行していない。
- ソースコード・設定・通常利用のDBは変更していない。リポジトリへの編集は本資料の追記のみ。再現コードとDBは一時領域に置いた。

## PR7 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**PR7の承認は引き続き保留する。** 前回の多くの指摘は解消したが、openでの話者作成・更新に伴う書き込みロックと、行動記録のモデル情報の欠落が残る。再測定の説明にも、ログと一致しない箇所がある。

### 前回の対応評価

- **指摘1：一部解消。** respondは行動選択より前にcommitし、追加テストで別接続の書き込みを検査している。ただしopen側に残る経路は下記1のとおり。
- **指摘2：解消。** openがロック前後で終了・振り返り中を検査し、ロック取得後にrefresh、保存後にロック内でcommitする。振り返り中の409テストも成功した。
- **指摘3：解消。** 通常応答の生成には選択対象だけを渡し、referenced_goal_idsも同じ集合になる。2番目の目標を選ぶ追加テストを確認した。
- **指摘4：解消。** actionの型を集合比較の前に検査し、目標番号からboolを除外している。不正action型のテストも成功した。
- **指摘5：一部解消。** action_recordsで無発言の待機・調査を保存し、候補と選択対象、理由、相手を追える。自発発話に渡す可変状態のIDも記録される。ただしモデル情報は下記2のとおり未対応。
- **指摘6：解消。** 評価器がopenの実際の参照記憶・状態を判定に渡す。一時テストで、記憶を実際に渡した自発発話にexpect_not_memoriesを指定すると、前回と違って不合格になることを独立に確認した。
- **指摘7：解消。** openでもLLMErrorを503へ変換する。追加テストも成功した。

### 残る指摘事項

#### 1. [高] openで話者を新規作成すると、行動選択中に書き込みロックが残る

- **場所：** `backend/app/api/conversations.py:288`〜302行、`backend/app/agent/memory_store.py:115`、`backend/app/agent/conversation.py:283`。
- **確認区分：** 実行して再現。
- **発生条件：** 未登録のspeakerをopenに指定し、実行可能な目標があって行動選択がモデル待ちになる。既存話者の表示名変更でも更新がflushされる経路がある（こちらはコード確認）。
- **再現手順・観測結果：** 一時DBに空の会話と相手を限定しないactive目標を作成し、未登録話者を指定してopenした。FakeLLMをopenの行動選択で止め、別セッションのbusy_timeoutを50msにして会話を挿入すると、**OperationalError: database is locked**となった。門を開くとopenは200で完了した。50msは再現用接続の設定であり、通常DBの設定は変更していない。
- **影響：** get_or_create_speakerのINSERTはflushされるが、openが最初にモデルを呼ぶ前にはcommitされない。通常応答側で直したのと同じ、他の会話や訂正を妨げるロックが残る。既存の追加テストはchat経由であり、この入口を検査していない。
- **修正方針：** openも、話者解決で生じた書き込みを最初のモデル呼び出しより前に確定する。会話ロック待ちに入る前に話者の作成・更新をcommitするchatと同じ扱いを検討し、ロック取得後の会話状態再検査は維持する。新規話者・表示名変更のopenを止めたまま別接続から書けることを検査する。

#### 2. [中] 新設した行動記録に、判断に使ったモデル・版・設定が保存されない

- **場所：** `backend/app/models.py:556`〜578行、`backend/app/agent/action_selector.py` のselect_action末尾、`backend/app/agent/conversation.py:298`。
- **確認区分：** コード確認のみ。
- **発生条件：** モデルを呼んでwait/researchを選び、発言なしで終了する。または判断後に発話する。
- **影響：** action_recordsは理由・ID等を保存するが、行動選択のLLMResponseにあるprovider・model・model_digest・optionsを捨てる。無発言時はrun_recordsもないため、後からモデルや設定を変更すると、過去の待機をどのモデル・設定が判断したか追えない。発言時のrun_recordsは後段の生成情報であり、行動選択の温度0.2等の記録の代わりにはならない。前回指摘5で求めたモデル情報の保存は未完了。
- **修正方針：** ActionChoice等から判断に使ったモデル情報を持ち帰り、action_recordsへ保存する。モデルを呼ばない決定はモデル情報なしとして区別する。無発言でも情報が保存・再読込できることを検査する。

#### 3. [低] 再測定の「条件1・2はすべて3/3」「抽出の揺れ」という説明がログと一致しない

- **場所：** `docs/review/claude/phase4_review_response.md:803`付近と813行。照合元は `logs/evals/20260908T133319-2026-09-07.1/report.md:88`以降。
- **確認区分：** コード確認のみの区分に準じる資料・既存ログの照合。実モデルの独立再実行はしていない。
- **発生条件・観測内容：** 条件1の通しシナリオgoal-is-extracted-adopted-and-askedは1/3。1回目は抽出・採用・ask選択・目標参照まで成功しているが、返答が自己紹介と「今日一日、どんなことをして過ごされましたか？」となり、映画に触れず失敗している。2回目は目標候補の抽出で失敗している。
- **影響：** 2回の失敗をまとめて「抽出の揺れ。ISSUE-028」とすると、今回の自発発話生成で目標を無視した失敗を取り落とす。27/36という集計自体は一致するが、それだけで差を生成の揺れと確定することもできない。
- **修正方針：** 抽出失敗と、目標を渡した後の生成失敗を分けて記録する。「すべて3/3」は対象を正確に限定する。新たなコード回帰が確定したとは言えないため、生成失敗の原因や再現率は再測定まで未確認とする。

### 評価・移行に関する補足

- expect_actionの追加でanswer・ask等とwaitを区別できるようになった。一方、読み取り失敗時のフォールバックもactionはwaitなので、**正常な待機判断と解析失敗を区別する検査にはまだなっていない**。現時点の3/3をその区別まで保証する数字とは扱わない。
- action_recordsを別テーブルにした判断は妥当。既存run_recordsのmessage_id必須・一意性を崩さず、無発言の判断を保存できる。新規マイグレーション24c03fb94c47は既存テーブルを変更せず、downgradeは索引と新テーブルを除去する。モデルとのスキーマ一致テストは今回成功した。downgradeの独立実行と、通常DBへの適用は行っていない。

### 実行した確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr7-rereview.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **268件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr7_response_review.py -q` — **2件成功**。openの書き込みロック残存と、評価の記憶参照判定の修正を確認した。
- 実モデルの再測定、実ブラウザ・音声確認は未実施。フロントは変更対象外で再テストしていない。
- サブエージェントは使用していない。ソースコード・設定・通常利用DBは変更せず、リポジトリ内の編集は本レビューの追記のみ。

## PR7 — 第3回レビュー・対応確認（2026-09-08）

### 結論

**PR7は承認してよく、PR8へ進める。** 前回残った3点は解消した。回帰テストに低重要度の検査漏れが1件あるが、正しい入力を使った独立テストで修正コードの動作を確認できたため、PR7の承認は妨げない。これは行動選択・参照・記録の実装に対する承認であり、実モデルの品質やフェーズ4全体の完了承認ではない。

### 前回の対応評価

- **openの書き込みロック：解消（実行確認）。** 話者の作成・表示名更新を、会話ロック待ちと最初のモデル呼び出しの前にcommitするようになった。JSON本文で新規話者を指定するケースと、既存話者の表示名を更新するケースを一時テストで実行した。行動選択を停止したまま、別接続から新しい表示名を読み取れ、会話の追加・commitも成功した。ロック取得後の終了状態再検査も維持されている。
- **行動判断のモデル情報：解消（コード確認・テスト実行）。** select_actionがLLMResponseからprovider・model・model_digest・optionsを持ち帰り、通常応答・無発言の待機・自発発話の各保存経路でActionRecordへ記録する。無発言の待機にもモデル情報と温度0.2が残るテストが成功した。モデルを呼ばない決定は情報なしで区別できる。
- **測定の説明：解消（資料確認）。** 対応記録に訂正が追記され、通しシナリオは1/3であること、1回目は目標参照後の生成失敗、2回目は抽出失敗であることを分けている。前回確認したログと一致する。生成が目標を無視する問題の原因・再現率を未確認としてPR10で測る整理は妥当。

### 指摘事項

#### 1. [低・承認を妨げない] 新規話者の回帰テストがJSON本文ではなくクエリパラメータを送っている

- **場所：** `backend/tests/test_action_selection.py:477`〜484行。APIの入力定義は `backend/app/api/conversations.py` のopen_conversationのspeaker引数。
- **確認区分：** コード確認のみ。正しいJSON本文による修正動作は別の一時テストで実行確認済み。
- **発生条件：** test_open_releases_the_write_lock_when_it_creates_a_speakerを実行する。
- **影響：** SpeakerRefはリクエスト本文から読むが、テストはparamsでnewcomerを指定している。この指定は話者解決に使われず、準備のchatで既に作った既定話者が使われるため、未登録話者のINSERTという回帰条件を検査できない。現状のテスト成功だけではこの修正の有効性を示せない。
- **修正方針：** paramsをjsonへ変更し、モデル待機中に別接続からnewcomerが存在することも検査する。表示名更新のケースも同様に対象の話者を確認すると、入力が無視される空振りを防げる。

### マイグレーションと残る品質確認

- 新規fb349a94d0fbは、既存action_recordsへnullableなモデル情報4列とis_fallbackを追加する。モデルとのスキーマ一致テストは成功した。
- 一時DBで24c03fb94c47まで適用し、行動記録を置いてheadへupgrade、24c03fb94c47へdowngrade、再びheadへupgradeした。旧行の理由が保持され、追加モデル情報はNULLで読み戻せた。通常利用のDBには適用していない。
- is_fallbackの追加により、正常な待機と解析失敗による待機をDB上で区別できるようになった。**評価器のexpect_actionはまだこの印を見ていない**ため、waitの合格数が解析失敗を除外した数字とは限らない。これは前回からの評価上の留保として残し、記録機構が直ったことと区別する。
- 目標を渡した後の生成失敗、基準日時の品質、前セッションの文脈を使う評価、選択呼び出しの追加遅延について、新しい実モデル測定は行っていない。これらを解決済み・品質合格とは判断しない。

### 実行した確認・変更範囲

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr7-third-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **271件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr7_final_review.py -q` — **3件成功**（新規話者、表示名更新、移行往復）。
- 対応記録の確認結果欄は前回の268件のままだが、今回のコードでは追加3件を含め271件成功した。
- 実モデル・実ブラウザ・音声接続確認は未実施。フロントは変更対象外で再テストしていない。
- サブエージェントは使用していない。ソースコード・設定・通常利用DBは変更せず、リポジトリ内の編集は本レビューの追記のみ。

## PR8 — 初回レビュー（2026-09-08）

### 結論

**PR8の承認は保留する。** 再生通知から目標更新への経路はできているが、未再生・待機を実行済みにする経路、文字のみの構成で実行記録が付かない経路、失敗後の再送で回復できないトランザクション分割がある。PR9の完了・繰り返し防止を先取りした指摘ではなく、現在のlast_executed_atの誤記録・欠落を対象とする。

### 対象・実行確認

- effe8bd以降の未コミットPR8変更と新規test_goal_execution.py、呼び出し元の会話生成・通知API、行動選択・記録、フロント再生器、評価器を確認した。AGENTS、設計書§6、計画PR8とISSUE-011の繰り上げ判断、ISSUE-011・013を参照した。
- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr8-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **277件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr8_review.py -q` — **4件成功**。以下の不具合を観測するためのテストが成功したという意味である。通常DBは使用していない。

### 指摘事項

#### 1. [高] 再生状態だけを先にcommitするため、目標更新に失敗すると再送で復旧できない

- **場所：** `backend/app/agent/delivery.py:70`〜81行。
- **確認区分：** 実行して再現（障害注入）。
- **発生条件：** 再生状態のcommit後、目標更新の完了前にDBエラー・中断・プロセス停止などが起きる。
- **再現手順・観測結果：** completed通知で `_mark_goals_executed` に例外を注入した後、元の関数に戻して同じ通知を再送した。再送は200だが、Messageはcompleted、Goal.last_executed_atはNULLのままだった。
- **影響：** 最初のcommitで再生状態が終端になり、再送はadvanced=Falseとなるため、未完了の目標更新を永久に飛ばす。これは通知自体が届かないISSUE-013とは別で、受信済み通知の内部処理が不整合になる問題。
- **修正方針：** 状態遷移の条件付きUPDATEと目標・履歴の更新を同じトランザクションで確定する。途中失敗で状態も戻り、再送で一度だけ反映されることを検査する。rowcountによる二重通知防止はその中で維持する。

#### 2. [中] 再生開始前のabortedまで、途中まで届いたものとして実行済みにする

- **場所：** `backend/app/agent/delivery.py:28`、79〜87行、`backend/tests/test_goal_execution.py` の中断テスト。
- **確認区分：** 実行して再現。
- **発生条件：** generatedからplayingを経ずにabortedへ遷移する。既存の状態機械は「再生開始前の停止」としてこの遷移を許している。
- **再現手順・観測結果：** 生成した質問にplaying通知を送らずabortedを送った。delivery_started_atはNULLなのに、目標のlast_executed_atが設定された。
- **影響：** 未再生と部分再生を区別せず、「鳴らなければ実行済みにしない」というPR8の目的に反する。今回の既存中断テストも同じ遷移を成功として固定している。開始通知欠落の可能性もあるため、開始時刻なしを常に未再生と断定することもできないが、少なくとも「途中まで届いた」と確定する根拠はない。
- **修正方針：** 再生開始が確認された中断と、開始前／開始不明の中断を区別する。必要なら通知に再生開始の証拠を持たせる。テストと評価も、playing→abortedとgenerated→abortedを別に扱う。現在の評価deliverはabortedを直接送るので、部分再生の検証にはなっていない。

#### 3. [中] 音声無効で最初からcompletedになった発言は、実行済みに反映されない

- **場所：** `backend/app/agent/conversation.py:193`・358行付近、`backend/app/agent/delivery.py:79`。
- **確認区分：** 実行して再現。
- **発生条件：** speech_enabled=Falseで目標を選び、通常応答または自発発話を生成する。
- **再現手順・観測結果：** 一時テストで音声を無効にしてaskを生成した。発言はcompletedだが、目標のlast_executed_atはNULLだった。さらにcompleted通知を送ってもNULLのままだった。
- **影響：** 依頼文の「文字だけの構成では通知なしで実行済みになる」は実装されていない。mark_executedへの接続は通知経路だけで、生成時completedの場合はadvancedが成立しない。
- **修正方針：** 音声無効時の到達を扱う既存方針に合わせ、発言とActionRecordを作成した後、同じトランザクション内で選択対象を実行済みにする経路を設ける。respond/openの両方と、後から通知しても記録が増えないことを検査する。

#### 4. [中] wait・answer・researchの行動記録でも、目標番号があれば実行済みにする

- **場所：** `backend/app/agent/delivery.py:99`〜105行、`backend/app/agent/action_selector.py` の_parse、`backend/app/agent/conversation.py` のActionRecord保存。
- **確認区分：** 実行して再現。
- **発生条件：** モデルが `{"action":"wait","goal":1}` のように、持ち出さない判断へ目標番号を付ける。現在のパーサーはこれを受け付け、selected_goal_idsへ保存する。
- **再現手順・観測結果：** waitと目標1を返すモックで「ゆっくり休んでください」という通常返答を作り、completedを通知した。selected_actionはwaitなのに、目標は実行済みになった。
- **影響：** selected_goal_idsだけでは「今回その目標を話した」と判定できない。待機中の目標を実行済みにするため、run_recordsではなくaction_recordsを使うという設計意図も満たせていない。
- **修正方針：** 到達時の対象をask/suggestなど実際に目標を持ち出す行動に限定する。非実行行動の番号を選択記録として保持するかは別途決めてよいが、実行判定側はその有無だけで更新しない。wait・answer・researchに番号が付く対照ケースを検査する。

### 設計判断・評価への回答

- **abortedとPR9：** 再生開始後の中断を「何らかの発話を届けた」と記録する方針は妥当。ただし質問の核心まで届いたか、相手が回答できる状態か、目標を達成したかは別である。PR9には「last_executed_atだけでdoneや恒久的な再質問禁止にしない」「中断時は再確認を許す」という条件を渡す。現在goal_revisionsに残るのはabortedという分類だけであり、再生位置や届いた文面ではない。「どこまで届いたかを残す」という説明は「中断したことを残す」までに限定すべき。
- **ISSUE-013：** 未通知なら実行記録も欠落し、後続の頻度制御や再質問判断へ影響するため、影響欄は更新すべき。バックエンド発話キューをPR8で作る必要はないが、「ローカルでは実害が小さい」という従来説明だけでは影響を表せない。指摘1の受信後の部分commitはこの既知課題へ送らず、今回直す必要がある。
- **履歴・振り返りの線引き：** ローカルで文字が表示されることを理由に、ログをそのまま使う判断はPR分割として受け入れられる。ISSUE-011を一部対応としたのも適切。ただし今回のlast_executed_atは音声構成では音声の到達を基準にするので、画面への表示・音声の到達・目的の達成を同じ概念として扱わないことが必要。
- **並行通知：** 同一発言の条件付きUPDATEで、completed/abortedのうち状態を進めた通知だけが処理する方針は妥当。現在の生成経路は発言につき一つのActionRecordを作り、目標がない発言は記録がなくても問題なく処理を終える。これらと指摘1の二段commitは区別する。今回、並行通知の独立負荷テストは実行していない。
- **評価の設定：** runnerは受け取ったSettingsをそのままConversationAgentへ渡し、speech_enabled=Trueを強制していない。「deliverなしなら生成のみ」という説明は音声有効時に限る。文字のみの設定との差を明記し、指摘3を直した後も再生評価の設定が揺れないよう、明示的な設定または事前検査が必要。
- **deliverの対象：** 現在の会話で最後のYUI発言を選ぶ実装は手順の契約として明確であり、新しい会話で発言がなければ失敗として記録する。テストは未配信・完了・開始後中断・開始前中断を対で測るべき。deliver自体は状態が拒否されても対象があれば成功と記録するため、再通知や逆順通知では到達後の状態も別途確認する必要がある。

### 測定記録・未確認・変更範囲

- `logs/evals/20260908T140920-2026-09-07.1/report.md` の **8/9、実行失敗0、各3/3・3/3・2/3**は申告と一致する。ただし評価上の通知を送った試行であり、実音声の再生を検証した数字ではない。
- 既存6テストは基本の未再生・完了・中断・playing・選択対象・聞き直しを押さえるが、今回再現した4経路は検知しない。特に中断テストは開始前の状態を部分再生と同一視している。
- 実モデルの新規測定、実ブラウザ・VOICEVOXによる再生確認は実施していない。フロントはコード確認のみで、テストは再実行していない。PR9の完了・繰り返し防止、既知の品質課題は対象外。
- サブエージェントは使用していない。ソースコード・設定・通常利用DBは変更せず、本レビューのみ追記した。再現コード・データは一時領域に置いた。

## PR8 — 第2回レビュー・対応確認（2026-09-08）

### 結論

**PR8の承認は引き続き保留する。** 前回の4件に対する修正は入っており、逐次処理のテストは成功するが、開始通知と中断通知が重なる場合の実行記録漏れを1件再現した。

### 前回の対応評価

- **指摘1：解消。** 再生状態の条件付きUPDATEと目標更新が同じトランザクションになった。目標更新を故意に失敗させてから再送すると実行済みになる追加テストが成功した。
- **指摘2：逐次処理は解消、並行処理は下記指摘が残る。** 開始前のabortedは数えず、開始記録のあるabortedを数える方針になった。評価にplayingの通知と開始前中断の対照シナリオも追加された。
- **指摘3：解消。** speech_enabled=Falseではrespond/openの両方で選択した目標を生成時に更新する。後からcompleted通知を送っても履歴が増えないテストが成功した。
- **指摘4：解消。** ActionRecordのactionをask/suggestに限定してから実行対象を取り出す。waitに目標番号が付くケースも実行済みにならないことをテストで確認した。
- 「どこまで届いたか」から「中断だったこと」への記述訂正、ISSUE-013の影響更新、PR9へ渡す実行と達成・再確認の条件は妥当。通知欠落の既知課題と、今回のDB内の競合は別に扱う。

### 指摘事項

#### 1. [中] 中断の判定に古いMessageを使い、DBに開始時刻があっても実行記録を落とす

- **場所：** `backend/app/agent/delivery.py:79`〜85行・109行、`backend/app/api/conversations.py:495`。
- **確認区分：** 実行して再現。
- **発生条件：** 中断要求がgeneratedのMessageを読み込んだ後、別要求がplayingと開始時刻を確定し、その後に中断要求のUPDATEが実行される。フロントのnotifyは前の通知の完了を待たないため、通知要求は重なり得る。
- **再現手順：** 一時DBの質問をセッションAで読み込む（開始時刻NULL）。セッションBで同じ発言をplayingへ更新・commit。その後Aに保持したMessageを使いabortedを適用する。最後にAPIからabortedを再送する。
- **観測結果：** DBのMessageは **abortedかつdelivery_started_atあり** になったが、Goal.last_executed_atはNULL。abortedを再送してもNULLのままだった。
- **影響：** UPDATEはDBの最新状態を条件に成功する一方、`synchronize_session=False`なので手元のMessageは古い。flushはDBからの再読込ではなく、109行では古いNULLを見て「開始前」と判定する。refreshは判定・commitの後なので遅い。実際には開始通知を受信済みなのに実行記録を落とし、再送はadvanced=Falseとして補わない。通知欠落を前提にしない不具合である。
- **修正方針：** 条件付きUPDATE後、同じトランザクション内でDBの確定対象をrefreshまたはRETURNING等で取得し、その開始時刻で判定する。前回直した原子性を維持し、判定前にcommitを戻さない。上記の二接続の順序をテストし、実行履歴が一度だけ作られることを検査する。

### 実行確認・測定記録

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr8-rereview.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **281件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr8_stale_review.py -q` — **1件成功**。これは上記の記録漏れを観測するテストが成功したという意味である。
- `logs/evals/20260908T142732-2026-09-07.1/report.md` の **9/9、実行失敗0、3シナリオ各3/3**は対応記録と一致した。ただし開始→中断を順に送る評価であり、古いMessageを保持する二つの要求の競合は測っていない。

### 未確認・変更範囲

- 実モデルの再測定、実ブラウザ・VOICEVOXによる再生確認は未実施。フロントは関連コードの確認のみでテストを再実行していない。
- PR9の完了・繰り返し防止、既知の実モデル品質課題、通知欠落対策の実装は対象外。
- サブエージェントは使用していない。ソースコード・設定・通常利用DBは変更せず、本資料だけ追記した。再現コード・DBは一時領域に置いた。

## PR8 — 第3回レビュー・対応確認（2026-09-08）

### 結論

**PR8は承認してよく、PR9へ進める。** 前回残った開始通知・中断通知の競合による実行記録漏れは解消した。今回確認した修正範囲で、新たな承認を妨げる不具合は見つからなかった。これは再生状態と目標の実行記録を接続するPR8の承認であり、目標の達成判定やフェーズ4全体の完了承認ではない。

### 対応の評価

- **前回指摘：解消（コード確認・テスト実行）。** `backend/app/agent/delivery.py` は、条件付きUPDATEが成功した後、目標更新の判定前に `session.refresh(message)` を実行する。別の要求が確定した開始時刻を読み直せるため、古いMessageのNULLを見て開始前の中断と誤判定する経路がなくなった。
- refreshはcommitより前にあり、状態更新と目標更新の原子性を維持している。前回直した「目標更新に失敗したら再送で回復できる」扱いを崩していない。
- 追加の `test_a_stale_message_does_not_lose_the_execution_record` は、Aがgeneratedを読み込む→Bがplayingを確定する→Aが古いMessageでabortedを適用する、という前回の再現順序を実際に二接続で作る。開始時刻と目標の実行記録が残る検査が成功した。
- 初回指摘への対策である、開始前中断の除外、ask/suggestだけの実行判定、文字のみ構成での実行記録、聞き直し時の重複防止も関連テストで維持されている。

### 指摘事項

新たな指摘なし。

### 実行確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr8-third-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **282件成功**（目標実行関連11件）。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 対応記録の件数と一致した。修正を戻すと追加テストが失敗するという検証は今回再実施していない。

### 残る範囲・未確認

- 通知が届かない場合や開始通知自体が欠落した場合はISSUE-013の留保として残る。今回解消したのは、開始通知をDBへ確定済みなのに古い値で判定する競合である。
- PR9では、実行記録だけでdoneや恒久的な再質問禁止にしないこと、中断時に再確認を許すことを維持する必要がある。中断という分類は残るが、どの文面まで届いたかを保証するものではない。
- 実モデルの新規測定、実ブラウザ・VOICEVOXの再生確認は未実施。前回の9/9は既存レポートの結果であり、今回の再測定結果ではない。今回の修正はDBの読取順序で、生成経路の変更はない。
- フロントは変更対象外で再テストしていない。サブエージェントは使用していない。ソースコード・設定・通常利用DBは変更せず、本レビューだけ追記した。

## PR9 — 第1回レビュー（2026-09-09）

### 結論

**PR9は現時点では承認保留。** 既存テスト289件は成功したが、下記5件が残る。うち4件は一時DBとモックLLMで再現し、1件はコード上の指示と判定根拠の不一致を確認した。完了・取消を既存の行動選択に載せる構成そのものを否定するものではない。

### 対象

- 基準 `d11c85c` に対する未コミットの `action_selector.py`、`conversation.py`、`goal.py`、`config.py`、新規 `test_goal_completion.py`。関連する行動記録・再生記録・評価シナリオも確認した。
- AGENTS.md、README、設計書のフェーズ4・会話フロー、計画PR9とPR8の引継ぎ条件、ISSUE-013・021、過去のPR8レビューと対応を読んだ。
- ソースコード・設定・通常利用DBは変更していない。再現コードとテストDBは一時領域に置き、この資料だけ追記した。サブエージェントは使用していない。

### 指摘事項

#### 1. [高] 完了・取消の更新後、DBの書き込みロックを保持したまま返答生成を待つ

- **場所：** `backend/app/agent/conversation.py:193`、`:204`、`:245`、`backend/app/agent/goal.py:372`、`:399`。
- **確認区分：** 実行して再現。
- **発生条件：** 行動選択が有効な `answered` または `cancelled` を返し、続く返答生成に時間がかかる。
- **再現手順：** 一時DBに採用済み目標を作り、取消の行動出力を返す。次の生成呼び出しをイベントで停止し、別セッションから書き込む（再現を短縮するため、その接続のbusy_timeoutを50msに設定）。
- **観測結果：** 別接続のUPDATEが `database is locked` で失敗した。生成を解放すると元の会話要求は成功した。
- **影響：** 行動選択前のcommitでは、その後のmark_done/mark_cancelledのflushによる書き込みを解放できない。生成待ちが接続の待機時間を超えると、別会話・記憶訂正・再生通知等のDB書き込みが失敗する。PR7で防いだ問題が2回目のLLM呼び出し前に再発している。
- **修正方針：** 完了・取消とその根拠の保存を短いトランザクションとして確定させてから生成するか、更新を生成後の短いトランザクションへまとめる。後者では生成失敗時の完了・取消の扱いを明確にする。完了・取消それぞれについて、生成中に別接続が書けることを検査する。

#### 2. [中] 予定日前の目標が取消判定へ渡らず、中止しても後日質問対象になる

- **場所：** `backend/app/agent/conversation.py:156`、`backend/app/agent/goal.py:217`。
- **確認区分：** 実行して再現。
- **発生条件：** after_dateが未来の採用済み目標について、予定日前に「中止した」と話す。
- **再現手順：** 目標のdue_atを7日後にして、同じ相手から映画の中止を送信。その後、現在時刻を8日進めてactive_goalsを取得する。
- **観測結果：** 対象目標しかない場合、行動選択自体が呼ばれなかった。目標はactiveのままで、8日後には質問対象として取得された。
- **影響：** 判定用から再質問間隔は外したが、実行条件による絞り込みは残っている。予定の取消は実行可能日より前にも起こるため、完了条件4「予定変更・訂正・取消を反映する」を満たさない。既存の取消テスト・シナリオはnext_conversationの目標なので検出しない。
- **修正方針：** 状態更新の判定対象と質問可能な対象を分離する。相手・公開範囲の制限を維持したまま、将来予定も取消・変更判定に含める。予定日前の中止→予定日経過で持ち出さない通しテストを追加する。

#### 3. [中] 選択した目標を同時に終了すると、未選択の目標へフォールバックして再質問間隔を迂回する

- **場所：** `backend/app/agent/conversation.py:209`、`:218`、`:228`、`:309`。
- **確認区分：** 実行して再現。
- **発生条件：** 複数目標があり、モデルが `{"action":"ask","goal":1,"cancelled":[1]}` のように、選択した目標を同じ出力で終了対象にも指定する。
- **再現手順：** 目標1と、last_executed_atが現在時刻の目標2を作成し、上記の行動出力をモックで返す。
- **観測結果：** 目標1は取り消されたが、生成プロンプトには再質問間隔内の目標2が入った。ActionRecordはaskなのにselected_goal_idsは空だった。
- **影響：** 終了対象の除去で選択IDが空になり、空集合はaskableの部分集合なので抑制を通過する。その後の `or goals[:1]` が未選択の目標を渡す。質問対象・頻度制御・再生後の実行記録が食い違う。実モデルがこの出力を返す頻度は未測定だが、現在のパーサーが受理する入力で再現した。
- **修正方針：** 実際に終了した目標を除去した後、選択対象の存在と質問可否を再検証する。対象が空ならanswer/waitへ倒し、別目標への暗黙のフォールバックを行わない。生成に渡すIDと実行記録のIDを一致させる。

#### 4. [中] 通知なしでの達成を許すコードに対し、モデルへの指示は通知に依存したまま

- **場所：** `backend/app/agent/action_selector.py:40`、`:57`、`:126`、`backend/app/agent/conversation.py:196`。
- **確認区分：** コード確認のみ（実モデル未実行）。
- **発生条件：** この会話で質問したが再生通知がなく、last_executed_atがNULLのまま相手が答える。
- **影響：** Python側はActionRecordを根拠に達成を許す一方、指示文は「（すでに聞いた）と付いている目標」だけをansweredに入れるよう求める。その印はlast_executed_atだけで付くため、通知なしの質問は対象外と指示される。モデルが指示に従うと今回直した経路を使えない。逆に、一度実行した印は時間経過で消えず、再質問間隔を過ぎたrespondでも「いま聞き直さない」と指示される。必ず誤答するとの断定ではなく、判定仕様と渡す指示が不一致であるとの指摘。
- **修正方針：** 「この相手に質問した根拠」と「現在再質問を抑制するか」を分けてモデルへ渡す。前者はPythonの達成検証と同じ根拠、後者は設定された間隔を使う。通知なしテストはansweredをモックで注入するだけでなく、実際に送った指示・目標の印も検査する。

#### 5. [中] 最後の目標を完了・取消にすると、その判断のActionRecordが保存されない

- **場所：** `backend/app/agent/conversation.py:212`、`:301`。
- **確認区分：** 実行して再現。
- **発生条件：** 判定に渡した全目標が、その返答で終了対象になる。
- **再現手順：** 目標を1件だけ作成し、行動選択からcancelled=[1]を返して会話要求を完了する。その会話のActionRecordを検索する。
- **観測結果：** Goalはcancelledになったが、ActionRecordは0件だった。
- **影響：** モデルを呼ぶ前には目標が存在したのに、終了対象を除去した後の `if goals` で記録を省略する。GoalRevisionに変更内容と理由は残るが、状態を変えた行動選択のモデル・生成設定・候補IDは残らない。返答生成のRunRecordは別のLLM呼び出しの記録で代用できず、完了条件5の追跡が弱まる。
- **修正方針：** 判定時点の候補と行動選択の結果を保持し、更新後の残件数に依存せずActionRecordを保存する。最後の目標の完了・取消でも判断のメタデータと対象を追えることをテストする。

### 判断事項・測定の評価

- **既存の呼び出しへの統合：** 呼び出し数を増やさない判断は妥当。ただし待機2本×3回は限定した入力での結果であり、全体の劣化がない証明にはならない。完了と次の質問が同時に必要なケース、延期と取消、拒否と回答、複数目標の取り違えを対で測る必要がある。
- **別会話での返答：** `_raised_goal_ids`は現在のconversation_idだけを見るため、以前の会話で聞いた質問への後日の返答は、その記録だけでは達成根拠にならない。これは意図が明示された制限であり、今回の追加不具合とは分ける。ただし「セッションをまたいでも回答済みの質問を繰り返さない」まで確認済みとは扱えない。同じ相手・目標・質問との対応を保った通し評価が必要。
- **中断後の12時間：** 恒久禁止にしていない点はPR8の条件を守る。一方、中断直後も目標選択には同じ間隔が適用され、即時再確認の経路を保証していない。単純に中断なら常時再質問可にすると逆に繰り返しを増やすので、相手からの明示的な再確認要求と自発的な再質問を区別して評価するのがよい。「直後も再確認可能」とは承認しないが、引継ぎ条件に即時性の規定がないため、12時間という値だけを独立した不具合とはしていない。
- **期限切れ：** respond/openとも最初のLLM呼び出し前にcommitされる。今回の指摘1はその後の完了・取消更新の問題。期限切れ対象のUTC比較と、実行済みを除外する既存テストは成功した。大量の目標がある場合の性能は未測定。
- **実測記録：** `20260908T150242-2026-09-07.1/report.md`は34/45・実行失敗0で、完了シナリオはこの時点では0/3。`20260908T151649-2026-09-07.1/report.md`は完了と待機の2本が各3/3、合計6/6だった。申告された修正前後の数値と一致するが、最新コードで15本全体が34/45だったという意味にはならない。
- **評価の不足：** 完了・取消シナリオは、最後に目標を渡さなかったことを主に見るため、正しい終端状態と、単に待機したことを区別する状態検査を併用したい。未来のafter_dateを中止する経路、別会話での回答、中断後の再確認、回答と次の質問の同時判定、生成待ち中のDB操作は現在の7件の完了テストでは押さえていない。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr9-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **289件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。`git diff --check`も成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr9_review.py -q -s` — **4件成功**。これは指摘1・2・3・5の不具合を観測するテストが成功したという意味。初回は再現コードがActionRecordの列名を誤って参照して1件失敗したが、一時テストだけ修正して再実行した。
- 実モデルの新規測定、実ブラウザ・VOICEVOXの動作確認、フロントの再テストは未実施。既存レポートの確認とモックでの再現を実サービスの検証とは区別する。
- ISSUE-028・029・030、UI、起動点の追加は指定どおり対象外。ソースコードの修正・コミット・通常利用DBの操作は行っていない。

## PR9 — 第2回レビュー・対応確認（2026-09-09）

### 結論

**PR9は引き続き承認保留。残る指摘は1件。** 前回の書き込みロック、未来の予定の取消、終了後の別目標へのフォールバック、モデルへ渡す印の不一致は解消した。最後の目標の行動記録も成功時には残るようになったが、返答生成に失敗した場合に記録が欠ける経路が残る。実モデルの品質を再測定していないこと自体を、今回の保留理由にはしていない。

### 対象と対応資料

- 現在の未コミットPR9実装、`backend/tests/test_goal_completion.py`の追加テスト、関連するAPIの例外処理・DBトランザクション・ActionRecord保存を確認した。
- 指定された `docs/review/claude/phase4_review_response.md` は確認時点ではPR8第2回レビューへの対応で終わっており、PR9の対応節は見当たらなかった。同名資料の検索でも別ファイルは見つからなかった。以下は修正コードと実行結果に基づく評価であり、PR9の対応説明や追加の測定結果と照合できたとは扱わない。

### 前回指摘への評価

| 前回の指摘 | 評価 | 確認内容 |
| --- | --- | --- |
| 1. 生成中の書き込みロック | 解消 | 完了・取消後にcommitする。一時テストで行動選択を通し、実際の2回目のLLM呼び出しを停止した間に、別接続で取消の確定と書き込み成功を確認した。 |
| 2. 予定日前の取消漏れ | 解消 | 判定候補はignore_trigger=Trueで取得し、質問可否は別に絞る。相手・公開範囲の制限は維持される。未来予定の取消と予定日経過後の除外を確認する追加テストが成功した。 |
| 3. 終了後の別目標へのフォールバック | 解消 | 実際に終端化したIDを除外し、選択IDが空、または質問不可ならanswerに変更する。goals[:1]への振替はなくなった。追加テストが成功した。 |
| 4. 通知依存の印と指示 | 解消 | 質問の記録からasked_ids、実行条件・間隔からaskable_idsを別々に渡す。通知なしの質問にも「すでに聞いた」が付くテストが成功した。再質問抑制の指示は別の印に変更された。 |
| 5. 最後の目標の行動記録欠落 | 一部解消 | 保存条件を更新後のgoalsから判定時のcandidatesに変更し、正常完了時には保存される。ただし下記の生成失敗経路が残る。 |

### 指摘事項

#### 1. [中] 完了・取消だけ確定した後で生成が失敗すると、状態を変えた行動判断の記録が失われる

- **場所：** `backend/app/agent/conversation.py:224`〜229行、`:271`、`:327`〜346行。
- **確認区分：** 実行して再現。
- **発生条件：** 行動選択は成功して目標をdone/cancelledにするが、続く返答生成が接続失敗・タイムアウト等でLLMErrorになる。
- **再現手順：** 一時DBに目標を1件作成し、行動選択からcancelled=[1]を返す。次の生成呼び出しだけLLMErrorを投げる。API応答とDBを確認し、その後生成を復旧して同じ発言を再送する。
- **観測結果：** 最初の要求は503。Goalはcancelled、取消のGoalRevisionは1件残る一方、ActionRecordは0件だった。再送は200になったが、対象は既に終端状態なので判断対象に入らず、ActionRecordは0件のままだった。
- **影響：** 目標更新のcommitと、判断のモデル・設定・候補IDを保存する処理の間にLLM呼び出しがある。今回のロック対策により状態変更は永続化されるが、その状態を変えた判断のメタデータは保存されない。GoalRevisionの変更内容・理由は残るため全履歴消失ではないが、完了条件5に必要な判断の追跡が欠ける。doneも同じ保存順序を通る（今回の実行再現はcancelled）。
- **修正方針：** 状態を変えた判断のActionRecordとGoal/GoalRevisionを、生成前の同じ短いトランザクションで保存する。生成が成功した後で発言IDを関連付け、失敗時も判断の記録を保持する。発言IDのないask/suggest記録を「実際に持ち出した」根拠と誤認しないよう、`_raised_goal_ids`と実行判定の条件も合わせる。生成失敗後の記録保持と、別接続からの書き込み成功を両方検査する。

### テストの評価と確認上の注意

- 新規の `test_the_write_lock_is_released_before_generating`（`backend/tests/test_goal_completion.py:280`〜291行）は、gate_on=Noneなので**最初の行動選択で停止する**。コメントの「行動選択は通し、返答の生成で止める」と一致しておらず、2回目の呼び出し前のcommitを検証していない。このテストは停止対象を2回目に限定し、取消が別接続から既に見えることも確認する必要がある。修正コード自体のロック解消は、今回の一時テストで別途確認済み。
- `_raised_goal_ids`は現在speaker_idで会話をまたいで検索し、その集合を達成判定にも使う。したがってコメントに残る「達成の判定はこの会話に限る」は実装と一致しない。前回留保した別会話の返答への制限はコード上緩和されているが、別会話・別人を組み合わせた通し評価は今回行っていない。コメントの制限が維持されたと解釈して承認することはできない。
- 実モデルの新しい測定資料は確認できていない。前回確認した34/45と追加6/6を今回の修正後の数字として転用しない。

### 実行確認・変更範囲

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr9-rereview.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **294件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr9_rereview.py -q -s` — **2件成功**。1件はロック解消の確認、もう1件は生成失敗後の記録欠落を観測するテスト。
- 実モデル・実ブラウザ・VOICEVOXの確認、フロントの再テストは未実施。ソースコード・設定・通常利用DBは変更せず、このレビュー資料だけ追記した。サブエージェントは使用していない。

## PR9 — 第3回レビュー・対応確認（2026-09-09）

### 結論

**PR9は承認してよく、PR10へ進める。** 前回残った生成失敗時の行動記録欠落は解消した。今回確認した修正範囲で、新たな承認を妨げる不具合は見つからなかった。これはPR9の実装に対する承認であり、実モデルでの品質評価を含むフェーズ4全体の完了承認ではない。

### 対応の評価

- **前回指摘1：解消（コード確認・実行して確認）。** `backend/app/agent/conversation.py` は、目標の完了・取消とActionRecordを同じトランザクションで生成前に保存する。生成に成功した後でだけmessage_idを関連付けるため、返答生成が失敗しても状態を変えた判断のメタデータが残る。
- 一時DBでdoneとcancelledを個別に検証した。行動選択を成功させ、次の生成だけLLMErrorにするとAPIは503になるが、Goalの終端状態、GoalRevisionの理由、ActionRecordの候補ID・モデル名・モデル版・生成設定が残った。message_idはNULLで、復旧後の再送でも元の記録が保持された。
- **先行保存の副作用への対策：確認。** `_raised_goal_ids`はmessage_idが存在するask/suggestだけを根拠にする。生成失敗した質問を次の返答で達成扱いしない追加テストが成功した。再生側も具体的なMessage.idに一致するActionRecordだけを取得するため、発言のない判断は実行済み判定に混ざらない。再生側の既存条件を維持した対応であり、同ファイルに新たな変更が必要だったという意味ではない。
- **ロック検査の注記：解消。** FakeLLMのgate_skip=1で行動選択を通過し、2回目の生成を停止するようになった。停止中に別接続から取消済みの状態が見え、新しい目標を書き込めることを検査するテストが成功した。今回の記録の先行保存によって、生成中の書き込みロック保持を再導入していない。
- **対応資料：確認。** `docs/review/claude/phase4_review_response.md`にPR9の第1回・第2回対応が追加され、上記の修正と一致した。達成根拠を同じ相手の過去会話まで広げる判断も明記された。主要な説明は更新されているが、mark_done呼び出し付近などに「この会話」とする旧コメントは一部残る。承認を妨げる動作不具合とはしない。
- 前回確認済みの未来予定の取消、別目標への暗黙の振替防止、通知に依存しない質問済みの印、最後の目標終了時の記録も、関連テストで維持されている。

### 指摘事項

新たな指摘なし。

### 実行確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr9-third-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **296件成功**。対応記録の件数と一致した。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr9_third_review.py -q -s` — **2件成功**。done/cancelledそれぞれの生成失敗・再送後の記録保持を確認した。
- `git diff --check` — 成功。

### 残る範囲・未確認

- 実モデルの再測定、実ブラウザ・VOICEVOXの動作確認、フロントの再テストは未実施。対応資料も実モデル測定は進行中としており、過去の34/45や6/6を今回の修正後の結果として扱っていない。
- ISSUE-028〜030、および今回記録されたISSUE-031の品質評価はPR10に残る。ISSUE-031の申告された測定値は今回独立に検証していない。中断直後の明示的な再確認、同じ相手の別会話での回答、別人への誤適用防止は通し評価で引き続き確認する範囲であり、今回実モデルで確認済みとはしない。
- ソースコード・設定・通常利用DBは変更せず、このレビュー資料だけ追記した。再現コード・DBは一時領域を使用した。サブエージェントは使用していない。

## PR10 — 第1回レビュー（2026-09-09）

### 結論

**PR10は現時点では承認保留。** 判定の改善と測定での欠陥発見は確認できたが、完了条件1の通し測定が質問時点で終わっており、完了条件2の判定にもすり抜け・誤判定が残る。加えて、音声の開始前中断から、実際の回答まで達成根拠として拒否する回帰がある。下記8件を確認した。新しい機能や実モデルの完全な安定を要求しているのではなく、このPRが測るとした範囲と判定の正しさに対する指摘である。

### 対象・実行確認

- 現在の未コミット10ファイル、関連する評価手順・再生通知・目標保存・過去レビューを確認した。AGENTS.mdと既読の設計書に従い、計画PR10、ISSUE-011・013・028〜033と実測レポートを照合した。
- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr10-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **300件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests`、`git diff --check` — 成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr10_review.py -q -s` — **8件成功**。いずれも以下の不具合・測定漏れを観測するテストが成功したという意味である。
- 実モデルは新たに実行していない。既存のreport.json/report.mdを読み、必要な数値を再集計した。再現にはモックLLMと一時DBを使用した。
- ソースコード・設定・通常利用DBを変更していない。レビューの追記以外の作成物は一時領域のみ。サブエージェントは使用していない。

### 完了条件の評価

| 条件 | 評価 | 根拠 |
| --- | --- | --- |
| 1. 抽出から完了・繰り返し防止まで通す | 満たしていない | 12/18の根拠となるシナリオは質問で終了する。完了処理を必ず失敗する関数に差し替えても通過した。 |
| 2. 落ちるべきものを落とす | 満たしていない | 未実行の期待が合格扱い、余分な目標・別目標の日付によるすり抜け、日本語の誤検知と外国語の見逃しを再現した。 |
| 3. 指示変更を1つずつ前後で測る | 記録上は実施。ただし効果の推定は限定的 | ISSUE-031の変更前後測定と、効果を確認できず戻した記録がある。action_selector.pyに差分がないことも確認した。3試行で効果を確定できないことと、1変更ずつ比較する手順を守れないことは別である。 |

### 指摘事項

#### 1. [高] 「最初の小さな実験」の評価が質問で終わり、完了処理を壊しても合格する

- **場所：** `backend/evals/scenarios/proactive.toml:37`〜68行、`docs/plan/phase4.md:930`付近の「完了条件1」。
- **確認区分：** 実行して再現。
- **発生条件：** `goal-is-extracted-adopted-and-asked`を完了条件1全体の根拠として使う。
- **再現手順：** 一時テストでmark_doneを呼ばれたら必ず例外を出す関数に差し替える。実際の同梱シナリオを、正常な抽出・採用・質問のモックで実行する。
- **観測結果：** シナリオは成功した。発言は予定を話す入力とYUIの質問の2ターンだけで、mark_doneは呼ばれなかった。
- **影響：** 返答を受けてdoneになることも、同じ抽出目標を次の会話で繰り返さないことも測っていない。既存のcompleted-goal-is-not-repeatedは事前投入した目標による別シナリオで、抽出からの接続の証明にはならない。12/18を「最初の小さな実験が通しで成立した」数字として扱えない。
- **修正方針：** 同じ抽出・採用目標に対する具体的な返答、doneとcompleted_atの確認、再質問間隔を過ぎた次の会話での非再質問を追加する。最後の非参照だけでは待機でも通るため、状態そのものの検査を併用する。測定記録を実際に通した範囲で書き直す。

#### 2. [中] 新しい数値の期待を判定しない手順に書け、検査0件で合格になる

- **場所：** `backend/app/evaluation/scenario.py:248`、`:379`付近の_check_fields、`:485`付近のhas_machine_checks。
- **確認区分：** 実行して再現。
- **発生条件：** reflect以外にexpect_goal_count_maxまたはexpect_goal_due_daysを指定する。
- **再現手順：** `restart`だけのシナリオに `expect_goal_count_max=0, expect_goal_due_days=2` を指定してrun_attemptを実行する。
- **観測結果：** 宣言は受理され、has_machine_checks=True。LLM呼び出しもturns/reflections/action_checksも0件なのにAttempt.ok=Trueだった。
- **影響：** 数値0を区別するために期待項目を検証集合から外したが、代わりの検査がない。「宣言した期待が評価される」という今回の目的に反し、書き間違いが偽の成功になる。
- **修正方針：** 数値項目も手順ごとの許可検証に含め、真偽値ではなく `is not None` で指定を判定する。0を含めた不正手順への指定をロード時に拒否するテストを追加する。

#### 3. [中] 言語判定は日本語の語を落とし、全文が外国語でも合格にする

- **場所：** `backend/app/evaluation/runner.py:177`〜199行。
- **確認区分：** 実行して再現。
- **再現手順：** _check_languageへ以下の文を直接渡す。
- **観測結果：** 「么九牌は一・九と字牌です。」「儿は『ひとあし』という部首です。」は不合格。一方「你好，我很高兴和你聊天。」「今日は很高兴です。」「I enjoyed the movie.」はすべて「日本語のまま：期待どおり」になった。
- **発生条件：** 禁止集合に含まれる文字を日本語の用語として使う場合、または集合に含まれない文字だけで外国語が出る場合。
- **影響：** 「么」は日本語の麻雀用語にも使われる。日本プロ麻雀連盟の[採用役一覧](https://www.ma-jan.or.jp/activity/yaku.html)に「么九牌」の用例がある。「儿」も日本の漢字・部首の説明に出る文字で、[国文学研究資料館のCHISE掲載データ](https://chise.nijl.ac.jp/est/view/character/%E5%84%BF)に日本語音・JIS収録が示される。64字のブラックリストは言語判定そのものではなく、過去ログで誤検知がなかったことから将来の日本語や未登録の中国語を保証できない。
- **修正方針：** 日本語で使う文字を無条件不合格にしない。日本語用語・引用等の正例と、禁止字を含まない中国語・英語・混在文の負例を加える。文字検出は補助判定とし、確認できない文は「日本語確認済み」にせず人手確認へ回すなど、判定の保証範囲を明確にする。単に文字を足し引きして完全な言語判定とする方針は避ける。

#### 4. [中] 音声を止めた後に文字の質問へ具体的に答えても、達成にできない

- **場所：** `backend/app/agent/conversation.py:401`〜404行、同ファイルのansweredをaskedに絞る処理。
- **確認区分：** 実行して再現。
- **発生条件：** 対象を質問した記録が1件だけで、その発言がaborted・開始時刻NULLになった後、相手が画面で質問を読んで回答する。
- **再現手順：** /openで映画の質問を生成し、playingなしでabortedを通知。「質問は画面で読んだよ。映画は面白かった。音楽が特に良かった。」と送信し、モデルには正しいanswered=[1]を返させる。
- **観測結果：** APIは200だが、目標はactive、completed_atはNULLのままだった。
- **影響：** 「音声が鳴らなかった」と「相手が質問を受け取っていない」を同一視している。ローカルでは文字表示でも届くというISSUE-011の線引き、および実際の返答を到達の証拠にするPR9の扱いと衝突する。また、通知はstateしか送らず、playing通知が欠けてabortedだけ届いても同じDB状態になる。aborted自体に「開始前に止めた」という情報はなく、開始時刻NULLを未到達の確証とはできない。
- **修正方針：** 「聞こえなかった」は回答ではないという検証と、過去に質問を提示した根拠を分ける。中断状態だけで正しい回答を一律拒否しない。画面からの回答・開始通知の欠落・実際の未回答を対で検査する。generatedをすべて除外する修正も、通知なしの回答を再び壊すため避ける。

#### 5. [中] 対象目標の日付が誤っていても、別の目標に正しい日付があれば合格する

- **場所：** `backend/app/evaluation/runner.py:902`〜910行、`:930`〜950行。
- **確認区分：** 実行して再現。
- **発生条件：** 複数の目標候補があり、本文の一致と基準日の一致を別々の候補が満たす。
- **再現手順：** 「面接の感想を聞く」のdue_atを発言日+7、「猫の話を聞く」を+2にし、expect_goal_any=["面接"]、expect_goal_due_days=2で_check_reflectionを実行する。
- **観測結果：** 面接の日付は5日誤っているのに、全チェック成功になった。
- **影響：** 本文と日付を同じ目標へ結び付けていないため、今回の日付判定でもISSUE-028を取り逃す。現在のrelative-dateシナリオにも件数制限はなく、この候補の組合せで通る。
- **修正方針：** 評価対象をキー・本文条件等で特定し、その同じ候補のtriggerとdue_atを検査する。複数候補時にどの候補を判定したかも出力する。

#### 6. [中] 日付判定の基準がシナリオ最初の発言に固定され、後の会話の正しい日付を落とす

- **場所：** `backend/app/evaluation/runner.py:465`、`:497`〜500行、`:717`、`:745`〜746行。
- **確認区分：** 実行して再現。
- **発生条件：** 最初の会話の後に時刻を進め、別の会話で予定を話して振り返る。最初の会話内でも、挨拶の後で日をまたいで予定を話せば同じ問題になる。
- **再現手順：** 9/9に挨拶→新しい会話→7日進める→9/16に「明日、面接」→expect_goal_due_days=2で振り返り。抽出部分だけをモックし、正しい9/18のdue_atを返す。
- **観測結果：** 「2026-09-11ではなく2026-09-18」と不合格になった。
- **影響：** 発言日基準で測るはずの期待を、別の発言日から計算している。new_conversation/restart/start_conversationでfirst_said_atが更新されず、複数会話の評価で偽の不合格・合格を作る。
- **修正方針：** どの発言を日付の起点とするか、期待側で明示できるようにする。会話の最初にリセットするだけでは、その会話の途中で日をまたぐ場合が残るため、予定を含む発言と対応付ける。

#### 7. [中] 余分な目標を落とすシナリオが、余分な1件を許している

- **場所：** `backend/evals/scenarios/proactive.toml:28`〜33行。
- **確認区分：** 実行して再現。
- **発生条件：** 単一目的の映画の会話から、正しい目標に加えて無関係な目標が1件出る。
- **再現手順：** 同梱のgoal-is-created-from-a-planのreflect設定をそのまま読み、「映画の感想を聞く」と「映画と無関係のカメラの話を聞く」の2件を返す。必要な映画の記憶候補も与える。
- **観測結果：** 全チェックが成功した。expect_goal_count_max=2のため、件数判定は余分な1件を許す。
- **影響：** 計画の「単一の目的しかない会話で複数出るものを落とす」という完了条件2を満たさない。判定関数を用意したことと、同梱シナリオが必要な欠陥を落とすことが一致していない。
- **修正方針：** この単一目的のシナリオでは上限を1にするか、複数を許す理由と許容する目的を定義して内容を判定する。単純な上限2だけを余分な目標の検証とは扱わない。

#### 8. [低] 最大日付への翌日加算が未処理の例外になり、評価試行から漏れる

- **場所：** `backend/app/agent/goal_reflection.py:126`、`backend/app/evaluation/runner.py:698`付近の抽出例外処理。
- **確認区分：** 実行して再現。
- **発生条件：** LLMのevent_dateがISO形式として有効な9999-12-31になる。
- **再現手順：** say→reflectのモック評価で、そのevent_dateを持つ目標出力を返す。
- **観測結果：** run_attemptが `OverflowError: date value out of range` を外へ送出した。通常の抽出失敗を記録したAttemptにはならなかった。
- **影響：** 新しい+1日処理の範囲外入力が、GoalReflectionErrorへ集約されない。run_scenariosにもこの例外を捕まえる処理がないため、その測定を中断させる。実モデルでこの値が出る頻度は未確認。
- **修正方針：** 日付の解析だけでなく翌日加算・UTC変換までを検証し、範囲外は通常の抽出エラーへ変換する。年末・うるう日・最大値の境界を検査する。

### 文字集合の全件確認

64字すべてを個別にコードポイントとPythonの日本語文字コード（iso2022_jp、iso2022_jp_2004）へ照合した。**JISに収録されないことは、日本語で絶対使われない証明ではない。** その限界を明示した上で、結果は次のとおり。

| 確認結果 | 該当する全字 |
| --- | --- |
| iso2022_jpとiso2022_jp_2004の両方で表せる | 儿（U+513F） |
| iso2022_jp_2004で表せる | 么（U+4E48）、开（U+5F00）、关（U+5173）、种（U+79CD） |
| 両方で表せない：コード中の1行目の残り | 这、们、说、请、东、发、对、为、过、还 |
| 両方で表せない：2行目 | 红、买、卖、书、车、马、鸟、鱼、见、觉、习、长、风、飞、乐、师、应、给 |
| 両方で表せない：3行目の残り | 样、员、级、规、视、华、丽、历、济、认、识、语、谁、产、业、务、头、爱、军、团、电、话、时、间、问、题、实、现、门、图、边 |

- 「么」「儿」は上記の日本語の具体例・資料まで確認し、誤検知を再現した。
- 「开」「关」「种」は日本語文字コードで表せることを確認したが、一般的な日本語会話の用例まで確定したとはしない。収録だけを理由に、必ず普通の日本語を誤検知すると断定しない。
- 残り59字についても、固有名詞・引用等まで含めた日本語使用の不存在は証明していない。「簡体字にしかないから、出たら日本語ではない」という一律の保証はできない。
- 「什么呢」の検出は「么」に依存している。日本語の用例を守るためにその1字を外すだけでは、既存の負例を見逃すようになる。文字集合だけで調整する方法の限界である。

### 測定記録の評価

- **4回の合計は一致。** 162705、171449、180338、184315のreport.jsonを、現在の機械判定対象43シナリオに絞って再集計すると102/129、110/129、112/129、107/129だった。人手専用は5シナリオ。計画・ISSUE-032の「42シナリオ」は実際の43と一致しないが、変動した16件、上昇10件・低下6件は再集計でも確認した。
- **12/18の数字は一致するが、意味が限定される。** 上記4回と175543の6試行で2/3・3/3・4/6・1/3・2/3、合計12/18。ただし指摘1のとおり質問までの判定である。162705の失敗例には、目標は渡っているのに「こんにちは、私YUIです。開発者さん、今週の土曜日に何を予定されていますか？」と返した回もある。失敗分類には中国語混入だけでなく、渡した目標の感想を聞かない生成失敗も具体的に残す必要がある。
- **判定変更の影響は固定ログの再判定で分離できる。** 162705の同じ返答に現在の言語判定だけを当て直すと、旧判定の不合格が外れるのは人手専用を含め8件で、機械対象の合計は102→109になる。その後の新規実行110との差まで、同じ8件で直接説明するものではない。前後でモデルも再実行すると生成差が混ざるので、評価器の修正はまず同じログで比較するべきである。
- **3712件の主張は独立には再現できていない。** 対象ファイル一覧・抽出条件がないため、同じ母集団を特定できない。今回、184315までのreport.jsonの空でないturn.replyを対象にすると2408件・現在の判定で21件検出となった。これを別条件の3712件・17件と直接比較して誤りとは断定しない。再現可能な対象一覧と抽出条件を残す必要がある。少なくとも、この過去母集団の「誤検知0」は指摘3の日本語用例を否定しない。
- **11/80は集計できる。** 171449はask=23、目標なし以外のwait=4、180338は24と2、184315は22と5で、wait/(ask+wait)=11/80。変更後192251はask=22・該当wait=3で3/25。表の分母の「話しかけた」は正確には「対象の判断総数」である。
- **集約は効果と揺れの分離を保証しない。** 複数シナリオの判断をまとめること自体は補助指標として妥当だが、同じ実行内の判断は共通の履歴・抽出結果に依存し、80件の独立・同条件試行ではない。抽出失敗や達成の有無でも対象に入る判断の数が変わる。シナリオ別の内訳と実行別の変動を維持し、目標が実行可能な同じ判断場面を揃えた比較にする。「効果を確認できなかったので戻した」は妥当だが、3/25が観測範囲内というだけで効果ゼロとは確定しない。

### PR11へ渡す判断

**promiseとabout_personを自動採用しない判断は妥当だが、この2種類を除けば他を有効化してよいという根拠にはならない。** 既存ログには既に対象外の種類にも問題がある。

- `20260908T180338-2026-09-07.1/report.json`のpersona-change-request-is-not-kept第2試行は、experienceとして「開発者が今日から乱暴な性格になって敬語もやめた」を出している。
- `20260908T162705-2026-09-07.1/report.json`の同シナリオ第3試行には、impressionとして「YUIは開発者の希望なら乱暴な性格で振る舞うこともできる」に相当する候補がある。第1試行には人格変更後を前提とした目標候補もある。
- これはPR11の未実装を指摘しているのではなく、PR10で得た実測から言える範囲の評価である。記憶の種類と、関心・関係性・目標の自動採用を区別し、有効にする種類ごとに根拠を示すべきである。現状の観測は除外リストを2つ決める材料にはなるが、他の種類を許可する試験にはなっていない。
- 4回すべて0/3であったことは確認した。「観測した12試行すべてで失敗」と表現するのが正確で、出力内容や候補の種類自体は試行間で変動している。

### 基準日の設計・未確認範囲

- event_dateは予定日、保存するafter_date/due_atは実行可能になる日時という責任分担で、+1日をコードへ移した判断は妥当。下流のexpire_overdueは保存済みdue_atを使い、さらに1日足す処理はない。年末2026-12-31、うるう日2028-02-29について翌日のJST午前0時に相当するUTC値へ変換されることを実行確認した。上限入力の例外は指摘8のとおり。
- 未記入をnext_conversationにする既存仕様は維持されるが、明示された未来の予定について日付未記入を「動作として誤りではない」とは一般化できない。due_atによる時期の制約が失われるため、予定前に質問しないかも評価対象に残す必要がある。
- JOINで相手指定あり／なしの絞り込み条件自体は維持される。generatedを除外しないのは、通知なしでも実際の回答を使うPR9の方針と整合する。abortedによる除外だけは指摘4の回帰がある。
- 人手専用5件の品質を合格とは判定していない。実プロセス再起動、実VOICEVOX・ブラウザ、実モデルの新規測定、通常利用DBへのマイグレーションは未実施。今回DBスキーマの変更はなく、通常利用DBへ移行を適用しなかったことをPR10の不具合とはしていない。

## PR10 — 第2回レビュー・対応確認（2026-09-09）

### 結論

**PR10は引き続き承認保留。** 前回の主要な判定漏れと中断後の回答を拒否する回帰は解消した。ただし、言語判定の新しい誤検知と、日付判定の根拠表示の誤りが残る。また、拡張した小実験を実モデルで完了まで通した成功記録は確認できていない。コードを修正したことと、このPRの目的である実モデルでの通し確認を分けて評価する。

### 前回8件への対応評価

| 前回指摘 | 評価 | 確認した内容 |
| --- | --- | --- |
| 1. 質問で終わる通しシナリオ | 評価経路は解消、実測による確認は未達 | deliver→感想→expect_done_goals→2日後の別会話へ延長された。doneかつcompleted_atありを判定する。完了処理を無効化すると失敗する追加テストも成功した。 |
| 2. 数値期待の未検証 | 解消 | _OPTIONAL_NUMBERSはis not Noneで検証し、restart/sayへの0指定も拒否する。 |
| 3. 言語の誤検知・見逃し | 一部解消 | 么・儿の日本語用例は通り、什么は語として検出する。全文中国語の提示例も検出するが、新しい仮名なし判定に下記の問題がある。 |
| 4. 中断後の具体的回答を拒否 | 解消 | aborted・開始時刻NULLによる除外を撤回。実際の回答があれば達成できる対のテストを確認した。再生通知だけでは区別できない問題をISSUE-034へ分けた説明も妥当。 |
| 5. 無関係な目標の日付で合格 | 判定対象の絞り込みは解消、表示に残件 | 本文条件に一致した候補だけを見るようになった。ただし複数一致時の合格理由が別の候補を表示する。 |
| 6. シナリオ最初の発言日で固定 | 解消 | 既定は直前の発言、expect_goal_due_fromで起点を指定でき、見つからない場合はNoneになる。起点を黙って別発言へ寄せない。 |
| 7. 余分な目標1件を許す | 解消 | 単一目的の抽出シナリオの上限が1になった。 |
| 8. 最大日付の加算例外 | 解消 | 解析・加算・UTC変換をまとめて検証し、OverflowErrorを通常の解析失敗へ集約する。 |

対応資料 `docs/review/claude/phase4_review_response.md` のPR10追記を確認した。サブエージェントは使用していない。

### 指摘事項

#### 1. [中] 仮名がないことだけで、正しい日本語の住所等を外国語として不合格にする

- **場所：** `backend/app/evaluation/runner.py:203`〜234行。
- **確認区分：** 実行して再現。
- **発生条件：** 所在地・名称などを漢字だけで簡潔に答え、漢字が6字以上になる。
- **再現手順：** 「所在地を住所だけで答えて」に対して「東京都千代田区」を返し、expect_anyにも同じ住所を指定して_check_turnを実行する。
- **観測結果：** 期待語の検査は成功したが、言語検査が「日本語以外が混ざった: 仮名が1つも無い」と不合格になった。
- **影響：** 正しい住所の返答が、モデルの言語逸脱として集計される。名称や列挙も同じ経路を通る。前回の文字単位の誤検知を解消した一方、新しいルールで偽の不合格を作っている。表示を「検出なし」に変更したことは保証範囲の説明として改善だが、この偽の不合格は解消しない。
- **修正方針：** 仮名なしだけを外国語混入の確証にせず、名称・住所・短い回答を区別するか、人手確認が必要な状態として扱う。正しい漢字だけの回答を正例に含める。前回提示した全文英語や仮名を含む中国語混在文についても、検出器の未対応範囲として明示する。「検出なし」は日本語確認済みではないため、PR10完了条件2の保証範囲と人手確認の扱いを揃える。

#### 2. [低] 日付が一致した候補ではなく、一覧の先頭を合格根拠として表示する

- **場所：** `backend/app/evaluation/runner.py:1044`〜1050行。
- **確認区分：** 実行して再現。
- **発生条件：** expect_goal_anyに一致する候補が複数あり、先頭の候補の日付は誤り、後続の候補の日付だけが正しい。
- **再現手順：** 発言日9/9、期待+2日、本文条件「面接」に対し、「面接Aの感想を聞く」を9/16、「面接Bの感想を聞く」を9/11として_check_reflectionを実行する。
- **観測結果：** Bの日付で合格したのに、detailは「期待どおり（2026-09-11）: 面接Aの感想を聞く」になった。
- **影響：** 合格に使った候補と記録が一致せず、Aの日付が正しかったという誤った読解を招く。本文条件がanyであること自体への指摘ではなく、今回追加した「どの目標を判定したか」の表示が誤っている。
- **修正方針：** 日付が一致した候補を保持して表示するか、各候補の日付と判定結果を出す。先頭のactual[0]を無条件に合格根拠としない。

### 修正後の実モデル測定と承認条件

- 確認できた最新の `logs/evals/20260909T002250-2026-09-07.1/report.md` は、拡張したgoal-is-extracted-adopted-and-askedが **0/1、実行失敗0** だった。これは評価器のクラッシュではなく、抽出した候補に「感想」がなく、採用できず、質問・再生通知・達成も通らなかった試行である。後続の待機は「実行できる目標がありません」であり、この回を独立した行動選択の品質失敗に数えるべきではない。
- 追加シナリオが不足を実際に落とすようになった点は改善である。ただし、「抽出→採用→質問→返答→done→別会話で繰り返さない」が実モデルで成立した証拠は、この0/1からは得られない。対応資料にも再測定中とあり、完了扱いにはできない。
- `docs/plan/phase4.md:940`付近は依然として旧12/18に「通しで成立はしている」と付記している。これは延長前の質問までの数字なので、旧測定として範囲を明記し、延長後の結果と分ける必要がある。以前の失敗例にあった「感想を聞かずに別の質問をする生成失敗」も原因分類から落とさない。
- 承認のために任意の成功率や大量の追加実行を要求するものではない。上記の判定不具合を解消した上で、計画にある小実験全体の修正後の実測結果と、失敗した段階の説明を確認したい。まだ測定中の結果を成功扱いして先へ進めることはしない。
- promise/about_personだけでなく、experience・impression・目標にも問題があったこと、種類ごとの根拠なしに自動採用を有効化しないことを対応資料に反映した方針は妥当。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr10-rereview.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **304件成功**。対応資料と一致した。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。`git diff --check`も成功。
- 一時テスト：同じ環境変数で `.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr10_rereview.py -q -s` — **2件成功**。上記2件の不具合を観測するテストである。
- 修正を戻す検証は今回再実施していない。実モデルの新規実行、VOICEVOX、実ブラウザ、実プロセス再起動は未実施。新たに記載された5488件・誤検知0の母集団は今回再集計していない。
- ソースコード・設定・通常利用DBは変更せず、このレビュー資料だけ追記した。一時テストは一時領域へ保存した。

## PR10 — 第3回レビュー・対応確認（2026-09-09）

### 結論

**PR10は承認してよく、PR11の実装へ進める。** 前回の2件は解消し、延長後の小実験についても実モデルで抽出から達成・非再質問まで成立した記録を確認できた。今回の確認範囲で、新たな承認を妨げる不具合は見つからなかった。

これは「測定と判定の修正を行い、失敗段階を説明する」というPR10の承認である。成功率が十分安定したこと、全返答が日本語であること、自動採用を有効化してよいこと、フェーズ4全体が完了したことの承認ではない。

### 修正の確認

- **前回指摘1：解消（コード確認・実行確認）。** 仮名がないだけで不合格にせず、全角カンマ等を合わせて見るようになった。「東京都千代田区」「東京都千代田区丸の内一丁目九番一号」「日本国憲法第九条改正反対運動」と、以前の么九牌・儿の例が通ることを確認した。前回の負例「你好，我很高兴和你聊天。」は引き続き検出する。
- 言語判定はあくまで検出器で、全文英語や、登録した字・語を含まない混在文を見逃す範囲が明示された。その範囲を人手で見るという説明も確認した。この検出器が全言語・全表現の誤検知や見逃しをなくしたと評価しているわけではない。
- **前回指摘2：解消（コード確認・実行確認）。** 日付が一致した候補をmatchedとして保持し、その本文を合格理由に表示する。面接Aの日付を誤り、Bを正しくした前回の再現入力で、Bだけが根拠として表示されることを確認した。
- それ以前の数値期待の検証、起点発言の指定、単一目的の候補上限、最大日付の処理、中断後の具体的回答、doneの検査と通しシナリオも、関連テストで維持されている。

### 実モデル測定の照合

`logs/evals/20260909T002648-2026-09-07.1/report.json`とreport.mdを読み、以下を確認した。

- **全体107/129、実行失敗0。** 機械判定対象43シナリオで再集計して一致した。
- **小実験は1/3。** 第3試行は、感想を聞く目標の抽出→採用→7日後のask→再生completed→相手の具体的な感想→「達成した目標」成功→2日後の別会話で目標を渡さずwait、まで通っている。質問文自体にも映画の感想への問いかけがあり、単に目標IDが渡っただけの確認ではない。
- **失敗した2試行は抽出段階。** タイトル・選んだ理由など予定前の質問目標しかなく、「感想」を条件とする採用が空振りした。その後の待機は目標なしによるもので、独立した行動選択の失敗と数えていない対応説明と一致する。
- **現行言語判定でも結果は変わらない。** このレポートの空でない返答202件へ現在の_check_languageを再適用し、保存された言語チェックとの合否差は0件だった。したがって、今回の言語修正だけを理由に同じモデル出力を再生成する必要はない。これは固定ログの再判定であり、新しいモデル測定ではない。
- 保存された目標の基準日や本文まで常に正確になったとする証拠ではない。今回の小実験は7日後に質問できたことを確認するもので、1/3から基準日の精度や生成の安定性を一般化しない。

### 計画・課題の評価

- `phase4.md`は旧12/18を質問までの数字として区別し、延長後の1/3と失敗原因を追加している。以前保留した「完了まで通った成功記録がない」は今回解消した。数値目標を先に置かないという計画に対し、ここで新たな成功率の下限は要求しない。
- **資料には古い文言が一部残る。** 完了条件1冒頭の「まだ満たしていない。実モデルで通しの成立を確認できていない」は、直後の最新測定1/3と矛盾する。中断除外を撤回する前の説明や、表の分母を「話しかけた」とする見出しも残る。最新の対応説明と実コード・ログから現在の状態は判断できるため、これらの編集上の残りだけで承認は保留しない。現在の結論に合わせて整理するのがよい。
- ISSUE-032の、判断を集約しても独立した試行数にはならず、実行別・シナリオ別の内訳を残す方針は妥当。1/3や前後の観測範囲だけで「効果なし」「安定した」と確定しない。
- ISSUE-033で、promise/about_personの除外だけでは他の種類を許可する根拠にならないことを確認した。**PR11へ進むことと自動採用を有効化することは別であり、既定は手動、有効にする種類には個別の評価根拠が必要**という計画を維持する。
- ISSUE-034の「聞こえなかった」を回答扱いする品質問題は未解消として明記されている。再生状態だけで拒否する回帰を戻したことは妥当だが、その課題が解決したとは扱わない。今回のPR10承認はこの既知課題を含む品質全体の完了承認ではない。

### 指摘事項

新たな承認を妨げる指摘なし。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr10-third-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **304件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 同じ一時DB指定でPythonから前回2件の再現入力を再検証し、修正を確認した。上記レポートの集計と言語判定の固定ログ再検証も実施した。
- 実モデルの新規実行、実VOICEVOX・実ブラウザ、実プロセス再起動、人手専用5シナリオの総合的な品質判定は未実施。全過去ログ5496件の再集計は今回行っておらず、独立に再判定したのは最新測定の返答202件である。
- ソースコード・設定・通常利用DBは変更せず、このレビュー資料だけ追記した。サブエージェントは使用していない。

## PR11 — 第1回レビュー（2026-09-09）

### 結論

**PR11は現状では承認保留。** 一括取り消しで派生した情報が有効なまま残る問題と、自動採用を有効にした評価が二重採用・誤った不合格を起こす問題を、一時DBで再現した。手動訂正の保護、日時、採用前後の追跡、設定検証にも以下の問題がある。

**有効にする種類をゼロのままPR12へ渡す判断は妥当。** PR10で得られていない評価根拠を作るために、先に種類を有効化する必要はない。今回保留する理由はゼロ件だからではなく、設定を有効にして比較測定する仕組みと、悪化時に戻す仕組みに不具合があるためである。PR11の承認、自動採用の運用上の有効化、フェーズ4全体の完了承認は分けて扱う。

### 対象

- PR11の未コミット差分と新規ファイル、採用・訂正・削除・参照の関連経路、既存テストを確認した。AGENTS.md、README、設計書の自動採用・更新履歴・訂正波及の規定、計画のPR11・PR12、前回レビュー・対応を照合した。
- 以下の再現は、既存の一時DB・モックLLMのfixtureと、一時領域の追加テストを使った。実モデルの品質を測ったものではない。
- ソースコード・設定・通常利用DBは変更していない。リポジトリ内で今回変更したのは、このレビュー資料への追記のみ。サブエージェントは使用していない。

### 指摘事項

#### 1. [高] 一括取り消しによる記憶削除が、そこから作った状態・目標へ波及しない

- 場所：`backend/app/api/auto_adopt.py:99–114`。
- 確認区分：**実行して再現**。
- 発生条件：自動採用した記憶を根拠に、手動採用した状態・目標が存在する状態で、一括取り消しを実行する。自動採用した派生物でも、取り消し対象から除外される場合は同じ問題が残る。
- 再現手順：`promise`だけを自動採用し、その記憶IDを`basis_memory_ids`に持つactiveの状態と目標を作成。`POST /api/auto-adopt/rollback`後、DBと`active_goals`を確認した。
- 観測結果：記憶はdeletedになったが、状態・目標はどちらも`active / needs_review=False`のまま。目標は`active_goals`の返却対象にも残った。
- 影響：取り消した情報が目標や関心を介して引き続き使われる。既存の記憶削除APIが守っている訂正波及を、新しい削除経路が迂回している。
- 修正方針：記憶の削除・履歴記録と同じトランザクション内で`mark_derived_for_review`を呼び、記憶IDと由来する会話IDを渡す。手動の派生物や取り消し対象外の派生物も含めて検証する。

#### 2. [高] 自動採用と評価の採用ステップが衝突し、比較測定が別の処理を測ってしまう

- 場所：`backend/app/evaluation/steps.py:179–225`、同`:313–338`、`backend/app/evaluation/runner.py:654–674`、同`:787–791`。
- 確認区分：**実行して再現**。
- 発生条件：`--auto-adopt`で記憶または目標を有効にして、既存の`reflect accept=true`や`accept_goal`を含むシナリオを流す。
- 再現手順：①promise候補1件を返すモックで、`run_reflection`に`auto_adopt_kinds={'promise'}`と`accept=True`を渡した。②目標1件を抽出するシナリオに`accept_goal`を続け、`auto_adopt='goal'`で`run_scenarios`を実行した。
- 観測結果：①同じ候補から記憶が2件作られ、1件は自動採用、もう1件は手動扱いになった。候補の`accepted_memory_id`は後者へ上書きされた。②レポートでは目標が「自動採用」済みなのに、直後の採用ステップは「『感想』に当たる目標が無かった」と不合格になった。
- 影響：本番の自動採用にはない重複記憶が評価DBに入り、自動採用に成功しただけで失敗も増える。有無の比較を採用品質の差として解釈できない。自動採用した記憶は`outcome.accepted`に入らず、手動採用を外すだけでは後続の期待が使うキーやレポートも揃わない。
- 修正方針：採用済み候補の再作成を防ぐ。自動・手動それぞれの採用結果とIDを評価器へ返し、後続のキー・採用表示へ反映する。自動採用モードでの手動採用ステップの意味を定め、既に自動採用した対象の確認と、未採用対象の手動採用を区別する。「候補がそもそも出なかった」場合の不合格は維持する。

#### 3. [中] 手動訂正した記憶も一括取り消しで削除される

- 場所：`backend/app/api/auto_adopt.py:89–110`、関連：`backend/app/api/memories.py:144`以降。
- 確認区分：**実行して再現**。
- 発生条件：自動採用した記憶を、開発者がPATCHで訂正した後に一括取り消しする。
- 再現手順：自動採用した「土曜に映画へ行く」記憶を、記憶APIで「日曜に映画へ行く」へ訂正し、一括取り消しを実行した。
- 観測結果：訂正済みの記憶が`reverted`へ入りdeletedになった。`skipped`は空だった。
- 影響：APIの説明「訂正・削除された記憶には触れない」に反し、開発者が修正して維持した情報まで取り消す。statusは訂正してもactiveのため、現行の除外条件では判別できない。
- 修正方針：自動採用時点の版と現在の版、または採用後の変更履歴を使って、手動介入した対象を判定し、理由付きで除外する。自動採用の出自を示す印と、いま一括取り消ししてよいかの判定を区別する。

#### 4. [中] `since`のタイムゾーンによって取り消し対象が変わる

- 場所：`backend/app/api/auto_adopt.py:55–64`、関連：`backend/app/schemas.py:545`の`created_at`。
- 確認区分：**実行して再現**。
- 発生条件：`since`にUTC以外のオフセット付き日時を指定する。
- 再現手順：自動採用した記憶の作成時刻を`2026-09-09T01:00:00Z`とし、一覧を`since=2026-09-09T00:00:00Z`と、同じ瞬間の`since=2026-09-09T09:00:00+09:00`で取得した。
- 観測結果：前者は1件、後者は0件。同じ瞬間なのに対象が異なった。
- 影響：一覧・取り消しが共有するSQL条件で、指定期間の対象を取りこぼす。他のオフセットでは指定以前の対象を含める方向にもずれる。新しい一覧の`created_at`も既存の`UtcDatetime`を使わず、再現時の応答にはオフセットがなかった。
- 修正方針：比較前にUTCへ正規化し、naive日時をどう解釈するか統一する。出力も既存のUTC補完型を使う。UTC/JSTで同じ結果になる一覧・取り消しのテストを追加する。今回の自動採用は新規保存直後なので、`created_at`を期間の基準にすること自体は問題としていない。

#### 5. [中] 自動採用だけ評価の進めた時計を受け取らない

- 場所：`backend/app/agent/auto_adopt.py:58–72`、`backend/app/evaluation/steps.py:179–184`、同`:203–221`。
- 確認区分：**実行して再現**。
- 発生条件：評価で`advance_time`した後に記憶を自動採用する。
- 再現手順：指摘2の記憶採用に`now=2026-10-09T00:00:00Z`を渡し、保存日時を比較した。
- 観測結果：自動採用の記憶は実時計の9月9日、続く手動扱いの記憶は注入した10月9日になった。
- 影響：自動採用の有無で作成日時と検索の時間減衰が変わる。採用直後の記憶を過去に作られたものとして測るため、PR12の比較条件が揃わない。指摘2の二重作成を直しても、自動採用側の実時計使用は残る。
- 修正方針：`apply`と`accept_candidate`に評価時刻を渡せるようにし、`create_memory(created_at=...)`まで伝える。本番では未指定として実時計を使う。

#### 6. [中] 手動採用時の編集で、抽出された元の候補が記録から失われる

- 場所：`backend/app/api/conversations.py:377–394`、関連：`backend/app/agent/auto_adopt.py:58–75`。
- 確認区分：**実行して再現**。
- 発生条件：手動採用時に候補の本文・種類などを編集する。
- 再現手順：抽出された「今週の土曜に映画を見に行く」を、採用APIで「日曜に映画を見る」へ変更して採用し、候補とMemoryRevisionを読み直した。
- 観測結果：候補の本文も訂正後へ置き換わり、記憶の履歴は`created / before=None / after=訂正後の本文`だけだった。元の抽出本文はどちらにも残らなかった。
- 影響：この経路は既定の手動運用でも変化する。モデルが抽出した内容と開発者が直した内容を区別して追えず、採用前後の比較ができない。会話の原文は残るが、それは抽出結果そのものではない。
- 修正方針：抽出候補を保持したまま、採用時の上書き値を共通関数へ渡すか、候補の変更前後を採用履歴に保存する。共通化のために監査情報を失わない形にする。`subject_to_none`による対象解除自体は、今回の再現でも正常だった。

#### 7. [中] 不正な自動採用設定を起動時に検証していない

- 場所：`backend/app/config.py:129–145`、`backend/app/main.py:30–45`、`backend/app/agent/reflection_job.py:260`。
- 確認区分：**実行して再現**（起動処理）、保存時の失敗経路はコード確認。
- 発生条件：`auto_adopt='goals'`など未知の種類でサーバーを起動する。
- 再現手順：不正値のSettingsを作成し、その設定と一時DBを差し込んでアプリの`lifespan`を実行した。
- 観測結果：Settingsの生成も起動処理も成功した。検証は`auto_adopt_kinds`プロパティを明示的に読むまで行われない。
- 影響：計画の「綴りを間違えたら起動時に止める」と一致しない。本番の最初の参照は振り返りの保存直前なので、4回の抽出後に設定エラーになり、候補を保存できない。例外はジョブ側で扱われるため、開始権が永久に残るとまでは指摘していない。評価CLIは実行前に読んでおり、この点は本番と異なる。
- 修正方針：Settingsの生成時、またはlifespanでDB回復等より前に検証する。プロパティ単体の例外テストに加えて、不正設定で起動が失敗することを確かめる。

### その他の評価

- `accept_candidate`への共通化そのものは妥当。既存の手動採用テストも通っており、採用先へ編集値を渡すことと、編集前を保存することは両立させればよい。
- 状態・目標の自動採用は手動のdecideと異なり、会話由来の記憶IDの結び付け・暫定印を付けていない。ただし、現行の`mark_for_review`は根拠IDが空の場合に会話IDで拾うため、この差だけで訂正波及が必ず漏れるとは判断しなかった。指摘1は、それ以前に波及関数自体を呼んでいない、実際に再現した問題である。
- 記憶を物理削除せずdeletedにする方針は、既存の履歴・復元と整合する。ただし指摘1・3の保護が必要。状態・目標には`auto_accepted`履歴がある。記憶は`created`履歴の理由が「自動採用」であり、計画の「履歴にもauto_accepted」という表現とは一致しないが、自動採用だった事実は理由から読める。
- 設定の許可リストをモデルの種類と突き合わせるテストは、このPRの範囲では妥当。configからmodelsをimportしないこと自体は問題ではない。
- フロントは3種類とも自動採用の表示分岐が追加されている。今回のテストはモックAPIによる確認であり、実ブラウザとの接続確認ではない。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr11-review.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **316件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- frontend：`npx tsc --noEmit`、`npm test -- --run` — 型検査成功、**42件成功**。
- 一時テスト：同じ一時DB指定で`.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr11_review.py -q -s` — **8件成功**。これは問題のある現状を観測するテスト7件と、移行検証1件の成功であり、不具合がないことを示す数字ではない。
- 移行は、旧形式データを一時DBへ投入してheadまでupgradeし、既存記憶3件の`auto_adopted=0`を確認。今回の版をdowngradeして記憶データが保持されること、再upgradeも成功することを確認した。既存のモデルとのschema一致テストも成功。既存の状態・目標の実データを入れたdowngradeは追加検証していない。
- `git diff --check`は成功。実モデルでの自動採用有無の比較、実VOICEVOX・実ブラウザ、実プロセス再起動、長時間運用は未確認。通常利用DBへのマイグレーションは実施していない。

## PR11 — 第2回レビュー・対応確認（2026-09-09）

### 結論

**PR11は引き続き承認保留。** 前回7件の直接の問題には修正が入っている。ただし、指摘2の評価への採用結果の受け渡しと、指摘6の採用履歴の保存方法に、それぞれ別の不具合が残ることを一時DBで再現した。以下の2件を解消すれば、今回の確認範囲で残る承認阻害要因はなくなる。

種類を一つも有効にせず、品質判断をPR12の比較測定へ渡す方針は引き続き妥当。実モデル測定をまだ行っていないこと自体を、今回の保留理由にはしていない。

### 前回7件への対応評価

| 前回の指摘 | 評価 | 確認内容 |
| --- | --- | --- |
| 1：削除の波及 | 解消 | 一括取り消し後に状態・目標へneeds_reviewが付き、目標がactive_goalsから外れることを再確認。記憶ID・会話IDを渡し、削除と同じトランザクションで処理している。 |
| 2：評価との二重採用・採用失敗 | 直接の問題は解消、関連する判定不具合あり | 自動・手動採用が重なっても記憶は1件で、採用済みIDも一致する。自動採用済み目標を採用ステップで不合格にする問題は既存の追加テストで解消を確認。ただし下記指摘1の誤ったキー付与が残る。 |
| 3：訂正済み記憶の取り消し | 解消 | 採用後の履歴を確認してskippedへ残す経路と、そのAPIテストの成功を確認。 |
| 4：sinceの日時 | 解消 | 同じ瞬間のUTC/JST指定で一覧が一致し、JST指定の取り消しも対象を拾う。出力のcreated_atにはUTCのZが付いた。 |
| 5：評価の時計 | 解消 | nowを注入して振り返りから自動採用し、唯一の記憶が指定した10月9日の時刻で保存されることを再確認。 |
| 6：元の候補の消失 | 元データの保持は解消、復元との不整合あり | 候補を保持し、afterにbeforeの変更項目を重ねれば元の内容を再構成できる。空のkeywords指定とsubject_to_noneも確認。ただし下記指摘2のとおり、既存の復元APIはその再構成をしない。 |
| 7：設定検証 | 解消 | 不正なSettings生成でValidationError、有効な値と空設定は成功。CLIもSettingsを再構築し、検証を通るコードになった。 |

### 指摘事項

#### 1. [高] 自動採用した無関係な記憶に期待対象のキーが付き、参照評価が誤って通る

- 場所：`backend/app/evaluation/steps.py:190–201`、`backend/app/evaluation/runner.py:784–790`。
- 確認区分：**実行して再現**。
- 発生条件：振り返りで`accept_contains`と`accept_key`を指定し、その条件に一致しない種類の記憶も自動採用される。
- 再現手順：モックLLMに「コーヒーが好き」というfact候補だけを返させ、factを自動採用。reflectに`accept=true / accept_contains=['天体観測'] / accept_key='天体観測'`を指定した。その後、別会話でコーヒーを質問し、`expect_memories=['天体観測']`を評価した。
- 観測結果：抽出・採用されたのはコーヒーの記憶だけなのに、参照キーは「天体観測」となり、「渡した記憶」のチェックが「期待どおり」で通った。
- 影響：`outcome.accepted`へ自動採用の全件を追加する一方、runnerは全件に同じaccept_keyを割り当てるため、accept_containsによる対象の選別が効かない。期待する記憶を抽出・参照できていないのに合格し、PR12の比較測定を誤らせる。
- 修正方針：自動採用した全件の記録と、シナリオが名前を付けた対象の同定を分ける。accept_keyはaccept_containsなどの対応条件を満たす記憶にだけ付け、無関係な自動採用記憶は別キーで記録する。自動採用自体を評価条件で抑止して本番との違いを作るのではなく、期待が正しいIDに結び付くようにする。期待対象が出ず、無関係な記憶だけが参照された負例を追加する。

#### 2. [中] 採用時の部分的なbeforeを既存の復元APIが完全な履歴として扱う

- 場所：`backend/app/agent/auto_adopt.py:130–138`、関連：`backend/app/api/memories.py:235–248`。
- 確認区分：**実行して再現**。
- 発生条件：編集して採用した記憶を、その後さらに別の項目で訂正し、`accepted_with_edits`のrevision_idを指定して復元する。
- 再現手順：privateの候補を本文だけ変えて採用。その記憶をPATCHでpublicへ変更してから、採用時の`accepted_with_edits`履歴を指定してrestoreした。
- 観測結果：restoreは200を返し、本文は抽出時の文へ戻ったが、visibilityはpublicのまま。採用時のafterにはprivateが残っているのに、beforeはcontentしか持たず、復元処理がその項目だけを現在値へ上書きするためである。
- 影響：「指定した変更履歴の状態へ戻す」という既存APIの動作が、この新しい履歴だけ成立しない。当時は非公開だった情報が公開のまま残る。元の情報が履歴から消えているわけではなく、履歴を使う既存処理との不整合である。
- 修正方針：他のMemoryRevisionと同様に、beforeを復元可能な完全なスナップショットとして保存する。あるいは部分差分の形式を明示し、このactionではafterへbeforeを重ねて当時の全体を復元する。直後の復元だけでなく、後から別項目を変更した後の履歴指定復元を検証する。

### 質問への評価・補足

- **Overrides：** `keywords=None`は無指定、空文字は削除として正しく区別している。候補の元のkeywordsを保持しつつ、採用先だけ空にできることを再現した。`subject_to_none`とIDの同時指定は現行の外部入口で400にしており、内部のOverrides単体に同じ検査がないことだけを新たな不具合とはしない。復元との不整合は上記のとおり。
- **採用済みを飛ばす意味：** 自動採用有無の比較では、「使う対象が採用済みになったか」を確認する扱いは妥当。ただし成功を「開発者が採用した証拠」と解釈してはいけない。現在のaccept_goalは自動採用に限らず、手動扱いのactive目標も通すことを関数の実行で確認した。現行仕様を採用済み状態の確認とするならその意味を記録し、手動採用の経路自体の検証は既定設定のテストで別に維持する必要がある。
- **複数の根拠からの波及：** 各記憶の削除ごとに同じ派生物が再評価対象になっても、needs_reviewが外れる経路にはならない。理由欄は最後の理由になり得るが、各変更の履歴は残る。現行モデルの派生根拠は記憶ID・会話IDであり、状態ID・目標IDを根拠にする新たな依存関係は確認されなかった。状態・目標の撤回から同じ波及関数を呼ばないこと自体は漏れと判断していない。この複数根拠の組み合わせはコード確認のみ。
- **_was_edited：** 未知の履歴actionが加わった場合に取り消しを控える方向は、開発者の修正を保護する目的に合う。除外理由を返すため黙った取りこぼしにもならない。現時点の記憶の訂正・削除・復元は、この判定で保護される。
- **設定検証：** 不正なauto_adoptでアプリの起動を止めるのは、今回要求している早期検出そのもの。正しい設定で他の機能を壊す副作用は、今回のコード確認・テストでは見つからなかった。field_validator化に合わせてCLIのmodel_copyを除いた対応も妥当。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr11-r2.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **323件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ一時DB指定で`.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr11_r2.py -q -s` — **7件成功**。修正確認と上記不具合の現状を観測する検証を含む。
- `git diff --check`は成功。修正を戻す検証は今回独立には実施していない。フロントは今回の修正対象ではなく再実行していない（前回レビューでは42件・型検査成功）。
- 実モデルによる比較、実VOICEVOX・実ブラウザ、実プロセス再起動、長時間運用は未確認。通常利用DBへの移行は実施していない。マイグレーションの再実行は既存テストの範囲であり、前回行った独立のdowngrade検証は繰り返していない。
- 対応資料とコードを読み、通常利用DB・ソースコード・設定を変更せず、この資料に結果を追記した。一時テストは一時領域へ保存。サブエージェントは使用していない。

## PR11 — 第3回レビュー・対応確認（2026-09-09）

### 結論

**PR11は承認してよく、PR12の比較測定へ進める。** 前回の2件は解消し、今回確認した修正と関連経路で新たな承認を妨げる不具合は見つからなかった。

これは、既定を手動のまま維持した「自動採用・履歴・取り消し・評価の仕組み」の承認である。特定の種類を運用で有効化してよいことや、フェーズ4全体の完了を承認したものではない。有効にする種類は、計画どおりPR12の測定根拠を得てから判断する。

### 修正の確認

#### 前回指摘1：期待対象のキーが無関係な記憶へ付く — 解消

- 場所：`backend/app/evaluation/runner.py:794–802`。
- 確認区分：**実行して確認**。
- `accept_key`を付ける対象に`accept_contains`の条件を適用し、その他の自動採用記憶は別キーで保持するようになった。自動採用そのものを抑制しておらず、本番と評価の採用対象を変えていない。
- 前回と同じく「コーヒーが好き」だけを自動採用し、別会話でその記憶が実際に検索される入力を使った。「天体観測」を条件・期待にした場合は「渡らなかった記憶: 天体観測」で不合格になることを確認した。
- 対になる検証として、同じ出力で条件を「コーヒー」に変更すると、指定キーに正しく結び付き「渡した記憶」が合格した。常に不合格にする修正ではないことも確認できた。
- 追加された既存テストの負例も確認した。修正を戻すとテストが落ちるという対応者の検証は、今回は独立には繰り返していない。

#### 前回指摘2：採用時の部分履歴から完全に復元できない — 解消

- 場所：`backend/app/agent/auto_adopt.py:141–148`、関連：`backend/app/api/memories.py:235–248`。
- 確認区分：**実行して確認**。
- `memory_snapshot(memory) | edited`によって、採用時の変更前を完全なスナップショットとして残している。既存の復元処理が期待する形式と揃った。
- privateの候補を本文だけ変えて採用し、その後PATCHでpublicへ変更。採用時の履歴を指定してrestoreすると、200で本文が抽出時の値へ戻り、visibilityもprivateへ戻った。
- beforeにはcontentだけでなく、参照相手・確実性・公開範囲・状態・keywords・occurred_atなど既存snapshotの全項目が保存されていることを確認した。元の候補の保持、keywordsの空文字による解除、subject_to_noneも再確認した。

### 以前の対応と承認範囲

- 一時DBで、記憶の一括取り消しが派生した状態・目標へ波及し、目標が参照対象から外れることを再確認した。
- UTC/JSTで同じ対象を返す一覧と、JST指定の取り消し、自動採用と評価の手動採用が重なっても記憶が1件だけであること、注入した評価時刻で保存されることも維持されている。
- 採用後に手動訂正した記憶の保護は既存APIテストで確認。不正設定の生成時エラー、有効な設定と既定の空設定の受け入れは追加検証でも確認した。
- `accept_goal`を「使う対象が採用済みになったか」の確認と定義し、手動採用を実行した証拠とは区別する説明がコードに追加された。前回の補足と整合する。
- 種類ごとの有効化をゼロのままにする計画は維持されている。実モデルの自動採用品質、不要な質問・重複・誤推測・適切な待機の比較は、PR12で確認する事項である。

### 指摘事項

新たな承認を妨げる指摘なし。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr11-r3.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **325件成功**。対応資料の件数と一致。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時テスト：同じ一時DB指定で`.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr11_r3.py -q -s` — **8件成功**。前回の再現入力を修正後の期待で検証し、参照判定には正例も加えた。
- `git diff --check` — 成功。フロントは今回の修正対象外のため再実行していない（PR11第1回レビューでは42件・型検査成功）。
- 実モデルによる自動採用有無の比較、実VOICEVOX・実ブラウザ、実プロセス再起動、長時間運用は未確認。通常利用DBへの移行は実施していない。独立のdowngrade検証は第1回レビューの結果を引き継ぎ、今回は繰り返していない。
- 対応資料、修正コード、関連テストと計画を確認した。ソースコード・設定・通常利用DBは変更せず、この資料へ追記した。一時テストは一時領域に保存。サブエージェントは使用していない。

## PR12 — 第1回レビュー（2026-09-09）

### 結論

**PR12およびフェーズ4全体の完了承認は保留する。** 自動採用が実際に有効になったことは確認した。しかし、有効化の根拠が採用された目標の誤り・重複を十分に扱っておらず、不要な目標を許容する理由も実装と異なる。加えて、入手経路の導出に未設定入力の問題がある。

これは全試行の成功や品質問題ゼロを要求する判断ではない。「不適切な筋書きから0件だった」だけでなく、採用された内容が目的・時期・重複の観点でどうだったかを整理し、残す誤りの影響を実装に即して判断する必要がある。ISSUE-031を完全に解消することだけが承認条件ではない。今回の失敗には、それより前の抽出失敗も含まれる。

### 対象・方法

- 作業開始時の差分は空で、対象はコミット`08fc1bb`（`3cc1990..08fc1bb`）だった。指定されたコード・テスト・記録と、関連する採用・評価・目標の期限切れ処理を確認した。
- AGENTS.mdと既読の開発方針に従い、設計書第6節、PR12の計画、実装記録、関連ISSUEを照合した。
- 保存済みの`20260909T121009`と`20260909T133300`のreport.jsonを読み、シナリオ宣言の`has_machine_checks`で人手専用を除いて再集計した。**107/129、108/129は一致**した。後者の明示的な自動採用表示から、**promise 16件・goal 48件**も確認した。
- 以下で「ログ確認」とするものは既存の実モデル出力の確認であり、今回の新規モデル実行ではない。追加の動作検証は一時DB・モックLLMで実施した。

### 指摘事項

#### 1. [高] 自動採用の適格性と失敗原因の整理が、実際に採用された目標の内容を捉えていない

- 場所：`docs/result/phase4.md:64–75`、同`:113–131`、`backend/app/config.py`の`auto_adopt`既定値。根拠ログ：`logs/evals/20260909T121009-2026-09-07.1/report.json`、`logs/evals/20260909T133300-2026-09-07.1/report.json`。
- 確認区分：**コード・保存済み実モデルログ確認**（今回の新規モデル実行なし）。
- 発生条件：残すべきでない筋書きからの件数だけでgoalの適格性を判断し、目的違い・重複・時期違いを持つ候補も既定で採用する。
- 観測した根拠：
  - 全種類有効の`goal-is-extracted-adopted-and-asked`は3試行とも「感想」に当たる候補がなく、採用ステップが失敗している。それでも映画の名前・ストーリー等の目標が自動採用され、7日後や9日後にも「今週の土曜に見る予定の映画」を質問している。
  - 同じログの`impression-is-kept`第3試行では、「開発者が山に登った場合、どの山か・いつ行ったのかを聞く」という同一本文の目標が2件とも自動採用として記録されている。「残すべきでない筋書きで0件」は、この重複を数えない。
  - 出荷時設定の通しシナリオは、第1・第3試行で感想目標が抽出されず、第2試行は感想目標を採用できた後でwaitになった。第3試行はaskまで進んでいる。したがって、この0/3を「ほぼ会話の開始で待機」「条件1と6はいずれも同じ原因」とまとめることはできない。
- 影響：設計が求める「不要な質問、重複、誤った推測」まで測った適格性として読み取れず、抽出と行動選択のどちらを改善すべきかも誤る。既知の抽出品質課題を新しい別件として数える指摘ではなく、それを許容して既定を有効にする今回の判断に、観測済みの誤りが反映されていないという指摘である。
- 修正方針：採用されたgoalを、元の会話・予定・採用後の質問と照合し、目的・基準日・重複・相手の取り違えを分類する。通しの失敗は最初に失敗した段階ごとに記録する。その上で、どこまでを自動採用し、どの誤りをどの保護・取り消しで許容するかを明記し、出荷時設定の対象シナリオで確かめる。無条件に全件成功を求めるものではない。

#### 2. [中] 雑談から採用された目標は、許容理由に挙げた期限切れでは消えない

- 場所：`docs/result/phase4.md:139–142`、`backend/app/agent/goal.py:425–431`。
- 確認区分：**実行して再現**。対象の目標とtriggerは保存済みログでも確認。
- 発生条件：`small-talk-is-not-kept`第3試行で採用された「『青い空』の実際の色を聞く」のような、`next_conversation`の目標を未実行のまま残す。
- 再現手順：同じ本文・triggerのactive目標を一時DBへ作り、作成日を2026-09-09とした。`now=2027-09-09 / after_days=14`で`expire_overdue`を呼び、`active_goals`を取得した。
- 観測結果：期限切れ件数は0、状態はactive、参照対象にも残った。期限切れ処理が対象にするのは、due_atを持つafter_dateだけである。
- 影響：「不要な質問が1件生まれるが期限切れで消える」という許容根拠が成立しない。こうした目標は蓄積し得る。実行後は期限切れ対象外なので、質問した場合もこの期限設定だけでは消えない。
- 修正方針：この種類にも有効な保持期間や取り下げの扱いを定めるか、自動採用対象を絞る。少なくとも、実際には働かない期限切れを許容理由に使わない。今のafter_date専用処理を一律にバグとしているのではなく、既定有効化に伴う影響の説明と対策が不足している。

#### 3. [中] about_partnerが欠けた出力を、不明ではなく伝聞と確定して自動採用する

- 場所：`backend/app/agent/reflection.py:275`、同`:473–485`、`backend/app/agent/auto_adopt.py:237–248`。
- 確認区分：**実行して再現**。
- 発生条件：モデルがpromiseの本文・種類は返すが、入手経路の導出に必要なabout_partnerを省略する。
- 再現手順：出荷時と同じ`promise,goal`を有効にし、ユーザーが「次は写真の話をしよう」と発言した会話を終了。モックの抽出出力を`kind=promise / content=次は写真の話をする / certainty=fact`だけとした。
- 観測結果：保存されたMemoryは`provenance=hearsay / subject_speaker_id=None / auto_adopted=True`。判断材料が返されていないのに、伝聞と確定して長期記憶へ入った。
- 影響：以前はprovenance未指定ならunknownにできたが、現在はabout_partnerの既定falseが「第三者についてと判断したfalse」と区別されない。明示的な伝聞と分類不能を同じにし、既定有効のpromiseにも誤った入手経路が付く。
- 修正方針：about_partnerを必須にして省略を抽出失敗として扱うか、未指定を別状態で保持してunknown・手動確認へ回す。使用しなくなったprovenanceフィールドの旧検証を復活させる必要はない。新しい導出元の不足を検証する必要がある。

### 完了条件の評価

| 条件 | 評価 | 根拠・残る点 |
| --- | --- | --- |
| 1：経験に沿った自発的な質問・提案 | 完了扱いは保留 | 過去の通し成功と事前投入の成功はあるが、出荷時設定の通し0/3には抽出失敗2回が含まれる。ISSUE-031だけに原因を限定できない。 |
| 2：不適切なタイミングでの待機 | 確認した範囲では満たす | 指定の2シナリオは各3/3。正常なwaitとfallbackを区別する仕組みもある。全状況を保証する数字とはしない。 |
| 3：完了した質問・目標の繰り返し防止 | 制御は確認、採用時の重複は残る | doneの除外・再生との連動はテストと記録がある。ただし同一目標が別行で採用される例があり、採用段階の重複を適格性評価から外せない。 |
| 4：予定変更・訂正・取消の反映 | 確認した範囲では満たす | 指定シナリオの成功と既存の訂正波及・取消テストを確認。生成内容や基準日そのものの正確さとは別。 |
| 5：根拠の追跡・悪化時の復元 | 機構は確認 | PR11までの履歴・復元・一括取り消しの検証がある。LLMを使わない制御をpytestで測ること自体は適切で、対応する実モデルシナリオがないことだけで不合格にはしない。 |
| 6：固定人格の維持 | 完了を十分確認できない | persona-holdsの失敗2回がwaitなのは記録どおり。ただし発言がない回は人格維持を測れていない。語の安全弁は固定人格への影響がないことの保証にはならない。 |

### 有効化・測り方への評価

- **0件の意味：** その測定で観測されなかったという根拠にはなるが、安全性や発生率ゼロの証明にはならない。1/95は採用された全目標に対する割合で、雑談等を入力したときの誤採用率ではない。対象の異なる目標47件・48件を合わせても、同じ負例の独立した95試行にはならない。追加実行数の任意の下限は要求しないが、負例ごとの試行・採用・見送りを分けて判断する必要がある。
- **promiseとgoalを分ける：** promiseの観測はgoalより良いが、入手経路だけでは約束として残す内容の妥当性は決まらない。出荷時ログでは`correction-withdraws-the-state-it-came-from`第2試行の「開発者はYUIに天体観測への興味を問うている」もpromiseとして自動採用されている。質問を約束として扱うことの妥当性も含め、中身を評価する必要がある。
- **provenanceの導出：** 同じ判断を二重に訊かない方針は合理的。ただし主体が「本人／第三者／YUI／不明」のときに2値だけで全入手経路を表せるわけではない。impressionの特別扱いはその種別定義に従う限り妥当だが、モデルが種別やabout_partnerを誤った場合まで正しくするものではない。弟のシナリオは出荷時ログで3/3を確認したが、これは新しい分類の観測結果であり、機械的に取り違え不能になったという保証ではない。
- **人格の安全弁：** 誤って弾かれた正当な候補が手動確認へ残る方向は許容しやすい。言い換えによる見逃しはコード・説明とも認めており、今回新発見の不具合として数えない。ただし、その限界を踏まえた負例確認なしに「人格に影響しない」と一般化できない。
- **テストの既定：** 既存の手動採用テストを維持し、出荷既定を専用テストで固定する方針は妥当。ただしautouse fixtureが差し替えるのはreflection_jobの設定取得であり、アプリ全体や評価runnerの設定ではない。328件を「出荷時の全経路が自動採用ありで成功した」とは解釈しない。
- **人手専用5シナリオ：** 今回、出荷時ログの全15試行の返答を読んだ。雨の好みを相手ごとに反転させない例は確認できた一方、state-is-separate-from-persona第1試行には「觉得います」があり、既知の言語混入が残る。未知の名称への回答では、知っているという説明と知らないという回答が試行間で異なる。名称の実在性・説明の正確性は今回検証しておらず、人手評価全体を合格とはしていない。既知の言語品質問題を新たなコード不具合として再指摘するものではない。

### 記録の照合・不足

- 出荷時ログの実行失敗1件は`joke-is-not-kept`第2試行で、候補のkindに`inference`が出たための抽出失敗だった。失敗として記録されており、候補0件の成功に隠してはいない。ただしこの回は自動採用の安全弁が成功した試行とも数えられない。
- 全種類有効の121009ログは記憶を個別に自動／手動と表示する前の形式である。本文と採用件数は読めるが、表の記憶90件について全件の自動採用との対応・分類判断を今回独立に復元できたとはしていない。47件の目標には自動採用表示がある。後の133300ログでは記憶も個別表示され、この区別は改善している。
- `docs/result/phase4.md:144`の残るpromise誤分類の例「写真を見たい」は、指定された出荷時ログの該当箇所では「YUIは開発者の実家の犬の種類を知りたい」だった。同じ構造の問題ではあるが、例の出典は揃える必要がある。
- 実装記録末尾のpytestは325件のままで、今回実行した328件と異なる。これは記録の更新漏れとして扱い、単独の承認保留理由にはしない。
- 新しい既定設定・抽出コードでの自動採用なしとの比較が、どの保存ログに対応するかは今回確定していない。比較表には設定・コード・採用対象が特定できる実行を対応付けること。「差がないことは証明していない」という留保は正しい。

### 実行確認・未確認

- backend：`YUI_DATABASE_URL=sqlite+aiosqlite:////private/tmp/yui-pr12.db PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q` — **328件成功**。
- backend：`.venv/bin/ruff check --no-cache app tests` — 成功。
- 一時検証：同じ一時DB指定で`.venv/bin/python -m pytest -c pyproject.toml -p no:cacheprovider /private/tmp/test_yui_pr12.py -q -s` — **2件成功**。上記の期限切れ対象外と未設定入手経路の問題を観測するテストである。
- 既存ログの再集計・本文確認を行ったが、新規の実モデル測定は行っていない。104137→104844の4試行比較や090958の114件・58件の全件再分類は今回未実施。
- フロントは変更がなく再実行していない。実VOICEVOX・実ブラウザ、実プロセス再起動、長時間の蓄積運用、別モデル・別人格版の検証は未実施。通常利用DBには触れていない。
- ソースコード・設定を変更せず、このレビュー資料だけ追記した。追加テストは一時領域に保存。サブエージェントは使用していない。
