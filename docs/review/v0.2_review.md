# v0.2 PR1 コードレビュー：会話状態の器と規則

レビュー日：2026-09-13

対象：コミット `7ed3069`（v0.2 PR1「会話状態の器と規則」）。`docs/plan/v0.2.md` の PR1 の範囲。レビューは `.claude/agents/reviewer.md` の手順（diff を先に読み、設計書は後）に従った。ソースコード・通常利用の DB は変更していない。再現に使った一時ファイルは削除済み。

**結論：Critical は無い。マージを止める不備は見つからなかった。Warning 4 件（改行を含む発言での延期・終了の誤検出／取りこぼし、「そうですか」の質問誤検出、プロンプト節に入る質問本文が無制限、target 絞り込みの片側がテストで検出できない）は PR2 以降の着手前に直すことを勧める。**

## 対象と確認方法

- `backend/app/agent/conversation_state.py`（新規 588 行）、`conversation.py`、`prompt.py`、`models.py`、Alembic migration `836089fcf4d9`、`tests/test_conversation_state.py`
- 実行：一時 DB を指定して `.venv/bin/python -m pytest -p no:cacheprovider` — **228 件成功**（既存の migration テストを含む）。`.venv/bin/ruff check app tests --no-cache` — 成功
- 呼び出し元の確認：`build_messages`／`build_system_prompt` の呼び出しは `conversation.py` のみ。`get_open_states` は `conversation.py` と新テストのみ。`ConversationState` を参照する既存コードは無い。会話・発言を削除する API は存在しないため、`responded_message_id` 等の `ondelete` 無し FK が実運用で衝突する経路は現時点では無い
- 再現：規則の純粋関数に境界値を通すスクリプトと、`get_open_states` の target フィルタを monkeypatch で無効化した状態で既存テストを走らせる変異検証を、一時ファイルで実行した

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：会話ごとの状態テーブルを足し、相手→YUI・YUI→相手の質問、延期、終了、提示済みを正規表現で検出して行を作り、「応答があった」（`responded_message_id`）と「解決した」（`status`）を分けて持つ。開いている状態を `# この会話で` 節として system prompt に差し込む。discrepancy／correction の重複制約は PR3 のために先行実装する。

設計書（`docs/plan/v0.2.md` §3・§4・§11）と照合した結果、意図のズレは無い。以下は設計と実装の差だが、いずれも不具合ではない：

- §4 は「取得（履歴・記憶・状態）の**後**に規則を適用」とあるが、実装は相手の発言保存直後（`_recent_history` の前）に適用している（`conversation.py:99`）。規則は履歴・記憶を参照しないので結果は同じ
- §3 の `question_to_yui` の語彙は「？／?／〜ますか／〜でしょうか／〜教えて／〜してほしい／〜してくれる」だが、実装は「ですか」「かしら」「お願い」「てもらえる」も加えている。後述の Warning 2 の原因になっている
- §5 の `/end` での `expired` 化は `expire_open_states` が用意されているだけで未配線。PR4 の範囲で、`chat.py:_ensure_open` が終了済みの会話への発言を拒むため、開いたままでも副作用は無い

## 指摘事項

### Warning 1. 改行を文の区切りとして扱わないため、延期の話題が前の行を飲み込み、終了の合図を取りこぼす

- 場所：`backend/app/agent/conversation_state.py:91`（`_SENTENCE_SPLIT_RE`）、`:94`（`_last_sentence`）、`:123`（`detect_deferral_topic`）、`:135`（`detect_closing`）
- 確認区分：**再現済み**
- 発生条件：発言に改行が含まれる。画面の入力欄は `textarea` で、Enter は改行、⌘/Ctrl+Enter が送信（`frontend/src/components/ChatPanel.tsx:248-259`）なので、通常操作で複数行の発言が届く
- 再現（一時スクリプト）：

  ```
  deferral: '疲れた\n映画'      # detect_deferral_topic("今日は疲れた\n映画の話はまた今度")
  closing : False              # detect_closing("今日は楽しかった\nそろそろ寝るね")
  closing (。区切り): True      # detect_closing("今日は楽しかった。そろそろ寝るね")
  ```

- 影響：`deferral.content` が「疲れた\n映画」で保存され、`# この会話で` 節の1行に改行が混ざって「- 相手が後にすると言った話題：疲れた／映画（自分からは持ち出さない…）」と2行に割れる。話題の語も誤る。終了は逆に検出されず、「終わりにしようとしている」の指示が出ない。どちらも PR3 の解釈が無効な構成（設計 §8 の既定）では取り消されずに会話の終わりまで残る。コミットメッセージで直したとされる「前の文を topic が飲み込む」問題が、区切りが改行のときにだけ再発している
- 修正方針：`_SENTENCE_SPLIT_RE` を `(?<=[。！!？?\n])` のように改行（と疑問符）でも区切る。あわせて `_DEFERRAL_RE` の topic の文字クラスから `\n` を除外する。テストに「改行で区切られた2行」の例を足す

### Warning 2. 「そうですか」「そうなんですか」が相手の質問として登録される

- 場所：`backend/app/agent/conversation_state.py:41-43`（`_QUESTION_ENDING_RE` の「ですか」）
- 確認区分：**再現済み**
- 発生条件：相手が相づちの「そうですか。」「なるほど、そうですか！」「へえ、そうなんですか」を送る
- 再現：

  ```
  'そうですか。' True
  'そうですか' True
  'なるほど、そうですか！' True
  'へえ、そうなんですか' True
  ```

- 影響：`question_to_yui` が作られ、その返答が来るまでの節に「まだ答えていない相手の質問：『そうですか。』」が出る。返答後は `responded` が入って節から消えるが、行は open のまま残り PR3 の判定対象になる（解釈を無効にした構成では永続）。設計 §3 の語彙は「ですか」を含まず、実装が独自に広げた分で起きている。`is_acknowledgement_only` は「そうですか」を相づちに含めていないので抑止も効かない
- 修正方針：「ですか」を語彙から外し設計の語彙に揃えるか、「そう(なん)?ですか」を除外する。`_ACKNOWLEDGEMENT_WORDS` に「そうですか」「そうなんですか」を足し、`detect_request_question` の前に `is_acknowledgement_only` で弾く

### Warning 3. 質問本文が無制限に system prompt へ入る（`presented` だけ 200 文字で切っている）

- 場所：`backend/app/agent/conversation_state.py:414`（`question_to_yui` の `content=text`）、`:480`（`question_to_partner` の `content=text`）、`:533`・`:544`・`:556`（節への出力）
- 確認区分：**再現済み**
- 発生条件：`ChatRequest.text` は最大 4000 文字（`schemas.py:139`）。末尾が「？」で終わる長文や、YUI の長い返答が「〜ますか？」で終わる場合
- 再現：

  ```
  section length: 4024           # 4000 文字の質問1件で節が 4024 文字
  partner-question section length: 5043   # YUI の質問 800 文字 × 6 件（待ち1＋応答済み上限5）
  ```

- 影響：`presented` は設計どおり 200 文字で切る一方、質問は全文を保持・出力するため、`# この会話で` 節だけで数千文字になり、ローカルモデルのコンテキストを圧迫する。コミットメッセージで「節が上限なく肥大する問題」を直したとあるが、件数の上限だけで1件あたりの長さは無制限
- 修正方針：`question_*` の `content` も保存時に上限（例：200 文字）で切るか、節に出すときに切る。`content` は設計 §3 で「開発者向けの本文」なので、保存は全文・出力だけ切る形でもよい

### Warning 4. `question_to_yui` の target 絞り込み（YUI の返答側）を検出できるテストが無い

- 場所：`backend/tests/test_conversation_state.py:257-293`（`test_state_targeted_at_one_speaker_is_not_affected_by_another`）、対象コード `backend/app/agent/conversation_state.py:455-506`（`apply_character_message_rules`）
- 確認区分：**変異検証で再現済み**。`get_open_states` の `target_speaker_id` フィルタを monkeypatch で無効化して既存テストを実行した結果：

  ```
  test_state_targeted_at_one_speaker_is_not_affected_by_another  passed   # 検出できない
  test_yuis_question_to_a_is_not_answered_by_b                   FAILED   # 相手の発言側は検出できる
  （追加した「A の質問→生成失敗→B のターン」のケース）              FAILED   # assert 3 is None
  ```

- 発生条件：A の質問は A 自身のターンの返答で `responded` が埋まるため、B のターンで上書きされない理由は「target が違う」ではなく「`mark_first_response` が最初の1件しか書かない」こと。このテストは絞り込みが無くても通る。絞り込みが実際に効くのは「A の質問が `responded` 空のまま残る」場合で、それは A のターンの生成が失敗（503）したときに起きる（相手の発言由来の状態は commit 済みで残る。設計 §8「生成の失敗」）
- 影響：`apply_character_message_rules` の `get_open_states(..., target_speaker_id=target_speaker_id)` を落とす退行が入っても既存テストは通る。設計 §3 の「`target_speaker_id` と一致しない相手の発言では、どの遷移も起こさない」の片側が固定されていない
- 修正方針：上記の変異で落ちたケース（A の質問 → `fake_llm.chat` を `LLMError` にして 503 → B のターン → A 宛 `question_to_yui` の `responded_message_id is None`）をテストに足す。既存テストの docstring「B のターン（B の発言・B 向けの返答）では変わらない」は実態と合わないので、`mark_first_response` の上書き防止の検証として書き直す

### Nit

- `_SENTENCE_SPLIT_RE` が「？」「?」で区切らないため、`detect_closing("元気？そろそろ寝るね")` は `False`（再現済み）。Warning 1 の修正に含める
- `create_or_supersede_correction`（`conversation_state.py:343`）で `target_speaker_id=None` を渡すと、`get_open_state_for_ref` が target を絞らず別の相手の訂正を `superseded` にする。PR1 の呼び出し元は無く PR3 の呼び方次第だが、`None` を「全員」の意味で受け付けるのか、禁止するのかを決めておく
- 生成失敗（503）後に相手が同じ質問を再送すると `question_to_yui` が2件になり、節に同じ質問が2行並ぶ。設計 §4 末尾で「重複しうる」と受容済みなので指摘ではないが、節では `content` の同一なものをまとめてよい（`deferral` と同じ扱い）

## 問題が無いことを確認した範囲

- 「応答があった」と「解決した」の分離：`mark_first_response` は `responded_message_id` だけを書き、`status` に触らない。聞き返しで上書きしないことをテストで確認。`resolve_state`／`withdraw_state` を `respond()` から呼ぶ経路は無い
- トランザクション境界：相手の発言由来の状態は `commit`（`conversation.py:144`）で確定し、返答由来の状態は返答の `Message` と同じトランザクション。生成失敗時に相手の発言と質問だけが残る動きは設計 §8 どおり
- 会話ロック：`respond()` 全体が `conversation_locks.hold` の中で、規則の適用と `get_open_states` は同じ会話で重ならない。規則は正規表現のみでイベントループを塞がない
- `presented` の窓：`window_message_ids` は直近 12 件と今の発言。窓の外にあるものだけを直近 5 件出す（設計 §3）
- discrepancy／correction の重複制約：`misdetected` の除外、`asked` 入りの行の扱い、`superseded` の連鎖はテストで固定されている
- migration と model の列・索引は一致。`server_default="0"` は SQLite で問題無い。既存の migration テストが head まで通る
- 秘密情報・権限：新規 API は無く、露出する情報は増えていない

---

## 再評価：対応後の確認と PR1 の承認判断

レビュー日：2026-09-13

**結論：Warning 4 件と Nit 1 件の対応を確認した。新たな不具合・退行は見つからなかった。PR1 を承認してよいと判断する。**

### 対象

[Claude の対応記録](../claude/v0.2_response.md)（未追跡ファイル）と、対応コミット `9f50b9f`（`conversation_state.py`、`test_conversation_state.py`）。ソースコード・通常利用の DB は変更していない。再現に使った一時ファイルは削除済み。

実行：一時 DB で `.venv/bin/python -m pytest -p no:cacheprovider` — **231 件成功**。`ruff check app tests --no-cache` — 成功。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | 改行を文の区切りにしない | `_SENTENCE_SPLIT_RE` に `？?\n` を追加、`_DEFERRAL_RE` の topic から `\n` を除外 | **解消**。`"今日は疲れた\n映画の話はまた今度"` → `'映画'`、`"今日は楽しかった\nそろそろ寝るね"` → `True`。Nit の `"元気？そろそろ寝るね"` → `True` も同時に解消。`\r\n`・末尾改行・空行連続・「？ 」（後に空白）でも正しく判定した |
| W2 | 「そうですか」の質問誤検出 | `_ACKNOWLEDGEMENT_QUESTION_RE` で `そう(なん)?ですか` の文末を `detect_question` の入口で除外 | **解消**。4 例すべて `False`。退行を疑った「山は好きなんですか」「本当にそうですか？」「そうですか？」は引き続き `True`（疑問符付きは質問として扱う。妥当） |
| W3 | 質問本文が無制限に節へ入る | `CONTENT_LIMIT = 200` を新設し、`question_to_yui`／`question_to_partner`／`closing`／`presented` の 4 箇所で統一 | **解消**。4000 文字の質問で節が 226 文字。テスト `test_long_question_is_truncated_in_content_and_prompt` は API 経由で保存後の長さを見ており、実装の写しになっていない |
| W4 | YUI 返答側の target 絞り込みをテストが検出できない | `test_yuis_reply_failure_then_b_does_not_answer_a_question_to_yui` を追加（コードは変更なし） | **解消**。前回と同じ変異（`get_open_states` の target フィルタを monkeypatch で無効化）で、新テストが `assert 3 is None` で**失敗する**ことを確認。既存の `test_yuis_question_to_a_is_not_answered_by_b` と合わせて、相手の発言側・YUI の返答側の両方が固定された |
| Nit | 節での同一質問の重複 | 未応答・待ち・応答済みの 3 区分で `content` の重複を除外 | **解消**。応答済みは上限 5 件に切ってから重複を除く順序で、新しいものを優先する意図と合っている |

対応記録の「修正不要と判断した点」（規則の適用位置、`/end` の `expired` 化未配線、`create_or_supersede_correction` の `target=None` を PR3 で決める）は、前回レビューの判断と同じで異論は無い。

### 対応で入りうる退行として確認した範囲

- 「？」を文の区切りに加えたことで、`detect_deferral_topic`・`detect_closing` の判定対象が疑問文の後ろに絞られる。延期の定型句は疑問符で終わらないため、既存の検出例に影響は無い（テスト全件成功）
- `detect_question` は `_last_sentence` を通さず全文の末尾を見るため、区切りの変更の影響を受けない
- `content` の 200 文字切り詰めは保存時に行うため、`build_conversation_state_section` の重複排除（`content` の一致）は切り詰め後の本文で比較される。同じ長文の再送は同一視される（意図どおり）
- 対応記録に「`git checkout --` で未コミットの修正 1〜3 を一度失った」とあるが、`9f50b9f` の diff に 3 件とも含まれていることを確認した

### 残る留意点（承認を妨げない）

- `create_or_supersede_correction` に `target_speaker_id=None` を渡した場合の意味は未決定。PR3 の着手時に決める（対応記録どおり）
- 「そうですか」は完全一致の相づち辞書 `_ACKNOWLEDGEMENT_WORDS` には入っておらず、`is_acknowledgement_only` は引き続き `False` を返す。PR3 で `confirmed` の抑止に使うときは `_ACKNOWLEDGEMENT_QUESTION_RE` も参照するか、辞書に足す必要がある

---

# v0.2 PR2 コードレビュー：出力検査と終了設計の最小形

レビュー日：2026-09-13

対象：`056ae8a` 上の未コミットの作業ツリー（`docs/plan/v0.2.md` §11 の PR2）。`conversation.py`、`conversation_state.py`、`evaluation/runner.py`、`evaluation/scenario.py`、`tests/test_conversation_state.py`、`tests/test_evaluation.py`、新規 `evals/scenarios/conversation_state.toml`。手順は前回と同じ（diff を先に読み、設計書は後）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

**結論：Critical は無い。Warning 2 件（「〜前に」＋依頼形の closing が習慣・一般論の質問を終了の合図にする／評価器の `expect_no_new_question` の「対象外」分岐がターン後の状態では到達できず、実装が意図的に通した返答を失敗と判定する）。どちらも再現済み。2 件を直せば PR2 を承認できる。**

## 対象と確認方法

- 実行：一時 DB で `pytest -p no:cacheprovider` — **239 件成功**（`test_bundled_scenarios_load` が新シナリオ TOML の読込を含む）。`ruff check app tests --no-cache` — 成功
- 呼び出し元：`_check_turn` は `runner.py` と `test_evaluation.py` のみ（引数追加は両方に反映済み）。`RunRecord.options` は `RunRecordOut`（`schemas.py:85`）経由で `ChatPanel.tsx:417` が `JSON.stringify` で表示している。JSON 列なので `checks` の追加で型は壊れない
- Ollama クライアント（`ollama_client.py:69`）は `messages` をそのまま渡すため、再生成時に末尾へ足す 2 つ目の system メッセージは問題無い。`FakeLLM` も `messages[0]` しか見ない
- `LLMResponse` は frozen でない dataclass なので `response.text = CLOSING_FALLBACK_SENTENCE` は成立する

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：生成後に「出力検査」を足す。closing が開いていて返答に質問があれば追加の system 指示で 1 回再生成し、それでも質問が残れば、相手の未応答の質問が無い場合だけ固定文に差し替える。延期の話題の語の一致と discrepancy 開時の断定らしさは記録のみ。結果を `RunRecord.options["checks"]` へ。「寝る前に…教えて」の形の closing 検出を足し、評価器に `expect_no_new_question`／`expect_conversation_states` を追加、シナリオ TOML を新設する。

設計 §4 手順 6・§7・§9・§11 と照合し、意図のズレは無い。§11 が PR2 に求めるシナリオ 1〜4・6・8・19 は TOML に揃っている（8 と 19 は対になる 2 本ずつ）。固定文は §9 どおり定数で設定にしていない。再生成に失敗したとき 1 回目の返答を使う扱いは §8 に明記が無いが、妥当。

## 指摘事項

### Warning 1. 「〜前に」＋依頼形の closing 検出が、睡眠・帰宅の習慣や一般論についての質問を終了の合図にする

- 場所：`backend/app/agent/conversation_state.py:106-108`（`_CLOSING_BEFORE_RE`）、`:181`（`detect_closing` の第 2 条件）
- 確認区分：**再現済み**
- 発生条件：最後の文に「寝る／帰る／落ちる／おやすみ＋前に」があり、文末が質問・依頼の形。終了の意図が無い質問がそのまま当たる：

  ```
  True  '寝る前にストレッチするといいって本当？'
  True  '寝る前に飲むといい薬を教えて'
  True  '成績が落ちる前に対策を教えてほしい'
  True  '帰る前にやることリストを教えてくれる？'
  True  'おやすみ前に読む絵本、おすすめある？'
  False '帰る前に一つだけ。明日の集合時間を教えて'   # 逆に、文を分けた本物のシナリオ 4 は拾えない
  ```

- 影響：closing は解釈（PR3、既定で無効）が `reopened` にするまで `/end` まで open のまま残る。以後のすべてのターンで「終わりにしようとしている」の指示が入り、YUI の返答に質問があるたびに再生成が走り（応答時間が倍）、相手の未応答の質問が無いターンでは固定文「今日はここまでにいたしましょう…」に差し替わる。会話の途中で「寝る前のストレッチ」を尋ねただけで、会話の残りが締めの文で埋まりうる。前回の対応で「寝る前にストレッチしてる」（依頼無し）を弾いたが、依頼形を要求しても同じ穴が残っている。上の 6 例目のように、シナリオ 4 の自然な言い方（依頼を別の文にする）は逆に取りこぼす
- 修正方針：「〜前に」の形は「依頼の対象が終了の直前に済ませたいこと」（「〜だけ」「一つだけ」「最後に」）を伴うときに限る、または `_CLOSING_BEFORE_RE` の直後を「明日の／今日の／最後に／一つ／だけ」等に絞る。シナリオ 4 の文言（「寝る前に明日の集合時間だけ教えて」）に「だけ」があるので、`(前に).{0,20}?だけ` を要求するだけで上の 5 例は全部外れる。加えて、複数文の形（「帰る前に一つだけ。〜教えて」）を拾うなら、直前の文が「〜前に(一つ|ひとつ)?だけ」で終わる場合を許す。いずれの場合も `test_detect_closing_matches_before_form_with_a_trailing_request` に上の誤検出側 5 例を足す

### Warning 2. 評価器の `expect_no_new_question` は「相手の質問が残っているため対象外」に到達できず、実装が意図的に通した返答を失敗と判定する

- 場所：`backend/app/evaluation/runner.py:360-365`（`_check_turn`）、`:596`（`_conversation_states_for_speaker` の呼び出し時点）、`backend/app/agent/conversation_state.py:572`（`has_unanswered_question_to_yui`）、`backend/evals/scenarios/conversation_state.toml:46-47`（シナリオ 4）
- 確認区分：**再現済み**。シナリオ 4 の発言を API 経由で流し、`FakeLLM` に 2 回とも質問付きの返答をさせた：

  ```
  IMPL:   {'closing_open': True, 'closing_new_question_detected': True,
           'closing_fell_back_to_template': False, 'closing_unresolved_due_to_open_question': True}
  AFTER-TURN responded: [2]          # question_to_yui.responded_message_id は返答で埋まる
  RUNNER has_unanswered: False       # 評価器がターン後に見る状態では「未応答の質問」は無い
  RUNNER check: [(False, '返答に新しい質問が残っている')]
  ```

- 発生条件：実装は**生成前**の状態で `has_unanswered_question_to_yui` を判定する（`conversation.py:176-198`。この時点では `responded_message_id` が空）。評価器は**ターン完了後**に同じ関数を呼ぶ（`runner.py:596`）が、`apply_character_message_rules` がその返答自身を `responded_message_id` に入れた後なので、同じターンの `question_to_yui` は常に「応答済み」に見える。つまり `runner.py:365` の「対象外」分岐は、その say で相手が質問したケースでは決して真にならない。真になるのは、以前のターンの質問が生成失敗で `responded` 空のまま残っている場合だけ
- 影響：シナリオ 4 は `expect_no_new_question = true` を持つ。実装は「相手の依頼が残っているターンでは定型へ落とさず記録のみ」（設計 §4 手順 6）として質問付きの返答を通すが、評価器はそれを機械判定で失敗にする。設計 §7 の「`question_to_yui` が `open` の場合、その答えは対象外」が評価器では実現されておらず、コメント「実装と同じ条件で見る」は成立していない。PR2 の受け入れ（シナリオ 1〜4・6・8・19 を 3 回）でシナリオ 4 が返答の言い回し次第で落ちる
- 付随：`has_unanswered_question_to_yui` は docstring に「`open` かつ `responded` が空」とあるが `status` を見ていない。実装側は `get_open_states` の結果を渡すので問題無いが、評価器は全ステータスの行を渡す（`_conversation_states_for_speaker`）ため、PR3 で `withdrawn`／`expired` の行ができると「未応答」に数える
- 修正方針：評価器は実装の判定結果をそのまま使う——`result.run.options["checks"]["closing_unresolved_due_to_open_question"]` が真なら「対象外」、そうでなければ `detect_question(reply)` を見る。または「その say 自身が依頼・質問の形か（`detect_request_question(step.text)`）」で対象外を判定する。`has_unanswered_question_to_yui` には `status == open` の条件を足す。再現に使った「2 回とも質問付きの返答でシナリオ 4 の文を流す」ケースを `test_evaluation.py` に足す

### Nit

- `conversation.py:258`：再生成したとき `latency_ms` は 2 回目の呼び出しだけになり、1 回目の時間が実行記録から消える。§4 末尾の「ターン全体の時間を PR5 で測る」に使うなら、両方の合計か `checks` に 1 回目の値を残す
- `ChatPanel.tsx:417` は `options` を丸ごと `JSON.stringify` して「設定」として表示するため、`checks` の 10 項目が生成設定と混ざって出る。PR4 の画面接続で分ける
- `looks_assertive`（`conversation_state.py:604`）は「確認」を含めば非断定とみなすため、「確認しました、日曜です。」が非断定になる（再現済み）。記録のみなので影響は無いが、ヒューリスティックの弱さは `checks` を読む側が知っている必要がある

## 問題が無いことを確認した範囲

- 再生成は 1 回で止まり（`len(fake_llm.calls) == 2`）、再生成の失敗で 503 にせず 1 回目の返答を使う。固定文に差し替えた返答にも `apply_character_message_rules` が通り、`presented` が固定文で残る（質問形でないので `question_to_partner` は作られない）
- `closing_open` の判定は `commit` 前に取得した `conversation_states` を使うため、このターンの発言で作られた closing を含む。target で絞り込み済み
- 定型への差し替え条件は `responded_message_id is None` で見ており、前回の memory の指摘（`status` だけで見ると一度質問された会話で経路が死ぬ）は反映されている。テスト `test_closing_falls_back_even_if_an_earlier_question_was_already_answered` で固定されている
- `expect_conversation_states` の空リスト（行が無いこと）と条件付きの一致は、`contains` の AND・`asked`／`responded` の有無の比較を含めて実装が §7 の定義どおり。kind／status／withdraw_reason の値は読込時に検証される
- deferral・discrepancy の検査は closing の結果と独立に走り、行動を変えない
- `checks` の辞書は `response.options` のコピーに足しており、生成設定を上書きしない

---

## PR2 再評価：対応後の確認と承認判断

レビュー日：2026-09-13

**結論：Warning 2 件の対応を確認した。新たな不具合・退行は見つからなかった。PR2 を承認してよいと判断する。** 下記の Nit 2 件（評価器側の回帰テストが無い、対応記録の相対リンク切れ）は承認を妨げない。

### 対象

[Claude の対応記録](v0.2_response.md)「PR2」節と、作業ツリー上の修正（`conversation_state.py`、`runner.py`、`test_conversation_state.py`、`test_evaluation.py`）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

実行：一時 DB で `pytest -p no:cacheprovider` — **239 件成功**。`ruff check app tests --no-cache` — 成功。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | 「〜前に」＋依頼形の closing が習慣・一般論の質問を拾う | `_CLOSING_BEFORE_RE` に「前に」の後 20 文字以内の「だけ／一つ／ひとつ／最後に」を要求（`conversation_state.py:107-115`） | **解消**。前回の誤検出 5 例はすべて `False`。正例「寝る前に明日の集合時間だけ教えて」「寝る前に一つだけ聞いてもいい？」「帰る前に最後に一つ教えて」は `True`。5 例は `test_detect_closing_matches_before_form_with_a_trailing_request` に追加されている |
| W2 | 評価器の「対象外」分岐がターン後の状態では到達不能 | `_check_turn` に `run_checks` を追加し、実装の `checks["closing_unresolved_due_to_open_question"]` をそのまま使う（`runner.py:266-273, 361-384, 615`）。`has_unanswered_question_to_yui` に `status == open` を追加 | **解消**。前回の再現手順（シナリオ 4 の文を FakeLLM の 2 回とも質問付き返答で流す）で `(True, '相手の質問が残っているため対象外（人手判定と併用）')` になった。呼び出し側は `result.run.options.get("checks", {})` を渡しており、`checks` が無い旧い実行記録でも `KeyError` にならない |

対応記録の「W1 は依頼形の要求だけでは弱すぎた」という原因分析は、前回レビューの見立てと一致する。

### 対応で入りうる退行として確認した範囲

- `_CLOSING_BEFORE_RE` の絞り込みで `_CLOSING_PATTERNS`（最後の文全体の一致）は変わっておらず、「そろそろ寝るね」等の定型は引き続き `True`
- 「帰る前に一つだけ。明日の集合時間を教えて」（文を分けた言い換え）は前回同様 `False` のまま。前回レビューで「拾うなら」と書いた任意の拡張で、対応記録は扱っていない。シナリオ 4 の文言は 1 文なので PR2 の受け入れには影響しない。設計 §7 の語彙を広げるかは PR5 の測定で判断すればよい
- 「寝る前に毎日飲む薬だけ教えて」は `True`（「だけ」が限定の意味で使われる習慣の質問）。残る穴だが、前回の 5 例より出現頻度は低く、設計が規則の限界として受容している範囲と判断する

### Nit（承認を妨げない）

- **評価器の回帰テストが無い。** W2 の対応記録は「再現スクリプトで確認」とし、`test_evaluation.py` には既存呼び出しの引数追加しかない（新規テスト関数 0 件）。`expect_no_new_question` の 2 分岐（`run_checks` が真なら対象外、偽なら `detect_question`）を固定するテストが無いため、`run_checks` の鍵名を変えると評価器だけが黙って壊れる。前回の再現手順をそのままテストにすることを勧める
- **対応記録の相対リンクが切れている。** `docs/review/claude/`・`codex/` を `docs/review/` 直下へ移した作業ツリーでは、`v0.2_response.md` 内の `../codex/v0.2_review.md` と `../../../backend/...` の 8 箇所がすべて解決しない（`docs/review/` からは `../../backend/...`、`v0.2_review.md`）。PR1 節の 4 箇所も同じ。コミット前に直す

---

# v0.2 PR3 コードレビュー：解釈（LLM）の呼び出しと状態への適用

レビュー日：2026-09-13

対象：`b1afd20` 上の未コミットの作業ツリー（`docs/plan/v0.2.md` §11 の PR3）。新規 `app/agent/interpretation.py`・`tests/test_interpretation.py`、`conversation_state.py`（+714 行）、`conversation.py`、`config.py`、`evaluation/run.py`、`tests/conftest.py`、`evals/scenarios/conversation_state.toml`。手順は前回と同じ（diff を先に読み、設計書は後）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

**結論：Critical 1 件（解釈の LLM 呼び出しを DB の書き込みトランザクションを開いたまま行うため、その間の別会話・別 API の書き込みが `database is locked` で失敗する。再現済み）。Warning 2 件（解釈へ渡す文脈に発言 ID が無く、`ref_kind="message"` の訂正・食い違いは実モデルでは検証を通れない／評価器がシナリオ 20 を必ず失敗にする）。Critical は解釈を有効にした構成（既定は無効）でだけ起きるが、PR3 が届ける機能そのものの経路なので、PR5 で有効化を判断する前に直す必要がある。**

## 対象と確認方法

- 実行：一時 DB で `pytest -p no:cacheprovider` — **258 件成功**。`ruff check app tests --no-cache` — 成功。Python 3.11.3（`asyncio.wait_for` の `TimeoutError` は組み込みと同一で、`except TimeoutError` は正しく捕まえる）
- 呼び出し元：`apply_interpretation_result`／`gather_interpretation_context`／`discrepancy_offer_ledger`／`select_discrepancy_offers` は `conversation.py` と新テストのみ。`build_conversation_state_section` の新引数 `discrepancy_offers` は省略可能で既存呼び出しを壊さない。`FakeLLM` は system 文で解釈の呼び出しを見分けるため、既存テストの `push` の消費順は変わらない（全件成功で確認）。評価器 `run.py` は `settings.model_copy` で解釈を有効にし、`run_attempt` → `ConversationAgent(settings=settings)` まで届いている
- 再現：一時テスト 3 本（解釈中の別会話の書き込み、解釈文脈の内容、シナリオ 20 の評価器実行）

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：規則で作った状態の「意味」（用件は何か、答えたか、確認したか、訂正か）を LLM に JSON で判定させ、ID・kind・status・target を検証してから適用する。食い違いの確認候補は `RunRecord.options["offered_discrepancy_ids"]` を台帳にして渡した回数と照合待ちを管理する。失敗は規則の結果だけで進む。既定は無効。

設計 §3・§4 手順 3〜5・§8・§9・§11 と照合した結果：

- 検証の規則（候補外 ID の破棄、両立しない操作の両方破棄、`superseded` は置き換え先の作成に伴ってのみ、`correction` と `discrepancy` が同じ `ref` なら `correction` 優先、`is_closing=false` は何もしない、相づちからは `confirmed` を作らない）は §4 手順 3・4 のとおり実装されている
- §9 の `conversation_state_presented_limit` は `Settings` に無く、`PRESENTED_LIMIT` 定数のまま。他の 4 つは追加されている。小さなズレ
- §4 のシーケンス図は解釈の呼び出しを `commit` の前に置いており、実装はそれに従っている。これが Critical 1 の原因で、設計側の記述も直す必要がある（後述）
- シナリオ 24 を実モデルではなく pytest で確かめる判断は妥当（解釈の 1 回失敗はモックでしか作れない）

## 指摘事項

### Critical 1. 解釈の LLM 呼び出しの間、DB の書き込みトランザクションを開いたままにするため、別会話・別 API の書き込みが `database is locked` で失敗する

- 場所：`backend/app/agent/conversation.py:150-167`（解釈の呼び出し）、`:225-229`（その後の `commit`）
- 発生条件：`conversation_state_llm=True` の構成で、ある会話の解釈（Ollama、timeout 既定 10 秒）が SQLite の busy timeout（aiosqlite 既定 5 秒）より長くかかっている間に、別の会話の `/chat`、`/end` の振り返り保存、記憶の訂正・削除など**書き込みを伴う操作**が来る。`respond()` は相手の `Message` と規則で作った状態を `flush` した状態（RESERVED ロック保持）で `interpret_conversation` を `await` する
- 再現（一時テスト。A の解釈を `FakeLLM` の門で止め、その間に別会話 B が発言する。B 側の busy timeout は再現を速くするため 1 秒にしたが、既定 5 秒 < 解釈 timeout 10 秒 なので条件は同じ）：

  ```
  RESULT B: 失敗 -> OperationalError: (sqlite3.OperationalError) database is locked
  1 passed in 1.32s
  ```

- 影響：既存コード自身が `conversation.py:225` のコメントで「応答を待つ間ロックを保持すると、他の会話や記憶の訂正が database is locked で失敗する」として避けてきた状態を、解釈を有効にすると再導入する。フェーズ 3 レビュー（`/end` の抽出中に書き込みトランザクションを開かない）の指摘 4・5 と同じ種類の退行。解釈の timeout（10 秒）と生成を合わせて 1 ターンが数十秒になる構成では、同時に使う開発画面の記憶訂正がほぼ確実に失敗する
- 修正方針：解釈の**前**に一度 `commit` して規則由来の状態と相手の発言を確定させ（設計 §8「相手の発言由来の状態は残る」と整合）、解釈は トランザクション外で `await` し、結果の適用（`apply_interpretation_result`）と `get_open_states` の読み直しを短いトランザクションで行ってから、既存どおり生成前に `commit` する。設計 §4 のシーケンス図と手順 5 の「ここで DB トランザクションを閉じる」の位置も、それに合わせて書き直す（`/end` の抽出と同じ「モデルを呼んでいる間は書き込みトランザクションを開かない」に統一する）

### Warning 2. 解釈へ渡す文脈に発言 ID が無く、`ref_kind="message"` の `correction`／`discrepancy` は実モデルでは検証を通れない

- 場所：`backend/app/agent/conversation_state.py:1040-1046`（`_format_transcript`）、`:1107`（`message_ids` を検証の候補にする）、`:1152-1158`（`_ref_is_valid`）、`app/agent/interpretation.py` の `_INSTRUCTION`（「id は渡された候補に載っているものだけを使う」）
- 確認区分：**再現済み**。`gather_interpretation_context` の出力：

  ```
  直近の会話:
  相手: 土曜に映画に行くよ
  YUI: 楽しみですね。
  相手: ごめん、土曜じゃなくて日曜だった
  ...
  参照できる記憶（id・内容）:
  - [42] 開発者は写真が好き
  検証で受け付ける message id: [1, 2, 3]
  文脈に載っている [id]: [42]
  ```

- 発生条件：訂正・食い違いの対象が記憶ではなく会話中の発言であるとき。§7 シナリオ 13（`explicit-correction-is-adopted-without-confirmation`、`conversation_state.toml:276`）と 22（`newer-correction-supersedes-the-older-one`、`:518`）は記憶を投入しないため、`correction.ref_kind` は `message` しかありえない
- 影響：モデルは発言の ID を知る手段が無いので、指示どおりに動けば `correction: null` を返し（訂正が記録されない）、ID を推測すれば `_ref_is_valid` で落ちて `dropped: "correction: ref が候補にありません"` になる。どちらでもシナリオ 13・22 の `expect_conversation_states` は満たせない。pytest は ID を直接埋め込むため通っており、実モデルで初めて露見する。「訂正を受け入れる」は v0.2 の到達目標 (4) そのもの
- 修正方針：`_format_transcript` の各行に `[id]` を付ける（`- [3] 相手: ごめん、…` のように、記憶・状態と同じ書式）。`_INSTRUCTION` の `correction`／`discrepancy` の説明に「`ref_kind="message"` の `ref_id` は直近の会話の `[id]`」と明記する。`test_interpretation.py` に「文脈に現れる `[id]` だけが `candidates.message_ids` に入る」ことを見る純粋関数テストを足す

### Warning 3. 評価器がシナリオ 20 を必ず失敗にする（A 宛の状態を B の target で絞り込む）

- 場所：`backend/app/evaluation/runner.py:602`（`target_speaker_id=speakers[step.speaker].id`）、`:638`（`_conversation_states_for_speaker`）、`evals/scenarios/conversation_state.toml:439-462`
- 確認区分：**再現済み**。`FakeLLM` で A への返答を質問にし、B のターンの後で評価器を流した：

  ```
  TURN 私は最近、新しい靴を買ったよ [('会話状態：question_to_partner', False, '条件 1 に一致する行が無い')]
  ATTEMPT ok: False
  ```

- 発生条件：`expect_conversation_states` を、その say の話者（B）以外を target とする状態に対して書いたとき。シナリオ 20 は「A 宛の `question_to_partner` が B の発言で変わらない」ことを B の say の後に見る設計（§7）なので、この形にしかならない
- 影響：PR3 の受け入れ（`--conversation-state-llm` で §7 のシナリオを流す）で、実装が正しくてもシナリオ 20 は常に落ちる。実装側は PR1 の pytest で固定されているため実害は評価結果の誤りだけだが、「対になる失敗を両方測る」§7 の趣旨に反する
- 修正方針：`expect_conversation_states` の条件に任意の `target`（`[[scenario.speakers]]` の key）を足し、指定があればその話者、無ければ say の話者で絞る。または複数話者のシナリオでは会話全体の状態を渡し、条件側で `target` を必須にする。シナリオ 20 は `target = "a"` を書く。前回 PR2 で指摘した「評価器がターン後の状態を実装と別の条件で見る」問題と同種で、評価器の状態取得は「誰の・いつの」を明示する形に揃えるべき

### Nit

- `conversation_state.py:471, 1347`：`create_or_supersede_request` が同じ内容で既存行を返した場合も `summary["created_request"] = True` になる。台帳を読む側が「作られた」と誤読する。戻り値で区別するか、既存と同一なら `False` にする
- `conversation_state.py:740`：訂正の節は「この会話では『日曜』として扱う」だけで、**何を**訂正したかが無い。設計 §4 手順 5 の雛形は `「…」は、この会話では「…」として扱う` と両側を持つ。`ref_kind=memory` なら記憶の本文、`message` なら発言の本文を前に置くと生成側が対象を取り違えにくい
- `Settings.conversation_state_presented_limit`（§9）が無い。`PRESENTED_LIMIT` を設定に出すか、§9 から落とす

## 問題が無いことを確認した範囲

- 失敗時の扱い：timeout・`LLMError`・JSON 解析失敗のいずれも `interpretation_status["error"]` に理由を残し、解決・取消を起こさず返答は返る（テスト 3 本）。`asyncio.wait_for` は `FakeLLM` の門で止めた呼び出しを実際に打ち切る
- 両立しない操作：`answered`×`unanswered`、解決系×`withdrawals` の両方を「その状態への操作を両方捨てる」で処理し、早い者勝ちにならない。`superseded` は `withdrawals` から受け付けず、置き換え先の作成に伴ってだけ起きる。`misdetected` の対象 kind は §3 の表と一致
- 判定対象の選び方（`find_judgment_candidate`）：`judged` が空なら最初の応答、入っていればそれより新しい応答側の発言。聞き返し→答え（シナリオ 12）と答え損ね→答え直し（シナリオ 11）の 2 ターン遷移を机上とテストで追い、`resolved`／`followup_needed` の出入りは §3 のとおり
- 食い違いの台帳：`RunRecord.options` だけを読み（`system_prompt` は取らない）、照合待ちは「渡した後に解釈が一度も成功していない間」で判定する。成功した解釈が `asked` に含めなかった候補は上限内で再び渡せる（§4 手順 4「確認しなかったと確定し、渡した回数に数える」）
- `has_unanswered_question_to_yui` が `followup_needed` を含むようになり、答え損ねた質問があるターンで closing の固定文がその答えを潰さない
- `_format_open_states` は `confirmed` を出さず `presented` を直近 5 件に絞るため、長い会話でも解釈への入力が肥大しない
- 解釈の指示文・文脈に API キー等の秘密情報は含まれない。`options["interpretation"]["summary"]["dropped"]` は状態 ID と定型文だけ

---

## PR3 再評価：対応後の確認と承認判断

レビュー日：2026-09-13

**結論：Critical 1 件・Warning 2 件の対応をすべて確認した。新たな不具合・退行は見つからなかった。PR3 を承認してよいと判断する。**

### 対象

[Claude の対応記録](v0.2_response.md)「PR3」節と、作業ツリー上の修正（`conversation.py`、`conversation_state.py`、`interpretation.py`、`evaluation/scenario.py`、`evaluation/runner.py`、`conversation_state.toml`、`test_interpretation.py`）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

実行：一時 DB で `pytest -p no:cacheprovider` — **259 件成功**。`ruff check app tests --no-cache` — 成功。前回の再現 3 本を修正後コードでそのまま再実行した。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| C1 | 解釈の LLM 呼び出し中に書き込みトランザクションを開いたまま | 解釈の直前に `commit`、結果の適用後にも短い `commit`（`conversation.py:167-206`） | **解消**。前回と同じ手順（A の解釈を門で止め、別会話 B が busy timeout 1 秒で書く）で `LOCK B: 成功`。追加された回帰テスト `test_a_pending_interpretation_call_does_not_block_another_conversations_write` は timeout を 30 秒にして「打ち切り」ではなく「ロック非保持」を見ており、検証対象が正しい。対応記録は修正前で実際に失敗することも確認している |
| W2 | 解釈文脈に発言 ID が無い | `_format_transcript` の各行に `[id]`、指示文に「`ref_kind="message"` の `ref_id` は直近の会話の `[id]`」を明記 | **解消**。`直近の会話（id・話者・本文）: - [1] 相手: … - [2] YUI: … - [3] 相手: …`。文脈に現れる `[id]` の集合が `candidates.message_ids` と一致（`[1, 2, 3] == [1, 2, 3]`） |
| W3 | 評価器がシナリオ 20 を必ず失敗にする | `StepSpec.target`（`speakers` の key、未知なら読込時エラー）を追加し、状態取得の話者を `step.target or step.speaker` に。シナリオ 20 に `target = "a"` | **解消**。FakeLLM で流して `('会話状態：question_to_partner', True, '期待どおり') ok: True` |

Nit のうち `created_request` の誤記録は修正済み。`conversation_state_presented_limit` を設定にしない判断（定数で固定し、変えたら測る対象にする）は §9 の「終了・延期・相づちの辞書」と同じ扱いで妥当。§9 の表側を直すのが筋なので、設計書の更新を勧める。

### 対応で入りうる退行として確認した範囲

- 解釈の前に `commit` することで、`candidates.open_states`・`user_message`・`history`・`memories` は commit をまたいで使われる。`expire_on_commit=False` なので属性は保たれ、`apply_interpretation_result` の更新は次のトランザクションで `flush`→`commit` される。answered／unanswered／asked／withdrawals／correction の各テストがこの経路を通って成功している
- 解釈で `LLMError`／`InterpretationError`／timeout 以外の例外が出た場合は 500 になるが、相手の発言と規則由来の状態は commit 済みで残る。修正前と同じ扱いで、設計 §8「生成の失敗」と整合
- `target` の追加は省略可能で、既存シナリオの読込・評価は変わらない（`test_bundled_scenarios_load` を含む全件成功）

### 残る留意点（承認を妨げない）

- 訂正の節（`conversation_state.py` の「相手の訂正（記憶より優先する）」行）は訂正**後**の内容だけを出し、設計 §4 手順 5 の雛形にある訂正**前**の側（「…」は、この会話では「…」として扱う）が無い。前回の Nit のままで、対応記録でも触れられていない。PR5 の測定で「日曜」だけでは対象を取り違える例が出たら、`ref` の本文を前に置く
- 解釈の呼び出しは会話ロックの内側で行われるため、同じ会話の次の発言は解釈＋生成の合計時間だけ待つ。§4 末尾の「ターン全体の時間は PR5 で測ってから決める」の対象であり、PR3 では問題にしない

---

# v0.2 PR4 コードレビュー：API と画面

レビュー日：2026-09-13

対象：`0611a1c` 上の未コミットの作業ツリー（`docs/plan/v0.2.md` §11 の PR4）。`api/chat.py`、`api/conversations.py`、`schemas.py`、`conversation_state.py`（`withdraw_reason_applies` の公開化）、新規 `tests/test_conversation_states_api.py`、frontend の `App.tsx`・`api.ts`・`types.ts`・`ChatPanel.tsx`・`CandidatePanel.tsx`・`App.test.tsx`。手順は前回と同じ。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

**結論：Critical は無い。Warning 1 件（`/chat` の応答と `GET /states` が、画面が一切使わない `presented` を含む全 open 行を返すため、応答サイズが会話の長さに比例して増える。40 ターンで 44KB を実測）。Nit 4 件。Warning を直せば承認できる。**

## 対象と確認方法

- backend：一時 DB で `pytest -p no:cacheprovider` — **273 件成功**。`ruff check app tests --no-cache` — 成功。`status.HTTP_422_UNPROCESSABLE_CONTENT` は導入されている FastAPI に存在する（`422`）
- frontend：`npx tsc --noEmit` — 成功。`npx vitest run` — **22 件成功**（`App.test.tsx` 12 件）
- 呼び出し元：`_withdraw_reason_applies` の旧名は残っていない。`ChatResponse.conversation_states` は既定が空リストで、既存クライアント・既存テスト（`chatResponse()` に項目追加済み）を壊さない。`/chat` の状態読み取りは `conversation_locks.hold` の内側・返答 `commit` の後（`chat.py:120`）で、同じ会話の次のターンと交錯しない
- 再現：40 ターンの会話で `/chat` 応答と `GET /states?status=open` のサイズを計測

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：会話状態の一覧と手動判断の API、`/chat` 応答への同梱、`/end` での失効、画面の「この会話で」の一文表示と、終了後の訂正候補の読み取り専用表示。

設計 §5・§6・§11 と照合した結果、意図のズレは無い。§6 の細目（`followup_needed` だけを「答え損ね」として出し未判定は出さない／状態の種類名を出さない／訂正候補に採用ボタンを付けない／`decided_by = operator`・`detected_by` は変えない／`/end` で `resolved` ではなく `expired`）はすべて実装とテストで確認できる。§6 に無い追加は `target_speaker_id` の絞り込みと `kind` のカンマ区切りで、どちらも画面側の要請から妥当。

## 指摘事項

### Warning 1. `/chat` の応答と `GET /states` が、画面が使わない `presented` を含む全 open 行を返し、会話の長さに比例して膨らむ

- 場所：`backend/app/api/chat.py:120-139`、`backend/app/api/conversations.py:333-372`、`frontend/src/App.tsx:138-143`（読込時の `status: "open"` 取得）、`frontend/src/components/ChatPanel.tsx:638-660`（表示は `question_*`・`deferral`・`closing`・`correction` だけ）
- 確認区分：**再現済み**。質問を 40 ターン続けた会話：

  ```
  SIZE turn=10 states=21 bytes=11334 kinds={'presented': 11, 'question_to_yui': 10}
  SIZE turn=20 states=41 bytes=22144 kinds={'presented': 21, 'question_to_yui': 20}
  SIZE turn=40 states=81 bytes=43764 kinds={'presented': 41, 'question_to_yui': 40}
  GET open states: 81 bytes 38452
  ```

- 発生条件：`presented` は返答ごとに 1 行でき、解決・取消が無いので `/end` まで open のまま残る（設計 §3）。解釈を無効にした既定の構成では `question_to_yui` も応答済みのまま open で残る。どちらも `ConversationStateSummary` は表示しない（`presented` は種類として扱わず、応答済みの `question_to_yui` は `followup_needed` でない限り出さない）
- 影響：毎ターンの `/chat` 応答が約 1.1KB/ターンずつ増え、履歴を開くたびの `GET` も同じ。ローカルの開発画面では致命的ではないが、`/chat` は音声再生の開始を待つ経路でもあり（`ChatPanel` は応答を受けてから合成を要求する）、長い会話ほど返答が遅れて見える。配信モード（v0.4 以降）で同じ経路を使うと実害になる
- 修正方針：`/chat` の同梱は画面が使う種類に絞る（`kind IN (question_to_yui, question_to_partner, deferral, closing, correction, request)` か、`presented`・`confirmed` を除外）。`App.tsx` の読込時 `GET` も同じ `kind` を渡す。`GET /states` 自体はデバッグ用途で全件を返してよいが、`ChatResponse.conversation_states` の docstring に「画面が使う種類だけ」と明記する。応答済みの `question_to_yui`（`responded` あり・`followup_needed` 無し）も表示に使わないので、同梱から外してよい

### Nit

- `conversations.py:396`：`decide` は `resolved` をどの種類にも受け付ける。`request` は設計 §3 で「解決は無い」、`presented`／`confirmed` は解決の概念が無い。`withdraw_reason_applies` と同じ発想で「`resolved` を受け付ける kind」を持たせると、operator 経路だけが LLM 経路より緩い状態を避けられる
- `conversations.py:333`：存在しない `conversation_id` の `GET /states` は `[]` を返す。`/candidates` 等の既存一覧も同じ挙動なら揃っているが、`decide` が 404 を返すのと非対称
- `App.tsx:141`：`selfSpeakerId` が未解決（`null`、話者一覧の読込前）のときは `target_speaker_id` を付けずに全員宛を取るため、複数話者の会話を開いた直後だけ他の相手の未回答質問が「この会話で」に出うる。`loadSpeakers` の完了を待つか、`speakerKey` から解決できるまで表示を保留する
- `decide` は会話ロックを取らないため、解釈（PR3）が同じ行を更新するターンと重なると後勝ちになる。開発者操作と会話が同時に走ることは稀で、状態は 1 段遷移なので実害は小さいが、`conversation_locks.hold` で囲えば揃う

## 問題が無いことを確認した範囲

- `/end`：`expire_open_states` は抽出成功後の最終トランザクション内（`conversations.py:260`）で、`update(Conversation)` と同じ commit で確定する。抽出失敗（503）では到達せず open のまま（テストで固定）。`decided_by` は空のまま（解決ではない）
- `decide`：他会話の状態は 404、open でなければ 409、`withdrawn` に理由必須、理由と kind の組み合わせは LLM 経路と同じ `withdraw_reason_applies` で検証、`superseded` は常に拒否。`detected_by` は変えない。すべてテストで固定
- `GET /states`：`status`・`kind` の未知の値は 400。`kind` のカンマ区切りと空要素の除去、`target_speaker_id` の絞り込みはテストで確認。SQL は ORM のバインド変数で、文字列連結は無い
- 画面：`App.tsx` の読込は `conversation` と `conversationStates` を `Promise.all` で待ってから反映し、`token !== viewRef.current` で遅延応答を捨てる（既存の会話切替の対策と同じ）。終了時に `conversationStates` を空にし、終了後の訂正候補は `open`／`expired` だけを読み取り専用で出す。`App.test.tsx` に「状態取得が遅れている間は送信できない」「種類名を出さない」「答え損ねと未回答を分ける」「採用ボタンが無い」の 4 本が追加され、実装の写しではなく画面の振る舞いを見ている
- `ConversationStateOut` に秘密情報は無い（本文・ID・状態のみ）

---

## PR4 再評価：対応後の確認と承認判断

レビュー日：2026-09-13

**結論：Warning 1 件と Nit 2 件の対応を確認した。新たな不具合・退行は見つからなかった。PR4 を承認してよいと判断する。** 残る Nit 1 件（下記）は承認を妨げない。

### 対象

[Claude の対応記録](v0.2_response.md)「PR4」節と、作業ツリー上の修正（`api/chat.py`、`api/conversations.py`、`schemas.py`、`conversation_state.py`、`tests/test_conversation_states_api.py`、`App.tsx`、`types.ts`）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

実行：backend は一時 DB で `pytest -p no:cacheprovider` — **275 件成功**、`ruff` — 成功。frontend は `npx tsc --noEmit` — 成功、`npx vitest run` — **22 件成功**。前回の応答サイズ計測を修正後コードでそのまま再実行した。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | `/chat` 応答と読込時 `GET` が `presented` を含む全 open 行を返し肥大 | `DISPLAYED_STATE_KINDS`（`request`／`question_*`／`deferral`／`closing`／`correction`）を backend（`conversation_state.py:713-722`）と frontend（`types.ts:157`）の両方に置き、`/chat` の同梱（`chat.py:124-131`）と `openConversation` の `GET`（`App.tsx:148`）を絞る | **解消**。同じ 40 ターン（全ターン質問）で `turn=40 states=40 bytes=21100 kinds={'question_to_yui': 40}`。`presented` は `/chat` から消え、`GET`（フィルタ無し）では引き続き見える（`test_chat_response_excludes_presented`） |
| Nit 1 | `decide` が解決の概念が無い種類にも `resolved` を受け付ける | `RESOLVABLE_KINDS`（`question_to_yui`／`question_to_partner`／`discrepancy`）を定義し 422 で拒否（`conversations.py`） | **解消**。計画 §3 の表で「解決」欄がある 3 種類と一致。`test_decide_rejects_resolved_for_a_kind_with_no_resolution` で固定 |
| Nit 4 | `decide` が会話ロックを取らない | `conversation_locks.hold` の内側で判定・更新・`commit` まで行う | **解消**。`get_session` 依存の後続 `commit` は空になるだけで害は無い |
| Nit 2 | 存在しない会話の `GET /states` が `[]` | 現状維持（既存の一覧系と同じ規約） | 妥当 |
| Nit 3 | `selfSpeakerId` 未解決の間だけ全員宛を取る | v0.2 では見送り（検証は 1 対 1） | 妥当。計画 §10 と整合 |

### 残る留意点（承認を妨げない）

- **応答済みで未判定の `question_to_yui` は引き続き同梱される。** 解釈を無効にした既定の構成では PR3 の `resolved` が付かないため、質問のたびに 1 行ずつ増える（上の計測は全ターン質問の最悪ケースで 40 行・21KB。通常の会話では質問のターンだけ増える）。`ConversationStateSummary` は `responded_message_id === null` か `followup_needed` のものしか出さないので、同梱側も同じ条件（`responded_message_id is None or followup_needed`）で落とせば `/chat` の応答は表示行と一致し、会話の長さに依存しなくなる。1 行の条件追加で済むため、PR5 の計測前に入れておくことを勧める

### 対応で入りうる退行として確認した範囲

- `DISPLAYED_STATE_KINDS` に `request` を含めているが `ConversationStateSummary` は `request` を表示しない（設計 §6 の表示項目にも無い）。不要な同梱だが 1 会話 1 行（`superseded` で置き換え）なので肥大しない
- `decide` のロック化で、同じ会話の `respond()` 実行中は `decide` が待つ。解釈（PR3）の適用と直列化される意図どおりで、`respond()` は生成中もロックを保持するため待ち時間は生成時間ぶん。開発者操作なので許容範囲
- 既存の `_check_turn`・評価器には変更が無く、`test_bundled_scenarios_load` を含む全件成功

---

# v0.2 PR5 コードレビュー：測定と判断

レビュー日：2026-09-14

対象：`92e5be8` 上の未コミットの作業ツリー（`docs/plan/v0.2.md` §11 の PR5）。`evaluation/steps.py`・`runner.py`・`scenario.py`・`report.py`・`run.py`、新規 `evaluation/measure_turn_latency.py`、`config.py`、`evals/scenarios/end_to_end.toml`、`tests/test_evaluation.py`、新規 `docs/result/v0.2.md`、`README.md`、`docs/design/ai_vtuber_architecture.md`、`docs/issues/issues.md`、`docs/plan/v0.2.md`。手順は前回と同じ（diff を先に読み、設計書は後）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

**結論：Critical は無い。Warning 1 件（設計書 §8.2 の成功条件 (3) は「閾値以下（値は測ってから）」のまま、PR5 で測った後も閾値が決まっておらず、記録の「△」判定に基準が無い）。Nit 2 件。記録の数字は引いているログとすべて一致し、評価器の変更は同梱シナリオで実測どおりに動く。Warning を直せば承認できる。**

## 対象と確認方法

- 実行：一時 DB で `pytest -p no:cacheprovider` — **282 件成功**（記録の数字と一致）。`ruff check app tests --no-cache` — 成功
- 記録の数字の再計算（`logs/evals/*/report.json`・`report.md`・`*-turn-latency.txt`）：
  - `141036`（解釈あり・各 3 回）：見出し 49/72 ✓。成功条件 (1) 12/12、(2) 9/9、(3) 2/3・2/3・0/3、(4) 1/3・2/3・0/3・0/3・3/3・3/3、(5) 3/3・2/3 ✓。安定 18/24（3/3 が 13、0/3 が 5）・不安定 6 ✓。0/3 の 5 本の ID ✓。実行できなかった試行 0 ✓
  - `165201`（解釈なし・各 1 回）：見出し 38/52 ✓。`conversation_state` 以外 33 本のうち機械判定 28 本中 25 本通過、不通過 3 本（`correction-changes-the-next-answer`・`no-memory-is-admitted`・`joke-is-not-kept`）✓。(1) 3/4（`closing-today-…` が不通過）、(2) 3/3、(5) 1/2 ✓。実行できなかった試行 0 ✓
  - 5 回連続（`165210`〜`170356`）：`correction-changes-the-next-answer` は 5 回とも「一致する候補が 2 件」で不通過、`correction-withdraws-…` は 3/5 通過・不通過 2 回のうちガード発火は `165445` の 1 回だけ ✓。`is_spec_error` キーは全 json に実在し、`failed_to_run` の再計算は全回 0 ✓
  - 待ち時間：`172947` の各ターン・平均 4189／11910ms・+7720ms（+184%）✓、`171509` の平均 11995ms・最大 14289ms ✓。「壁時計−生成」の見積もり 6021／7995／7798／8643／8204ms と `171509` ターン 5 の 10018ms ✓。`detected_by`／`decided_by` の表は `172947` と完全一致 ✓
  - 設計書 §2.1：pytest 282、シナリオ 58 本（機械判定 52・人手のみ 6）、38/52、49/72 ✓。計画 §9 の設定表と `config.py` のコメント（平均 11.9〜12.0 秒、最大 12.7〜14.3 秒）✓
- 同梱シナリオの実測：`end_to_end.toml` の `correction-changes-the-next-answer` を `load_scenarios` で読み、FakeLLM で抽出候補を 2 件／1 件にして `run_scenarios` に流した。2 件：`ok=False, failed_to_run=0, is_spec_error=True`、後続の `correct_memory` は「記憶が無かった」で実行継続。1 件：`ok=True`、訂正が `#1` に適用され後続の検査も通過。記録・issue の説明と一致
- `measure_turn_latency.py` の `OllamaClient(...)` の引数は実際のシグネチャ（`host, model, temperature, num_ctx, timeout, think`）と一致。`report.py` の `conversation_state_llm` は `run.py` から `settings` 経由で渡る
- README の「既定構成で動く／動かない」の区分：規則だけで作る種類と `apply_interpretation_result` でしか作らない種類（`request`・`correction`・`discrepancy`）の分け方は実装と一致

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：評価器の穴（ISSUE-035）を「一致が複数なら止める」＋「採用件数が期待と違えば止める」で塞ぎ、その失敗をモデル失敗と区別して集計する。レポートに解釈の有無と機械判定の合計を持たせる。待ち時間の測定スクリプトを固定し、結果と既定値の判断を記録する。README・設計書・issue を「縮小版として完了」に更新する。

設計 §9・§11 PR5 行・§8 と照合した結果、PR5 に求められた項目（待ち時間、`detected_by`／`decided_by` の内訳、既定値の決定、人手 5 シナリオ、ISSUE-035、成功条件 (1)〜(8) を解釈ありで測る、安定性を分けて数える、縮小版の記録）はすべて揃っている。設計 §7 が求める「各 3 回」は解釈ありの `141036` で満たし、解釈なしの回帰は各 1 回であることを記録が明記している。

## 指摘事項

### Warning 1. 成功条件 (3) の閾値が測定後も決まっておらず、記録の「△」に基準が無い

- 場所：`docs/design/ai_vtuber_architecture.md` §8.2「成功条件」行（`(3) … 閾値以下（値は測ってから）`）、`docs/result/v0.2.md`「実モデルでの成功条件 (1)〜(8) の測定」の表（(3) 2/3, 2/3, 0/3 → △）
- 確認区分：文書の突き合わせ
- 発生条件：設計書は (3) の値を「測ってから」決めるとし、PR5 がその測定にあたる。記録は 9・10・11 の合否（4/9）を示して △ としたが、何割なら ○ かはどこにも書かれていない。(1)(2) は「0 件」、(5) は `expect_not_repeating`、(8) は本数と明確だが、(3) だけ基準が無いまま「縮小版」の判断根拠に使われている
- 影響：次に解釈を有効化する検討（記録の「今後への申し送り」）で、(3) が改善したかを判定できない。§8.2「完了の扱い」の「規則だけで満たせるのは (1)(2)(5)(7)」は (3) を解釈頼みとしているので、その到達基準が無いと v0.2→v0.3 の判断（§2.4）でも同じ問題が残る
- 修正方針：設計書 §8.2 の (3) に、今回の実測を根拠にした閾値を書く（例：「答え損ね系 3 本で各 3 回中 2 回以上通過」。今回は 2/3・2/3・0/3 で未達＝△ではなく×、と判定が変わる可能性もあるので、値は人間が決める）。記録側は「閾値未設定のため △」と明記するか、決めた閾値で判定し直す

### Nit

- `docs/design/ai_vtuber_architecture.md` §8.2「対象外と既知の制限」の末尾「解釈の LLM 呼び出しが増えるため待ち時間が伸びうる（**測ってから常時有効にするか決める**）」は、PR5 で決めた後も未来形のまま。「測った結果、既定無効（result/v0.2.md）」に直す
- `steps.py` の `accept_limit` ガードは「抽出結果の揺れ」と「シナリオの書き間違い」を同じ `is_spec_error` に入れる。コメントとフィールド定義はその旨を書いているので現状で足りるが、レポートの「実行できなかった試行」から外れた失敗が何件あったかは md に集計されない（各 reflect の行を数えるしかない）。次に評価器を触るときに「前提を満たせなかった手順」の件数を見出しに足すと、記録の「ガードが立った回」を後から数えやすい

## 問題が無いことを確認した範囲

- `_find_memory`：`ORDER BY id` で決定的、2 件以上は `AmbiguousMemoryMatchError`。`runner.py` はこれを `action_checks` の失敗として記録し、後続の手順を実行し続ける（テストで `say` が続行されることまで固定）
- `accept_limit`：件数がちょうど一致するときだけ採用、不一致は何も採用せず `is_spec_error` で失敗。候補の順序に依存しない（順序を入れ替えても結果が変わらない構造）。`accept` 無しの `accept_limit` は読込時に拒否
- `failed_to_run`：ターンの失敗と、`is_spec_error` でない振り返りの失敗だけを数える。記録の「実行できなかった試行 0」は全ログで再計算が一致
- `report.py`：`conversation_state_llm` の有無を md・json 双方に、`machine_passed／machine_total` を json に持ち、md の見出しと一致することをテストで固定。失敗時も候補の中身を md に残す
- `measure_turn_latency.py`：無効→有効の順で 5 ターン固定会話を流し、`logs/evals/<時刻>-turn-latency.txt` へ人格の版・モデル・timeout を含めて書き出す。順序によるウォームアップの偏りは記録が自ら注記し、判断の方向（有効側が遅い）を変えない
- 記録の「同時実行で数字が下がる」注記：`131950`／`131958` の同時刻開始がログで確認でき、単独再実行 `141036` で 49/72 に上がったという記述と整合
- README・設計書・issue の相互リンクは `docs/review/` 移動後の深さで解決する。ISSUE-035 は完了欄へ移動し、§8.1 の既知の失敗一覧からも消えている（`ISSUE-036` は残る）
- 秘密情報：レポート・測定スクリプトの出力にホスト名以外の接続情報は無い

---

## PR5 再評価：対応後の確認と承認判断

レビュー日：2026-09-14

**結論：Warning 1 件と Nit 1 件の対応を確認した。新たな不整合は無い。PR5 を承認してよいと判断する。** v0.2 は記録どおり「縮小版」として完了と扱う。

### 対象

[Claude の対応記録](v0.2_response.md)「PR5」節と、作業ツリー上の修正（`docs/design/ai_vtuber_architecture.md` §8.2、`docs/result/v0.2.md`）。コードの変更は無い（`backend/` の diff は前回レビュー時と同じ 5 ファイル）。ソースコード・通常利用の DB は変更していない。

実行：一時 DB で `pytest -p no:cacheprovider` — **282 件成功**。`ruff` — 成功。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | 成功条件 (3) の閾値が測定後も未設定 | 設計書 §8.2 の (3) に「対応する各シナリオで 3 回中 2 回以上（過半数）は起きない（PR5 で実測して決めた閾値）」を明記（`ai_vtuber_architecture.md:524`）。記録の (3) 行を「9・10 は閾値を満たすが 11 は満たさない。△（3 本中 2 本が閾値達成、1 本は未達）」に書き直し（`result/v0.2.md:135`） | **解消**。閾値は `141036` の実測（2/3・2/3・0/3）に対して検証可能で、記録の既定値の表（「(3)(4) が △〜×」）・縮小版の結論（「(3)(4)(6) は解釈があっても精度が足りない」）と矛盾しない |
| Nit 1 | 「測ってから常時有効にするか決める」が未来形のまま | 「測った結果、既定は無効のまま。[result/v0.2.md]」に修正（`:523`） | **解消** |
| Nit 2 | `is_spec_error` の件数が md の見出しに無い | 現状維持（json から数えられる。次に評価器を触るときに） | 妥当。前回も「現状で足りる」としていた |

### 残る留意点（承認を妨げない）

- 閾値は「各シナリオ」単位で書かれており、**(3) 全体を満たすには何本のシナリオが閾値を超えればよいか**は設計書に無い。記録は 3 本中 2 本で △ としているが、「全本必須」と読めば × になる。次に解釈の有効化を検討するとき、△／○ の境界を設計書側に一言足す（例：「3 本すべて」または「過半数のシナリオ」）
