# v0.2 PR1 コードレビュー：会話状態の器と規則

レビュー日：2026-09-13

対象：コミット `7ed3069`（v0.2 PR1「会話状態の器と規則」）。`docs/plan/v0.2.md` の PR1 の範囲。レビューは `.claude/agents/reviewer.md` の手順（diff を先に読み、設計書は後）に従った。ソースコード・通常利用の DB は変更していない。再現に使った一時ファイルは削除済み。

**結論：Critical は無い。マージを止める不備は見つからなかった。Warning 4 件（改行を含む発言での延期・終了の誤検出／取りこぼし、「そうですか」の質問誤検出、プロンプト節に入る質問本文が無制限、target 絞り込みの片側がテストで検出できない）は PR2 以降の着手前に直すことを勧める。**

## 対象と確認方法

- `backend/app/agent/conversation_state.py`（新規 588 行）、`conversation.py`、`prompt.py`、`models.py`、Alembic migration `836089fcf4d9`、`tests/test_conversation_state.py`
- 実行：一時 DB を指定して `.venv/bin/python -m pytest -p no:cacheprovider` — **228 件成功**（既存の migration テストを含む）。`.venv/bin/ruff check app tests --no-cache` — 成功
- 呼び出し元の確認：`build_messages`／`build_system_prompt` の呼び出しは `conversation.py` のみ。`get_open_states` は `conversation.py` と新テストのみ。`ConversationState` を参照する既存コードは無い。会話・発言を削除する API は存在しないため、`responded_message_id` 等の `ondelete` 無し FK が実運用で衝突する経路は現時点では無い
- 再現：規則の純粋関数に境界値を通すスクリプトと、`get_open_states` の target フィルタを monkeypatch で無効化した状態で既存テストを走らせる変異検証を、一時ファイルで実行した

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：会話ごとの状態テーブルを足し、相手→YUI・YUI→相手の質問、延期、終了、提示済みを正規表現で検出して行を作り、「応答があった」（`responded_message_id`）と「解決した」（`status`）を分けて持つ。開いている状態を `# この会話で` 節として system prompt に差し込む。discrepancy／correction の重複制約は PR3 のために先行実装する。

設計書（`docs/plan/v0.2.md` §3・§4・§11）と照合した結果、意図のズレは無い。以下は設計と実装の差だが、いずれも不具合ではない：

- §4 は「取得（履歴・記憶・状態）の**後**に規則を適用」とあるが、実装は相手の発言保存直後（`_recent_history` の前）に適用している（`conversation.py:99`）。規則は履歴・記憶を参照しないので結果は同じ
- §3 の `question_to_yui` の語彙は「？／?／〜ますか／〜でしょうか／〜教えて／〜してほしい／〜してくれる」だが、実装は「ですか」「かしら」「お願い」「てもらえる」も加えている。後述の Warning 2 の原因になっている
- §5 の `/end` での `expired` 化は `expire_open_states` が用意されているだけで未配線。PR4 の範囲で、`chat.py:_ensure_open` が終了済みの会話への発言を拒むため、開いたままでも副作用は無い

## 指摘事項

### Warning 1. 改行を文の区切りとして扱わないため、延期の話題が前の行を飲み込み、終了の合図を取りこぼす

- 場所：`backend/app/agent/conversation_state.py:91`（`_SENTENCE_SPLIT_RE`）、`:94`（`_last_sentence`）、`:123`（`detect_deferral_topic`）、`:135`（`detect_closing`）
- 確認区分：**再現済み**
- 発生条件：発言に改行が含まれる。画面の入力欄は `textarea` で、Enter は改行、⌘/Ctrl+Enter が送信（`frontend/src/components/ChatPanel.tsx:248-259`）なので、通常操作で複数行の発言が届く
- 再現（一時スクリプト）：

  ```
  deferral: '疲れた\n映画'      # detect_deferral_topic("今日は疲れた\n映画の話はまた今度")
  closing : False              # detect_closing("今日は楽しかった\nそろそろ寝るね")
  closing (。区切り): True      # detect_closing("今日は楽しかった。そろそろ寝るね")
  ```

- 影響：`deferral.content` が「疲れた\n映画」で保存され、`# この会話で` 節の1行に改行が混ざって「- 相手が後にすると言った話題：疲れた／映画（自分からは持ち出さない…）」と2行に割れる。話題の語も誤る。終了は逆に検出されず、「終わりにしようとしている」の指示が出ない。どちらも PR3 の解釈が無効な構成（設計 §8 の既定）では取り消されずに会話の終わりまで残る。コミットメッセージで直したとされる「前の文を topic が飲み込む」問題が、区切りが改行のときにだけ再発している
- 修正方針：`_SENTENCE_SPLIT_RE` を `(?<=[。！!？?\n])` のように改行（と疑問符）でも区切る。あわせて `_DEFERRAL_RE` の topic の文字クラスから `\n` を除外する。テストに「改行で区切られた2行」の例を足す

### Warning 2. 「そうですか」「そうなんですか」が相手の質問として登録される

- 場所：`backend/app/agent/conversation_state.py:41-43`（`_QUESTION_ENDING_RE` の「ですか」）
- 確認区分：**再現済み**
- 発生条件：相手が相づちの「そうですか。」「なるほど、そうですか！」「へえ、そうなんですか」を送る
- 再現：

  ```
  'そうですか。' True
  'そうですか' True
  'なるほど、そうですか！' True
  'へえ、そうなんですか' True
  ```

- 影響：`question_to_yui` が作られ、その返答が来るまでの節に「まだ答えていない相手の質問：『そうですか。』」が出る。返答後は `responded` が入って節から消えるが、行は open のまま残り PR3 の判定対象になる（解釈を無効にした構成では永続）。設計 §3 の語彙は「ですか」を含まず、実装が独自に広げた分で起きている。`is_acknowledgement_only` は「そうですか」を相づちに含めていないので抑止も効かない
- 修正方針：「ですか」を語彙から外し設計の語彙に揃えるか、「そう(なん)?ですか」を除外する。`_ACKNOWLEDGEMENT_WORDS` に「そうですか」「そうなんですか」を足し、`detect_request_question` の前に `is_acknowledgement_only` で弾く

### Warning 3. 質問本文が無制限に system prompt へ入る（`presented` だけ 200 文字で切っている）

- 場所：`backend/app/agent/conversation_state.py:414`（`question_to_yui` の `content=text`）、`:480`（`question_to_partner` の `content=text`）、`:533`・`:544`・`:556`（節への出力）
- 確認区分：**再現済み**
- 発生条件：`ChatRequest.text` は最大 4000 文字（`schemas.py:139`）。末尾が「？」で終わる長文や、YUI の長い返答が「〜ますか？」で終わる場合
- 再現：

  ```
  section length: 4024           # 4000 文字の質問1件で節が 4024 文字
  partner-question section length: 5043   # YUI の質問 800 文字 × 6 件（待ち1＋応答済み上限5）
  ```

- 影響：`presented` は設計どおり 200 文字で切る一方、質問は全文を保持・出力するため、`# この会話で` 節だけで数千文字になり、ローカルモデルのコンテキストを圧迫する。コミットメッセージで「節が上限なく肥大する問題」を直したとあるが、件数の上限だけで1件あたりの長さは無制限
- 修正方針：`question_*` の `content` も保存時に上限（例：200 文字）で切るか、節に出すときに切る。`content` は設計 §3 で「開発者向けの本文」なので、保存は全文・出力だけ切る形でもよい

### Warning 4. `question_to_yui` の target 絞り込み（YUI の返答側）を検出できるテストが無い

- 場所：`backend/tests/test_conversation_state.py:257-293`（`test_state_targeted_at_one_speaker_is_not_affected_by_another`）、対象コード `backend/app/agent/conversation_state.py:455-506`（`apply_character_message_rules`）
- 確認区分：**変異検証で再現済み**。`get_open_states` の `target_speaker_id` フィルタを monkeypatch で無効化して既存テストを実行した結果：

  ```
  test_state_targeted_at_one_speaker_is_not_affected_by_another  passed   # 検出できない
  test_yuis_question_to_a_is_not_answered_by_b                   FAILED   # 相手の発言側は検出できる
  （追加した「A の質問→生成失敗→B のターン」のケース）              FAILED   # assert 3 is None
  ```

- 発生条件：A の質問は A 自身のターンの返答で `responded` が埋まるため、B のターンで上書きされない理由は「target が違う」ではなく「`mark_first_response` が最初の1件しか書かない」こと。このテストは絞り込みが無くても通る。絞り込みが実際に効くのは「A の質問が `responded` 空のまま残る」場合で、それは A のターンの生成が失敗（503）したときに起きる（相手の発言由来の状態は commit 済みで残る。設計 §8「生成の失敗」）
- 影響：`apply_character_message_rules` の `get_open_states(..., target_speaker_id=target_speaker_id)` を落とす退行が入っても既存テストは通る。設計 §3 の「`target_speaker_id` と一致しない相手の発言では、どの遷移も起こさない」の片側が固定されていない
- 修正方針：上記の変異で落ちたケース（A の質問 → `fake_llm.chat` を `LLMError` にして 503 → B のターン → A 宛 `question_to_yui` の `responded_message_id is None`）をテストに足す。既存テストの docstring「B のターン（B の発言・B 向けの返答）では変わらない」は実態と合わないので、`mark_first_response` の上書き防止の検証として書き直す

### Nit

- `_SENTENCE_SPLIT_RE` が「？」「?」で区切らないため、`detect_closing("元気？そろそろ寝るね")` は `False`（再現済み）。Warning 1 の修正に含める
- `create_or_supersede_correction`（`conversation_state.py:343`）で `target_speaker_id=None` を渡すと、`get_open_state_for_ref` が target を絞らず別の相手の訂正を `superseded` にする。PR1 の呼び出し元は無く PR3 の呼び方次第だが、`None` を「全員」の意味で受け付けるのか、禁止するのかを決めておく
- 生成失敗（503）後に相手が同じ質問を再送すると `question_to_yui` が2件になり、節に同じ質問が2行並ぶ。設計 §4 末尾で「重複しうる」と受容済みなので指摘ではないが、節では `content` の同一なものをまとめてよい（`deferral` と同じ扱い）

## 問題が無いことを確認した範囲

- 「応答があった」と「解決した」の分離：`mark_first_response` は `responded_message_id` だけを書き、`status` に触らない。聞き返しで上書きしないことをテストで確認。`resolve_state`／`withdraw_state` を `respond()` から呼ぶ経路は無い
- トランザクション境界：相手の発言由来の状態は `commit`（`conversation.py:144`）で確定し、返答由来の状態は返答の `Message` と同じトランザクション。生成失敗時に相手の発言と質問だけが残る動きは設計 §8 どおり
- 会話ロック：`respond()` 全体が `conversation_locks.hold` の中で、規則の適用と `get_open_states` は同じ会話で重ならない。規則は正規表現のみでイベントループを塞がない
- `presented` の窓：`window_message_ids` は直近 12 件と今の発言。窓の外にあるものだけを直近 5 件出す（設計 §3）
- discrepancy／correction の重複制約：`misdetected` の除外、`asked` 入りの行の扱い、`superseded` の連鎖はテストで固定されている
- migration と model の列・索引は一致。`server_default="0"` は SQLite で問題無い。既存の migration テストが head まで通る
- 秘密情報・権限：新規 API は無く、露出する情報は増えていない

---

## 再評価：対応後の確認と PR1 の承認判断

レビュー日：2026-09-13

**結論：Warning 4 件と Nit 1 件の対応を確認した。新たな不具合・退行は見つからなかった。PR1 を承認してよいと判断する。**

### 対象

[Claude の対応記録](../claude/v0.2_response.md)（未追跡ファイル）と、対応コミット `9f50b9f`（`conversation_state.py`、`test_conversation_state.py`）。ソースコード・通常利用の DB は変更していない。再現に使った一時ファイルは削除済み。

実行：一時 DB で `.venv/bin/python -m pytest -p no:cacheprovider` — **231 件成功**。`ruff check app tests --no-cache` — 成功。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | 改行を文の区切りにしない | `_SENTENCE_SPLIT_RE` に `？?\n` を追加、`_DEFERRAL_RE` の topic から `\n` を除外 | **解消**。`"今日は疲れた\n映画の話はまた今度"` → `'映画'`、`"今日は楽しかった\nそろそろ寝るね"` → `True`。Nit の `"元気？そろそろ寝るね"` → `True` も同時に解消。`\r\n`・末尾改行・空行連続・「？ 」（後に空白）でも正しく判定した |
| W2 | 「そうですか」の質問誤検出 | `_ACKNOWLEDGEMENT_QUESTION_RE` で `そう(なん)?ですか` の文末を `detect_question` の入口で除外 | **解消**。4 例すべて `False`。退行を疑った「山は好きなんですか」「本当にそうですか？」「そうですか？」は引き続き `True`（疑問符付きは質問として扱う。妥当） |
| W3 | 質問本文が無制限に節へ入る | `CONTENT_LIMIT = 200` を新設し、`question_to_yui`／`question_to_partner`／`closing`／`presented` の 4 箇所で統一 | **解消**。4000 文字の質問で節が 226 文字。テスト `test_long_question_is_truncated_in_content_and_prompt` は API 経由で保存後の長さを見ており、実装の写しになっていない |
| W4 | YUI 返答側の target 絞り込みをテストが検出できない | `test_yuis_reply_failure_then_b_does_not_answer_a_question_to_yui` を追加（コードは変更なし） | **解消**。前回と同じ変異（`get_open_states` の target フィルタを monkeypatch で無効化）で、新テストが `assert 3 is None` で**失敗する**ことを確認。既存の `test_yuis_question_to_a_is_not_answered_by_b` と合わせて、相手の発言側・YUI の返答側の両方が固定された |
| Nit | 節での同一質問の重複 | 未応答・待ち・応答済みの 3 区分で `content` の重複を除外 | **解消**。応答済みは上限 5 件に切ってから重複を除く順序で、新しいものを優先する意図と合っている |

対応記録の「修正不要と判断した点」（規則の適用位置、`/end` の `expired` 化未配線、`create_or_supersede_correction` の `target=None` を PR3 で決める）は、前回レビューの判断と同じで異論は無い。

### 対応で入りうる退行として確認した範囲

- 「？」を文の区切りに加えたことで、`detect_deferral_topic`・`detect_closing` の判定対象が疑問文の後ろに絞られる。延期の定型句は疑問符で終わらないため、既存の検出例に影響は無い（テスト全件成功）
- `detect_question` は `_last_sentence` を通さず全文の末尾を見るため、区切りの変更の影響を受けない
- `content` の 200 文字切り詰めは保存時に行うため、`build_conversation_state_section` の重複排除（`content` の一致）は切り詰め後の本文で比較される。同じ長文の再送は同一視される（意図どおり）
- 対応記録に「`git checkout --` で未コミットの修正 1〜3 を一度失った」とあるが、`9f50b9f` の diff に 3 件とも含まれていることを確認した

### 残る留意点（承認を妨げない）

- `create_or_supersede_correction` に `target_speaker_id=None` を渡した場合の意味は未決定。PR3 の着手時に決める（対応記録どおり）
- 「そうですか」は完全一致の相づち辞書 `_ACKNOWLEDGEMENT_WORDS` には入っておらず、`is_acknowledgement_only` は引き続き `False` を返す。PR3 で `confirmed` の抑止に使うときは `_ACKNOWLEDGEMENT_QUESTION_RE` も参照するか、辞書に足す必要がある

---

# v0.2 PR2 コードレビュー：出力検査と終了設計の最小形

レビュー日：2026-09-13

対象：`056ae8a` 上の未コミットの作業ツリー（`docs/plan/v0.2.md` §11 の PR2）。`conversation.py`、`conversation_state.py`、`evaluation/runner.py`、`evaluation/scenario.py`、`tests/test_conversation_state.py`、`tests/test_evaluation.py`、新規 `evals/scenarios/conversation_state.toml`。手順は前回と同じ（diff を先に読み、設計書は後）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

**結論：Critical は無い。Warning 2 件（「〜前に」＋依頼形の closing が習慣・一般論の質問を終了の合図にする／評価器の `expect_no_new_question` の「対象外」分岐がターン後の状態では到達できず、実装が意図的に通した返答を失敗と判定する）。どちらも再現済み。2 件を直せば PR2 を承認できる。**

## 対象と確認方法

- 実行：一時 DB で `pytest -p no:cacheprovider` — **239 件成功**（`test_bundled_scenarios_load` が新シナリオ TOML の読込を含む）。`ruff check app tests --no-cache` — 成功
- 呼び出し元：`_check_turn` は `runner.py` と `test_evaluation.py` のみ（引数追加は両方に反映済み）。`RunRecord.options` は `RunRecordOut`（`schemas.py:85`）経由で `ChatPanel.tsx:417` が `JSON.stringify` で表示している。JSON 列なので `checks` の追加で型は壊れない
- Ollama クライアント（`ollama_client.py:69`）は `messages` をそのまま渡すため、再生成時に末尾へ足す 2 つ目の system メッセージは問題無い。`FakeLLM` も `messages[0]` しか見ない
- `LLMResponse` は frozen でない dataclass なので `response.text = CLOSING_FALLBACK_SENTENCE` は成立する

## diff から読み取った意図と、設計書とのズレ

設計書を読む前の読み：生成後に「出力検査」を足す。closing が開いていて返答に質問があれば追加の system 指示で 1 回再生成し、それでも質問が残れば、相手の未応答の質問が無い場合だけ固定文に差し替える。延期の話題の語の一致と discrepancy 開時の断定らしさは記録のみ。結果を `RunRecord.options["checks"]` へ。「寝る前に…教えて」の形の closing 検出を足し、評価器に `expect_no_new_question`／`expect_conversation_states` を追加、シナリオ TOML を新設する。

設計 §4 手順 6・§7・§9・§11 と照合し、意図のズレは無い。§11 が PR2 に求めるシナリオ 1〜4・6・8・19 は TOML に揃っている（8 と 19 は対になる 2 本ずつ）。固定文は §9 どおり定数で設定にしていない。再生成に失敗したとき 1 回目の返答を使う扱いは §8 に明記が無いが、妥当。

## 指摘事項

### Warning 1. 「〜前に」＋依頼形の closing 検出が、睡眠・帰宅の習慣や一般論についての質問を終了の合図にする

- 場所：`backend/app/agent/conversation_state.py:106-108`（`_CLOSING_BEFORE_RE`）、`:181`（`detect_closing` の第 2 条件）
- 確認区分：**再現済み**
- 発生条件：最後の文に「寝る／帰る／落ちる／おやすみ＋前に」があり、文末が質問・依頼の形。終了の意図が無い質問がそのまま当たる：

  ```
  True  '寝る前にストレッチするといいって本当？'
  True  '寝る前に飲むといい薬を教えて'
  True  '成績が落ちる前に対策を教えてほしい'
  True  '帰る前にやることリストを教えてくれる？'
  True  'おやすみ前に読む絵本、おすすめある？'
  False '帰る前に一つだけ。明日の集合時間を教えて'   # 逆に、文を分けた本物のシナリオ 4 は拾えない
  ```

- 影響：closing は解釈（PR3、既定で無効）が `reopened` にするまで `/end` まで open のまま残る。以後のすべてのターンで「終わりにしようとしている」の指示が入り、YUI の返答に質問があるたびに再生成が走り（応答時間が倍）、相手の未応答の質問が無いターンでは固定文「今日はここまでにいたしましょう…」に差し替わる。会話の途中で「寝る前のストレッチ」を尋ねただけで、会話の残りが締めの文で埋まりうる。前回の対応で「寝る前にストレッチしてる」（依頼無し）を弾いたが、依頼形を要求しても同じ穴が残っている。上の 6 例目のように、シナリオ 4 の自然な言い方（依頼を別の文にする）は逆に取りこぼす
- 修正方針：「〜前に」の形は「依頼の対象が終了の直前に済ませたいこと」（「〜だけ」「一つだけ」「最後に」）を伴うときに限る、または `_CLOSING_BEFORE_RE` の直後を「明日の／今日の／最後に／一つ／だけ」等に絞る。シナリオ 4 の文言（「寝る前に明日の集合時間だけ教えて」）に「だけ」があるので、`(前に).{0,20}?だけ` を要求するだけで上の 5 例は全部外れる。加えて、複数文の形（「帰る前に一つだけ。〜教えて」）を拾うなら、直前の文が「〜前に(一つ|ひとつ)?だけ」で終わる場合を許す。いずれの場合も `test_detect_closing_matches_before_form_with_a_trailing_request` に上の誤検出側 5 例を足す

### Warning 2. 評価器の `expect_no_new_question` は「相手の質問が残っているため対象外」に到達できず、実装が意図的に通した返答を失敗と判定する

- 場所：`backend/app/evaluation/runner.py:360-365`（`_check_turn`）、`:596`（`_conversation_states_for_speaker` の呼び出し時点）、`backend/app/agent/conversation_state.py:572`（`has_unanswered_question_to_yui`）、`backend/evals/scenarios/conversation_state.toml:46-47`（シナリオ 4）
- 確認区分：**再現済み**。シナリオ 4 の発言を API 経由で流し、`FakeLLM` に 2 回とも質問付きの返答をさせた：

  ```
  IMPL:   {'closing_open': True, 'closing_new_question_detected': True,
           'closing_fell_back_to_template': False, 'closing_unresolved_due_to_open_question': True}
  AFTER-TURN responded: [2]          # question_to_yui.responded_message_id は返答で埋まる
  RUNNER has_unanswered: False       # 評価器がターン後に見る状態では「未応答の質問」は無い
  RUNNER check: [(False, '返答に新しい質問が残っている')]
  ```

- 発生条件：実装は**生成前**の状態で `has_unanswered_question_to_yui` を判定する（`conversation.py:176-198`。この時点では `responded_message_id` が空）。評価器は**ターン完了後**に同じ関数を呼ぶ（`runner.py:596`）が、`apply_character_message_rules` がその返答自身を `responded_message_id` に入れた後なので、同じターンの `question_to_yui` は常に「応答済み」に見える。つまり `runner.py:365` の「対象外」分岐は、その say で相手が質問したケースでは決して真にならない。真になるのは、以前のターンの質問が生成失敗で `responded` 空のまま残っている場合だけ
- 影響：シナリオ 4 は `expect_no_new_question = true` を持つ。実装は「相手の依頼が残っているターンでは定型へ落とさず記録のみ」（設計 §4 手順 6）として質問付きの返答を通すが、評価器はそれを機械判定で失敗にする。設計 §7 の「`question_to_yui` が `open` の場合、その答えは対象外」が評価器では実現されておらず、コメント「実装と同じ条件で見る」は成立していない。PR2 の受け入れ（シナリオ 1〜4・6・8・19 を 3 回）でシナリオ 4 が返答の言い回し次第で落ちる
- 付随：`has_unanswered_question_to_yui` は docstring に「`open` かつ `responded` が空」とあるが `status` を見ていない。実装側は `get_open_states` の結果を渡すので問題無いが、評価器は全ステータスの行を渡す（`_conversation_states_for_speaker`）ため、PR3 で `withdrawn`／`expired` の行ができると「未応答」に数える
- 修正方針：評価器は実装の判定結果をそのまま使う——`result.run.options["checks"]["closing_unresolved_due_to_open_question"]` が真なら「対象外」、そうでなければ `detect_question(reply)` を見る。または「その say 自身が依頼・質問の形か（`detect_request_question(step.text)`）」で対象外を判定する。`has_unanswered_question_to_yui` には `status == open` の条件を足す。再現に使った「2 回とも質問付きの返答でシナリオ 4 の文を流す」ケースを `test_evaluation.py` に足す

### Nit

- `conversation.py:258`：再生成したとき `latency_ms` は 2 回目の呼び出しだけになり、1 回目の時間が実行記録から消える。§4 末尾の「ターン全体の時間を PR5 で測る」に使うなら、両方の合計か `checks` に 1 回目の値を残す
- `ChatPanel.tsx:417` は `options` を丸ごと `JSON.stringify` して「設定」として表示するため、`checks` の 10 項目が生成設定と混ざって出る。PR4 の画面接続で分ける
- `looks_assertive`（`conversation_state.py:604`）は「確認」を含めば非断定とみなすため、「確認しました、日曜です。」が非断定になる（再現済み）。記録のみなので影響は無いが、ヒューリスティックの弱さは `checks` を読む側が知っている必要がある

## 問題が無いことを確認した範囲

- 再生成は 1 回で止まり（`len(fake_llm.calls) == 2`）、再生成の失敗で 503 にせず 1 回目の返答を使う。固定文に差し替えた返答にも `apply_character_message_rules` が通り、`presented` が固定文で残る（質問形でないので `question_to_partner` は作られない）
- `closing_open` の判定は `commit` 前に取得した `conversation_states` を使うため、このターンの発言で作られた closing を含む。target で絞り込み済み
- 定型への差し替え条件は `responded_message_id is None` で見ており、前回の memory の指摘（`status` だけで見ると一度質問された会話で経路が死ぬ）は反映されている。テスト `test_closing_falls_back_even_if_an_earlier_question_was_already_answered` で固定されている
- `expect_conversation_states` の空リスト（行が無いこと）と条件付きの一致は、`contains` の AND・`asked`／`responded` の有無の比較を含めて実装が §7 の定義どおり。kind／status／withdraw_reason の値は読込時に検証される
- deferral・discrepancy の検査は closing の結果と独立に走り、行動を変えない
- `checks` の辞書は `response.options` のコピーに足しており、生成設定を上書きしない

---

## PR2 再評価：対応後の確認と承認判断

レビュー日：2026-09-13

**結論：Warning 2 件の対応を確認した。新たな不具合・退行は見つからなかった。PR2 を承認してよいと判断する。** 下記の Nit 2 件（評価器側の回帰テストが無い、対応記録の相対リンク切れ）は承認を妨げない。

### 対象

[Claude の対応記録](v0.2_response.md)「PR2」節と、作業ツリー上の修正（`conversation_state.py`、`runner.py`、`test_conversation_state.py`、`test_evaluation.py`）。ソースコード・通常利用の DB は変更していない。再現用の一時ファイルは削除済み。

実行：一時 DB で `pytest -p no:cacheprovider` — **239 件成功**。`ruff check app tests --no-cache` — 成功。

### 指摘への対応評価

| # | 指摘 | 対応 | 確認結果 |
| --- | --- | --- | --- |
| W1 | 「〜前に」＋依頼形の closing が習慣・一般論の質問を拾う | `_CLOSING_BEFORE_RE` に「前に」の後 20 文字以内の「だけ／一つ／ひとつ／最後に」を要求（`conversation_state.py:107-115`） | **解消**。前回の誤検出 5 例はすべて `False`。正例「寝る前に明日の集合時間だけ教えて」「寝る前に一つだけ聞いてもいい？」「帰る前に最後に一つ教えて」は `True`。5 例は `test_detect_closing_matches_before_form_with_a_trailing_request` に追加されている |
| W2 | 評価器の「対象外」分岐がターン後の状態では到達不能 | `_check_turn` に `run_checks` を追加し、実装の `checks["closing_unresolved_due_to_open_question"]` をそのまま使う（`runner.py:266-273, 361-384, 615`）。`has_unanswered_question_to_yui` に `status == open` を追加 | **解消**。前回の再現手順（シナリオ 4 の文を FakeLLM の 2 回とも質問付き返答で流す）で `(True, '相手の質問が残っているため対象外（人手判定と併用）')` になった。呼び出し側は `result.run.options.get("checks", {})` を渡しており、`checks` が無い旧い実行記録でも `KeyError` にならない |

対応記録の「W1 は依頼形の要求だけでは弱すぎた」という原因分析は、前回レビューの見立てと一致する。

### 対応で入りうる退行として確認した範囲

- `_CLOSING_BEFORE_RE` の絞り込みで `_CLOSING_PATTERNS`（最後の文全体の一致）は変わっておらず、「そろそろ寝るね」等の定型は引き続き `True`
- 「帰る前に一つだけ。明日の集合時間を教えて」（文を分けた言い換え）は前回同様 `False` のまま。前回レビューで「拾うなら」と書いた任意の拡張で、対応記録は扱っていない。シナリオ 4 の文言は 1 文なので PR2 の受け入れには影響しない。設計 §7 の語彙を広げるかは PR5 の測定で判断すればよい
- 「寝る前に毎日飲む薬だけ教えて」は `True`（「だけ」が限定の意味で使われる習慣の質問）。残る穴だが、前回の 5 例より出現頻度は低く、設計が規則の限界として受容している範囲と判断する

### Nit（承認を妨げない）

- **評価器の回帰テストが無い。** W2 の対応記録は「再現スクリプトで確認」とし、`test_evaluation.py` には既存呼び出しの引数追加しかない（新規テスト関数 0 件）。`expect_no_new_question` の 2 分岐（`run_checks` が真なら対象外、偽なら `detect_question`）を固定するテストが無いため、`run_checks` の鍵名を変えると評価器だけが黙って壊れる。前回の再現手順をそのままテストにすることを勧める
- **対応記録の相対リンクが切れている。** `docs/review/claude/`・`codex/` を `docs/review/` 直下へ移した作業ツリーでは、`v0.2_response.md` 内の `../codex/v0.2_review.md` と `../../../backend/...` の 8 箇所がすべて解決しない（`docs/review/` からは `../../backend/...`、`v0.2_review.md`）。PR1 節の 4 箇所も同じ。コミット前に直す
