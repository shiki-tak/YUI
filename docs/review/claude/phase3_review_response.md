# フェーズ3 Pre review への対応

対象：[docs/review/codex/phase3_review.md](../codex/phase3_review.md)（ISSUE-014：固定人格の版管理）

対応日：2026-09-07

**結論：指摘2件はいずれも実際の不具合でした。2件とも修正し、再発を検知するテストを追加しました。**

どちらも「コード確認のみ」として報告されたものですが、こちらで再現を確認してから直しています。修正前に追加したテストが実際に失敗することも確認しました。

## 指摘1：人格の読込に失敗すると、振り返り開始済みの状態だけが残る

**妥当。修正した。**

### 確認したこと

`POST /conversations/{id}/end` は、振り返りの開始権を UPDATE で確定して `commit()` した後、`try:` の中で `extract_candidates(..., character_name=load_persona().name)` を呼んでいました。引数の評価は `try:` の内側ですが、`except (LLMError, ReflectionParseError)` は `PersonaError` を捕まえません。そのため `_release_reflection()` が実行されず、開始権を握ったまま 500 で終わります。

指摘のとおり、`reflection_stale_seconds`（既定180秒）を過ぎるまで再試行が409になります。人格ファイルを直しても待たされます。

これは今回の変更で新しく入った経路です。以前の `BASE_PERSONA.name` は定数で、失敗しませんでした。ファイルを読む処理を、開始権を取った後の区間に置いてしまったのが原因です。

### 直し方

**開始権を取る前に人格を解決する**ようにしました（[conversations.py:130](../../../backend/app/api/conversations.py#L130)）。失敗しても、DBには何も残りません。`try` の中に人格の読み込みを残して `except` を増やす方法もありますが、開始権を取った後に失敗しうる処理をこれ以上増やさない形を選びました。

あわせて、指摘にあった「起動時検証が無い」点も直しています。

- `app.main` に lifespan を足し、**起動時に既定の人格を読んで確かめる**ようにしました。設定の誤りが、最初のリクエストではなく起動で分かります。
- `PersonaError` の例外ハンドラを足し、起動後に版のファイルが壊れた場合などを **503 と原因の分かる本文**で返すようにしました（500 で潰さない）。
- `PersonaError` の説明文が「起動時に落とす」と実際の挙動から外れていたので、実装に合わせて直しました。

### 確認

- `tests/test_persona_versions.py::test_reflection_claim_is_released_when_the_persona_cannot_be_read` を追加。人格を読めない状態で終了APIを呼び、**503が返ること**と、**人格を直した直後にやり直せる（200）こと**を見ます。修正前は 500 のあと 409 になり、このテストは失敗します。
- 起動時検証は実際に動かして確認しました。人格ディレクトリを空にすると lifespan が `PersonaError` で停止し、正常時は起動します。

## 指摘2：personas 内のシンボリックリンクからディレクトリ外を読める

**妥当。修正した。再現も確認した。**

### 確認したこと

再現しました。`personas/sneaky.toml` を外部ファイルへのシンボリックリンクにすると、

```
一覧に出るか: ['sneaky']
読めたか: 外部のファイル ['personas の外から読み込まれた。']
```

版名の文字種検証（`_VERSION_PATTERN`）は名前しか見ておらず、`Path.is_file()` と `read_text()` はリンク先を辿ります。不変条件4「personas ディレクトリの外を読めない」を満たしていませんでした。

指摘が「遠隔から任意ファイルを読めると断定するものではない」と限定しているとおり、リンクを作る経路はAPIにありません。ローカルにリンクが存在する場合の不備です。

### 直し方

`_persona_file()` を追加し、**解決した先が人格ディレクトリの直下にある場合だけ**パスを返すようにしました（[persona.py](../../../backend/app/persona.py)）。

- 読み込み（`load_persona`）と一覧（`available_versions`）で同じ判定を使います。一覧に出るのに読めない版があると、会話ごとの指定が通ったり通らなかったりするためです。
- ディレクトリ自体を別の場所へ向ける設定（`YUI_PERSONA_DIR`）は、運用者の明示的な指定なので許します。制限するのは、そのディレクトリの中から外へ出ることです。

修正後は一覧に出ず、直接指定すると `PersonaError` になります。

### 確認

- `tests/test_persona_versions.py::test_symlink_out_of_the_persona_directory_is_not_loaded` を追加。同じディレクトリ内の実ファイルは読めることも一緒に見ます。修正前は一覧に `sneaky` が出て失敗します。
- 実際のファイルシステムでも、修正後にリンク先を読まないことを確認しました。

## 「対象外・未確認」への判断

指摘として数えられていない項目です。今回は直していません。理由を残します。

| 項目 | 判断 |
| --- | --- |
| キャッシュのキーに設定が入らない（`load_persona()` と `load_persona(None)` が別キーになる） | 直さない。どちらも同じ定義を返すため、動作の差はありません。稼働中に設定を読み直す仕様は持たせていません。**TOML を編集したら再起動が要る**点は、版を比較する作業で効くため、ISSUE-014 の記録に明記しました |
| `frozen=True` でもリストの中身は変更できる | 直さない。キャッシュを共有するため、変更されれば以後の全リクエストが汚れます。ただし現在の呼び出し元に変更する処理は無く、指摘も未確認としています。タプルへ変えると `PersonaOut` とテストにも波及するため、実害が出るか、3Aで人格を触る処理が増える時点で判断します |
| downgrade で版の情報が失われる | 直さない。列を落とす以上、避けられません。移行の記録に「既存行は NULL のまま」と書いてあり、戻す運用は想定していません |
| 移行の往復（既存データを含む upgrade／downgrade）が未実行 | 未確認のままにします。追加した列は nullable で、既存行に触れません |
| テストの任意順序・並列実行が未確認 | 未確認のままにします。現在 pytest に並列実行の設定はありません |
| 実 Ollama・VOICEVOX・実ブラウザ | 未確認です。今回の変更はすべてモック越しの確認です |

## 変更したファイル

- [backend/app/persona.py](../../../backend/app/persona.py)：`_persona_file()` の追加、一覧と読み込みの条件を統一、`PersonaError` の説明を実装に合わせた
- [backend/app/api/conversations.py](../../../backend/app/api/conversations.py)：振り返りの開始権を取る前に人格を解決
- [backend/app/main.py](../../../backend/app/main.py)：lifespan での起動時検証、`PersonaError` を 503 にするハンドラ
- [backend/tests/test_persona_versions.py](../../../backend/tests/test_persona_versions.py)：再発検知のテスト2件を追加（14件 → 16件）

## 確認結果

```
pytest 101件成功（既存85 + ISSUE-014 の16）
ruff All checks passed
tsc エラーなし / vitest 16件成功
起動時検証：人格ディレクトリが空なら PersonaError で停止、正常時は起動（実行して確認）
シンボリックリンク：修正前に外部ファイルを読めることを再現し、修正後に読めないことを確認
```

実モデル（Ollama）と実ブラウザでの確認は行っていません。

---

# フェーズ3 全体レビューへの対応

対象：[docs/review/codex/phase3_review.md](../codex/phase3_review.md) のフェーズ3全体レビュー（`07c77d9..3277611`）

対応日：2026-09-07

**結論：指摘7件はすべて実際の不具合でした。6件を修正し、1件（指摘2）は一部だけの修正にとどめました。**

（2026-09-08 追記：当初ここに「7件とも修正しました」と書いたが、同じ文書の指摘2で「直していません」と書いており、矛盾していた。再レビューで指摘され、記述を直したうえで指摘2も修正した。下の「再レビューへの対応」を参照。）

すべて「コード確認のみ」として報告されたものですが、こちらで再現を確かめてから直しています。指摘1〜5については、修正だけを戻すと追加したテストが失敗することも確認しました。

**このレビューで重かったのは、3件が「自分で直したはずの問題が、別の経路に残っていた」ことです。**

| 指摘 | 同じ問題を直した課題 | 残っていた経路 |
| --- | --- | --- |
| 1 | [ISSUE-020](../../issues/issues.md)（相手の取り違え） | 通常の返答には発言者名を付けたが、**振り返りの会話ログには付けていなかった** |
| 3 | [ISSUE-010](../../issues/issues.md)（参照範囲が既定で全員） | 記憶の作成APIでは必須にしたが、**状態の作成APIでは既定のまま**だった |
| 2 | [ISSUE-016](../../issues/issues.md)（訂正の波及） | 訂正・削除には付けたが、**復元には付けていなかった** |

## 指摘1［高］複数話者の振り返りで、全員の発言と抽出結果が最後の話者に帰属する

**妥当。修正した。**

`format_transcript` が相手の発言をすべて同じ `partner_name` で並べ、候補の対象者・参照範囲を「最後に話した人」で保存していました。Aさんの好みが、Bさんの情報として保存されます。

- 会話ログに**実際の発言者名**を付けるようにした（通常の返答と同じ形）。
- 候補の帰属を、**根拠になった発言をした人**から決めるようにした。根拠が不明な場合は、相手がひとりの会話ならその人、複数なら空にする。
- 関係性の候補は、**相手がひとりの会話でだけ**作るようにした。「その相手との関係」を最後の話者へ寄せると別人の関係になる。

**副次的な効果**：会話ログに発言者名が入ったことで、伝聞の抽出（`hearsay-is-not-the-partner`）が実モデルで **0/3 → 2/2** に改善しました。「弟のB」が別人だとモデルが読み取れるようになったためと見ています。

## 指摘2［高］訂正・削除が振り返りから作った状態に波及せず、復元でも再評価されない

**妥当。復元を修正した。振り返りから作った状態の根拠は、まだ会話単位のまま。**

- **復元**（`POST /memories/{id}/restore`）でも再評価の印を付けるようにした。訂正に合わせて直した状態が、記憶を戻したあと古いままになっていました。
- 振り返りから作った状態が `basis_memory_ids` を持たない点は、**直していません**。候補の段階では記憶がまだ採用されておらず、記憶IDが存在しないためです。指摘の「同じ根拠会話の状態を保守的に再評価する」案は、会話単位で広く印を付けることになり、関係のない状態まで止めます。**採用の時点で根拠を結び付ける操作**（画面から選ぶ）を先に用意すべきだと考えており、[ISSUE-016](../../issues/issues.md) の残りとして記録しました。

## 指摘3［高］状態の作成APIが根拠記憶の公開範囲を継承しない

**妥当。修正した。**[ISSUE-010](../../issues/issues.md) で記憶に入れた「参照範囲は既定ではなく選んだ結果」を、状態側に適用し忘れていました。

- 状態の作成でも**参照範囲を必須**にした（`visible_to_speaker_id` か `visible_to_all`）。
- `basis_memory_ids` を指定した場合、**根拠の記憶より広い範囲を拒否**する。相手を限定した記憶から、限定しない状態や公開の状態は作れない。
- 相手を決められない会話では、**状態の候補そのものを作らない**。誰に限るか決められないまま作ると別の相手へ渡ります。

## 指摘4［中］3回目の呼び出しが失敗すると、記憶候補だけが確定保存される

**妥当。修正した。**

`_release_reflection` が同じセッションで commit するため、flush 済みの候補が確定していました。再試行すると二重になります。

抽出中は**DBへ書かない**ようにしました。`extract_candidates` は候補を作るだけでセッションへ入れず、状態の抽出も内容だけを返します。**すべて成功してから1つのトランザクションで保存**します。失敗時は rollback してから開始権を解放します。

## 指摘5［中］記憶候補の flush 後にモデルを待ち、別会話の書き込みを止める

**妥当。指摘4と同じ修正で解消した。**

指摘のとおり、通常の返答では「生成前にトランザクションを閉じる」方針を守っているのに、振り返りでは守れていませんでした。上の変更で、モデルを呼んでいる間に書き込みのトランザクションを開かなくなりました。振り返り中に別の記憶を書けることをテストで固定しています。

## 指摘6［中］人手判定が未入力でも通過数に含まれ、振り返りの人手確認項目が出力されない

**妥当。修正した。**

- 機械の判定項目が**1つも無いシナリオを、通過数から外した**。レポートには「人手（未判定）」と出し、まとめにも「読んで判断するまで、通ったとも通らなかったとも言えない」と書く。
- 振り返りの `human_check` をレポートへ出すようにした。
- 候補の表示に、**入手経路・対象者・根拠の発言ID**を含めるようにした。種別と本文だけでは、伝聞かどうかや誰についての記憶かを人が判定できませんでした。

## 指摘7［中］相対日付を発言日ではなく会話終了日から解釈させている

**妥当。修正した。**

会話ログの各発言に**日付**を付け、指示文も「その言い方が出てきた発言の日付を基準に数える」に直しました。振り返りの実行日は「振り返りを行っている日」として別に渡します。

## 「評価用会話で測っていない観点」への対応

8項目の指摘は、いずれも妥当だと考えます。**この回のレビューで最も価値があった部分です。**シナリオを書いた本人が測っている構造の弱さが、具体的に出ています。

今回は指摘1〜7の修正を優先し、シナリオの追加は行っていません。次に扱う順序を [docs/result/phase3.md](../../result/phase3.md) へ記録します。特に次の2つは、完了判定の根拠に直接関わるため先に扱うべきだと考えています。

- **条件1**：記憶が作られるところから再起動後まで通す（いまは記憶を事前投入している）。
- **条件2**：訂正APIを実際に実行してから次の返答を見る（いまは訂正済みの記憶を投入している）。

## 確認結果

```
pytest 170件成功（新規 tests/test_review_phase3.py 6件を含む）
ruff All checks passed ／ tsc エラーなし
実モデル（振り返り11シナリオ×2回）：20/22。伝聞が 0/3 → 2/2 に改善
```

指摘1〜5は、修正だけを戻すと追加したテスト5件が失敗することを確認しました。

実 Ollama・VOICEVOX・実ブラウザでの通し確認は行っていません。

---

# フェーズ3 再レビューへの対応

対象：[docs/review/codex/phase3_review.md](../codex/phase3_review.md) の再レビュー（2026-09-08）

対応日：2026-09-08

**結論：指摘3件はいずれも実際の不具合でした。3件とも修正し、あわせて記録の誤りも直しました。**

## 記録の矛盾について（先に）

指摘のとおりでした。

- 対応記録の冒頭に「7件とも修正しました」と書きながら、同じ文書の指摘2で「直していません」と書いていました。**読む人を誤らせる書き方**で、こちらの誤りです。冒頭を「6件を修正し、1件は一部だけ」に直し、経緯も残しました。
- [ISSUE-016](../../issues/issues.md) を「完了」、[phase3.md](../../result/phase3.md) の完了条件2を「○」のままにしていました。**満たしていない条件を満たしたことにしていた**ので、記録を直しました。

「画面ができるまで待つ」という判断についても、指摘のとおりです。**先送りの理由にはなっても、条件を満たさない状態を残してよい理由にはなりません。**採用時の関連付けと会話単位の再評価で、画面を待たずに不変条件を守れました。

## 指摘1［高］自動抽出した状態に記憶の訂正・削除が届かない

**妥当。修正した。**

3か所を直しました。

- **採用の時点で根拠を結び付ける。**状態を採用するとき、根拠が空なら、その会話から採用された有効な記憶を根拠にする。候補の時点では記憶がまだ採用されておらず、記憶IDを持てないため、採用の時点が最初の機会になる。
- **記憶IDを持たない状態は、会話単位で拾う。**記憶の訂正・削除・復元のとき、`source_conversation_id` が同じ状態にも印を付ける。状態を記憶より先に採用した場合に効く。
- **根拠が分かっている状態は、会話つながりで巻き込まない。**関係のない状態まで止めないため。

会話単位は粗い、というのが当初こちらが先送りした理由でしたが、**印は会話に渡さなくするだけで、確認すれば戻せます。**届かないまま古い内容を話すほうが重い、という判断に改めました。

確認：記憶を先に採用した場合と、状態を先に採用した場合の両方を `tests/test_review_phase3.py` で固定しました。

## 指摘2［中］発言日時のUTC補完がなく、日本時間の発言日を前日として渡す

**妥当。修正した。再現も確認した。**

```
保存された値（UTC）: 2026-09-07 15:30  → 本来の日本時間: 2026-09-08 00:30
いまの実装:          2026-09-07 15:30  ← 日付がずれる
UTC を補うと:        2026-09-08 00:30
```

`config.to_local()` を追加し、timezone を持たない値には UTC を補ってから直すようにしました。**同じ書き方が3か所ありました**（振り返りの会話ログ、プロンプトの記憶表示、評価レポート）。プロンプトの記憶表示は指摘に含まれていませんでしたが、同じ誤りなので直しています。

## 指摘3［中］モデル失敗でチェック未実行のシナリオが人手専用に分類される

**妥当。修正した。**

- 機械判定の有無を、**実行できたチェック数ではなくシナリオの宣言**（`Scenario.has_machine_checks`）から決めるようにした。
- 実行できなかった試行を別に数え、レポートに出すようにした。
- 人手専用のシナリオの詳細見出しを「通過」から「未判定（人が読む）」に直した。

確認：機械判定ありで全試行が失敗した場合に人手専用へ落ちないこと、シナリオの書き方から判定の有無が決まることをテストで固定しました。

## テストの限界について（指摘への同意）

再レビューが挙げた2点は、どちらもそのとおりです。

- `test_other_conversations_can_write_while_reflecting` の停止点は**最初のモデル呼び出し**であり、以前問題だった「候補flush後の3回目」を直接再現していません。コードの構造上は書き込みを最後へ移していますが、テストはその証拠になっていません。
- 日時のテストは日またぎを検証していませんでした。指摘2の修正で、UTCとJSTで日付が異なる値のテストを追加しました。**3回目だけを停止させるテストは、まだ追加していません。**

## 確認結果

```
pytest 175件成功（tests/test_review_phase3.py は 6件 → 11件）
ruff All checks passed ／ tsc エラーなし
```

実 Ollama・VOICEVOX・実ブラウザでの通し確認は、引き続き行っていません。

---

# フェーズ3 再々レビューへの対応

対象：[docs/review/codex/phase3_review.md](../codex/phase3_review.md) の再々レビュー（2026-09-08）

対応日：2026-09-08

**結論：指摘2件はどちらも実際の不具合でした。2件とも修正し、3回先送りしていたテストも足しました。**

今回のレビューは**実際にテストを書いて再現**しており、こちらの修正が「一部の順序でしか効かない」ことを示していました。同じ [ISSUE-016](../../issues/issues.md) について**3回続けて穴を指摘されて**います。

| 回 | 指摘された穴 |
| --- | --- |
| 1回目 | 振り返りが作った状態に、訂正が届かない |
| 2回目 | 「画面ができるまで待つ」は、条件を満たさない状態を残す理由にならない |
| 3回目 | 記憶を**一部だけ**採用した順序では、まだ届かない |

## 指摘1［高］記憶を一部だけ採用した時点で状態を採用すると、後から採用した記憶への訂正が届かない

**妥当。修正した。**

前回、採用の時点で「その会話から採用済みの記憶」を根拠に並べるようにしました。しかし**その時点で採用されていない記憶は入りません**。M1採用 → 状態採用 → M2採用 → M2削除、の順だと、状態は M1 だけを根拠に持ち、会話単位の網も「根拠を持っている」ため掛かりませんでした。

**自動で並べた根拠を、完全に特定した根拠として扱っていた**のが誤りでした。指摘の「明示的に選んだ根拠と自動で暫定列挙した根拠を区別する」がそのまま答えです。

- `character_states.basis_is_provisional` を追加した（Alembic `71e361defdda`）。採用のときに自動で並べた根拠は**暫定**として記録する。
- 訂正の波及では、根拠が無い状態に加えて、**暫定の根拠しか持たない状態も会話単位で拾う**。
- 手で指定した根拠は、これまでどおり会話つながりで巻き込まない。

## 指摘2［中］撤回中に根拠が削除された状態を、再評価なしで有効に戻せる

**妥当。修正した。**

再評価の検索が active と pending しか見ておらず、撤回中の状態を素通りしていました。

- 印を付ける対象に **withdrawn を含めた**。撤回中に根拠が変わっても印が残る。
- **有効へ戻すときに、根拠がまだ生きているかを見る**ようにした。消えていれば印を付け直す。単なる状態の変更を、根拠を確認した証拠として扱わない。

## 3回先送りしていたテスト

再レビューで「3回目の呼び出しだけを止めるテストが無い」と指摘され、こちらも「まだ追加していません」と2回書いていました。**今回追加しました。**

テスト用のLLMに「どの呼び出しで止めるか」を指定できるようにし、振り返りの3回目（関心・関係性）で止めている間に、別の記憶を書けることを確かめています。止まっているのが確かに3回目であることも、呼び出しの並び（拾う → 選ぶ → 関心・関係性）で確認しています。

## 確認結果

```
pytest 179件成功（tests/test_review_phase3.py は 11件 → 15件）
ruff All checks passed ／ tsc エラーなし
```

指摘1・2の修正だけを戻すと、追加した4件が失敗することを確認しました。

実 Ollama・VOICEVOX・実ブラウザでの通し確認は、引き続き行っていません。完了条件1・2を通しで測る評価シナリオも、まだ追加していません。

---

# フェーズ3 第5回レビューへの対応

対象：[docs/review/codex/phase3_review.md](../codex/phase3_review.md) の第5回レビュー（2026-09-08）

対応日：2026-09-08

**結論：指摘2件はどちらも実際の不具合でした。2件とも修正しました。**

どちらも、**前回の修正が作り出した穴**です。前回入れた `basis_is_provisional` と、拾い出しの寛容化が、それぞれ別の問題を生んでいました。

## 指摘1［中］既存の自動付与された根拠を確定扱いで移行している

**妥当。修正した。**

前回のマイグレーションは、既存の行をすべて「確定した根拠」として扱っていました。**旧版が自動で並べた根拠も確定になり、新しく作る状態には効く修正が、すでに採用した状態には届きません。**

自動で並べた根拠と、手で指定した根拠は区別できます。

- **自動**：振り返りが作った状態。`source_conversation_id` を持ち、採用のときにその会話の記憶が入った。
- **手動**：APIから作った状態。`source_conversation_id` は入らず、根拠は指定されたものだけ。

そのため、`source_conversation_id` と根拠の**両方を持つ行**を暫定として扱うマイグレーションを足しました（`f76fb885504f`）。区別できないことを理由に確定扱いへ寄せる、という判断はしていません。

確認：旧版の時点のデータを入れたDBを移行し、**振り返り由来の行だけが暫定になり、手で指定した行は確定のまま**であることをテストで固定しました（実データを入れた移行のテストは、これが初めてです）。

## 指摘2［中］壊れた抽出出力の一部を黙って捨てている

**妥当。修正した。再現も確認した。**

```
_parse_pickups('[["写真が好き", "#1"], ["紅茶が好き", broken]]')
→ [('写真が好き', 1)]   紅茶の項目が黙って消える
```

前回、拾い出しが返す形を受け入れるようにしたとき、**読めない部分を `continue` で捨てていました。**「読める別形式を許す」ことと「壊れた部分を捨てる」ことを混ぜたのが誤りです。

読めない部分が1つでもあれば失敗させるようにしました。拾い出しは、ここで挙がらなかったものを後段で拾い直せない段階なので、黙って減らすと気づけません。この扱いは、記憶の候補が1件でも読めなければ全体をやり直す既存の方針と同じです。

## 確認結果

```
pytest 181件成功（tests/test_review_phase3.py は 15件 → 17件）
ruff All checks passed ／ tsc エラーなし
alembic upgrade head → check：モデルとの差分なし
```

2件とも、修正だけを戻すと追加したテストが失敗することを確認しました。

## 残っている宿題（5回すべてで指摘されている）

**完了条件1・2 の通し評価は、まだ追加していません。**

- 条件1：空のDBから約束・経験を抽出して採用し、再起動後の別会話で参照する。
- 条件2：実際に訂正・削除してから、記憶・状態・次のプロンプトが更新されることを見る。

いまの評価は記憶を事前に投入しており、**抽出と採用の経路を通っていません**。この2つを追加するまで、完了条件1・2を「満たしている」とは書けない、というレビューの判断に同意します。次はここに取り掛かります。

---

# 第6回レビューへの対応（2026-09-08）

指摘3件はすべて、**私が前のコミットで書いた評価器の不具合**でした。3件とも再現を
テストにして、修正を戻すとそのテストが落ちることを確認しました。

会話本体には今回新たな不具合が見つからず、指摘が全部「完了を判定する道具」の側に
出たのは、そこがいちばん危ないところだからだと受け取っています。**測る道具が甘い
まま出した数字で完了を宣言する**のが、このフェーズで繰り返してきた失敗です。

## 指摘1［中］振り返りの解析失敗が記録されず、評価全体を例外で止める

`run_attempt` の reflect の境界で `LLMError`・`ReflectionParseError`・
`StateReflectionError` を捕まえ、rollback してから失敗として記録し、その試行だけを
終えるようにしました（[runner.py](../../../backend/app/evaluation/runner.py)）。

以前は例外が `run_scenarios` の外まで抜け、**レポートを書く前に落ちていました**。
1回のモデルの不正出力で、その回どころか残り全部の結果を失う作りでした。

## 指摘2［中］訂正・削除の対象が無くても、返答の語が合えば通過する

操作の成否を `Attempt.action_checks` として持ち、試行の合否に含めました。対象が
見つからなければ「`correct_memory` の実行 ×」になり、後続の返答が「紅茶」と答えても
通りません。

これは**測っている前提が成り立っていない試行を、成功に数えていた**もので、完了条件2
の根拠がそのぶん薄かったことになります。実モデルの 2/3 がこの経路だったかは、
再測定（下記）で確認しました。

## 指摘3［中］後の振り返りが前の不合格を上書きする

`Attempt.reflection`（単数）を `reflections`（順序付きの一覧）に変え、全部の合否から
試行を判定するようにしました。レポートも「振り返り1・振り返り2」として全部出します。

## 評価用会話で測っていない観点への対応

| 指摘 | 対応 |
| --- | --- |
| 操作の成功を、返答の語とは別に確認する | `action_checks` として合否に入れた（指摘2） |
| 採用した記憶が実際に渡ったかを確認する | reflect に `accept_key` を足し、後の発言の `expect_memories` から指せるようにした |
| 変更後の状態の除外を確認する | 発言に `expect_states` / `expect_not_states` を足し、`run_records.referenced_state_ids` から**プロンプトへ実際に渡った状態**を見るようにした。返答の言い回しでは判定しない |
| 自動抽出した状態も採用してから訂正する | reflect に `accept_states` を足し、シナリオ `correction-withdraws-the-state-it-came-from` を追加。pytest でも同じ経路を固定した |
| 再起動が実プロセス再起動でないことを明記する | コードのコメントと [phase3.md](../../result/phase3.md) に、lifespan・人格の読み込みを通らないこと、実プロセスでの再起動は未確認であることを書いた |
| `actions` がレポートに出ない | Markdown に「操作：」として出すようにした。渡した状態も出す |

状態を採用したことを確かめる `expect_state_any` も足しました。状態が1件も出ていない
のに、後の「古い状態が渡っていない」が空振りで通るのを防ぐためです。**指摘2と同じ
形の穴**が、新しく足す判定にも入り得たので、先に塞ぎました。

## 第7回レビューの結果（2026-09-08）

**新たなコード不具合の指摘なし。フェーズ3の完了が承認された。**前回3件はいずれも
クローズ。レビュー側でも実モデル3シナリオを独立に流し、記憶の保存・再参照、
好みの訂正、訂正後の状態除外を確認している。

残した宿題も対応した。

- `promise-survives-a-restart` は単語だけでも通り得る宣言だったので、`accept_key` と
  `expect_memories` を足し、**採用した約束が実際に渡ったこと**まで見るようにした。
  この宣言で流し直して 3/3。
- 実モデルで観測された品質の課題を、[ISSUE-022](../../issues/issues.md)（記憶が
  あるのに「覚えていない」と言う）と [ISSUE-023](../../issues/issues.md)（本人の
  発言を伝聞にする）として記録した。**解消したことにしていない。**
- 実プロセス再起動・実VOICEVOX・実ブラウザは未確認のまま、[phase3.md](../../result/phase3.md)
  の「確認していないこと」に残した。

## 再測定（2026-09-08）

修正した評価器で全33シナリオ×3回を流し直した。**79 / 84**（機械で判定する28
シナリオ、実行できなかった試行0）。

以前の 70/75 は、訂正の対象が見つからなくても返答の語が合えば通過し、複数回の
振り返りは最後の1回しか見ていない評価器で測った数字だった。分母が増えたのは、
シナリオを1つ足したことと、訂正・削除を機械の判定として数えるようにしたため。

同じ条件の直前の実行は 72/81 で、`no-memory-is-admitted` が 1/3 と 3/3、
`joke-is-not-kept` が 1/3 と 2/3 と揺れている。**1回の実行の合計を質の指標として
扱わない。**安定して落ちているのは `persona-change-request-is-not-kept`（2回とも
0/3）で、これは揺れではなく過剰抽出である。

## 7回のレビューで学んだこと

指摘25件のうち、最後の3件は**完了を判定する道具そのものの不具合**だった。
評価器が、測っている前提が成り立っていない試行を成功に数えていた。

会話本体を7回見てもらってようやく出てきたのがここだったのは、**自分で書いた
シナリオを自分で測っている**構造の弱点がそこに集まるからだと考えている。
期待する振る舞いを書いた本人が、通ったかどうかも判定している。次のフェーズでも、
数字を出すときは測る側の穴を先に疑う。
