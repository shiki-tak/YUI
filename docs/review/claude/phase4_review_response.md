# フェーズ4 レビューへの対応

対象：[docs/review/codex/phase4_review.md](../codex/phase4_review.md)

## PR1：目標の器 — 第1回レビューへの対応（2026-09-08）

**結論：指摘1件は実際の不具合でした。修正し、再発を検知するテストを追加しました。
あわせて、指摘ではなく「補強できる」とされたテストの穴を1つ埋めました。**

## 指摘1：`active_goals` が現在時刻を UTC へそろえず、実行日時を誤判定する

**妥当。修正した。再現も確認した。**

### 確認したこと

`due_at` は `normalize_due_at` で保存前に UTC へ直していましたが、**比較に使う
`now` はそのまま SQL へ渡していました**。SQLite は timezone を落として比較する
ため、日本時間で渡された `now` は、その壁時計の値が UTC として扱われます。

指摘の表のとおり、同じ瞬間でも表記によって結果が変わります。基準日時 9/13 15:30
UTC の目標に対し、9/13 15:00 UTC（まだ実行してはいけない）を同じ瞬間の
9/14 00:00 JST として渡すと、実行してよいと判定されました。

**保存側だけそろえても足りない**、というのが今回の穴です。記憶の `occurred_at`
で踏んだずれ（日本時間 00:30 の発言が前日扱いになる）と同じ形が、比較側に
残っていました。

### 直し方

`as_utc()` を切り出し、保存（`normalize_due_at`）と比較（`active_goals`）の
両方で同じ関数を通すようにしました（[goal.py](../../../backend/app/agent/goal.py)）。

- timezone を持つ値は UTC へ直す。
- timezone を持たない値は UTC として解釈する。保存が UTC なので、DB から読み
  戻した値をそのまま渡しても同じ基準になります（`config.to_local` と同じ約束）。

`now` を受け取る関数は今後も増えます（評価に `advance_time` を足す PR3、期限
切れを判定する PR9）。経路ごとに直すのではなく、**時刻を受け取る側が1か所で
そろえる**形にしました。

### 確認

`tests/test_goal.py::test_the_same_moment_is_judged_the_same_in_any_timezone` を
追加しました。同じ瞬間を UTC・UTC+9・UTC−7 の3通りで渡し、基準日時の前後で
判定が一致することを見ます。あわせて次の2つも入れています。

- 境界：`now == due_at` は「以降」に含める。
- timezone を持たない `now` を UTC として扱う。

`now` の正規化を戻すと、このテストが失敗することを確認しました（修正前の状態を
再現して1件失敗、戻して11件成功）。

## 補強：配信で相手を指定した目標

指摘ではなく「補強できる」とされた項目のうち、**配信で相手を指定するケース**を
テストに足しました（`test_a_public_goal_about_someone_stays_with_that_person`）。

公開してよいことと、誰に対する目標かは別の軸です。公開の印だけを見ると、本人が
いない配信で「この前の映画どうだった？」を持ち出すことになります。実装は
すでに相手で絞っていましたが、**その組み合わせを測っていませんでした**。

相手の指定と公開範囲の総当たりまでは広げていません。行動選択が入る PR7 で、
評価シナリオ側から同じ組み合わせを通します。

## 対応しなかったもの

いずれもレビューが「不具合ではない」と判定しているものです。記録として残します。

- **ORM を直接操作すれば `normalize_due_at` を迂回できる。** PR4 で更新処理を
  足すときに同じ検査を通します。DB 制約は入れていません。SQLite の batch
  マイグレーションで扱いが増えるためで、判断は [phase4.md](../../plan/phase4.md) の PR4 に書いています。
- **`snapshot` に `source_conversation_id` を含めていない。** 変更しない項目で、
  目標本体に残ります。`character_state.snapshot` と同じ扱いです。
- **schema 一致テストが外部キー・既定値・索引の構成列まで見ていない。** 列名・
  型・NULL 可否・索引名までの比較です。手書きマイグレーションのずれを見つける
  目的には足りていると判断しました。

## 確認結果

- pytest **208件成功**（PR1 のテストは11件）、ruff 成功。
- 実モデル（Ollama）での評価は流していません。PR1 は会話経路に接続されていません。

---

## PR3：評価の器と proactive.toml — 第1回レビューへの対応（2026-09-08）

**結論：指摘4件すべて妥当でした。4件とも直し、再発を検知するテストを追加しました。**

測る対象ではなく、測る道具の不具合が3件でした。フェーズ3の第7回レビューでも
最後の3件は評価器そのものの不具合で、同じ場所に穴が集まっています。

## 指摘1［高］：手順に適用されない期待条件が受理され、自動合格に数えられる

**妥当。修正した。再現も確認した。**

### 確認したこと

指摘のとおりでした。`say` に `expect_goal_any`（振り返り用）を書いた
シナリオを流すと、`has_machine_checks` は真、実行された判定は空、
`attempt.ok` は真、`human_only` は偽になりました。**期待を書いたのに一度も
判定せず、機械判定の成功数に入ります。**

同じ形は他の組み合わせでも起きます。`reflect` に `expect_memories`、
`accept_goal` に `expect_any`、`advance_time` に `expect_goals` など、
指定できる場所が手順の種類と結び付いていませんでした。

### 直し方

手順の種類ごとに書ける指定を表にし、**読み込みの時点で弾く**ようにしました
（[scenario.py](../../../backend/app/evaluation/scenario.py)）。実モデルを何度も呼んでから気づくと高くつくため、
既存の書き間違いの検査と同じ位置に置いています。

- `say` / `start_conversation`：返答に対する期待（記憶・状態・目標・語・繰り返し）
- `reflect`：候補に対する期待と採用の指定
- `correct_memory` / `delete_memory`：対象と、印が付く目標
- `accept_goal`：対象と名前、`advance_time`：日数
- `new_conversation` / `restart`：なし

`human_check` はどの手順にも書けます（人が読む欄なので）。

### 確認

- `test_expectations_that_do_not_apply_to_the_step_are_rejected`：5種類の
  組み合わせが読み込みで止まること。
- `test_no_bundled_scenario_passes_without_executing_a_check`：同梱の全43本を
  モックで流し、機械判定ありと数えたシナリオが**判定を1つも実行しないまま
  終わっていない**ことを見ます。指摘の「機械判定ありと分類した条件が、実行時に
  判定結果へ対応することも検査する」に対応します。

## 指摘2［中］：訂正波及シナリオの目標が、訂正する記憶を根拠に持っていない

**妥当。修正した。**

### 確認したこと

`GoalSpec` に根拠を指定する場所がなく、`corrected-basis-marks-the-goal` の
目標は `basis_memory_ids=None`・`source_conversation_id=None` でした。
訂正の波及そのものは動きますが、**この目標は対象になりません**。

測定として成立していませんでした。`expect_not_goals`（渡っていないこと）は、
目標を一切渡さない実装でも通ります。いまは `start_conversation` の未実装失敗に
隠れていますが、PR7 が入れば「何も起きていないのに通る」状態になります。

### 直し方

2つ入れました。

1. `GoalSpec.basis`（記憶の鍵の並び）。投入のときに実際のIDへ直します。
   シナリオでは `basis = ["movie-plan"]` と書き、訂正する記憶と結び付けました。
2. `expect_marked_goals`。**印が付いたことを記録で確かめます。** 訂正・削除の
   手順が、波及で印を付けた目標の鍵を返すようにし
   （`steps.MemoryChange`）、期待と突き合わせます。

否定条件だけに頼らない形にしたのは、指摘の「対象の存在、採用状態、根拠、日時
等の前提を検査し、抑制理由も別に観測すべき」に沿ったものです。

### 確認

`test_correcting_the_basis_is_measured_by_the_mark_not_by_absence` で、根拠を
結び付けた目標では印が付き、**結び付けていない目標では判定が落ちる**ことを
見ます。前提が欠けたまま通らないことを、テストの側で押さえました。

## 指摘3［中］：時間を進めた後の発言が実時計の日付で保存される

**妥当。修正した。再現も確認した。**

### 確認したこと

30日進めて「昨日、映画を見たよ」と発言し、振り返りへ渡るプロンプトを見ました。

```
[#1／2026-09-08] 開発者: 昨日、映画を見たよ        ← 実時計
振り返りを行っている日: 2026-10-08                  ← 進めた時計
```

振り返りは「相対的な日付は、その言い方が出てきた発言の日付から数える」設計
なので、**1か月ずれた入力を渡していました**。

### 直し方

出来事の時刻と、監査の記録を分けました。

- 発言（`messages.created_at`）は、会話に渡した現在時刻と同じ基準で保存する。
- 評価で採用した記憶（`memories.created_at`）も同じ。検索の減衰が、まだ作られて
  いない記憶を新しいものとして扱わないため。
- 再生の記録（`delivery_started_at` / `delivery_finished_at`）は実時計のまま。
  いつ鳴らしたかは監査の記録で、会話の中の時間の進み方とは別のもの。

通常の呼び出しは `now` を渡さないので、これまでと同じ実時計で動きます。

### 確認

`test_the_advanced_clock_reaches_the_saved_message` で、会話ログの発言日と
振り返りが「今日」とする日が揃うことを見ます。

## 指摘4［低］：基準測定の失敗原因の説明が、ログと異なる

**妥当。訂正した。**

レポートを読み直しました。`correction-changes-the-next-answer` の不合格は
[ISSUE-022](../../issues/issues.md#issue-022) ではありません。返答は「開発さんが最近**红茶**をよく飲むのが
お好きだってことを覚えています」で、**訂正後の好みは覚えています**。期待語
「紅茶」に一致しなかったのは、簡体字で書いたためです。

[phase4.md](../../plan/phase4.md) の基準値表を、次の内訳へ訂正しました。

- 約束を覚えていない：2試行（[ISSUE-022](../../issues/issues.md#issue-022)）
- 訂正後の表記の不一致：1試行
- 過剰抽出：3試行（[ISSUE-021](../../issues/issues.md#issue-021)）

あわせて、分母の84が「機械判定を持つ28シナリオ×3回」であること（99試行すべてを
自動採点した数ではないこと）も明記しました。

**この1件は、指摘6で挙げられた「expect_none / expect_any は言い換えを検知
しない」の実例でもあります。** 訂正が反映されているかを測りたいのに、判定して
いるのは表記の一致です。意味が通っている返答を落としており、[ISSUE-022](../../issues/issues.md#issue-022) の
修正方針に書いた「否定表現を並べる形は誤判定を起こす」と同じ構図です。

## 対応しなかったもの

指摘ではなく「限界」として挙がったもののうち、後続の PR で扱うと判断したものです。

- **抽出 → 採用 → 期限後の参照を一続きに測るシナリオ。** いまはどのシナリオも
  事前投入から始まり、`adopted-goal-is-asked-later` も事前作成した候補の採用です。
  会話から目標を抽出する経路は PR6 で入るため、そこで `accept_goal` の
  `goal_key` を使った通しのシナリオを足します。**フェーズ3で5回続けて指摘された
  形と同じなので、PR6 の完了条件に含めます。**
- **`does-not-pile-up-questions` が「渡した目標の数」しか測れないこと。**
  実際に選んだ行動は `run_records.selected_action` に残る予定で、書き手が入るのは
  PR7 です。そこで機械判定へ接続します。それまでは人手欄のままです。
- **完了条件3・6の機械判定が目標の参照だけであること。** 実行時刻・完了時刻の
  検査は PR8・PR9、人格の前後比較は PR10 の測定で扱います。
- **`selected_action` が未記録であることと、待機を選んだことを同一視しない。**
  指摘のとおりです。PR7 で待機も明示的に記録します。

## 確認結果

- pytest **222件成功**（評価ハーネス 31件）、ruff 成功。
- 同梱の proactive 10本をモックで流し、`corrected-basis-marks-the-goal` の
  失敗が「自発発話の未実装」1件だけになったことを確認しました（訂正の波及は
  記録の側で通っています）。
- 実モデルでの再測定は行っていません。今回の修正は評価器とシナリオの定義で、
  会話の生成経路の既定動作は変えていません（`now` を渡さない経路は従来どおり）。

---

## PR4：目標の API と画面 — 第1回レビューへの対応（2026-09-08）

**結論：指摘4件すべて妥当でした。4件とも直し、再発を検知するテストを追加しました。**

3件は「終わったはずの目標や、無効な根拠を持つ目標が、印の付かない実行候補に
なる」という同じ形でした。状態の遷移を表にして1か所で検査するようにしています。

## 状態の遷移を決めた

指摘2・3への対応の前提として、API が許す遷移を確定しました
（[goals.py](../../../backend/app/api/goals.py) の `_ALLOWED_TRANSITIONS`）。

| いまの状態 | 変えられる先 | 理由 |
| --- | --- | --- |
| pending | なし（decide のみ） | 採用は根拠の補完と `accepted` の履歴を伴う |
| active | done / withdrawn / cancelled / expired | 終わり方は区別して残す |
| withdrawn | active / cancelled / expired | 一時的に外した状態なので戻せる |
| done / rejected / cancelled / expired | なし | 終わり。前提が戻ったのなら作り直す |

## 指摘1［中］：削除済みの記憶を根拠に新規作成した目標を採用できる

**妥当。修正した。**

`_check_scope_against_basis` は記憶の存在と参照範囲しか見ておらず、
`status` を見ていませんでした。**訂正の波及は「後から変わったもの」を拾う
仕組みなので、作る前に無効だった根拠は拾えません。**

2か所を直しました。

- 作成（POST）：無効な記憶を根拠に指定したら 400 で拒否する。訂正後の記憶を
  根拠にするよう促します。
- 採用（decide accept）：候補を置いてから採用するまでの間に根拠が消えることが
  あるため、`_dead_basis` を見て、消えていれば `needs_review` を立てる。
  拒否はしません。目標を捨てるかは開発者が決め、印が付いている間は行動選択へ
  渡りません。PATCH で有効へ戻すときの扱いと揃えました。

## 指摘2［中］：終了中の根拠訂正を検知せず、完了日時付きで再有効化できる

**妥当。修正した。**

終了状態から `active` へ戻せないようにしました。指摘のとおり、PR1 で終了状態を
波及の対象外にした前提は「もう実行されない」ことでした。**再有効化を許すなら
その前提が崩れます。** 完了日時の扱いを定義するより、戻せなくするほうが
完了条件3（一度完了した質問を繰り返さない）を状態の上で保証できます。

前提が戻った場合は、新しい目標として作り直します（根拠も履歴も新しくなります）。
撤回（withdrawn）だけは戻せます。開発者が一時的に外すための状態で、戻すときに
根拠の有効性を確かめる既存の経路がそのまま使えます。

## 指摘3［中］：PATCH が採用の経路を迂回する

**妥当。修正した。** 依頼時に自分でも挙げた点で、塞ぐ側の判断をもらえたので
そのとおりにしました。

候補に対する `status` の変更を 409 で拒否し、`decide` へ誘導します。内容や
実行条件の修正は候補のままでもできます（採用前に直せないと、いったん採用して
から直すことになり、その間に実行されます）。

## 指摘4［中］：画面から撤回・再有効化・期限切れを行えない

**妥当。修正した。**

API が許す遷移と画面をそろえました。

- 採用済み：達成にする／撤回する／取り消す／期限切れにする
- **撤回中を独立した一覧にした**（戻せる状態なので、終わったものと混ぜない）。
  「戻す」「取り消す」を置いています。
- 終わったもの：操作を出さず、「ここからは状態を戻せません。前提が戻ったのなら
  新しい目標として作り直します」と明記しました。API の拒否と画面の説明を
  一致させています。

## テストの補強

指摘の「フロントは表示検査だけ」に対応し、5件足しました（合計10件）。

- 採用済みの4つのボタンが、それぞれ正しい `status` を送ること
- 撤回中が終わったものと分かれ、「戻す」が `active` を送ること
- 終わったものに状態変更の操作が出ないこと
- 「指定日以降」がラベルだけでなく**日付の値**を出すこと
- API が失敗したとき、その内容が画面に出ること

## 訂正

依頼文に「目標関連のテスト24件」と書きましたが、**19件の誤り**でした
（今回の追加で23件）。数え間違いです。

## 同じ形が残っている場所（今回は直していません）

`api/states.py` の `_check_scope_against_basis` にも、指摘1と同じ穴があります
（記憶の `status` を見ていないため、削除済みの記憶を根拠にした状態を作れる）。
PR4 の範囲外なので直していません。**別の PR で直すか、ISSUE として登録するかを
判断してください。**

## 確認結果

- backend：pytest **231件成功**（目標関連23件）、ruff 成功
- frontend：tsc 成功、**28件成功**（GoalPanel 10件）
- 実ブラウザ・実サーバーでの確認はしていません。フロントのテストはモックした
  API に対するもので、実際の API と繋いだ確認ではありません。

### 第2回レビュー（指摘4の残り）への対応

**残っていた指摘1件に対応しました。**「API が許す遷移と画面をそろえた」と書き
ながら、そろっていない状態が2つ残っていました。

- **撤回中 → 期限切れ**。API は許すのに画面に無く、いったん有効へ戻す必要が
  ありました。「期限切れにする」を足しました。
- **要確認 → 撤回**。印を下ろす操作（このままでよい）を先に行う必要が
  ありました。**根拠が変わったまま「確認済み」にしないと外せないのでは、
  確認の意味が無くなります。**「撤回する」と「期限切れにする」を足し、
  印を下ろさずに外せるようにしました。

#### 1つだけ、意図して置いていない操作があります

**要確認の一覧に「達成にする」は置いていません。** 達成は「相手が答えた」と
いう記録なので、根拠が変わったまま付けると、古い前提のやりとりを達成として
残すことになります。先に確認して採用済みへ戻してから付ける想定です（画面の
コメントにも理由を書きました）。

API は `active → done` を needs_review に関わらず許しているので、**画面の側だけ
で絞っている状態**です。この線引きが不適切なら、API 側で拒否する形に変えます。

#### テスト

遷移の網羅を、API の許可表と突き合わせる形にしました（合計13件、うち重複した
1件を削除して30件）。

- 採用済み・撤回中それぞれで、押せるボタンが送る `status` の集合が、
  `_ALLOWED_TRANSITIONS` と一致すること
- 要確認から、印を下ろさずに撤回・取消・期限切れができること

#### 確認結果

- frontend：tsc 成功、**30件成功**（GoalPanel 13件）
- backend：変更なし（231件成功のまま）

---

## PR5：振り返りを会話後のジョブへ分離 — 第1回レビューへの対応（2026-09-08）

**結論：指摘3件すべて妥当でした。3件とも直し、再発を検知するテストを追加しました。**

指摘1は自分で挙げた懸念（並行実行・中断）の隣にあった穴で、**挙げた範囲の外に
同じ形が残っていました**。指摘2・3は、自分で「問題だと思う」と挙げた画面の
ポーリングそのものです。

## 指摘1［高］：保存段階の例外・キャンセルで、開始権と処理中表示が残る

**妥当。修正した。再現も確認した。**

### 確認したこと

指摘のとおりでした。抽出だけを `try` に入れており、**保存の直前（`saving` への
進行更新）と保存の最中は、失敗の扱いにも中断の扱いにも入っていませんでした**。

- ケースA（保存直前の例外）：ジョブは終わっているのに進行は `running`、
  `error` は null。
- ケースB（保存中の停止）：`shutdown` 後も `running`、`step` は `saving` のまま。

どちらも、画面はポーリングし続け、再試行は回収期限まで 409 になります。
「停止時に開始権を解放する」「想定外の例外も失敗として残す」という、この PR で
掲げた保証を満たしていませんでした。

### 直し方

ジョブの本体を `_run` へ切り出し、**抽出・進行の記録・保存を1つの
`try` にまとめました**（[reflection_job.py](../../../backend/app/agent/reflection_job.py)）。中断・既知の失敗・想定外の
例外の3つを、同じ場所で受け止めます。

あわせて2つ入れました。

- `_release` に `reflection_completed_at IS NULL` の条件を足し、**完了したものを
  失敗で上書きしない**ようにしました（修正方針の「すでに完了した結果を失敗へ
  上書きしない」）。保存を commit した直後に停止されるとこれが起きます。
- `shutdown(factory)` が、止めたあとに残っている開始権を回収します。取り消しの
  最中に DB を書けなかった場合の取りこぼしを拾うためで、起動時の回収と同じ処理を
  呼んでいます。

### 確認

`tests/test_reflection_job.py` に3件足しました。

- 保存直前の例外で `failed` になり、開始権が解放されること
- 保存中に停止しても解放され、**期限切れを待たずにやり直せる**こと
- 保存の commit 後に停止しても `completed` のままであること

保存を `try` の外へ戻すと1件目が落ちることを確認しました。

## 指摘2［中］：会話切り替え・アンマウント後もポーリングが続く

**妥当。修正した。**

依頼時に自分でも挙げた点です。`while` ループに中断条件がなく、`setReflection`・
`setError` が世代を見ていませんでした。

監視を**終了ボタンのハンドラから `useEffect` へ移しました**。

- cleanup でループを止める（会話の切り替えとアンマウントの両方）。
- 返ってきた時点で世代（`view`）が変わっていたら、結果を捨てる。送信の返答で
  すでに使っている考え方に揃えました。
- 世代が変わったら、進行の表示自体も消す。

## 指摘3［中］：振り返り中の会話を開き直しても監視が始まらない

**妥当。修正した。**

監視が終了ボタンの中にしかなく、別のタブで終了した会話や、処理中に画面を
再読み込みした会話を開いても、進行を見に行きませんでした。**DB に進行を
置いた意味が、画面側で活きていませんでした。**

`useEffect` の起動条件を「終了を押した直後（`watching`）**または** 会話が
`reflecting` の状態」にしました。指摘のとおり、指摘2と同じ寿命の話なので
1か所で扱えます。

あわせて、レビューが「その他」で挙げた**最初の取得結果が反映されない経路**も
消えています。監視は取得のたびに必ず反映します。

### 確認

`ChatPanel.reflection.test.tsx` に2件足しました（合計5件）。

- `reflecting` の会話を開いただけで監視が始まり、完了が親へ伝わること
  （終了は積み直さない）
- 会話を切り替えたら前の会話の進行が消え、**それ以上見に行かない**こと

## その他の指摘への対応

- **`end_and_candidates` が成功を確かめていない**（テストの限界として挙げられた
  もの）：POST が 202 であることと、最終状態が `completed` であることを
  確かめてから候補を読むようにしました。**この強化で1件落ちました**
  （`test_valid_candidates_are_kept` が終了を2回呼んでいた）。直しています。

## 確認結果

- backend：pytest **242件成功**（`test_reflection_job.py` 9件）、ruff 成功
- frontend：tsc 成功、**35件成功**（`ChatPanel.reflection.test.tsx` 5件）
- 実モデル・実ブラウザでの確認はしていません。26秒待たされる状況での進行表示は
  モックでしか見ていません。

### 第2回レビュー（残り2件）への対応

**2件とも妥当でした。どちらも画面側で、監視の「入口」と「出口」に残っていた
穴です。ループ本体だけを直して、その前後を見落としていました。**

#### 指摘1［中］：振り返り中として開いた会話が失敗すると、その場でやり直せない

失敗の処理がエラー表示と `watching=false` だけで、**親の状態を戻していません
でした**。履歴から開いた会話は `state="reflecting"` のままなので読み取り専用で、
終了ボタンも押せません。完了には `onEnded` があるのに、失敗には親へ伝える経路が
ありませんでした。

`onReflectionFailed(view)` を足し、親が会話を「続けられる状態」へ戻すように
しました（[App.tsx](../../../frontend/src/App.tsx)）。**失敗は「終わった」ではない**ので、候補の取り直しへは
進みません。世代を渡して、別の会話へ移っていたら反映しない点は `onEnded` と
同じです。

#### 指摘2［中］：終了POSTの遅延応答が、切り替え先の会話で監視を始める

監視ループには世代の検査を入れましたが、**それを起動する POST 側に同じ保護が
ありませんでした**。会話Aの終了が返る前にBへ移ると、Aの進行をBの表示に入れ、
`watching` を立てて、いまの会話（B）の監視を始めてしまいます。

押した時点の世代を覚え、応答・例外・`finally` のいずれでも、いまの世代と
違えば何も反映しないようにしました。

#### 確認

`ChatPanel.reflection.test.tsx` に2件足しました（合計7件）。

- `reflecting` で開いた会話が失敗したとき、親へ失敗が伝わり、`onEnded` は
  呼ばれないこと
- 終了の応答を保留したまま別の会話へ移り、その後で応答が届いても、切り替え先で
  進行を出さず監視も始めないこと

どちらも、対応する修正を戻すと落ちることを確認しました。

#### 確認結果

- backend：pytest **242件成功**、ruff 成功（変更なし）
- frontend：tsc 成功、**37件成功**

### 第3回レビュー（残り1件）への対応

**妥当。修正した。**

#### 指摘1［中］：終了POST待機中に会話を切り替えると、busy が解除されず操作不能になる

第2回の指摘2を直したときに作った問題です。応答・例外・`finally` のすべてで
「世代が違えば何もしない」ようにしたため、**旧要求が握った `busy` を誰も
解かなくなりました**。切り替え先の会話で、送信も終了もできなくなります。

会話を切り替えたときのリセット（`reflection` と `watching` を消している場所）で
`busy` も解くようにしました。レビューが避けるよう書いていた「旧 `finally` を
無条件に戻す」形は採っていません。それだと新しい要求の `busy` を消します。
**世代が変わった直後はまだ新しい要求が始まっていない**ので、リセット側で解けば
取り違えは起きません。

#### 確認

`ChatPanel.reflection.test.tsx` に2件足しました（合計9件）。終了の応答を保留した
まま会話を切り替え、旧応答が**成功した場合と失敗した場合の両方**で、切り替え先の
入力欄と終了ボタンが操作できることを見ます。失敗の場合は、前の会話のエラーが
切り替え先に出ないことも確かめています。

送信ボタンではなく入力欄で見ているのは、送信ボタンが本文の有無でも無効になり、
`busy` を確かめたことにならないためです。

`setBusy(false)` を戻すと2件とも落ちることを確認しました。

#### 確認結果

- frontend：tsc 成功、**39件成功**
- backend：変更なし（242件成功、ruff 成功）

### 第4回レビュー（残り1件）への対応

**妥当。修正した。**

#### 指摘1［中］：旧送信の後始末が、切り替え先で始めた要求の busy を解く

`busy` は送信と終了で共有しています。終了側にだけ世代の検査を入れ、**送信側の
後始末は無条件のままでした**。会話Aの送信を待っている間にBへ移り、Bで要求を
始めると、後から返ったAの送信が `setBusy(false)` を呼んで、Bの要求が終わって
いないのにボタンが有効になります。二重に送れる状態です。

送信側にも同じ検査を入れました。`setError`・`setBusy` は、始めたときの世代を
まだ見ているときだけ触ります。

#### 入力の扱いを1つ変えました

`setText("")` は世代で抑えるのではなく、**送った時点で空にする**形にしました。
返答が返ってから消すと、待っている間に別の会話で書き始めた下書きを、遅れて
届いた返答が消してしまいます。送信に失敗したときは、同じ画面を見ていれば
書き戻します（打ち直させないため）。

#### 世代で抑えなかったもの

「別の会話へ移ったため、いまの返答はこの画面に出していません」の通知は、
**世代に関わらず出します**。これは移った先の人に、送った発言がどうなったかを
伝えるための文なので、抑えると誰にも伝わりません。いったん世代で抑えたところ、
既存テスト（`返答を待つ間に別の会話を開くと、遅れて届いた返答を画面に出さない`）が
落ちて気づきました。

#### 確認

`ChatPanel.reflection.test.tsx` に2件足しました（合計11件）。

- Aの送信を保留 → Bへ切替 → Bで終了を開始 → Aの送信が返る、の順で、
  **Bの要求が終わるまで押せないまま**であること
- 遅れて返ったAの送信の失敗を、Bの画面に出さないこと。書き戻しもしないこと

送信側の検査を外すと1件目が落ちることを確認しました。

#### 確認結果

- frontend：tsc 成功、**41件成功**
- backend：変更なし（242件成功、ruff 成功）

---

## PR6：目標の抽出 — 第1回レビューへの対応（2026-09-08）

**結論：指摘4件すべて妥当でした。4件とも直しました。**

指摘1〜3は、**新しく足した4回目の呼び出しが、既存の3回と同じ扱いになって
いなかった**ものです。失敗の扱い、日付の基準、読めない値の扱いが、それぞれ
違っていました。

## 指摘1［中］：目標抽出の不正出力で、評価が全体を終了する

**妥当。修正した。**

`GoalReflectionError` を、評価器の既知の失敗一覧へ入れ忘れていました。本番の
ジョブには入れたのに、評価側が漏れています。1回の失敗で評価全体が止まり、
**残りの試行とシナリオが走らず、集計そのものが取れません**。フェーズ3の
第6回レビューの指摘1（1回の失敗で評価を止めない）と同じ形です。

`runner.py` の except に追加し、試行の失敗として記録して次へ進むようにしました。
`test_a_failed_goal_extraction_is_recorded_per_attempt` で固定しています。

## 指摘2［中］：相対日付を、発言日ではなく振り返り日から数えていた

**妥当。修正した。**

記憶の抽出は「相対的な日付は、その言い方が出てきた発言の日付から数える」と
指示しているのに、目標の抽出は「この日を基準に数える（振り返りの日）」と
書いていました。**同じ会話を別の日に振り返っただけで、予定の日付が変わります。**
同じ予定から作った記憶と目標で、日付の基準が食い違います。

指示文を発言日基準に揃え、振り返りの日は「予定がもう過ぎたかどうかの判断」に
だけ使うと明記しました。

## 指摘3［中］：読めない基準日を、黙って「次の会話」に変換していた

**妥当。修正した。**

`来週くらい` のような読めない日付を `next_conversation` へ落としていました。
記憶の `occurred_at` と同じ扱いのつもりでしたが、**意味が違います**。

- `occurred_at` が欠けても、記憶の意味は変わらない。
- **実行条件が欠けると、意味が変わる。** 予定より前に聞いてよい目標になる。
  制限が緩む方向へ黙ってずれます。

空欄（次の会話）と、書いてあるのに読めない値を区別し、後者は抽出の失敗として
扱うようにしました。既存の「読み取れない候補があれば全体を失敗にする」という
扱いと揃います。

## 指摘4［低］：4回目の処理中に、進行の説明が空になる

**妥当。修正した。** フロントの型とラベル表に `goals` を足し、この段階を挟んで
完了する画面テストに直しました。ラベルを消すとテストが落ちることを確認済みです。

## レビューの指摘に沿って、測り方も直しました

「本文の語だけでなく trigger と due_at を見るべき」という指摘に従い、評価の
レポートへ**基準日時を出す**ようにしました。これまでは実行条件と本文だけで、
日付が正しいかを読み手が確かめられませんでした。

## 測り直しについて

「6試行では揺れと効果を分離できず、改善を確立したとは扱わない」という指摘は
そのとおりです。指摘2の修正（日付の基準）は実モデルでないと効果が見えないため、
同じ2シナリオで測り直しています。結果は下に追記します。

## 確認結果

- backend：pytest **253件成功**（`test_goal_reflection.py` 11件）、ruff 成功
- frontend：tsc 成功、**41件成功**

### 測り直しの結果：前回の結論は支持されなかった

同じ2シナリオ×3回を、レビュー対応後に測り直しました。

| 測定 | 条件 | 予定の「後」を指す書き方 | 自動判定 |
| --- | --- | --- | --- |
| 1回目 | 書式の指定なし | 6件中1件 | 2/6 |
| 2回目 | 書式の指定あり | 6件中4件 | 2/6 |
| 3回目 | 書式の指定あり＋日付の基準を発言日へ | 7件中2件 | 3/6 |

**3回目で、2回目に書いた「自動判定は動かず本文の形だけが良くなった」が
支持されませんでした。** 本文の形は戻り、自動判定だけが上がっています。

指摘のとおり、**6試行では生成の揺れと指示文の効果を分離できません**。書式の
指定は残しますが、**効果を確かめたとは扱いません**。[phase4.md](../../plan/phase4.md) の記録も、
2回目だけを書いた形から3回ぶんの表へ訂正しました。

日付の基準（指摘2）の効果は、この測定では見えません。シナリオが同じ日に
振り返るため、発言日と振り返り日が一致するからです。**測るには
`say → advance_time → reflect` の形と、基準日時そのものを見る判定が要ります。**
どちらも PR10 の「判定の作り方の見直し」に含めます。

### 日跨ぎの観測（未確認としていた項目）

再レビューで「実モデルの抽出品質・日跨ぎ精度は未確認」と整理されましたが、
**日跨ぎは測る仕組みが無いだけだったので、シナリオを1本足して観測しました。**

`relative-date-is-counted-from-the-utterance`（発言 → 4日進める → 振り返り）を
実モデルで3回。発言日 09-08、振り返り日 09-12、正しい基準日時は 09-10。

| 回 | 基準日時 | 内容 |
| --- | --- | --- |
| 1 | 2026-09-13 | 振り返り日の翌日。発言日を見ていない |
| 2 | 2026-09-09 | 発言日基準だが、面接当日（翌日ではない） |
| 3 | 2026-09-13 | 1回目と同じ誤り |

**正解 0/3。指摘2の修正は効いていません。** 2回が同じ誤った値なので、揺れでは
なく系統的な誤りです。**自動判定は3回とも通りました**（語を含むかしか見て
いないため）。測る道具の穴も同時に実証されました。

[ISSUE-028](../../issues/issues.md#issue-028) として登録し、対応は **PR10（通し測定と調整）** としました。
先に判定（基準日時そのものを見る形）を作らないと、指示文を触っても直ったかを
機械で確かめられないためです。PR7〜9 は事前に日付を入れた目標で進められるので、
この件には依存しません。

同じ観測で、話者の取り違え（相手の話を YUI 自身のことにする）も目標の本文に
出ることが分かりました。[ISSUE-023](../../issues/issues.md#issue-023) と合わせて見ます。

---

## PR7：目標の参照と行動選択 — 第1回レビューへの対応（2026-09-08）

**結論：指摘7件すべて妥当でした。7件とも直し、再発を検知するテストを追加しました。**

[高] の2件は、どちらも**既存の仕組みに後から処理を足したときに、前提を
確かめ直さなかった**ものです。

## 指摘1［高］：行動選択の待ち時間に、書き込みロックを保持する

**妥当。修正した。**

既存の設計は「生成に入る前にトランザクションを閉じる」で、SQLite が書き込み
ロックを1つしか持てないためでした。**行動選択を足したときに、その commit を
動かし忘れていました。** 1回目のモデル呼び出しがロックを握ったままになり、
別の会話や記憶の訂正が `database is locked` で失敗します。

commit を最初のモデル呼び出しの前へ移しました。コメントに「モデルを呼ぶ処理を
足すときは、その前に閉じているかを必ず確かめる」と残しています。**同じ形で
もう一度踏むのを防ぐため**です。

指摘にあった「選択が失敗しても入力は履歴に残る」も、これで満たします。

## 指摘2［高］：open が振り返り中の会話へ発言を追加できる

**妥当。修正した。**

`chat` にある検査（終了済み・振り返り中）が `open` にありませんでした。ロック
取得後の再読み込みもありません。振り返りの対象を固めた後に発言が入ると、
抽出した内容と会話ログが食い違います。`_ensure_open` をロックの前後で通し、
保存の commit までロックの内側で終えるようにしました。

## 指摘3［中］：選んだ目標が生成へ引き継がれない

**妥当。修正した。** 聞くと決めたときは、選んだ目標だけを渡します。全部渡して
生成側に選び直させると、相手の様子を見て選んだ判断が伝わりません。記録
（`referenced_goal_ids`）も、実際に渡したものに揃えました。触れないときは、
持っているものとして全部渡します（渡さないことで待機させないため）。

## 指摘4［中］：action の型が不正だと例外になる

**妥当。修正した。** 文字列であることを確かめてから集合と比べます。`goal: true`
が Python では 1 番として通る点も直しました（bool を除いてから見る）。

## 指摘5［中］：待機の判断が記録に残らない

**妥当。修正した。** 依頼時に自分でも挙げた点で、置き場所の判断をもらえたので
そのとおりにしました。

`action_records` を新しく作りました（マイグレーション1版）。**発言を伴わない
判断も残します。** 会話・相手・行動・理由・候補の目標・選んだ目標・発言との
結び付き（待機なら NULL）・自発かどうかを持ちます。指摘のとおり、架空の空発言を
作って辻褄を合わせることはしていません。

`run_records` を広げなかったのは、`message_id` が必須かつ一意で、既存の
「発言から実行記録を引く」経路と一覧がそこに依存しているためです。

あわせて、自発発話で渡した可変状態のIDを記録していなかった点も直しました。

## 指摘6［中］：評価が自発発話の記憶・状態を空として判定する

**妥当。修正した。** `open` に記憶検索を足したのに、評価側へ渡していません
でした。渡ってはいけない記憶が渡っても落ちません。返答と同じ経路で読むように
しました。

## 指摘7［中］：open のモデル失敗が 503 にならない

**妥当。修正した。** `LLMError` を 503 に変換します。モデルに繋がらないことと、
正しく待機したことを区別して返します。

## 評価の緩さについて

「待機・読み取り失敗・無関係な発言を区別できない」という指摘に対し、
`expect_action` を足しました。`waits-when-the-partner-is-tired` に
`expect_action = "wait"` を書き、**返答に語が出ないことではなく、待機を選んだ
ことを見る**ようにしています。

## 再測定：27/36（実行失敗0）

修正で渡す目標が変わったため、proactive を測り直しました。前回 28/36 で、
揺れの範囲内です。**完了条件1・2 のシナリオはすべて 3/3 のまま保たれました。**

| シナリオ | 前 | 後 |
| --- | --- | --- |
| `adopted-goal-is-asked-later`（条件1） | 3/3 | 3/3 |
| `waits-when-the-partner-is-tired`（条件2） | 3/3 | 3/3 |
| `does-not-pile-up-questions`（条件2） | 3/3 | 3/3 |
| `does-not-claim-to-have-searched` | 3/3 | 3/3 |
| `goal-is-extracted-adopted-and-asked` | 3/3 | 1/3（抽出の揺れ。ISSUE-028） |
| `completed-goal-is-not-repeated`（条件3） | 1/3 | 0/3（PR9） |
| `cancelled-plan-cancels-the-goal`（条件4） | 0/3 | 1/3（PR9） |

**待機の判定を強くしたうえで 3/3 が保たれた**のが収穫です。

### 訂正：再測定の説明が、ログと一致していませんでした（第2回レビューの指摘3）

2つ誤りがありました。

**1. 「完了条件1・2 のシナリオはすべて 3/3」は誤りです。** 完了条件1 には
`goal-is-extracted-adopted-and-asked`（1/3）も含まれます。正しくは
「**事前に目標を置いたシナリオ**は 3/3 を保った」です。抽出から始める通しの
シナリオは 1/3 でした。

**2. 「抽出の揺れ」で2回の失敗をまとめたのは誤りです。** 内訳が違います。

| 回 | どこで落ちたか |
| --- | --- |
| 1回目 | 抽出・採用・ask の選択・目標の参照まで成功。**生成が目標を無視した**（自己紹介と「今日一日どう過ごされましたか」で、映画に触れない） |
| 2回目 | 抽出が「感想」を含む目標を出さず、採用が空振り（[ISSUE-028](../../issues/issues.md#issue-028) の周辺） |

**1回目は、私が「直した」と書いた不具合と同じ形です。** PR7 で
「自発発話が目標に触れない」を 0/3 → 3/3 にしたと報告しましたが、それは
事前に目標を置いたシナリオでの話で、**抽出から始める通しでは再発しています**。
記憶の受け渡しと開始時の指示で改善はしましたが、なくなってはいません。

原因と再現率は測り直すまで未確認です。PR10 の測定で、抽出の失敗と、目標を
渡した後の生成の失敗を分けて数えます。


## 確認結果

- backend：pytest **268件成功**（`test_action_selection.py` 15件）、ruff 成功
- 指摘1・3 の修正を戻すと、該当テストが落ちることを確認
- 開発用DBにマイグレーション適用済み（`24c03fb94c47`）

---

## PR8：ISSUE-011 の線引き — 第1回レビューへの対応（2026-09-08）

**結論：指摘4件すべて妥当でした。4件とも直し、再発を検知するテストを追加しました。**

## 指摘1［高］：状態だけ先に確定するため、失敗すると再送で復旧できない

**妥当。修正した。** 再生状態を先に commit していたため、目標の更新に失敗すると
**状態だけ終端になり、同じ通知を送り直しても「もう進んだ」と判断されて永久に
反映されません**。状態と目標の更新を同じトランザクションで確定するようにしました。

## 指摘2［中］：鳴り始める前の中断も実行済みにする

**妥当。修正した。** `generated → aborted`（一度も鳴っていない）を「途中まで
届いた」として扱っていました。**PR8 の目的そのものに反します。** 開始時刻の
有無で見分け、`playing → aborted` のときだけ実行済みにします。

評価にも `deliver` で `playing` を送れるようにし、鳴らずに中断する対のシナリオを
足しました。

## 指摘3［中］：音声無効の構成で実行済みにならない

**妥当。修正した。** 依頼文に「文字だけの構成では通知なしで実行済みになる」と
書きましたが、**実装されていませんでした**。生成時に completed になる経路を
通していません。生成の時点で実行済みにし、あとから通知が来ても記録が増えない
ことを確認しました。

## 指摘4［中］：待機の判断でも目標番号があれば実行済みにする

**妥当。修正した。** `selected_goal_ids` の有無だけで判定していました。目標を
持ち出す行動（ask / suggest）の記録だけを見るようにしました。

## 指摘に沿って直した記述

- 「どこまで届いたかを残す」→ **「中断だったことを残す」**。残るのは分類だけで、
  再生位置や届いた文面ではありません。
- **[ISSUE-013](../../issues/issues.md#issue-013) の影響欄を更新**しました。通知が欠けると目標が実行済みに
  ならず、同じことをもう一度聞きます。「ローカルでは実害が小さい」は目標には
  当てはまりません。
- **PR9 へ渡す条件を4点、[phase4.md](../../plan/phase4.md) に明記**しました。`last_executed_at` だけで
  `done` にしない／恒久的な再質問禁止に使わない／中断時は再確認を許す／頻度の
  上限に使う場合も矛盾させない。

## 再測定：3本×3回で 9/9

| シナリオ | 判定 |
| --- | --- |
| 鳴ったら実行済み | 3/3 |
| 鳴ってから中断＝実行済み | 3/3 |
| 鳴らずに中断＝実行済みにしない | 3/3 |

**3通りを区別できています。** 前回は中断を一律で扱っていたため、この区別自体が
ありませんでした。

## 確認結果

- backend：pytest **281件成功**（`test_goal_execution.py` 10件）、ruff 成功
- 実モデルでの再測定（上記 9/9）

### 第2回レビュー（残り1件）への対応

**妥当。修正した。再現も確認した。**

#### 指摘1［中］：中断の判定に古い Message を使い、実行記録を落とす

条件付き UPDATE は DB の最新状態を見て成功しますが、**手元のオブジェクトは
読み込んだときのまま**です（`synchronize_session=False`）。開始の通知と中断の
通知が重なると、DB には開始時刻が入っているのに、古い値を見て「鳴り始めて
いない」と判定し、**実際には届いた質問の実行記録を落とします**。

前回の修正（指摘2：開始時刻で見分ける）を入れたことで生まれた経路です。
判定の材料を増やしたのに、その材料を最新の状態から読んでいませんでした。

UPDATE の後、**commit の前に**確定値を読み直してから判定するようにしました。
原子性は保ったままです（判定の前に commit を戻していません）。

#### 確認

`test_a_stale_message_does_not_lose_the_execution_record` を追加しました。
指摘の再現手順どおり、中断の要求が `generated` の発言を読み込んだ後に別の要求が
`playing` を確定させ、その古い発言のまま中断を適用します。読み直しを戻すと
落ちることを確認しました。

画面の通知は前の通知の完了を待たないため、この重なりは実際に起こりえます
（`useSpeechPlayer.ts` の `notify`）。

#### 確認結果

- backend：pytest **282件成功**（`test_goal_execution.py` 11件）、ruff 成功
- 実モデルの再測定は行っていません。今回の修正は DB の読み取り順序で、
  生成の経路は変えていません。

---

## PR9：完了・繰り返し防止・予定変更 — レビューへの対応（2026-09-09）

**まず訂正：第1回レビューへの対応記録を、この資料に書いていませんでした。**
計画（[phase4.md](../../plan/phase4.md)）には測定結果を残していましたが、対応の記録はこちらに
書くべきでした。第2回レビューで「PR9 の対応節が見当たらない」と指摘されて
気づきました。以下に第1回・第2回をまとめて記録します。

## 第1回レビュー（5件）への対応

### 指摘1［高］：完了・取消の更新後、書き込みロックを保持したまま生成を待つ

**妥当。修正した。** 行動選択の**前**では閉じていたのに、その後の
`mark_done` / `mark_cancelled` の書き込みを開いたまま生成を待っていました。

**これで3回目です**（PR7 の respond、PR7 の open の話者作成、今回）。同じ形を
繰り返しているので、コメントに「モデルを呼ぶ処理の前に、開いている書き込みが
無いかを必ず確かめる」と残しました。

### 指摘2［中］：予定日前の目標が取消判定へ渡らない

**妥当。修正した。** 予定の取消は、実行してよくなる日より前に起こります。判定の
対象を実行条件で絞っていたため、中止したのに後日その話を持ち出す状態でした。
判定用は実行条件で絞らず（`ignore_trigger`）、質問してよいかは別に決めます。

### 指摘3［中］：終わらせた目標から、別の目標へ振り替える

**妥当。修正した。** 選んだ目標を同時に取り消すと `goal_ids` が空になり、空集合は
部分集合なので抑制を素通りして `or goals[:1]` が別の目標を渡していました。
**選び直しは行動選択の仕事**なので、ここでは補わず、聞ける目標が残っていなければ
聞かない側へ倒します。

### 指摘4［中］：指示と判定根拠の不一致

**妥当。修正した。** 「すでに聞いた」の印を `last_executed_at` から付けていたため、
通知が無い質問は「聞いていない」ことになり、コード側で許した経路を指示文が
塞いでいました。逆に、その印は消えないので間隔を過ぎても聞き直せません。
2つの印を別の根拠から付けます（話した記録 ／ 間隔の内側かどうか）。

### 指摘5［中］：最後の目標を終わらせると判断が記録されない

**妥当。修正した。** 更新後の残りで記録の有無を決めていたため、状態を変えた
判断そのものが記録から消えていました。判定に渡した候補で決めます。

## 第2回レビュー（1件と注記2件）への対応

### 指摘1［中］：生成に失敗すると、状態を変えた判断の記録が失われる

**妥当。修正した。** 目標の状態は確定しているのに、何がその状態にしたのかを
追えませんでした。行動の判断を**生成より前に**保存し、生成が成功してから発言と
結び付けるようにしました。

指摘にあった副作用も潰しています。判断を先に保存すると、生成に失敗した記録も
残ります。それを「持ち出した」根拠にすると、**一度も話していない質問を聞いた
ことになる**ので、`_raised_goal_ids` と実行済みの判定を **発言が結び付いている
記録だけ**に限りました。

### 注記1：テストが1回目の呼び出しで止まっていた

**そのとおりでした。** `gate_on=None` では最初の呼び出し（行動選択）で止まり、
PR7 で直した箇所を測るだけになっていました。FakeLLM に `gate_skip` を足し、
**2回目（返答の生成）で止める**ようにしました。返答の生成は人格のプロンプトで
呼ばれるため、指示文では選べません。取消が別接続からすでに見えることも
確かめています。

### 注記2：コメントと実装の不一致（達成の判定範囲）

**そのとおりでした。** コメントに「達成の判定はこの会話に限る」と書きながら、
実装は相手単位で会話をまたいで探していました。

**実装のほうを正とし、コメントを直しました。** 「昨日聞かれたやつだけど」と後から
答えることがあり、会話に閉じると達成にできません（第1回レビューでも限界として
挙げられていた点です）。話した記録がある目標だけが対象なので、根拠は保たれます。

## 確認結果

- backend：pytest **296件成功**（`test_goal_completion.py` 14件）、ruff 成功
- 実モデルの再測定：修正後のコードで測り直し中（前回の 34/45 は修正前の数字
  なので、そのまま転用しません）

## PR10：通し測定と調整 — 第1回レビューへの対応（2026-09-09）

8件すべて自分で再現し、**8件とも直しました。** 指摘は全部当たっていました。
特に指摘4は、私が入れた修正が正しい動作を壊していたもので、撤回しました。

### 指摘1［高］通しの評価が質問で終わり、完了処理を壊しても合格する

**そのとおりでした。** `goal-is-extracted-adopted-and-asked` は
`start_conversation` で質問を作ったところで終わっていて、設計書の小実験が
求める「返答を受けて完了とし、同じ質問を繰り返さない」を一度も通って
いませんでした。12/18 を完了条件1の数字として使えません。

シナリオを伸ばしました。deliver（届いたことにする）→ 感想を答える
（`expect_done_goals`）→ 聞き直しの間隔を越えて別の会話（`expect_not_goals`）
まで測ります。達成を見る `expect_done_goals` は新設で、**実行済みとは別に
`status == done` と `completed_at` を見ます**。

再現手順も残しました。`test_the_small_experiment_scenario_catches_a_broken_completion`
が、**同梱シナリオそのもの**をモックで2回流します。`mark_done` を空の関数に
差し替えると落ち、正常なら通ります。実モデルは揺れるので、これでないと
「壊すと落ちる」を保証できません。

### 指摘2［中］数値の期待を判定しない手順に書け、検査0件で合格になる

**そのとおりでした。** 0 を区別するために `_EXPECTATION_FIELDS` から外した
結果、reflect 以外にも書けて弾かれなくなっていました。**判定の穴を塞ぐために
作った仕組みに、同じ穴を開けていた**ことになります。

検査から外すのではなく、`is not None` で見るようにしました
（`_OPTIONAL_NUMBERS`）。`restart` や `say` に書くと読み込みで止まります。

### 指摘3［中］言語判定は日本語の語を落とし、全文が外国語でも合格にする

**そのとおりでした。** 「么」（麻雀の么九牌）と「儿」（部首の「ひとあし」）は
日本語の用例があるので外しました。「什么呢」の検出は、1文字ではなく語
（`_SIMPLIFIED_WORDS`）で見るようにして残しています。

見逃しの側も塞ぎました。**仮名が1つも無いこと**を見る判定を足しています
（`_looks_unjapanese`）。「你好，我很高兴和你聊天。」は字の一覧に1つも当たり
ませんが、日本語の文はほぼ必ず仮名を含むので、これで落ちます。短い返答を
誤って落とさないよう、漢字が6字以上あるときだけ見ます。

**「これは言語判定ではなく検出器である」という線引きを、名前とコメントに
書きました。** 判定名を「日本語のまま」から「日本語以外の混入」に、通過時の
表示を「期待どおり」から「検出なし」に変えています。通っても「日本語だと
確かめた」ことにはなりません。

### 指摘4［中］音声を止めた後に文字の質問へ具体的に答えても、達成にできない

**そのとおりでした。私の修正が回帰でした。撤回しました。**

「aborted で開始時刻が無いのは、届かなかったという通知である」という私の
理由付けは成り立ちません。ご指摘のとおり、`playing` の通知だけが落ちれば
鳴っていても開始時刻は NULL になります（ISSUE-013 そのもの）。ローカルの
会話では文字が画面に出ているので、音が鳴らなくても相手は読めます。

`_raised_goal_ids` を元に戻し、テストを**対で測る**形に置き換えました
（`test_an_answer_after_an_aborted_question_still_completes`）。片方だけでは、
常に拒否する実装でも通ってしまいます。

観測した誤りは、相手の「聞こえなかった」を行動選択が `answered` と判定した
ことです。**再生の状態からは区別できない**ので、[ISSUE-034](../../issues/issues.md#issue-034)
として分け、フェーズ5Aで指示文を1つだけ変えて測ることにしました。

### 指摘5［中］対象目標の日付が誤っていても、別の目標に正しい日付があれば合格する

**そのとおりでした。** `expected in actual` で、どの目標の日付かを見て
いませんでした。本文の一致（`expect_goal_any`）と日付の一致が別の目標で
満たされます。作ったばかりの判定が、作った目的（ISSUE-028 を捕まえる）を
果たしていませんでした。

`expect_goal_any` で絞った目標だけを対象にし、どの目標を判定したかを
detail に出すようにしました。テストは、対象が5日ずれていて無関係な目標が
合っている場合に落ちること、正しい場合に通ることの両方を見ます。

### 指摘6［中］日付判定の基準がシナリオ最初の発言に固定される

**そのとおりでした。** `first_said_at` を最初の1回だけ入れていたので、
会話を分けたり時刻を進めたりすると別の日から数えていました。

発言の記録を持ち、**既定を「その振り返りの直前の発言」**に変えました。
会話の途中で日をまたぐ場合は既定では足りないというご指摘も踏まえ、
`expect_goal_due_from`（起点の発言に含まれる語）で名指しできるようにしました。

### 指摘7［中］余分な目標を落とすシナリオが、余分な1件を許している

**そのとおりでした。** `goal-is-created-from-a-plan` にある予定は「土曜に
映画を見に行く」だけなので、上限は1が正しいです。2にしていたため、
完了条件2の「単一の目的しかない会話で複数出るものを落とす」を満たして
いませんでした。1に直しました。**この変更で数字は下がります。**

### 指摘8［低］最大日付への翌日加算が未処理の例外になる

**そのとおりでした。** `9999-12-31` に1日足すと `OverflowError` が
`GoalReflectionError` に集約されず、測定の外まで飛びます。解析だけでなく
**足し算と UTC 変換まで**同じ try で囲み、範囲外は読み取れない日付として
扱うようにしました。境界値をテストに足しています。

### 測定記録の指摘について

- **「42シナリオ」は43の誤りでした。** 記録を直します。
- **3712件の母集団を再現できないというご指摘は、そのとおりです。** 抽出条件を
  書いていませんでした。今回は条件を明記して数え直しています
  （`logs/evals/**/*.json` の `reply`/`content`/`text`、38ファイル・5488件、
  現在の判定で21件検出・誤検知0）。
- **「評価器の修正はまず同じログで比較するべき」というご指摘を採ります。**
  162705 のログに現在の判定を当て直すと 102 → 109 で、その後の新規実行 110
  との差は生成差を含みます。分けて書きます。
- **11/80 の分母は「対象の判断総数」が正確**というご指摘、そのとおりです。
  表の見出しを直します。また「同じ実行内の判断は独立ではない」というご指摘も
  そのとおりで、ISSUE-032 の修正方針に、シナリオ別の内訳と実行別の変動を
  維持することを足します。「3/25 が観測範囲内というだけで効果ゼロとは確定
  しない」という点も、記録の書き方を直します。
- **PR11 へ渡す判断について。** 「除外リストを2つ決める材料にはなるが、
  他の種類を許可する試験にはなっていない」というご指摘を受け入れます。
  ISSUE-033 に、experience・impression・目標にも同シナリオで問題が出ている
  ことと、**種類ごとに根拠を示さないかぎり有効化しない**ことを足します。

### 確認結果

- backend：pytest **304件成功**、ruff check 成功
- 指摘1・2・5・6・8 は、修正を戻すとテストが落ちることを確認済み
- 実モデルの再測定：**判定を鋭くしたので数字は下がる見込み**。修正後のコードで
  測り直しています（指摘7の上限変更と、指摘1のシナリオ延長を含むため、
  修正前の 107/129 とは母数の意味が変わります）

## PR10 — 第2回レビューへの対応（2026-09-09）

**2件とも妥当でした。2件とも直しました。** どちらも、前回の修正で新しく
作ってしまった穴です。

### 指摘1［中］仮名が無いことだけで、正しい日本語の住所等を落とす

**そのとおりでした。** 前回の誤検知（日本語の漢字を一覧に混ぜていた）を直す
ために足した判定が、別の形で同じ誤りを作っていました。「東京都千代田区」は
漢字7字・仮名ゼロで、正しい日本語です。

**中国語の句読点「，」を併せて見る**ようにしました。日本語の読点は「、」で、
全角カンマは普通の会話では出ません。実測ログで確かめた根拠は次のとおりです。

- 仮名ゼロ（漢字6字以上）の返答は4件。**全部が中国語で、全部が「，」を含む。**
- 「，」を含む返答は全体で5件。

これで「東京都千代田区」「東京都千代田区丸の内一丁目九番一号」「日本国憲法
第九条改正反対運動」はいずれも通り、「你好，我很高兴和你聊天。」は落ちます。
3つとも回帰テストに入れました。

**拾えない範囲も明示しました**（ご指摘のとおり）。`_check_language` の
docstring に、全文英語と、仮名を含む中国語混在文のうち一覧の字も語も使って
いないものは拾えないと書き、人が読む欄で見ることにしました。ここを増やすと
正しい日本語を落とす側の危険が上がるので、**判定が落とすのは落ちるべきものだけ**
という原則を優先します。

過去ログ全件（39ファイル・5496件）に当て直し、検出21件・誤検知0を維持して
いることを確認しました。

### 指摘2［低］日付が一致した候補ではなく、一覧の先頭を根拠として表示する

**そのとおりでした。** `actual[0][0]` を無条件に出していたので、Bの日付で
合格したのに「面接Aの感想を聞く」と表示していました。**どの目標を判定したかを
出すために足した表示が、誤った読解を招いていました。**

一致した候補を保持して表示するようにしました。テストで、Aが誤りBが正しい
ときに detail が「面接Bの感想を聞く」を含み、「面接Aの感想を聞く」を含まない
ことを見ます。

### 測定記録の指摘について

- **`phase4.md` の 12/18 を、延長前の数字として範囲を明記しました。**
  「通しで成立はしている」という書き方を削り、**「まだ満たしていない。実モデル
  で通しの成立を確認できていない」**に改めました。この数字は質問を作るところ
  までのものです。
- **「感想を聞かずに別の質問をする生成失敗」を原因分類に戻しました。**
  162705 の実例（「こんにちは、私 YUI です。開発者さん、今週の土曜日に何を
  予定されていますか？」）を挙げています。
- **0/1 を行動選択の失敗に数えていません。** ご指摘のとおり、あれは抽出に
  「感想」が無く採用が空振りした試行で、後続の待機は「実行できる目標が
  ありません」です。あの実行は `mark_done` を壊した状態での確認用で、
  延長後の実測としては使いません。
- **測定中の結果を成功として扱いません。** 延長後の実モデルの結果は、出てから
  `phase4.md` の該当節に追記します。

### 確認結果

- backend：pytest **304件成功**、ruff check 成功
- 言語判定：過去ログ39ファイル・5496件で検出21件・誤検知0
- 延長後シナリオの実モデル測定：**完了（`20260909T002648`）。**
  全体 107/129・実行失敗0。「仮名が1つも無い」の判定は0回だったので、この実行は
  指摘1を直した後のコードと同じ結果です。

### 延長後の小実験の結果

**3回中1回、通しで成立しました。** 3回目が、抽出 → 採用 → 7日後の質問 →
再生完了 → 感想の返答 → **達成** → 2日後の別会話で**聞き直さない**まで、
全段階を通っています。延長前は質問までしか測っていなかったので、これが最初の
確認です。

落ちた2回は**どちらも抽出の失敗で、原因が同じ**でした。予定の「後」に感想を
聞く目標ではなく、**予定の「前」に聞く目標**（映画のタイトル、選んだ理由）を
出しています。行動選択でも生成でもありません。詳細は
[phase4.md](../../plan/phase4.md) の完了条件1の節に書きました。

なお `goal-is-created-from-a-plan` は 2/3 → 0/3 になりました。**上限を1にした
指摘7の直接の効果**で、落ちた理由は「2件で上限1を超えた」と「感想が含まれない」
です。数字は下がりましたが、実態が見えるようになったものです。

## PR11：自動採用 — 第1回レビューへの対応（2026-09-09）

**7件すべて妥当でした。7件とも直しました。** 指摘1・2・6は、私が入れた変更が
既存の保護を壊していたものです。

### 指摘1［高］一括取り消しが、派生した状態・目標へ波及しない

**そのとおりでした。** 記憶の削除APIは `mark_derived_for_review` を呼んで
いますが、**新しい削除経路がそれを迂回**していました。取り消した記憶が、
目標や関心を介して使われ続けます。

削除・履歴記録と同じトランザクションで `mark_derived_for_review` を呼ぶように
しました（記憶IDと由来の会話IDを渡す）。手動採用した派生物でも印が付くことを
テストで見ています。

### 指摘2［高］自動採用と評価の採用ステップが衝突し、比較測定が別物を測る

**そのとおりでした。** 3つ直しました。

- `apply` は**採用済みの候補に触れない**（`status == pending` のものだけ）。
  二重に記憶を作ると、本番には無い重複が評価DBに入ります。
- 評価の accept ステップも、採用済みの候補を飛ばす。
- `accept_goal` は、**すでに自動採用された目標を「無かった」と落とさない**。
  採用済みという結果は同じなので、この手順は満たされています。落としていると
  **自動採用に成功しただけで失敗が増え**、有無の比較を採用品質の差として
  読めなくなります。

自動採用した記憶を `outcome.accepted` に入れるようにもしました。入れないと
後続の鍵とレポートの表示が揃いません。「候補がそもそも出なかった」場合の
不合格は維持しています。

### 指摘3［中］手動訂正した記憶も一括取り消しで削除される

**そのとおりでした。** 訂正しても status は active のままなので、状態では
見分けられません。**採用後の変更履歴**で判定するようにしました
（`created` と `accepted_with_edits` 以外の履歴があれば手が入っている）。
理由を添えて `skipped` に残します。ご指摘のとおり、自動採用の出自を示す印と、
いま取り消してよいかの判定は別のものとして分けました。

### 指摘4［中］`since` のタイムゾーンで対象が変わる

**そのとおりでした。** 比較前に既存の `as_utc` を通し、naive へ落としてから
比べるようにしました。出力の `created_at` も既存の `UtcDatetime` に変えて
います。UTC と JST で同じ結果になることをテストで見ます。

### 指摘5［中］自動採用だけ評価の進めた時計を受け取らない

**そのとおりでした。** `apply` と `accept_candidate` に `created_at` を通し、
`create_memory` まで届くようにしました。本番では未指定＝実時計です。

### 指摘6［中］手動採用時の編集で、元の候補が失われる

**そのとおりでした。私の共通化がこれを壊しました。** 共通関数へ寄せるために
候補の行を書き換えていて、モデルが抽出した内容が消えていました。

`Overrides` を作り、**候補は書き換えず**に上書き値を渡す形にしました。直した
項目は `accepted_with_edits` の履歴として記憶側に残します。設計書は「更新前後を
比較し」て自動採用へ移すとしており、比較する元が消えるのは致命的でした。
`subject_to_none` の扱いも `Overrides` の中に移してあります。

### 指摘7［中］不正な設定を起動時に検証していない

**そのとおりでした。** `field_validator` に移し、**設定を作った時点で**落ちる
ようにしました。プロパティを読むまで検証しないと、本番の最初の参照は振り返りの
保存の直前なので、4回の抽出を終えてから設定エラーになります。

**この修正で、評価CLIに別の穴ができていたので併せて直しました。**
`model_copy` は検証を通さないため、`--auto-adopt` の綴り違いが素通りします。
`Settings(**{...})` で作り直す形にしました。

### 確認結果

- backend：pytest **323件成功**（test_auto_adopt.py 19件）、ruff check 成功
- **7件すべて、修正を戻すとテストが落ちることを確認しました**（指摘1・3・4を
  戻して3件失敗、指摘2・5・6を戻して3件失敗、指摘7は起動の検証で1件）
- 実モデルでの自動採用の有無の比較は PR12 の作業として未実施です

## PR11 — 第2回レビューへの対応（2026-09-09）

**2件とも妥当でした。2件とも直しました。** どちらも、前回の修正で新しく作った
穴です。前回の指摘2と6の直し方が、それぞれ別のところを壊していました。

### 指摘1［高］無関係な自動採用の記憶に、期待対象の鍵が付く

**そのとおりでした。** 前回、指摘2への対応として `outcome.accepted` へ自動採用を
全件入れました。ところが runner は**全件に同じ `accept_key` を割り当てて**いた
ので、`accept_contains` による選別が効かなくなっていました。

「コーヒーが好き」の記憶に「天体観測」の鍵が付き、**期待する記憶を一度も抽出
できていないのに参照の判定が合格**します。PR12 の比較測定を誤らせる形です。

鍵を付けるのを、シナリオが名指しした記憶だけに限りました。選別の条件は
`steps.py` の採用と同じ `accept_contains` にしています。自動採用そのものは
抑止していません（本番との違いを作らないため）。

負例をテストに固定しました（`test_an_unrelated_auto_adopted_memory_does_not_get_the_expected_key`）。
**その記憶が実際に引かれる問い方**にしてあります。引かれないと、鍵の付け方に
関係なく落ちてしまい、判定になりません。最初に書いたテストがそれで、修正を
戻しても通ってしまったので直しました。

### 指摘2［中］採用時の部分的な before を、既存の復元APIが完全な履歴として扱う

**そのとおりでした。** 他の履歴は全項目を持ち、復元APIは `before` を丸ごと
現在値へ被せます。変わった項目だけを入れたので、その履歴を指した復元が他の
項目を戻さず、**採用時は非公開だった記憶が公開のまま残り**ます。

完全なスナップショットとして残すようにしました。採用直後の状態に、直した項目
だけ抽出時の値を重ねたもの — つまり「開発者が直さなかった場合の記憶」です。
これは意味としても正しく、そのまま戻せる状態でもあります。

再現手順をテストにしています（本文を直して採用 → PATCH で公開へ → その履歴を
指して復元 → 本文が抽出時へ戻り、**公開範囲も非公開へ戻る**）。

### 補足への対応

- **「採用済みを飛ばす」の意味を記録しました。** ご指摘のとおり、成功を
  「開発者が採用した証拠」と読んではいけません。`accept_goal` は自動採用に
  限らず active の目標を通すので、**「使う対象が採用済みになったか」の確認**
  として関数の docstring に書きました。手動採用の経路そのものの検証は、既定
  設定（自動採用なし）のテストで別に維持しています。
- Overrides の `keywords=None`／空文字の区別、`subject_to_none`、複数根拠からの
  波及、`_was_edited` の方向、設定検証の副作用については、評価をそのまま
  受け取ります。追加の変更はしていません。

### 確認結果

- backend：pytest **325件成功**（test_auto_adopt.py 21件）、ruff check 成功
- **2件とも、修正を戻すとテストが落ちることを確認しました**
- frontend は今回の修正対象外です（前回から変更なし）
- 実モデルでの自動採用の有無の比較は PR12 の作業として未実施です

## PR12 — 第1回レビューへの対応（2026-09-09）

**3件とも妥当でした。3件とも直しました。指摘1を受けて、`goal` を既定から
外しています。**

### 指摘1［高］有効化の根拠が、採用された目標の中身を捉えていない

**そのとおりでした。私の根拠が薄すぎました。**

「残すべきでない筋書きから0件」だけを見て `goal` を有効にしましたが、採用
された48件を読み直すと**1つの会話から2件以上を採用した回が 33回中13回**
ありました。

```
promise-guitar-chords 3回目
  - コードの練習について、理論的な話や実際の感覚を聞く
  - コードの練習で感じたことを聞く          ← 言い換えの重複

goal-is-created-from-a-plan 2回目
  - 今週の土曜に見る予定の映画の名前を聞く
  - 今週の土曜に映画を見に行く計画の感想や詳細を聞く   ← 予定の「前」に聞く目標
```

ご指摘の完全重複（`impression-is-kept` 3回目の同一本文2件）も確認しました。
**採用された目標はそのまま質問になる**ので、記憶と違って害が直接出ます。

**`goal` を既定から外しました。** 既定は `promise` だけです。

`promise` も同じ目で読み直しました。16回すべて1件ずつで重複はありませんが、
**3件は約束ではありません**（「YUI は開発者の実家の犬の種類を知りたい」、
ご指摘の「開発者は YUI に天体観測への興味を問うている」）。この内容の誤りは
記録に残し、**質問を生まないので許容**と判断しました。判断の根拠を
`config.py` の表に書いています。

**失敗の帰属も誤っていました。** 出荷時設定の通し 0/3 を「ほぼ会話の開始で
待機」とまとめましたが、実際は次のとおりです。

| 回 | 最初に失敗した段階 |
| --- | --- |
| 1回目 | **抽出**（「感想」の目標が出ない） |
| 2回目 | 行動選択（採用は成功、その後 wait） |
| 3回目 | **抽出** |

3回中2回は抽出の失敗です。「条件1と6は同じ原因」という整理も取り下げます。

### 指摘2［中］雑談から採用された目標は、期限切れでは消えない

**そのとおりでした。** `expire_overdue` は `after_date` で `due_at` を持つ
ものだけを対象にしていたので、**「次の会話」の目標は永久に消えません**。
私が許容理由に挙げた期限切れは、実際には働いていませんでした。

「次の会話」の目標も対象にしました（基準日が無いので作られた日から数える）。
`goal` を既定から外したので自動採用の話ではなくなりましたが、**手動採用した
目標にも同じ穴があった**ので直しています。テストを追加しました。

### 指摘3［中］about_partner が欠けた出力を、伝聞と確定する

**そのとおりでした。** 導出元を変えたときに、以前の表現力を落としていました。
`provenance` を訊いていた頃は省略を `unknown` にできたのに、`about_partner`
の既定 `False` が「第三者と判断した false」と区別できなくなっていました。

`bool | None` にして、**省略は `unknown`** にしました。あわせて
**`unknown` の候補は自動採用しません**（確認を省けるのは判断材料が揃って
いるときだけ）。候補としては残るので開発者が採用できます。

この修正で7件のテストが落ちました。モックが `about_partner` を省いていた
ためで、**新しい挙動が効いている証拠**です。

### 記録の照合への対応

- **pytest の件数**（325 → 331）を直しました。
- **`promise` 誤分類の例の出典**を、指定ログの実際の本文（「YUI は開発者の
  実家の犬の種類を知りたい」）に揃えました。
- **比較表に、設定・コード・対象が特定できる実行を対応付け**ました。
- **`121009` は記憶を個別表示する前の形式**である旨を明記しました。あの表の
  記憶90件は、種類と本文から分類したもので、自動採用との対応を全件復元した
  ものではありません。
- 人手専用5シナリオの評価（言語混入が残る、未知の名称への回答が試行間で
  割れる）を、未確認事項として取り込みました。

### 「0件」の意味について

ご指摘を受け入れます。「その測定で観測されなかった」以上のことは言えず、
1/95 も負例に対する誤採用率ではありません。記録の書き方を直し、**負例ごとの
試行数を分けて**示すようにします。

### 確認結果

- backend：pytest **331件成功**、ruff check 成功
- 指摘2・3は、修正を戻すとテストが落ちることを確認済み
- **既定を `promise` のみにした実モデル測定を実行中**です。結果が出るまで
  完了とは言いません
