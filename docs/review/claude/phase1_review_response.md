# フェーズ1 レビューへの見解と対応

対象：[docs/codex/phase1_review.md](../codex/phase1_review.md)
作成日：2026-09-06
対応コミット：`71c48f5`

## 総評

指摘8件はすべて実在する不具合でした。誤検知はありません。重要度の高いものは、こちらでも再現手順を組んで確認しています。

- **#1（DBロック）** は、生成中に記憶を訂正すると **5.3秒後に `database is locked`** となることを再現しました。レビューが挙げた発生時間まで一致します。
- **#7（画面の検索）** は、同じ「写真」という語で **画面相当の呼び出しが0件、会話と同じ呼び出しが3件** になることを再現しました。
- 残りはコード上で指摘どおりであることを確認しました。

このうち #5・#6・#8 は、レビュー前に実装側でも把握していた未実装項目と同じものです。#1・#2・#3・#4・#7 は把握できていませんでした。

## 重要度について、レビューと見解が異なる点

### #4 は[中]ではなく[高]と考える

フェーズ1の完了条件「参照した記憶と根拠の会話を開発者が確認できる」を直接崩すためです。2ターン以上の会話では、根拠のポインタが常に誤った発言を指していました。

実装側は当初この機能を「動いている」と報告していましたが、検証したのは1ターンの会話だけでした。複数ターンでの確認が漏れていたことが原因です。

### #2 は[高]ではなく[中]と考える

`subject_speaker_id=NULL` は「キャラクター自身の経験」を表す設計であり、一人の人格が相手をまたいで経験を持ち越すこと自体は意図どおりです。

実際の穴は、`visibility` に「誰が参照してよいか」の次元が無かったことでした。これは複数話者が入るフェーズ4で顕在化します。ただしスキーマの変更を伴うため、DBが小さいうちに対応する判断をしました。

### #3 は実用上のリスクが低いと考える

ローカルで一人が使う前提では起きにくい事象です。ただし対策が安価なため、今回あわせて対応しました。

## 対応した指摘

### #1 [高] 生成中のDBロック

**原因**：発言を `flush()` して SQLite の書き込みロックを取ったまま、LLMの応答を待っていました。SQLiteは書き込みロックを1つしか持てないため、待機中の他の書き込みがロック待ちのタイムアウト（既定5秒）で失敗します。

**対応**：プロンプトを組み終えた時点でコミットし、応答待ちの間はロックを保持しないようにしました。相手の発言はこの時点で確定するため、生成に失敗しても会話履歴には残ります。会話の終了（振り返り）も同じ構造だったため、あわせて直しました。

**確認**：実サーバーで、生成（6.3秒）の最中に記憶を訂正して **HTTP 200 / 0.10秒**。

### #2 [高→中] 非公開記憶が別の相手へ渡る

**対応**：記憶に `visible_to_speaker_id`（誰との会話で参照してよいか）を追加し、`subject_speaker_id`（誰についての記憶か）と別の軸に分けました。ローカル会話では「公開可能な記憶」「相手を限定しない記憶」「この相手に許可された記憶」だけを使います。配信モードは公開可能な記憶のみです。

既存の非公開記憶は、由来した会話の相手をマイグレーションで補完します。手動で追加した記憶（由来する会話が無いもの）は限定しないまま残します。

**確認**：Aとの会話に由来する非公開記憶がBの会話へ渡らないこと、公開記憶は相手を問わず使えること、配信モードで非公開記憶が除外されることをテストで固定しました。

### #3 [中] 会話終了の二重実行

**対応**：終了状態の確定を `UPDATE ... WHERE ended_at IS NULL` の1文で行い、更新できた側だけが振り返りへ進みます。もう一方は409を返します。

あわせて、抽出に失敗した場合は終了状態を戻すようにしました。これを入れないと、失敗した会話が終了済みのまま残り、やり直せなくなるためです（#6を悪化させないため）。

**確認**：実サーバーで同時に2回投げて **200 と 409**、保存された候補は1組のみ。

### #4 [中→高] 記憶候補の根拠

**対応**：会話ログに `[#発言ID]` を付けてLLMへ渡し、候補ごとに根拠の発言を選ばせます。返された番号はその会話の発言に含まれるかを検証し、存在しない番号なら会話内の発言に寄せます。画面では根拠の発言をクリックして本文を開けるようにしました。

**確認**：実モデルで2ターンの会話を試し、「開発者は最近ギターを始めた」がギターの発言を、「次回お会いする約束をした」が別れの挨拶を、それぞれ根拠として指すことを確認しました。

### #7 [中] 画面の記憶検索

**原因**：フロントエンドが話者を送っておらず、バックエンドの既定に頼っていました。検索でも相手を渡していなかったため、会話とは別の条件で検索していました。

**対応**：フロントに自分の識別情報（`source` と `external_id`）を定数として持たせ、会話でも検索でも明示的に送るようにしました。

**確認**：同じ「写真」で **0件 → 3件**。

## 対応しなかった指摘

今回の指示範囲外のため、次の3件は未対応です。

| | 内容 | 現状 |
| --- | --- | --- |
| #5 | 未採用の候補を再読み込み後に画面から回収できない | 候補はDBに残るが、画面から採用できない |
| #6 | 不正な振り返り出力を「候補なしの成功」と扱う | 失敗と空の結果を区別できない |
| #8 | 記憶の復元で `occurred_at` だけ元に戻らない | 訂正後の日時が残る |

#8 については、対応時にテストを先に書いた段階で未修正であることを確認しています。範囲外のためテストごと取り下げました。

**訂正（2026-09-06）**：初版で「#3 への対応で #6 の影響範囲が狭まった」と書きましたが、これは誤りです。巻き戻しは `LLMError` のときにしか動かず、JSONとして壊れた出力は `200 []` のまま終了扱いになります。改善したのは、例外として通知される接続失敗などの経路だけです。再評価（[2回目のレビュー](../codex/phase1_review.md)）の指摘のとおりです。

## 確認したこと

- pytest **22件成功**（既存10件＋新規12件）。新規分は `backend/tests/test_review_fixes.py`。
- #1・#3 のテストは実際に並行実行して検証しています。
- ruff、TypeScriptの型チェック、Viteのビルドがいずれも成功。
- Alembic：新規DBへ適用後、モデルとの差分なし。downgrade→upgrade の往復も確認。
- テストは一時DBを使い、通常利用の会話・記憶を変更していません。実DBへのマイグレーション適用前にはバックアップを取得しました。
- 実Ollama（qwen3.5:9b）を通した #1・#3・#4・#7 の動作確認済み。

## レビュー自体について

- レビューは `AGENTS.md`・`CLAUDE.md` が追加される直前に実行されたため、「見つかりませんでした」と記録されています。指摘内容には影響していません。
- レビュー時点の `AGENTS.md` は設計書の場所を `docs/` と記載していましたが、実際は `design/` にありました（現在は `docs/design/` へ移動済み）。
- ドキュメントを `docs/` 配下へ移動したことで、設計書の画像参照・レビュー文書のコード参照・READMEの相対リンクが壊れていたため、あわせて修正しました。

## 残っている課題

レビューの対象外として明示されていたものを含め、フェーズ1で未着手の項目です。

- 会話履歴を画面から読み直せない（過去の会話を開く導線が無い）。
- 記憶検索の「時刻」が減衰のみで、`occurred_at` は振り返りで埋めていない。
- 理想の返答は記録できるが、画面で見返す手段と教師データとしての書き出しが無い。
- 評価用会話（設計書 §10、フェーズ1から開始とされている作業）が未整備。

---

# 再評価（phase1_review2.md）への対応

対象：[docs/review/codex/phase1_review.md](../codex/phase1_review.md)（2回目のレビュー）
作成日：2026-09-06

## 評価

追加指摘4件はすべて再現しました。文書とテストへの批判も、いずれも当たっています。

| 指摘 | 再現結果 |
| --- | --- |
| 1. 候補の移行漏れ | 旧形式DBの未採用候補が移行後も `visible_to_speaker_id=NULL` |
| 2. 同一会話での順序逆転 | 履歴が「質問A → 質問B → 返答B → 返答A」になった |
| 3. 中断後に再試行不可 | 候補0件のまま再試行が409 |
| 4. 不正IDの付け替え | 「写真が趣味」の根拠が「今日はここまで」になった |

指摘1については、対応時点の `data/yui.db` に未採用のまま `visible_to_speaker_id=NULL` の候補が5件残っていました。理論上の問題ではなく、実データが該当していました。

指摘2は、こちらが持ち込んだ後退です。修正前は DB の書き込みロックが偶然の直列化として働いていました。ロックを外したときに、代わりの順序制御を用意していませんでした。硬い失敗を、静かな履歴破損に置き換えたことになります。

## 対応内容

### 1. 記憶候補の参照範囲の補完

新しいマイグレーション（`a71c525f0285`）で、`memory_candidates` の参照範囲を由来した会話の相手から補完します。相手を特定できない候補（利用者の発言が無い会話）は NULL のまま残します。

実データの5件が正しく補完されたことを確認しました。振り返りで新しく作る候補が必ず参照範囲を持つことは、テストで固定しています。

### 2. 会話単位の直列化

`app/agent/turn_lock.py` を追加し、会話 id ごとのプロセス内ロックで直列化しました。DB の書き込みロックとは切り離してあるため、他の会話や記憶の訂正は止まりません。発言の保存もロックの内側で行い、到着順がそのまま履歴の順になります。振り返りにも同じロックを掛けています。

設計書の発話管理（asyncio による順序管理）のうち、フェーズ1で必要な範囲にあたります。MVP は単一プロセスのため、プロセス内のロックで足ります。複数プロセスに分ける段階では、DB か外部のロックへ置き換える必要があります。

実サーバーで同じ会話へ同時に2つ送り、「質問A → 返答A → 質問B → 返答B」の順になることを確認しました。

### 3. 振り返りの中断からの回収

会話に `reflection_started_at` と `reflection_completed_at` を追加し、「処理中」と「完了」を分けました。

- 開始の確定は 1 文の UPDATE で行い、同時実行では片方だけを通します。
- 完了時にだけ `ended_at` を設定します。
- `LLMError` では開始を取り消します。
- 中断やプロセス停止で開始だけが残った場合は、`reflection_stale_seconds`（既定180秒）を過ぎたらやり直せます。LLM 自体のタイムアウトより長くとっています。

完了した振り返りは、時間が経っても再実行しません。

### 4. 不正な根拠IDの扱い

直近の発言へ寄せる処理をやめ、`source_message_id` を NULL のまま残すようにしました。無関係な発言を確かな根拠として保存するより、未確認として残すほうが正しいためです。画面では記憶・候補の双方に「根拠未確認」と表示し、採用前に気づけるようにしました。

## テストの修正

指摘のとおり、`test_candidate_keeps_its_own_source_message` は `conversation_id` を渡しておらず、2回の発言が別々の会話になっていました。会話1には発言が1つしかないため、アサーションは自明に通っていました。複数ターンの検証になっていません。

ヘルパー `_say()` に `conversation_id` を追加し、同じ会話の2ターン目であることを明示的に確認するよう直しました。実モデルでの手動確認は複数ターンで行っていましたが、自動テストがそれを守っていなかったのが原因です。

再評価で見つかった4件についても、再発検知テストを追加しました（合計26件）。

## 修正の途中で見つかった不具合

振り返りの開始判定に日時の比較を入れたところ、`can't compare offset-naive and offset-aware datetimes` で失敗しました。SQLite は timezone を落として返すため、SQLAlchemy が UPDATE の条件を Python 側で評価しようとして型が食い違います。条件の判定を SQL に任せる（`synchronize_session=False`）ことで解消しました。

同種の問題は記憶検索側にもあり、そちらは読み出し時に UTC を補う処理で回避しています。日時の扱いは今後も落とし穴になりやすい箇所です。

## 残っている課題

#5・#6・#8 は引き続き未対応です。特に #6（不正な振り返り出力を成功として扱う）は、上の訂正のとおり解消していません。壊れた出力は `200 []` で完了扱いになり、完了した振り返りは再実行できないため、その会話からの再抽出はできません。

---

# 3回目のレビューへの対応と、#5・#6・#8 の完了

対象：[docs/review/codex/phase1_review.md](../codex/phase1_review.md)（3回目のレビュー）
作成日：2026-09-06

## 評価

追加指摘2件は、どちらも再現しました。

| 指摘 | 再現結果 |
| --- | --- |
| 1. 終了後の会話に発言が追加される | 終了・割り込みとも200になり、終了時刻より後に2件の発言が入った |
| 2. 期限切れで振り返りが二重実行される | 両方200となり、同じ候補が2件保存された |

指摘2は、最初の手順では再現しませんでした（先行処理が完了済みだったため正しく409）。レビューの条件どおり、**先行処理がロックを保持したまま**期限切れにすることで再現しました。

### 原因は共通の設計ミス

どちらも、**状態の判定をロックの外に置いたまま**だったことが原因です。

- 会話：終了判定 → ロック取得（判定が古いまま実行）
- 振り返り：開始権の確定 → ロック取得（確定が古いまま実行）

判定と実行の間にロック待ちが挟まるため、待っている間に前提が崩れます。前回、直列化のロックを入れた際に「ロックを取ってから状態を読み直す」という基本を外していました。

指摘1については、こちらの前回の修正で窓が広がってもいました。`ended_at` を完了時にだけ設定するよう変えたため、振り返りの実行中はずっと「終了していない」と判定されます。

## 対応内容

### 3回目 #1・#2：状態確認をロックの内側へ

- 会話は、ロック取得後に会話を読み直して状態を確認します。終了済み・振り返り中なら409を返します。
- 振り返りは、開始権の確定をロック取得後に行います。ロック待ちの時間が回収期限に含まれなくなりました。単一プロセスではロックが排他を保証するため、期限判定はプロセスが落ちて開始だけが残った場合の回収用として残しています。

振り返り中の送信は**拒否**する方針にしました。受け付けて振り返りに含める案もありますが、終了操作の意味が曖昧になるためです。

実サーバーでも、振り返り中の送信が `409 この会話は振り返り中です` になることを確認しました。

### #6：振り返りの失敗と空の結果を区別

`ReflectionParseError` を追加し、出力を候補として読み取れない場合は例外にしました。正しい空配列（残す価値が無かった）とは区別されます。

失敗した場合は開始権を取り消すため、会話は終了状態にならず、続きを話すことも振り返りをやり直すこともできます。エラーには読み取れなかった出力の一部を含めるため、何が返ってきたかを画面で確認できます。

### #5：未判断の候補を会話をまたいで回収

`GET /api/conversations/candidates/pending` を追加し、画面の読み込み時に未判断の候補を取り直します。判断済みの候補は一覧から外れます。

画面を再読み込みしても、抽出済みの候補を採用できるようになりました。

### #8：復元で日時も戻す

変更履歴に ISO 文字列で入っている `occurred_at` を datetime へ戻すようにしました。NULL も戻ります。

なお **記憶の復元はフェーズ1の要求ではありません**。設計書ではフェーズ5の実装内容7・完了条件にあたります。フェーズ1の項目7は「記憶一覧・訂正・削除」までで、こちらが先回りして実装したものでした（AGENTS.md の「将来フェーズの機能を先回りして追加しない」に反していました）。すでに画面にボタンがあり、押すと日時だけ戻らない状態だったため、フェーズ5まで放置せずに直しています。

## テストの修正と追加

### 名前が実態と合っていなかったテスト

`test_migrated_candidate_keeps_visibility_scope` は、名前に反してマイグレーションを実行していませんでした。新規候補の生成を検証しているだけです。`test_new_candidate_always_has_visibility_scope` へ改名しました。

### マイグレーションそのものを実行するテスト

`backend/tests/test_migration.py` を追加しました。参照範囲を持たない版まで上げてから旧形式のデータを入れ、`alembic upgrade head` を実行して補完を確認します。

- 非公開かつ由来する会話がある記憶・候補は、その会話の相手に紐づく
- 公開記憶と、由来する会話が無い手動追加の記憶は限定しない
- 終了済みの会話は振り返り完了として扱う

これまで移行の確認は一時DBでの手動実行だけで、自動テストがありませんでした。

### 追加した再発検知テスト

3回目の指摘2件、#6（失敗と空の区別）、#5（候補の回収）、#8（日時の復元）について追加し、合計34件になりました。

## 確認したこと

- pytest **34件成功**（既存26件＋新規8件）。
- ruff、TypeScriptの型チェック、Viteのビルドがいずれも成功。
- Alembic：新規DBへ適用後、モデルとの差分なし。
- 実サーバーで、振り返り中の送信が409になること、未判断の候補が会話をまたいで取得できることを確認。
- 画面の読み込み直後に「記憶の候補（7）」が表示されることを確認。

## フェーズ1の状態

初回レビューの指摘8件と、2回目・3回目の追加指摘6件は、これですべて対応しました。

フェーズ1の完了条件4項目に加え、実装内容7項目のうち、これまで成立していなかった項目5（会話終了時に候補を抽出し、画面で確認して保存する）も通るようになりました。

残っているのは、フェーズ1の要求ではないものの、実運用で効いてくる次の項目です。

- 会話履歴を画面から読み直せない（過去の会話を開く導線が無い）。
- 記憶検索の「時刻」が減衰のみで、`occurred_at` は振り返りで埋めていない。
- 理想の返答を画面で見返す手段と、教師データとしての書き出しが無い。
- 評価用会話（設計書 §10、フェーズ1から開始とされている作業）が未整備。
