---
name: cross-doc-claim-drift
description: docs だけを直す diff は、同じ主張を持つ別の文書（設計書 §3.2 の技術表・§8 の版表・README・config コメント）が片方だけ古いまま残る。docs 変更は「同じ語」を docs/ README.md backend/app/config.py に grep して突き合わせる
metadata:
  type: project
---

docs を直す diff で、**同じ主張を書いた別の場所が更新されずに矛盾する**形が v0.2 で 6 回以上出ている（2026-09-15 時点）。
- PR5：`docs/result` 本文を再測定に更新、`docs/plan/v0.2.md` の設定表と `config.py` のコメントは旧値
- PR5：数字表を更新、同じ設計書の §8.1「直近の回帰」と既知の失敗一覧は前の版
- PR5：README の機能箇条書きが、既定で無効な経路を実装済みとして書く
- 2026-09-15（構成図の修正）：`architecture.mmd` から WebSocket を消し ISSUE-040 で「図を実装へ合わせた」と書いたが、
  設計書 §3.2 の技術表（「HTTP・WebSocket」）と §3 の図は未修正。
  同じ diff で ISSUE-040 が「v0.6 は push が要る」と書いたが、設計書 §8.5/§8.6 は
  「セッション開始の API」「K ターン後の再試行」「話題の区切りの検出はしない」で、どれもクライアント発の要求で足りる
- 同 diff：issues.md 前文が「6 件はいったんクローズにしていた」と書いたが、ISSUE-030 は HEAD で「未着手」のまま開いていた
- 2026-09-15（ISSUE-044 再測定）：`docs/result/v0.2.md` に「再測定」節を足して「9B の精度不足は主因ではない」と
  書いたが、同じ文書の §「人手で読んだ5シナリオ」3 項（「連続する訂正の処理は9Bモデルにははっきり弱い」）、
  「今後への申し送り」、設計書 §8.2（「連続する訂正が弱い」）、未追跡の `docs/plan/v0.3.md` の前提表
  （「連続する訂正は 0/3」を v0.3 の設計制約の根拠にしている）は前のまま

- 2026-09-15（ISSUE-044 最終版）：`docs/result/v0.2.md` の §「人手で読んだ5シナリオ」3 項・設定表・
  「今後への申し送り」には「2026-09-15 追記」を入れたが、同じ文書の「まとめ」（連続する訂正を 9B の精度不足の
  例として列挙）と v0.3 への引き継ぎ箇条書き（「`_INSTRUCTION` の改善。連続する訂正」を入口として引き継ぐ）は
  前のまま。**同じ語を含む段落に 1 つずつ追記する方式だと、追記し忘れた段落が残る**（grep で「連続する訂正」を
  同一文書内に引いて全件を見る）

- 2026-09-15（ISSUE-047）：`docs/result/v0.2.md` の設定表で `conversation_state_llm` の行に追記したが、
  **同じ表の隣の行** `conversation_state_llm_timeout_seconds`（「呼び出し単体の所要時間は測っておらず…次に測るときは
  記録に残す」）は、この diff 自身が所要時間を測って記録に残したのに前のまま。設計書・README・config は更新済み。
  同じ表の中でも行ごとに見る

- 2026-09-16（既定を `True` に転換）：`config.py` と設計書 §8.2 本文・計画 v0.2 設定表・記録末尾は更新したが、
  **同じ diff が触った README の同じ箇条書き**（42 行「解釈（LLM）は既定無効：」）と README 14・56 行、設計書 §8.2 の
  表（532 行「既定は無効のまま」）、計画 v0.2 §232・§353、記録 218 行（`turn_time_budget_seconds` の根拠）・376 行
  （「判断は変えない」に取り消し線なし）、`run.py --conversation-state-llm` の help（「既定は無効のまま」。既定 True で
  この flag は no-op になり CLI から解釈なしの基準を流せない）が旧値。**既定値の転換は「既定」「無効」「True/False」を
  README・設計書の表・計画の表・記録の表・CLI help まで grep**する。同じ diff の ISSUE-049「いつ扱うか：有効化を判断する前」も
  同じ diff で有効化しており自己矛盾

**Why:** README は設計書を唯一の設計の出所として指し、設計書は版ごとの表・技術表・図・既知の制限一覧に
同じ事実を複数回書いている。1 か所だけ直すと、残りが「実装より広い」または「実装より古い」主張として残り、
次の版の計画がそれを根拠に組まれる。

**How to apply:** docs 変更の diff では、変えた主張のキーワード（技術名・版番号・状態名・「実装済み」等）を
`docs/ README.md backend/app/config.py` に grep し、同じ主張が別の場所に残っていないかを列挙する。
issue の「いつ扱うか」が版を指すときは、設計書のその版の表（追加する仕組み・対象外）を読んで、
本当にその版で必要になるかを確かめる。issue を削除・状態変更する diff では、HEAD の一覧表の状態欄と
本文の「状態：」行を見て、前文の要約（「クローズだった」等）が全件に当てはまるかを見る。
関連：[[evaluator-state-mismatch]]
