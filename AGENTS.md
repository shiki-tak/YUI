# 開発ガイド

## プロジェクト

ローカル会話とYouTube配信を行うAIキャラクターを開発する。人格・長期記憶・行動判断を自作し、経験を次の会話や行動に反映する。Discord連携は対象外。

## 設計と作業範囲

- 作業前に `README.md` と、存在する場合は以下を読む。
  - `docs/design/ai_vtuber_architecture.md`
  - `docs/design/ai_vtuber_development_phases.md`
- ユーザーが指定したフェーズ・変更範囲に集中する。将来フェーズの機能を先回りして追加しない。
- ユーザーの最新の指示を優先する。設計書と実装に食い違いがある場合は、関連コードや変更履歴を確認して意図を把握する。
- 既存の実装・命名・依存管理に合わせ、変更は目的に必要な範囲に収める。既存の未コミット変更を上書き・破棄しない。

## 技術と責任分担

- バックエンド：Python／FastAPI。人格、記憶、会話進行、検索、LLM呼び出し、YouTube連携を担当する。
- フロントエンド：React／TypeScript。操作、録音、アバター表示、字幕、音声再生を担当する。
- 通常の推論はOllama、難しい分析や追加判定は必要に応じてクラウドLLMを使う。モデル名や接続先は設定で変更可能にする。
- LLMへの接続は専用モジュールにまとめ、将来のMLXによる推論・学習に備える。
- データ保存はSQLite／SQLAlchemyを基本とする。DB定義の変更は既存のマイグレーション方式に従う。

## 実装上の原則

- 会話履歴、長期記憶、人格の状態を区別する。記憶には根拠・相手・公開範囲を保持し、訂正・削除を反映する。
- 最新情報は検索や公式APIで確認し、出典・取得日時を保持する。
- LLM出力や外部入力を検証する。タイムアウト、中断、接続失敗を処理し、停止後の結果や重複した発言を再生しない。
- FastAPIのイベントループを重い同期処理で塞がない。学習などの重い処理は別プロセスで実行する。
- APIキーなどの秘密情報をソースコードやログへ出力しない。

## 確認と報告

- 変更に関係する既存テストを実行する。不具合修正では、必要に応じて再発を検知するテストを追加する。
- テストには一時的なDB・データを使い、通常利用の会話・記憶を変更しない。外部APIは原則モックで検証する。
- 日本語で、変更内容、確認結果、未確認事項を簡潔に報告する。モックでの成功と実サービスでの動作確認を区別する。
- レビュー依頼ではコードを変更せず、実際の不具合・仕様漏れを重要度順に報告する。各指摘にファイル・行番号、発生条件、影響、修正方針を示す。
